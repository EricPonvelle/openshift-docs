<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE book [
<!ENTITY % sgml.features "IGNORE">
<!ENTITY % xml.features "INCLUDE">
<!ENTITY % DOCBOOK_ENTS PUBLIC "-//OASIS//ENTITIES DocBook Character Entities V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/dbcentx.mod">
%DOCBOOK_ENTS;
]>
<?asciidoc-toc?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0">
<info>
<title>Images</title>
<date>2026-02-25</date>
<title>Images</title>
<productname>OpenShift Container Platform</productname>
<productnumber>4.16</productnumber>
<subtitle>Enter a short description here.</subtitle>
<abstract>
    <para>A short overview and summary of the book's subject and purpose, traditionally no more than one paragraph long.</para>
</abstract>
<authorgroup>
    <orgname>Red Hat OpenShift Documentation Team</orgname>
</authorgroup>
<xi:include href="Common_Content/Legal_Notice.xml" xmlns:xi="http://www.w3.org/2001/XInclude" />
</info>
<chapter xml:id="overview-of-images">
<title>Overview of images</title>
<simpara role="_abstract">To understand how containerized applications work in OpenShift Container Platform, you need to know about containers, images, and image streams. This overview explains these core concepts and how they work together in your cluster.</simpara>
<section xml:id="images-about_overview-of-images">
<title>Images</title>
<simpara role="_abstract">Container images are binaries that include all requirements for running a single container. You can use images to package applications and deploy them consistently across multiple containers and hosts in OpenShift Container Platform.</simpara>
<simpara>Containers only have access to resources defined in the image unless you give the container additional access when creating it. By deploying the same image in multiple containers across multiple hosts and load balancing between them, OpenShift Container Platform can provide redundancy and horizontal scaling for a service packaged into an image.</simpara>
<simpara>You can use the <link xlink:href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux_atomic_host/7/html-single/managing_containers/#using_podman_to_work_with_containers">podman</link> or <literal>docker</literal> CLI directly to build images, but OpenShift Container Platform also supplies builder images that assist with creating new images by adding your code or configuration to existing images.</simpara>
<simpara>Because applications develop over time, a single image name can actually refer to many different versions of the same image. Each different image is referred to uniquely by its hash, a long hexadecimal number such as <literal>fd44297e2ddb050ec4f&#8230;&#8203;</literal>, which is usually shortened to 12 characters, such as <literal>fd44297e2ddb</literal>.</simpara>
<itemizedlist>
<title>Additional resources</title>
<listitem>
<simpara><link linkend="creating-images">Creating images</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="managing-images-overview">Managing images</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="using-images-overview">Using images</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-image-registry-about_overview-of-images">
<title>Image registry</title>
<simpara role="_abstract">An image registry is a content server that stores and serves container images in OpenShift Container Platform. You can use registries to access container images from external sources or the integrated registry in OpenShift Container Platform.</simpara>
<simpara>Registries contain a collection of one or more image repositories, which contain one or more tagged images. Red&#160;Hat provides a registry at <link xlink:href="https://catalog.redhat.com/en">registry.redhat.io</link> for subscribers. OpenShift Container Platform can also supply its own OpenShift image registry for managing custom container images.</simpara>
</section>
<section xml:id="images-container-repository-about_overview-of-images">
<title>Image repository</title>
<simpara role="_abstract">An image repository is a collection of related container images and tags that identify them. You can use image repositories to organize and manage related container images in OpenShift Container Platform.</simpara>
<simpara>For example, the OpenShift Container Platform Jenkins images are in the following repository:</simpara>
<programlisting language="text" linenumbering="unnumbered">docker.io/openshift/jenkins-2-centos7</programlisting>
</section>
<section xml:id="images-tag_overview-of-images">
<title>Understanding image tags in image streams</title>
<simpara role="_abstract">Image tags in OpenShift Container Platform help you organize, identify, and reference specific versions of container images in image streams. Tags are human-readable labels that act as pointers to particular image layers and digests.</simpara>
<simpara>Tags function as mutable pointers within an image stream. When a new image is imported or tagged into the stream, the tag is updated to point to the new image&#8217;s immutable SHA digest. A single image digest can have multiple tags simultaneously assigned to it. For example, the <literal>:v3.11.59-2</literal> and <literal>:latest</literal> tags are assigned to the same image digest.</simpara>
<simpara>Tags offer two main benefits:</simpara>
<itemizedlist>
<listitem>
<simpara>Tags serve as the primary mechanism for builds and deployments to request a specific version of an image from an image stream.</simpara>
</listitem>
<listitem>
<simpara>Tags help maintain clarity and allow for easy promotion of images between environments. For example, you can promote an image from the <literal>:test</literal> tag to the <literal>:prod</literal> tag.</simpara>
</listitem>
</itemizedlist>
<simpara>While image tags are primarily used for referencing images in configurations, OpenShift Container Platform provides the <literal>oc tag</literal> command for managing tags directly within image streams. This command is similar to the <literal>podman tag</literal> or <literal>docker tag</literal> commands, but it operates on image streams instead of directly on local images. It is used to create a new tag pointer or update an existing tag pointer within an image stream to point to a new image.</simpara>
<simpara>Image tags are appended to the image name or image stream name by using a colon (<literal>:</literal>) as a separator.</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="3">
<colspec colname="col_1" colwidth="25*"/>
<colspec colname="col_2" colwidth="25*"/>
<colspec colname="col_3" colwidth="50*"/>
<thead>
<row>
<entry align="left" valign="top">Context</entry>
<entry align="left" valign="top">Syntax Format</entry>
<entry align="left" valign="top">Example</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">External Registry</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>&lt;registry_path&gt;:&lt;tag&gt;</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>registry.access.redhat.com/openshift3/jenkins-2-rhel7:v3.11.59-2</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Local Image Stream</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>&lt;image_stream_name&gt;:&lt;tag&gt;</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>jenkins:latest</literal></simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
</section>
<section xml:id="images-id_overview-of-images">
<title>Image IDs</title>
<simpara role="_abstract">Image IDs are Secure Hash Algorithm (SHA) codes that uniquely identify container images in OpenShift Container Platform. You can use image IDs to pull specific versions of images that never change.</simpara>
<simpara>For example, the following image ID is for the <literal>docker.io/openshift/jenkins-2-centos7</literal> image:</simpara>
<programlisting language="text" linenumbering="unnumbered">docker.io/openshift/jenkins-2-centos7@sha256:ab312bda324</programlisting>
</section>
<section xml:id="containers-about_overview-of-images">
<title>Containers</title>
<simpara role="_abstract">Containers are isolated running instances of container images that serve as the basic units of OpenShift Container Platform applications. By understanding containers, you can work with containerized applications and manage how they run in your cluster.</simpara>
<simpara>Many application instances can be running in containers on a single host without visibility into each others' processes, files, network, and so on. Typically, each container provides a single service, often called a micro-service, such as a web server or a database, though containers can be used for arbitrary workloads.</simpara>
<simpara>The Linux kernel has been incorporating capabilities for container technologies for years. The Docker project developed a convenient management interface for Linux containers on a host. More recently, the <link xlink:href="https://github.com/opencontainers/">Open Container Initiative</link> has developed open standards for container formats and container runtimes. OpenShift Container Platform and Kubernetes add the ability to orchestrate OCI- and Docker-formatted containers across multi-host installations.</simpara>
<simpara>Though you do not directly interact with container runtimes when using OpenShift Container Platform, understanding their capabilities and terminology is important for understanding their role in OpenShift Container Platform and how your applications function inside of containers.</simpara>
<simpara>Tools such as <link xlink:href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux_atomic_host/7/html-single/managing_containers/#using_podman_to_work_with_containers">Podman</link> can be used to replace Docker command-line tools for running and managing containers directly. By using the <literal>podman</literal> CLI, you can experiment with containers separately from OpenShift Container Platform.</simpara>
</section>
<section xml:id="images-imagestream-use_overview-of-images">
<title>Using image streams</title>
<simpara role="_abstract">Image streams provide an abstraction for referencing container images from within OpenShift Container Platform. You can use image streams to manage image versions and automate builds and deployments in your cluster.</simpara>
<simpara>Image streams do not contain actual image data, but present a single virtual view of related images, similar to an image repository.</simpara>
<simpara>You can configure builds and deployments to watch an image stream for notifications when new images are added and react by performing a build or deployment, respectively.</simpara>
<simpara>For example, if a deployment is using a certain image and a new version of that image is created, a deployment could be automatically performed to pick up the new version of the image.</simpara>
<simpara>However, if the image stream tag used by the deployment or build is not updated, then even if the container image in the container image registry is updated, the build or deployment continues using the previous, presumably known good
image.</simpara>
<simpara>The source images can be stored in any of the following:</simpara>
<itemizedlist>
<listitem>
<simpara>OpenShift Container Platform&#8217;s integrated registry.</simpara>
</listitem>
<listitem>
<simpara>An external registry, for example registry.redhat.io or quay.io.</simpara>
</listitem>
<listitem>
<simpara>Other image streams in the OpenShift Container Platform cluster.</simpara>
</listitem>
</itemizedlist>
<simpara>When you define an object that references an image stream tag, such as a build or deployment configuration, you point to an image stream tag and not the repository. When you build or deploy your application, OpenShift Container Platform queries the repository using the image stream tag to locate the associated ID of the image and uses that exact image.</simpara>
<simpara>The image stream metadata is stored in the etcd instance along with other cluster information.</simpara>
<simpara>Using image streams has several significant benefits:</simpara>
<itemizedlist>
<listitem>
<simpara>You can tag, rollback a tag, and quickly deal with images, without having to re-push using the command line.</simpara>
</listitem>
<listitem>
<simpara>You can trigger builds and deployments when a new image is pushed to the registry. Also, OpenShift Container Platform has generic triggers for other resources, such as Kubernetes objects.</simpara>
</listitem>
<listitem>
<simpara>You can mark a tag for periodic re-import. If the source image has changed, that change is picked up and reflected in the image stream, which triggers the build or deployment flow, depending upon the build or deployment configuration.</simpara>
</listitem>
<listitem>
<simpara>You can share images using fine-grained access control and quickly distribute images across your teams.</simpara>
</listitem>
<listitem>
<simpara>If the source image changes, the image stream tag still points to a known-good version of the image, ensuring that your application does not break unexpectedly.</simpara>
</listitem>
<listitem>
<simpara>You can configure security around who can view and use the images through permissions on the image stream objects.</simpara>
</listitem>
<listitem>
<simpara>Users that lack permission to read or list images on the cluster level can still retrieve the images tagged in a project using image streams.</simpara>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Additional resources</title>
<listitem>
<simpara><link linkend="managing-image-streams">Managing image streams</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="using-imagestreams-with-kube-resources">Using image streams with Kubernetes resources</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="triggering-updates-on-imagestream-changes">Triggering updates on image stream updates</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-tag_overview-of-images">
<title>Image stream tags</title>
<simpara role="_abstract">Image stream tags are named pointers to images in image streams in OpenShift Container Platform. You can configure image stream tags to reference specific versions of container images.</simpara>
</section>
<section xml:id="images-imagestream-image_overview-of-images">
<title>Image stream images</title>
<simpara role="_abstract">Image stream images are API resource objects in OpenShift Container Platform that retrieve specific container images from image streams. You can use image stream images to access metadata about particular image SHA identifiers.</simpara>
</section>
<section xml:id="images-imagestream-trigger_overview-of-images">
<title>Image stream triggers</title>
<simpara role="_abstract">Image stream triggers in OpenShift Container Platform cause specific actions when image stream tags change. You can configure triggers to automatically start builds or deployments when new images are imported.</simpara>
<simpara>For example, importing a new image can cause the value of the tag to change, which causes a trigger to fire when there are deployments, builds, or other resources listening for those.</simpara>
</section>
<section xml:id="how-you-can-use-the-cluster-samples-operator_overview-of-images">
<title>How you can use the Cluster Samples Operator</title>
<simpara role="_abstract">To manage sample image streams and templates in OpenShift Container Platform, you can use the Cluster Samples Operator. The Cluster Samples Operator creates default samples during initial startup to initiate image streams and templates in the <literal>openshift</literal> namespace.</simpara>
</section>
<section xml:id="additional-resources_overview-of-images" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara><link linkend="samples-operator-alt-registry">Use the Operator with an alternate registry</link></simpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="configuring-samples-operator">
<title>Overview of the Cluster Samples Operator</title>
<simpara role="_abstract">The Cluster Samples Operator, which operates in the <literal>openshift</literal> namespace, installs and updates the OpenShift Container Platform image streams and OpenShift Container Platform templates.</simpara>
<important>
<itemizedlist>
<listitem>
<simpara>The Cluster Samples Operator is deprecated. No new templates, samples, or non-Source-to-Image (Non-S2I) image streams are added to the Cluster Samples Operator. However, the existing S2I builder image streams and templates will continue to receive updates until the Cluster Samples Operator is removed in a future release. S2I image streams and templates include:</simpara>
<itemizedlist>
<listitem>
<simpara>Ruby</simpara>
</listitem>
<listitem>
<simpara>Python</simpara>
</listitem>
<listitem>
<simpara>Node.js</simpara>
</listitem>
<listitem>
<simpara>Perl</simpara>
</listitem>
<listitem>
<simpara>PHP</simpara>
</listitem>
<listitem>
<simpara>HTTPD</simpara>
</listitem>
<listitem>
<simpara>Nginx</simpara>
</listitem>
<listitem>
<simpara>EAP</simpara>
</listitem>
<listitem>
<simpara>Java</simpara>
</listitem>
<listitem>
<simpara>Webserver</simpara>
</listitem>
<listitem>
<simpara>.NET</simpara>
</listitem>
<listitem>
<simpara>Go</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>The Cluster Samples Operator will stop managing and providing support to the non-S2I samples (image streams and templates). You can contact the image stream or template owner for any requirements and future plans. In addition, refer to the following link:</simpara>
<itemizedlist>
<listitem>
<simpara><link xlink:href="https://github.com/openshift/library/blob/master/official.yaml">List of the repositories hosting the image stream or templates</link></simpara>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
</important>
<section xml:id="samples-operator-overview_configuring-samples-operator">
<title>Understanding the Cluster Samples Operator</title>
<simpara role="_abstract">During installation, the Operator creates the default configuration object for itself and then creates the sample image streams and templates, including quick start templates.</simpara>
<note>
<simpara>To facilitate image stream imports from other registries that require credentials, a cluster administrator can create any additional secrets that contain the content of a Docker <literal>config.json</literal> file in the <literal>openshift</literal> namespace needed for image import.</simpara>
</note>
<simpara>The Cluster Samples Operator configuration is a cluster-wide resource. The deployment of the Operator is within the <literal>openshift-cluster-samples-operator</literal> namespace.</simpara>
<simpara>The image for the Cluster Samples Operator has image stream and template definitions for the associated OpenShift Container Platform release. When each sample is created or updated, the Cluster Samples Operator includes an annotation that denotes the version of OpenShift Container Platform. The Operator uses this annotation to ensure that each sample matches the release version. Samples outside of its inventory are ignored, as are skipped samples. Modifications to any samples that are managed by the Operator, where that version annotation is modified or deleted, are reverted automatically.</simpara>
<note>
<simpara>The Jenkins images are part of the image payload from installation and are tagged into the image streams directly.</simpara>
</note>
<simpara>The Cluster Samples Operator configuration resource includes a finalizer which cleans up the following upon deletion:</simpara>
<itemizedlist>
<listitem>
<simpara>Operator managed image streams.</simpara>
</listitem>
<listitem>
<simpara>Operator managed templates.</simpara>
</listitem>
<listitem>
<simpara>Operator generated configuration resources.</simpara>
</listitem>
<listitem>
<simpara>Cluster status resources.</simpara>
</listitem>
</itemizedlist>
<simpara>Upon deletion of the samples resource, the Cluster Samples Operator recreates the resource by using the default configuration.</simpara>
<simpara>If the Cluster Samples Operator is removed during installation, you can use the Cluster Samples Operator with an alternate registry so that content can be imported. Then you can set the Cluster Samples Operator to <literal>Managed</literal> to get the samples. Use the following instructions:</simpara>
<itemizedlist>
<listitem>
<simpara><link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html/images/samples-operator-alt-registry">Using the Cluster Samples Operator with an alternate registry</link></simpara>
</listitem>
</itemizedlist>
<simpara>For more information about configuring credentials, see the following link:</simpara>
<itemizedlist>
<listitem>
<simpara><link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html/images/managing-images#using-image-pull-secrets">Using image pull secrets</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="samples-operator-bootstrapped_configuring-samples-operator">
<title>Cluster Samples Operator use of management state</title>
<simpara role="_abstract">The Cluster Samples Operator is bootstrapped as <literal>Managed</literal> by default or if global proxy is configured.</simpara>
<simpara>In the <literal>Managed</literal> state, the Cluster Samples Operator is actively managing its resources and keeping the component active to pull sample image streams and images from the registry and ensure that the requisite sample templates are installed.</simpara>
<simpara>Certain circumstances result in the Cluster Samples Operator bootstrapping itself as <literal>Removed</literal> including:</simpara>
<itemizedlist>
<listitem>
<simpara>If the Cluster Samples Operator cannot reach the registry after three minutes on initial startup after a clean installation.</simpara>
</listitem>
<listitem>
<simpara>If the Cluster Samples Operator detects that it is on an IPv6 network.</simpara>
</listitem>
</itemizedlist>
<note>
<simpara>For OpenShift Container Platform, the default image registry is
<literal>registry.access.redhat.com</literal> or <literal>quay.io</literal>.</simpara>
</note>
<simpara>However, if the Cluster Samples Operator detects that it is on an IPv6 network and an OpenShift Container Platform global proxy is configured, then the IPv6 check supersedes all the checks. As a result, the Cluster Samples Operator bootstraps itself as <literal>Removed</literal>.</simpara>
<important>
<simpara>IPv6 installations are not currently supported by the registry. The Cluster Samples Operator pulls most of the sample image streams and images from the registry.</simpara>
</important>
</section>
<section xml:id="samples-operator-retries_configuring-samples-operator">
<title>Cluster Samples Operator tracking and error recovery of image stream imports</title>
<simpara role="_abstract">After creation or update of a samples image stream, the Cluster Samples Operator monitors the progress of each image stream tag&#8217;s image import.</simpara>
<simpara>If an import fails, the Cluster Samples Operator retries the import through the image stream image import API at a rate of about every 15 minutes until either one of the following occurs:</simpara>
<itemizedlist>
<listitem>
<simpara>The import succeeds.</simpara>
</listitem>
<listitem>
<simpara>The Cluster Samples Operator configuration is changed such that either the image stream is added to the <literal>skippedImagestreams</literal> list, or the management state is changed to <literal>Removed</literal>.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-samples-operator-deprecated-image-stream_configuring-samples-operator">
<title>Removing deprecated image stream tags from the Cluster Samples Operator</title>
<simpara role="_abstract">The Cluster Samples Operator leaves deprecated image stream tags in an image stream because users can have deployments that use the deprecated image stream tags.</simpara>
<simpara>You can remove deprecated image stream tags by editing the image stream with the  <literal>oc tag</literal> command.</simpara>
<note>
<simpara>Deprecated image stream tags that the samples providers have removed from their image streams are not included on initial installations.</simpara>
</note>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You installed the OpenShift CLI (<literal>oc</literal>).</simpara>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Remove deprecated image stream tags by editing the image stream with the following <literal>oc tag</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag -d &lt;image_stream_name:tag&gt;</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Deleted tag default/&lt;image_stream_name:tag&gt;.</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="samples-operator-alt-registry">
<title>Using the Cluster Samples Operator with an alternate registry</title>
<simpara role="_abstract">You can use the Cluster Samples Operator with an alternate registry by first creating a mirror registry. Before you create the mirror registry, you must prepare the mirror host.</simpara>
<section xml:id="installation-about-mirror-registry_samples-operator-alt-registry">
<title>About the mirror registry</title>
<simpara role="_abstract">You must have access to the internet to obtain the necessary container images. Using an alternative registry means that you place the mirror registry on a mirror host that has access to both your network and the internet.</simpara>
<simpara>You can mirror the images that are required for OpenShift Container Platform installation and subsequent product updates to a container mirror registry such as Red&#160;Hat Quay, JFrog Artifactory, Sonatype Nexus Repository, or Harbor. If you do not have access to a large-scale container registry, you can use the <emphasis>mirror registry for Red&#160;Hat OpenShift</emphasis>, a small-scale container registry included with OpenShift Container Platform subscriptions.</simpara>
<simpara>You can use any container registry that supports <link xlink:href="https://docs.docker.com/registry/spec/manifest-v2-2">Docker v2-2</link>, such as Red&#160;Hat Quay, the <emphasis>mirror registry for Red&#160;Hat OpenShift</emphasis>, Artifactory, Sonatype Nexus Repository, or Harbor. Regardless of your chosen registry, the procedure to mirror content from Red Hat hosted sites on the internet to an isolated image registry is the same. After you mirror the content, you configure each cluster to retrieve this content from your mirror registry.</simpara>
<important>
<simpara>The OpenShift image registry cannot be used as the target registry because it does not support pushing without a tag, which is required during the mirroring process.</simpara>
</important>
<simpara>If choosing a container registry that is not the <emphasis>mirror registry for Red&#160;Hat OpenShift</emphasis>, it must be reachable by every machine in the clusters that you provision. If the registry is unreachable, installation, updating, or normal operations such as workload relocation might fail. For that reason, you must run mirror registries in a highly available way, and the mirror registries must at least match the production availability of your OpenShift Container Platform clusters.</simpara>
<simpara>When you populate your mirror registry with OpenShift Container Platform images, you can follow two scenarios. If you have a host that can access both the internet and your mirror registry, but not your cluster nodes, you can directly mirror the content from that machine. This process is referred to as <emphasis>connected mirroring</emphasis>. If you have no such host, you must mirror the images to a file system and then bring that host or removable media into your restricted environment. This process is referred to as <emphasis>disconnected mirroring</emphasis>.</simpara>
<simpara>For mirrored registries, to view the source of pulled images, you must review the <literal>Trying to access</literal> log entry in the CRI-O logs. Other methods to view the image pull source, such as using the <literal>crictl images</literal> command on a node, show the non-mirrored image name, even though the image is pulled from the mirrored location.</simpara>
<note>
<simpara>Red&#160;Hat does not test third party registries with OpenShift Container Platform.</simpara>
</note>
<section xml:id="cli-installing-cli-linux_samples-operator-alt-registry">
<title>Installing the OpenShift CLI on Linux</title>
<simpara role="_abstract">To manage your cluster and deploy applications from the command line, install the OpenShift CLI (<literal>oc</literal>) binary on Linux.</simpara>
<important>
<simpara>If you installed an earlier version of <literal>oc</literal>, you cannot use it to complete all of the commands in OpenShift Container Platform.</simpara>
<simpara>Download and install the new version of <literal>oc</literal>.</simpara>
</important>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Navigate to the <link xlink:href="https://access.redhat.com/downloads/content/290">Download OpenShift Container Platform</link> page on the Red&#160;Hat Customer Portal.</simpara>
</listitem>
<listitem>
<simpara>Select the architecture from the <emphasis role="strong">Product Variant</emphasis> list.</simpara>
</listitem>
<listitem>
<simpara>Select the appropriate version from the <emphasis role="strong">Version</emphasis> list.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Download Now</emphasis> next to the <emphasis role="strong">OpenShift v4.16 Linux Clients</emphasis> entry and save the file.</simpara>
</listitem>
<listitem>
<simpara>Unpack the archive:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ tar xvf &lt;file&gt;</programlisting>
</listitem>
<listitem>
<simpara>Place the <literal>oc</literal> binary in a directory that is on your <literal>PATH</literal>.</simpara>
<simpara>To check your <literal>PATH</literal>, execute the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ echo $PATH</programlisting>
</listitem>
</orderedlist>
<itemizedlist>
<title>Verification</title>
<listitem>
<simpara>After you install the OpenShift CLI, it is available using the <literal>oc</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc &lt;command&gt;</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="cli-installing-cli-windows_samples-operator-alt-registry">
<title>Installing the OpenShift CLI on Windows</title>
<simpara role="_abstract">To manage your cluster and deploy applications from the command line, install OpenShift CLI (<literal>oc</literal>) binary on Windows.</simpara>
<important>
<simpara>If you installed an earlier version of <literal>oc</literal>, you cannot use it to complete all of the commands in OpenShift Container Platform.</simpara>
<simpara>Download and install the new version of <literal>oc</literal>.</simpara>
</important>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Navigate to the <link xlink:href="https://access.redhat.com/downloads/content/290">Download OpenShift Container Platform</link> page on the Red Hat Customer Portal.</simpara>
</listitem>
<listitem>
<simpara>Select the appropriate version from the <emphasis role="strong">Version</emphasis> list.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Download Now</emphasis> next to the <emphasis role="strong">OpenShift v4.16 Windows Client</emphasis> entry and save the file.</simpara>
</listitem>
<listitem>
<simpara>Extract the archive with a ZIP program.</simpara>
</listitem>
<listitem>
<simpara>Move the <literal>oc</literal> binary to a directory that is on your <literal>PATH</literal> variable.</simpara>
<simpara>To check your <literal>PATH</literal> variable, open the command prompt and execute the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">C:\&gt; path</programlisting>
</listitem>
</orderedlist>
<itemizedlist>
<title>Verification</title>
<listitem>
<simpara>After you install the OpenShift CLI, it is available using the <literal>oc</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">C:\&gt; oc &lt;command&gt;</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="cli-installing-cli-macos_samples-operator-alt-registry">
<title>Installing the OpenShift CLI on macOS</title>
<simpara role="_abstract">To manage your cluster and deploy applications from the command line, install the OpenShift CLI (<literal>oc</literal>) binary on macOS.</simpara>
<important>
<simpara>If you installed an earlier version of <literal>oc</literal>, you cannot use it to complete all of the commands in OpenShift Container Platform.</simpara>
<simpara>Download and install the new version of <literal>oc</literal>.</simpara>
</important>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Navigate to the <link xlink:href="https://access.redhat.com/downloads/content/290">Download OpenShift Container Platform</link> on the Red&#160;Hat Customer Portal.</simpara>
</listitem>
<listitem>
<simpara>Select the appropriate version from the <emphasis role="strong">Version</emphasis> drop-down list.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Download Now</emphasis> next to the <emphasis role="strong">OpenShift v4.16 macOS Clients</emphasis> entry and save the file.</simpara>
</listitem>
<listitem>
<simpara>Unpack and unzip the archive.</simpara>
</listitem>
<listitem>
<simpara>Move the <literal>oc</literal> binary to a directory on your <literal>PATH</literal> variable.</simpara>
<simpara>To check your <literal>PATH</literal> variable, open a terminal and execute the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ echo $PATH</programlisting>
</listitem>
</orderedlist>
<itemizedlist>
<title>Verification</title>
<listitem>
<simpara>Verify your installation by using an <literal>oc</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc &lt;command&gt;</programlisting>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="installation-adding-registry-pull-secret_samples-operator-alt-registry">
<title>Configuring credentials that allow images to be mirrored</title>
<simpara role="_abstract">Create a container image registry credentials file so that you can mirror images from Red&#160;Hat to your mirror. Complete the following steps on the installation host.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You configured a mirror registry to use.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Download your <literal>registry.redhat.io</literal> <link xlink:href="https://console.redhat.com/openshift/install/pull-secret">pull secret from Red Hat OpenShift Cluster Manager</link>.</simpara>
</listitem>
<listitem>
<simpara>Make a copy of your pull secret in JSON format by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ cat ./pull-secret | jq . &gt; &lt;path&gt;/&lt;pull_secret_file_in_json&gt;</programlisting>
<simpara>Specify the path to the directory to store the pull secret in and a name for the JSON file that you create.</simpara>
<formalpara>
<title>Example pull secret</title>
<para>
<programlisting language="json" linenumbering="unnumbered">{
  "auths": {
    "cloud.openshift.com": {
      "auth": "b3BlbnNo...",
      "email": "you@example.com"
    },
    "quay.io": {
      "auth": "b3BlbnNo...",
      "email": "you@example.com"
    },
    "registry.connect.redhat.com": {
      "auth": "NTE3Njg5Nj...",
      "email": "you@example.com"
    },
    "registry.redhat.io": {
      "auth": "NTE3Njg5Nj...",
      "email": "you@example.com"
    }
  }
}</programlisting>
</para>
</formalpara>

</listitem>
<listitem>
<simpara>Generate the base64-encoded user name and password or token for your mirror registry by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ echo -n '&lt;user_name&gt;:&lt;password&gt;' | base64 -w0</programlisting>
<simpara>For <literal>&lt;user_name&gt;</literal> and <literal>&lt;password&gt;</literal>, specify the user name and password that you configured for your registry.</simpara>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">BGVtbYk3ZHAtqXs=</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Edit the JSON file and add a section that describes your registry to it:</simpara>
<programlisting language="json" linenumbering="unnumbered">  "auths": {
    "&lt;mirror_registry&gt;": {
      "auth": "&lt;credentials&gt;",
      "email": "you@example.com"
    }
  },</programlisting>
<itemizedlist>
<listitem>
<simpara>For the <literal>&lt;mirror_registry&gt;</literal> value, specify the registry domain name, and optionally the port, that your mirror registry uses to serve content. For example, <literal>registry.example.com</literal> or <literal>registry.example.com:8443</literal>.</simpara>
</listitem>
<listitem>
<simpara>For the <literal>&lt;credentials&gt;</literal> value, specify the base64-encoded user name and password for the mirror registry.</simpara>
<formalpara>
<title>Example modified pull secret</title>
<para>
<programlisting language="json" linenumbering="unnumbered">{
  "auths": {
    "registry.example.com": {
      "auth": "BGVtbYk3ZHAtqXs=",
      "email": "you@example.com"
    },
    "cloud.openshift.com": {
      "auth": "b3BlbnNo...",
      "email": "you@example.com"
    },
    "quay.io": {
      "auth": "b3BlbnNo...",
      "email": "you@example.com"
    },
    "registry.connect.redhat.com": {
      "auth": "NTE3Njg5Nj...",
      "email": "you@example.com"
    },
    "registry.redhat.io": {
      "auth": "NTE3Njg5Nj...",
      "email": "you@example.com"
    }
  }
}</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
</section>
<section xml:id="installation-mirror-repository_samples-operator-alt-registry">
<title>Mirroring the OpenShift Container Platform image repository</title>
<simpara role="_abstract">Mirror the OpenShift Container Platform image repository to your registry to use during cluster installation or upgrade. Complete the following steps on the mirror host.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Your mirror host has access to the internet.</simpara>
</listitem>
<listitem>
<simpara>You configured a mirror registry to use.</simpara>
</listitem>
<listitem>
<simpara>You downloaded the <link xlink:href="https://console.redhat.com/openshift/install/pull-secret">pull secret from Red Hat OpenShift Cluster Manager</link> and modified it to include authentication to your mirror repository.</simpara>
</listitem>
<listitem>
<simpara>If you use self-signed certificates, you have specified a Subject Alternative Name in the certificates.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Review the
<link xlink:href="https://access.redhat.com/downloads/content/290/">Download OpenShift Container Platform</link> page to determine the version of OpenShift Container Platform that you want to install and determine the corresponding tag on the <link xlink:href="https://quay.io/repository/openshift-release-dev/ocp-release?tab=tags">Repository Tags</link> page.</simpara>
</listitem>
<listitem>
<simpara>Set the following required environment variables:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Export the release version:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ OCP_RELEASE=&lt;release_version&gt;</programlisting>
<simpara>For <literal>&lt;release_version&gt;</literal>, specify the tag that corresponds to the version of OpenShift Container Platform to install, such as <literal>4.21.1</literal>.</simpara>
</listitem>
<listitem>
<simpara>Export the local registry name and host port:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ LOCAL_REGISTRY='&lt;local_registry_host_name&gt;:&lt;local_registry_host_port&gt;'</programlisting>
<simpara>For <literal>&lt;local_registry_host_name&gt;</literal>, specify the registry domain name for your mirror repository, and for <literal>&lt;local_registry_host_port&gt;</literal>, specify the port that it serves content on.</simpara>
</listitem>
<listitem>
<simpara>Export the local repository name:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ LOCAL_REPOSITORY='&lt;local_repository_name&gt;'</programlisting>
<simpara>For <literal>&lt;local_repository_name&gt;</literal>, specify the name of the repository to create in your registry, such as <literal>ocp4/openshift4</literal>.</simpara>
</listitem>
<listitem>
<simpara>Export the name of the repository to mirror:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ PRODUCT_REPO='openshift-release-dev'</programlisting>
<simpara>For a production release, you must specify <literal>openshift-release-dev</literal>.</simpara>
</listitem>
<listitem>
<simpara>Export the path to your registry pull secret:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ LOCAL_SECRET_JSON='&lt;path_to_pull_secret&gt;'</programlisting>
<simpara>For <literal>&lt;path_to_pull_secret&gt;</literal>, specify the absolute path to and file name of the pull secret for your mirror registry that you created.</simpara>
</listitem>
<listitem>
<simpara>Export the release mirror:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ RELEASE_NAME="ocp-release"</programlisting>
<simpara>For a production release, you must specify <literal>ocp-release</literal>.</simpara>
</listitem>
<listitem>
<simpara>Export the type of architecture for your cluster:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ ARCHITECTURE=&lt;cluster_architecture&gt;</programlisting>
<simpara>Specify the architecture of the cluster, such as <literal>x86_64</literal>, <literal>aarch64</literal>, <literal>s390x</literal>, or <literal>ppc64le</literal>.</simpara>
</listitem>
<listitem>
<simpara>Export the path to the directory to host the mirrored images:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ REMOVABLE_MEDIA_PATH=&lt;path&gt;</programlisting>
<simpara>Specify the full path, including the initial forward slash (/) character.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Mirror the version images to the mirror registry:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Directly push the release images to the local registry by using following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc adm release mirror -a ${LOCAL_SECRET_JSON}  \
     --from=quay.io/${PRODUCT_REPO}/${RELEASE_NAME}:${OCP_RELEASE}-${ARCHITECTURE} \
     --to=${LOCAL_REGISTRY}/${LOCAL_REPOSITORY} \
     --to-release-image=${LOCAL_REGISTRY}/${LOCAL_REPOSITORY}:${OCP_RELEASE}-${ARCHITECTURE}</programlisting>
<simpara>This command pulls the release information as a digest, and its output includes the <literal>imageContentSources</literal> data that you require when you install your cluster.</simpara>
</listitem>
<listitem>
<simpara>Record the entire <literal>imageContentSources</literal> section from the output of the previous command. The information about your mirrors is unique to your mirrored repository, and you must add the <literal>imageContentSources</literal> section to the <literal>install-config.yaml</literal> file during installation.</simpara>
<note>
<simpara>The image name gets patched to Quay.io during the mirroring process, and the Podman images show <literal>quay.io</literal> in the registry on the bootstrap virtual machine.</simpara>
</note>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>To create the installation program that is based on the content that you mirrored, extract it and pin it to the release by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc adm release extract -a ${LOCAL_SECRET_JSON} --command=openshift-install "${LOCAL_REGISTRY}/${LOCAL_REPOSITORY}:${OCP_RELEASE}-${ARCHITECTURE}"</programlisting>
<important>
<simpara>To ensure that you use the correct images for the version of OpenShift Container Platform that you selected, you must extract the installation program from the mirrored content.</simpara>
<simpara>You must perform this step on a machine with an active internet connection.</simpara>
</important>
</listitem>
<listitem>
<simpara>For clusters using installer-provisioned infrastructure, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ openshift-install</programlisting>
</listitem>
</orderedlist>
</section>
<section xml:id="installation-restricted-network-samples_samples-operator-alt-registry">
<title>Using Cluster Samples Operator image streams with alternate or mirrored registries</title>
<simpara role="_abstract">You can use an alternate or mirror registry to host your images streams instead of using the Red&#160;Hat registry.</simpara>
<simpara>Most image streams in the <literal>openshift</literal> namespace managed by the Cluster Samples Operator point to images located in the Red&#160;Hat registry at <link xlink:href="https://registry.redhat.io">registry.redhat.io</link>.</simpara>
<note>
<simpara>The <literal>cli</literal>, <literal>installer</literal>, <literal>must-gather</literal>, and <literal>tests</literal> image streams, while part of the install payload, are not managed by the Cluster Samples Operator. These are not addressed in this procedure.</simpara>
</note>
<important>
<simpara>The Cluster Samples Operator must be set to <literal>Managed</literal> in a disconnected environment. To install the image streams, you must have a mirrored registry.</simpara>
</important>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the <literal>dedicated-admin</literal> role.</simpara>
</listitem>
<listitem>
<simpara>Create a pull secret for your mirror registry.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Access the images of a specific image stream to mirror, for example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get is &lt;imagestream&gt; -n openshift -o json | jq .spec.tags[].from.name | grep registry.redhat.io</programlisting>
</listitem>
<listitem>
<simpara>Mirror images from <link xlink:href="https://registry.redhat.io">registry.redhat.io</link> associated with any image streams you need</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc image mirror registry.redhat.io/rhscl/ruby-25-rhel7:latest ${MIRROR_ADDR}/rhscl/ruby-25-rhel7:latest</programlisting>
</listitem>
<listitem>
<simpara>Create the image configuration object for the cluster by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create configmap registry-config --from-file=${MIRROR_ADDR_HOSTNAME}..5000=$path/ca.crt -n openshift-config</programlisting>
</listitem>
<listitem>
<simpara>Add the required trusted CAs for the mirror in the image configuration object:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch image.config.openshift.io/cluster --patch '{"spec":{"additionalTrustedCA":{"name":"registry-config"}}}' --type=merge</programlisting>
</listitem>
<listitem>
<simpara>Update the <literal>samplesRegistry</literal> field in the Cluster Samples Operator configuration object to contain the <literal>hostname</literal> portion of the mirror location defined in the mirror configuration:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit configs.samples.operator.openshift.io -n openshift-cluster-samples-operator</programlisting>
<important>
<simpara>This step is required because the image stream import process does not use the mirror or search mechanism at this time.</simpara>
</important>
</listitem>
<listitem>
<simpara>Add any image streams that are not mirrored into the <literal>skippedImagestreams</literal> field of the Cluster Samples Operator configuration object. Or if you do not want to support any of the sample image streams, set the Cluster Samples Operator to <literal>Removed</literal> in the Cluster Samples Operator configuration object.</simpara>
<note>
<simpara>The Cluster Samples Operator issues alerts if image stream imports are failing but the Cluster Samples Operator is either periodically retrying or does not appear to be retrying them.</simpara>
</note>
<simpara>Many of the templates in the <literal>openshift</literal> namespace reference the image streams. You can use <literal>Removed</literal> to purge both the image streams and templates. This eliminates the possibility of attempts to use the templates if they are not functional because of any missing image streams.</simpara>
</listitem>
</orderedlist>
<section xml:id="installation-images-samples-disconnected-mirroring-assist_samples-operator-alt-registry">
<title>Cluster Samples Operator assistance for mirroring</title>
<simpara role="_abstract">During installation, OpenShift Container Platform creates a config map named <literal>imagestreamtag-to-image</literal> in the <literal>openshift-cluster-samples-operator</literal> namespace.</simpara>
<simpara>The <literal>imagestreamtag-to-image</literal> config map contains an entry, the populating image, for each image stream tag.</simpara>
<simpara>The format of the key for each entry in the data field in the config map is <literal>&lt;image_stream_name&gt;_&lt;image_stream_tag_name&gt;</literal>.</simpara>
<simpara>During a disconnected installation of OpenShift Container Platform, the status of the Cluster Samples Operator is set to <literal>Removed</literal>. If you choose to change it to <literal>Managed</literal>, it installs samples.</simpara>
<note>
<simpara>The use of samples in a network-restricted or discontinued environment might require access to services external to your network. Some example services include: Github, Maven Central, npm, RubyGems, PyPi and others. There might be additional steps to take that allow the Cluster Samples Operators objects to reach the services they require.</simpara>
</note>
<simpara>Use the following principles to determine which images you need to mirror for your image streams to import:</simpara>
<itemizedlist>
<listitem>
<simpara>While the Cluster Samples Operator is set to <literal>Removed</literal>, you can create your mirrored registry, or determine which existing mirrored registry you want to use.</simpara>
</listitem>
<listitem>
<simpara>Mirror the samples you want to the mirrored registry using the new config map as your guide.</simpara>
</listitem>
<listitem>
<simpara>Add any of the image streams you did not mirror to the <literal>skippedImagestreams</literal> list of the Cluster Samples Operator configuration object.</simpara>
</listitem>
<listitem>
<simpara>Set <literal>samplesRegistry</literal> of the Cluster Samples Operator configuration object to the mirrored registry.</simpara>
</listitem>
<listitem>
<simpara>Then set the Cluster Samples Operator to <literal>Managed</literal> to install the image streams you have mirrored.</simpara>
</listitem>
</itemizedlist>
</section>
</section>
</chapter>
<chapter xml:id="creating-images">
<title>Creating images</title>
<simpara role="_abstract">Learn how to create your own container images, based on pre-built images that are ready to help you. The process includes learning best practices for writing images, defining metadata for images, testing images, and using a custom builder workflow to create images to use with OpenShift Container Platform.</simpara>
<section xml:id="images-create-guidelines_create-images">
<title>Learning container best practices</title>
<simpara role="_abstract">When creating container images to run on OpenShift Container Platform there are a number of best practices to consider as an image author to ensure a good experience for consumers of those images. Because images are intended to be immutable and used as-is, the following guidelines help ensure that your images are highly consumable and easy to use on OpenShift Container Platform.</simpara>
<section xml:id="images-create-guide-general_create-images">
<title>General container image guidelines</title>
<simpara role="_abstract">Follow fundamental guidelines for creating container images to ensure they are secure, efficient, and reproducible for deployment in OpenShift Container Platform.</simpara>
<section xml:id="_reuse-images">
<title>Reuse images</title>
<simpara>Wherever possible, base your image on an appropriate upstream image using the <literal>FROM</literal> statement. This ensures your image can easily pick up security fixes from an upstream image when it is updated, rather than you having to update your dependencies directly.</simpara>
<simpara>In addition, use tags in the <literal>FROM</literal> instruction, for example, <literal>rhel:rhel7</literal>, to make it clear to users exactly which version of an image your image is based on. Using a tag other than <literal>latest</literal> ensures your image is not subjected to breaking changes that might go into the <literal>latest</literal> version of an upstream image.</simpara>
</section>
<section xml:id="_maintain-compatibility-within-tags">
<title>Maintain compatibility within tags</title>
<simpara>When tagging your own images, try to maintain backwards compatibility within a tag. For example, if you provide an image named <literal>image</literal> and it currently includes version <literal>1.0</literal>, you might provide a tag of <literal>image:v1</literal>. When you update the image, as long as it continues to be compatible with the original image, you can continue to tag the new image <literal>image:v1</literal>, and downstream consumers of this tag are able to get updates without being broken.</simpara>
<simpara>If you later release an incompatible update, then switch to a new tag, for example <literal>image:v2</literal>. This allows downstream consumers to move up to the new version at will, but not be inadvertently broken by the new incompatible image. Any downstream consumer using <literal>image:latest</literal> takes on the risk of any incompatible changes being introduced.</simpara>
</section>
<section xml:id="_avoid-multiple-processes">
<title>Avoid multiple processes</title>
<simpara>Do not start multiple services, such as a database and <literal>SSHD</literal>, inside one container. This is not necessary because containers are lightweight and can be easily linked together for orchestrating multiple processes. OpenShift Container Platform allows you to easily colocate and co-manage related images by grouping them into a single pod.</simpara>
<simpara>This colocation ensures the containers share a network namespace and storage for communication. Updates are also less disruptive as each image can be updated less frequently and independently. Signal handling flows are also clearer with a single process as you do not have to manage routing signals to spawned processes.</simpara>
</section>
<section xml:id="_use-exec-in-wrapper-scripts">
<title>Use <literal>exec</literal> in wrapper scripts</title>
<simpara>Many images use wrapper scripts to do some setup before starting a process for the software being run. If your image uses such a script, that script uses <literal>exec</literal> so that the script&#8217;s process is replaced by your software. If you do not use <literal>exec</literal>, then signals sent by your container runtime go to your wrapper script instead of your software&#8217;s process. This is not what you want.</simpara>
<simpara>If you have a wrapper script that starts a process for some server. You start your container, for example, using <literal>podman run -i</literal>, which runs the wrapper script, which in turn starts your process. If you want to close your container with <literal>CTRL+C</literal>. If your wrapper script used <literal>exec</literal> to start the server process, <literal>podman</literal> sends SIGINT to the server process, and everything works as you expect. If you did not use <literal>exec</literal> in your wrapper script, <literal>podman</literal> sends SIGINT to the process for the wrapper script and your process keeps running like nothing happened.</simpara>
<simpara>Also note that your process runs as <literal>PID 1</literal> when running in a container. This means that if your main process terminates, the entire container is stopped, canceling any child processes you launched from your <literal>PID 1</literal> process.</simpara>
</section>
<section xml:id="_clean-temporary-files">
<title>Clean temporary files</title>
<simpara>Remove all temporary files you create during the build process. This also includes any files added with the <literal>ADD</literal> command.  For example, run the <literal>yum clean</literal> command after performing <literal>yum install</literal> operations.</simpara>
<simpara>You can prevent the <literal>yum</literal> cache from ending up in an image layer by creating your <literal>RUN</literal> statement as follows:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">RUN yum -y install mypackage &amp;&amp; yum -y install myotherpackage &amp;&amp; yum clean all -y</programlisting>
<simpara>Note that if you instead write:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">RUN yum -y install mypackage
RUN yum -y install myotherpackage &amp;&amp; yum clean all -y</programlisting>
<simpara>Then the first <literal>yum</literal> invocation leaves extra files in that layer, and these files cannot be removed when the <literal>yum clean</literal> operation is run later. The extra files are not visible in the final image, but they are present in the underlying layers.</simpara>
<simpara>The current container build process does not allow a command run in a later layer to shrink the space used by the image when something was removed in an earlier layer. However, this may change in the future. This means that if you perform an <literal>rm</literal> command in a later layer, although the files are hidden it does not reduce the overall size of the image to be downloaded. Therefore, as with the <literal>yum clean</literal> example, it is best to remove files in the same command that created them, where possible, so they do not end up written to a layer.</simpara>
<simpara>In addition, performing multiple commands in a single <literal>RUN</literal> statement reduces the number of layers in your image, which improves download and extraction time.</simpara>
</section>
<section xml:id="_place-instructions-in-the-proper-order">
<title>Place instructions in the proper order</title>
<simpara>The container builder reads the <literal>Dockerfile</literal> and runs the instructions from top to bottom. Every instruction that is successfully executed creates a layer which can be reused the next time this or another image is built. It is very important to place instructions that rarely change at the top of your <literal>Dockerfile</literal>. Doing so ensures the next builds of the same image are very fast because the cache is not invalidated by upper layer changes.</simpara>
<simpara>For example, if you are working on a <literal>Dockerfile</literal> that contains an <literal>ADD</literal> command to install a file you are iterating on, and a <literal>RUN</literal> command to <literal>yum install</literal> a package, it is best to put the <literal>ADD</literal> command last:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">FROM foo
RUN yum -y install mypackage &amp;&amp; yum clean all -y
ADD myfile /test/myfile</programlisting>
<simpara>This way each time you edit <literal>myfile</literal> and rerun <literal>podman build</literal> or <literal>docker build</literal>, the system reuses the cached layer for the <literal>yum</literal> command and only generates the new layer for the <literal>ADD</literal> operation.</simpara>
<simpara>If instead you wrote the <literal>Dockerfile</literal> as:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">FROM foo
ADD myfile /test/myfile
RUN yum -y install mypackage &amp;&amp; yum clean all -y</programlisting>
<simpara>Then each time you changed <literal>myfile</literal> and reran <literal>podman build</literal> or <literal>docker build</literal>, the <literal>ADD</literal> operation would invalidate the <literal>RUN</literal> layer cache, so the <literal>yum</literal> operation must be rerun as well.</simpara>
</section>
<section xml:id="_mark-important-ports">
<title>Mark important ports</title>
<simpara>The EXPOSE instruction makes a port in the container available to the host system and other containers. While it is possible to specify that a port should be exposed with a <literal>podman run</literal> invocation, using the EXPOSE instruction in a <literal>Dockerfile</literal> makes it easier for both humans and software to use your image by explicitly declaring the ports your software needs to run:</simpara>
<itemizedlist>
<listitem>
<simpara>Exposed ports show up under <literal>podman ps</literal> associated with containers created from your image.</simpara>
</listitem>
<listitem>
<simpara>Exposed ports are present in the metadata for your image returned by <literal>podman inspect</literal>.</simpara>
</listitem>
<listitem>
<simpara>Exposed ports are linked when you link one container to another.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="_set-environment-variables">
<title>Set environment variables</title>
<simpara>It is good practice to set environment variables with the <literal>ENV</literal> instruction. One example is to set the version of your project. This makes it easy for people to find the version without looking at the <literal>Dockerfile</literal>. Another example is advertising a path on the system that could be used by another process, such as <literal>JAVA_HOME</literal>.</simpara>
</section>
<section xml:id="_avoid-default-passwords">
<title>Avoid default passwords</title>
<simpara>Avoid setting default passwords. Many people extend the image and forget to remove or change the default password. This can lead to security issues if a user in production is assigned a well-known password. Passwords are configurable using an environment variable instead.</simpara>
<simpara>If you do choose to set a default password, ensure that an appropriate warning message is displayed when the container is started. The message should inform the user of the value of the default password and explain how to change it, such as what environment variable to set.</simpara>
</section>
<section xml:id="_avoid-sshd">
<title>Avoid sshd</title>
<simpara>It is best to avoid running <literal>sshd</literal> in your image. You can use the <literal>podman exec</literal> or <literal>docker exec</literal> command to access containers that are running on the local host. Alternatively, you can use the <literal>oc exec</literal> command or the <literal>oc rsh</literal> command to access containers that are running on the OpenShift Container Platform cluster. Installing and running <literal>sshd</literal> in your image opens up additional vectors for attack and requirements for security patching.</simpara>
</section>
<section xml:id="_use-volumes-for-persistent-data">
<title>Use volumes for persistent data</title>
<simpara>Images use a <link xlink:href="https://docs.docker.com/reference/builder/#volume">volume</link> for persistent data. This way OpenShift Container Platform mounts the network storage to the node running the container, and if the container moves to a new node the storage is reattached to that node. By using the volume for all persistent storage needs, the content is preserved even if the container is restarted or moved. If your image writes data to arbitrary locations within the container, that content could not be preserved.</simpara>
<simpara>All data that needs to be preserved even after the container is destroyed must be written to a volume. Container engines support a <literal>readonly</literal> flag for containers, which can be used to strictly enforce good practices about not writing data to ephemeral storage in a container. Designing your image around that capability now makes it easier to take advantage of it later.</simpara>
<simpara>Explicitly defining volumes in your <literal>Dockerfile</literal> makes it easy for consumers of the image to understand what volumes they must define when running your image.</simpara>
<simpara>See the <link xlink:href="https://kubernetes.io/docs/concepts/storage/volumes/">Kubernetes
documentation</link> for more information on how volumes are used in OpenShift Container Platform.</simpara>
<note>
<simpara>Even with persistent volumes, each instance of your image has its own volume, and the filesystem is not shared between instances. This means the volume cannot be used to share state in a cluster.</simpara>
</note>
</section>
</section>
<section xml:id="images-create-guide-openshift_create-images">
<title>OpenShift Container Platform-specific guidelines</title>
<simpara role="_abstract">Use the integrated image-building capabilities of OpenShift Container Platform to create, manage, and deploy reproducible container images directly from source code or Dockerfiles.</simpara>
<section xml:id="enable-images-for-source-to-image_create-images">
<title>Enable images for source-to-image (S2I)</title>
<simpara>For images that are intended to run application code provided by a third party, such as a Ruby image designed to run Ruby code provided by a developer, you can enable your image to work with the <link xlink:href="https://github.com/openshift/source-to-image">Source-to-Image (S2I)</link> build tool. S2I is a framework that makes it easy to write images that take application source code as an input and produce a new image that runs the assembled application as output.</simpara>
</section>
<section xml:id="use-uid_create-images">
<title>Support arbitrary user ids</title>
<simpara>By default, OpenShift Container Platform runs containers using an arbitrarily assigned user ID. This provides additional security against processes escaping the container due to a container engine vulnerability and thereby achieving escalated permissions on the host node.</simpara>
<simpara>For an image to support running as an arbitrary user, directories and files that are written to by processes in the image must be owned by the root group and be read/writable by that group. Files to be executed must also have group execute permissions.</simpara>
<simpara>Adding the following to your Dockerfile sets the directory and file permissions to allow users in the root group to access them in the built image:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">RUN chgrp -R 0 /some/directory &amp;&amp; \
    chmod -R g=u /some/directory</programlisting>
<simpara>Because the container user is always a member of the root group, the container user can read and write these files.</simpara>
<warning>
<simpara>Care must be taken when altering the directories and file permissions of the sensitive areas of a container. If applied to sensitive areas, such as the <literal>/etc/passwd</literal> file, such changes can allow the modification of these files by unintended users, potentially exposing the container or host. CRI-O supports the insertion of arbitrary user IDs into a container&#8217;s <literal>/etc/passwd</literal> file. As such, changing permissions is never required.</simpara>
<simpara>Additionally, the <literal>/etc/passwd</literal> file should not exist in any container image. If it does, the CRI-O container runtime will fail to inject a random UID into the <literal>/etc/passwd</literal> file. In such cases, the container might face challenges in resolving the active UID. Failing to meet this requirement could impact the functionality of certain containerized applications.</simpara>
</warning>
<simpara>In addition, the processes running in the container must not listen on privileged ports, ports below 1024, since they are not running as a privileged user.</simpara>
</section>
<section xml:id="use-services_create-images">
<title>Use services for inter-image communication</title>
<simpara>For cases where your image needs to communicate with a service provided by another image, such as a web front end image that needs to access a database image to store and retrieve data, your image consumes an OpenShift Container Platform service. Services provide a static endpoint for access which does not change as containers are stopped, started, or moved. In addition, services provide load balancing for requests.</simpara>
</section>
<section xml:id="provide-common-libraries_create-images">
<title>Provide common libraries</title>
<simpara>For images that are intended to run application code provided by a third party, ensure that your image contains commonly used libraries for your platform. In particular, provide database drivers for common databases used with your platform. For example, provide JDBC drivers for MySQL and PostgreSQL if you are creating a Java framework image. Doing so prevents the need for common dependencies to be downloaded during application assembly time, speeding up application image builds. It also simplifies the work required by application developers to ensure all of their dependencies are met.</simpara>
</section>
<section xml:id="use-env-vars_create-images">
<title>Use environment variables for configuration</title>
<simpara>Users of your image are able to configure it without having to create a downstream image based on your image. This means that the runtime configuration is handled using environment variables. For a simple configuration, the running process can consume the environment variables directly. For a more complicated configuration or for runtimes which do not support this, configure the runtime by defining a template configuration file that is processed during startup. During this processing, values supplied using environment variables can be substituted into the configuration file or used to make decisions about what options to set in the configuration file.</simpara>
<note>
<simpara>It is also possible and recommended to pass secrets such as certificates and keys into the container using environment variables. This ensures that the secret values do not end up committed in an image and leaked into a container image registry.</simpara>
</note>
<simpara>Providing environment variables allows consumers of your image to customize behavior, such as database settings, passwords, and performance tuning, without having to introduce a new layer on top of your image. Instead, they can simply define environment variable values when defining a pod and change those settings without rebuilding the image.</simpara>
<simpara>For extremely complex scenarios, configuration can also be supplied using volumes that would be mounted into the container at runtime. However, if you elect to do it this way you must ensure that your image provides clear error messages on startup when the necessary volume or configuration is not present.</simpara>
<simpara>This topic is related to the Using Services for Inter-image Communication topic in that configuration like datasources are defined in terms of environment variables that provide the service endpoint information. This allows an application to dynamically consume a datasource service that is defined in the OpenShift Container Platform environment without modifying the application image.</simpara>
<simpara>In addition, tuning is done by inspecting the <literal>cgroups</literal> settings for the container. This allows the image to tune itself to the available memory, CPU, and other resources. For example, Java-based images tune their heap based on the <literal>cgroup</literal> maximum memory parameter to ensure they do not exceed the limits and get an out-of-memory error.</simpara>
</section>
<section xml:id="set-image-metadata_create-images">
<title>Set image metadata</title>
<simpara>Defining image metadata helps OpenShift Container Platform better consume your container images, allowing OpenShift Container Platform to create a better experience for developers using your image. For example, you can add metadata to provide helpful descriptions of your image, or offer suggestions on other images that are needed.</simpara>
</section>
<section xml:id="clustering_create-images">
<title>Clustering</title>
<simpara>You must fully understand what it means to run multiple instances of your image. In the simplest case, the load balancing function of a service handles routing traffic to all instances of your image. However, many frameworks must share information to perform leader election or failover state; for example, in session replication.</simpara>
<simpara>Consider how your instances accomplish this communication when running in OpenShift Container Platform. Although pods can communicate directly with each other, their IP addresses change anytime the pod starts, stops, or is moved. Therefore, it is important for your clustering scheme to be dynamic.</simpara>
</section>
<section xml:id="logging_create-images">
<title>Logging</title>
<simpara>It is best to send all logging to standard out. OpenShift Container Platform collects standard out from containers and sends it to the centralized logging service where it can be viewed. If you must separate log content, prefix the output with an appropriate keyword, which makes it possible to filter the messages.</simpara>
<simpara>If your image logs to a file, users must use manual operations to enter the running container and retrieve or view the log file.</simpara>
</section>
<section xml:id="liveness-and-readiness-probes_create-images">
<title>Liveness and readiness probes</title>
<simpara>Document example liveness and readiness probes that can be used with your image. These probes allow users to deploy your image with confidence that traffic is not be routed to the container until it is prepared to handle it, and that the container is restarted if the process gets into an unhealthy state.</simpara>
</section>
<section xml:id="templates_create-images">
<title>Templates</title>
<simpara>Consider providing an example template with your image. A template gives users an easy way to quickly get your image deployed with a working configuration. Your template must include the liveness and readiness probes you documented with the image, for completeness.</simpara>
</section>
</section>
</section>
<section xml:id="images-create-metadata_create-images">
<title>Including metadata in images</title>
<simpara role="_abstract">Define comprehensive image metadata during creation to ensure OpenShift Container Platform correctly configures image runtime settings and tracks image lineage and compliance. This helps to provide a better experience for developers using your image.</simpara>
<simpara>For example, you can add metadata to provide helpful descriptions of your image, or offer suggestions on other images that may also be needed.</simpara>
<simpara>This topic only defines the metadata needed by the current set of use cases. Additional metadata or use cases may be added in the future.</simpara>
<section xml:id="_defining-image-metadata">
<title>Defining image metadata</title>
<simpara>You can use the <literal>LABEL</literal> instruction in a <literal>Dockerfile</literal> to define image metadata. Labels are similar to environment variables in that they are key value pairs attached to an image or a container. Labels are different from environment variable in that they are not visible to the running application and they can also be used for fast look-up of images and containers.</simpara>
<simpara><link xlink:href="https://docs.docker.com/engine/reference/builder/#label">Docker
documentation</link> for more information on the <literal>LABEL</literal> instruction.</simpara>
<simpara>The label names are typically namespaced. The namespace is set accordingly to reflect the project that is going to pick up the labels and use them. For OpenShift Container Platform the namespace is set to <literal>io.openshift</literal> and for Kubernetes the namespace is <literal>io.k8s</literal>.</simpara>
<simpara>See the <link xlink:href="https://docs.docker.com/engine/userguide/labels-custom-metadata">Docker custom metadata</link> documentation for details about the format.</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Supported Metadata</title>
<tgroup cols="2">
<colspec colname="col_1" colwidth="27.2727*"/>
<colspec colname="col_2" colwidth="72.7273*"/>
<thead>
<row>
<entry align="left" valign="top">Variable</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>io.openshift.tags</literal></simpara></entry>
<entry align="left" valign="top"><simpara>This label contains a list of tags represented as a list of comma-separated string values. The tags are the way to categorize the container images into broad areas of functionality. Tags help UI and generation tools to suggest relevant container images during the application creation process.</simpara>
<screen>LABEL io.openshift.tags   mongodb,mongodb24,nosql</screen></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>io.openshift.wants</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Specifies a list of tags that the generation tools and the UI uses to provide relevant suggestions if you do not have the container images with specified tags already. For example, if the container image wants <literal>mysql</literal> and <literal>redis</literal> and you do not have the container image with <literal>redis</literal> tag, then UI  can suggest you to add this image into your deployment.</simpara>
<screen>LABEL io.openshift.wants   mongodb,redis</screen></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>io.k8s.description</literal></simpara></entry>
<entry align="left" valign="top"><simpara>This label can be used to give the container image consumers more detailed information about the service or functionality this image provides. The UI can then use this description together with the container image name to provide more human friendly information to end users.</simpara>
<screen>LABEL io.k8s.description The MySQL 5.5 Server with master-slave replication support</screen></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>io.openshift.non-scalable</literal></simpara></entry>
<entry align="left" valign="top"><simpara>An image can use this variable to suggest that it does not support scaling. The UI then communicates this to consumers of that image. Being not-scalable means that the value of <literal>replicas</literal> should initially not be set higher than <literal>1</literal>.</simpara>
<screen>LABEL io.openshift.non-scalable     true</screen></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>io.openshift.min-memory</literal> and <literal>io.openshift.min-cpu</literal></simpara></entry>
<entry align="left" valign="top"><simpara>This label suggests how much resources the container image needs to work properly. The UI can warn the user that deploying this container image may exceed their user quota. The values must be compatible with Kubernetes quantity.</simpara>
<screen>LABEL io.openshift.min-memory 16Gi
LABEL io.openshift.min-cpu     4</screen></entry>
</row>
</tbody>
</tgroup>
</table>
</section>
</section>
<section xml:id="images-create-s2i_create-images">
<title>Creating images from source code with source-to-image</title>
<simpara role="_abstract">Source-to-image (S2I) is a framework that makes it easy to write images that take application source code as an input and produce a new image that runs the assembled application as output.</simpara>
<simpara>The main advantage of using S2I for building reproducible container images is the ease of use for developers. As a builder image author, you must understand two basic concepts in order for your images to provide the best S2I performance, the build process and S2I scripts.</simpara>
<section xml:id="images-create-s2i-build_create-images">
<title>Understanding the source-to-image build process</title>
<simpara role="_abstract">Leverage the Source-to-image (S2I) process in OpenShift Container Platform to seamlessly transform application source code into ready-to-run, reproducible container images.</simpara>
<simpara>The build process consists of the following three fundamental elements, which are combined into a final container image:</simpara>
<itemizedlist>
<listitem>
<simpara>Sources</simpara>
</listitem>
<listitem>
<simpara>S2I scripts</simpara>
</listitem>
<listitem>
<simpara>Builder image</simpara>
</listitem>
</itemizedlist>
<simpara>S2I generates a Dockerfile with the builder image as the first <literal>FROM</literal> instruction. The Dockerfile generated by S2I is then passed to Buildah.</simpara>
</section>
<section xml:id="images-create-s2i-scripts_create-images">
<title>How to write source-to-image scripts</title>
<simpara role="_abstract">Define mandatory Source-to-image (S2I) scripts, such as <literal>assemble</literal> and <literal>run</literal> to enable OpenShift Container Platform to build, customize, and execute highly reproducible container images.</simpara>
<simpara>You can write S2I scripts in any programming language, as long as the scripts are executable inside the builder image. S2I supports multiple options providing <literal>assemble</literal>/<literal>run</literal>/<literal>save-artifacts</literal> scripts. All of these locations are checked on each build in the following order:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>A script specified in the build configuration.</simpara>
</listitem>
<listitem>
<simpara>A script found in the application source <literal>.s2i/bin</literal> directory.</simpara>
</listitem>
<listitem>
<simpara>A script found at the default image URL with the <literal>io.openshift.s2i.scripts-url</literal> label.</simpara>
</listitem>
</orderedlist>
<simpara>Both the <literal>io.openshift.s2i.scripts-url</literal> label specified in the image and the script specified in a build configuration can take one of the following forms:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>image:///path_to_scripts_dir</literal>: absolute path inside the image to a directory where the S2I scripts are located.</simpara>
</listitem>
<listitem>
<simpara><literal>file:///path_to_scripts_dir</literal>: relative or absolute path to a directory on the host where the S2I scripts are located.</simpara>
</listitem>
<listitem>
<simpara><literal>http(s)://path_to_scripts_dir</literal>: URL to a directory where the S2I scripts are located.</simpara>
</listitem>
</itemizedlist>
<table frame="all" rowsep="1" colsep="1">
<title>S2I scripts</title>
<tgroup cols="2">
<colspec colname="col_1" colwidth="27.2727*"/>
<colspec colname="col_2" colwidth="72.7273*"/>
<thead>
<row>
<entry align="left" valign="top">Script</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>assemble</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The <literal>assemble</literal> script builds the application artifacts from a source and places them into appropriate directories inside the image. This script is required. The workflow for this script is:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Optional: Restore build artifacts. If you want to support incremental builds, make sure to define <literal>save-artifacts</literal> as well.</simpara>
</listitem>
<listitem>
<simpara>Place the application source in the desired location.</simpara>
</listitem>
<listitem>
<simpara>Build the application artifacts.</simpara>
</listitem>
<listitem>
<simpara>Install the artifacts into locations appropriate for them to run.</simpara>
</listitem>
</orderedlist></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>run</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The <literal>run</literal> script executes your application. This script is required.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>save-artifacts</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The <literal>save-artifacts</literal> script gathers all dependencies that can speed up the build processes that follow. This script is optional. For example:</simpara>
<itemizedlist>
<listitem>
<simpara>For Ruby, <literal>gems</literal> installed by Bundler.</simpara>
</listitem>
<listitem>
<simpara>For Java, <literal>.m2</literal> contents.</simpara>
</listitem>
</itemizedlist>
<simpara>These dependencies are gathered into a <literal>tar</literal> file and streamed to the standard output.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>usage</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The <literal>usage</literal> script allows you to inform the user how to properly use your image. This script is optional.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>test/run</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The <literal>test/run</literal> script allows you to create a process to check if the image is working correctly. This script is optional. The proposed flow of that process is:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Build the image.</simpara>
</listitem>
<listitem>
<simpara>Run the image to verify the <literal>usage</literal> script.</simpara>
</listitem>
<listitem>
<simpara>Run <literal>s2i build</literal> to verify the <literal>assemble</literal> script.</simpara>
</listitem>
<listitem>
<simpara>Optional: Run <literal>s2i build</literal> again to verify the <literal>save-artifacts</literal> and <literal>assemble</literal> scripts save and restore artifacts functionality.</simpara>
</listitem>
<listitem>
<simpara>Run the image to verify the test application is working.</simpara>
</listitem>
</orderedlist>
<note>
<simpara>The suggested location to put the test application built by your <literal>test/run</literal> script is the <literal>test/test-app</literal> directory in your image repository.</simpara>
</note></entry>
</row>
</tbody>
</tgroup>
</table>
<simpara><emphasis role="strong">Example S2I scripts</emphasis></simpara>
<simpara>The following example S2I scripts are written in Bash. Each example assumes its <literal>tar</literal> contents are unpacked into the <literal>/tmp/s2i</literal> directory.</simpara>
<formalpara>
<title><literal>assemble</literal> script:</title>
<para>
<programlisting language="bash" linenumbering="unnumbered">#!/bin/bash

# restore build artifacts
if [ "$(ls /tmp/s2i/artifacts/ 2&gt;/dev/null)" ]; then
    mv /tmp/s2i/artifacts/* $HOME/.
fi

# move the application source
mv /tmp/s2i/src $HOME/src

# build application artifacts
pushd ${HOME}
make all

# install the artifacts
make install
popd</programlisting>
</para>
</formalpara>
<formalpara>
<title><literal>run</literal> script:</title>
<para>
<programlisting language="bash" linenumbering="unnumbered">#!/bin/bash

# run the application
/opt/application/run.sh</programlisting>
</para>
</formalpara>
<formalpara>
<title><literal>save-artifacts</literal> script:</title>
<para>
<programlisting language="bash" linenumbering="unnumbered">#!/bin/bash

pushd ${HOME}
if [ -d deps ]; then
    # all deps contents to tar stream
    tar cf - deps
fi
popd</programlisting>
</para>
</formalpara>
<formalpara>
<title><literal>usage</literal> script:</title>
<para>
<programlisting language="bash" linenumbering="unnumbered">#!/bin/bash

# inform the user how to use the image
cat &lt;&lt;EOF
This is a S2I sample builder image, to use it, install
https://github.com/openshift/source-to-image
EOF</programlisting>
</para>
</formalpara>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://blog.openshift.com/create-s2i-builder-image/">S2I Image Creation Tutorial</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="images-test-s2i_create-images">
<title>About testing source-to-image images</title>
<simpara role="_abstract">Verify your Source-to-image (S2I) environment and resulting application images to ensure successful, reproducible container deployment within OpenShift Container Platform.</simpara>
<simpara>As an S2I builder image author, you can test your S2I image
locally and use the OpenShift Container Platform build system for automated testing and
continuous integration.</simpara>
<simpara>S2I requires the
<literal>assemble</literal> and <literal>run</literal> scripts to be present to successfully run
the S2I build. Providing the <literal>save-artifacts</literal> script reuses the build
artifacts, and providing the <literal>usage</literal> script ensures that usage information is
printed to console when someone runs the container image outside of the S2I.</simpara>
<simpara>The goal of testing an S2I image is to make sure that all of these described
commands work properly, even if the base container image has changed or the tooling
used by the commands was updated.</simpara>
<section xml:id="images-test-s2i-testing-requirements_create-images">
<title>Understanding testing requirements</title>
<simpara>The standard location for the <literal>test</literal> script is <literal>test/run</literal>. This script is
invoked by the OpenShift Container Platform S2I image builder and it could be a simple Bash
script or a static Go binary.</simpara>
<simpara>The <literal>test/run</literal> script performs the S2I build, so you must have the S2I binary
available in your <literal>$PATH</literal>. If required, follow the installation instructions
in the
<link xlink:href="https://github.com/openshift/source-to-image/blob/master/README.md#installation">S2I
README</link>.</simpara>
<simpara>S2I combines the application source code and builder image, so to test
it you need a sample application source to verify that the source successfully
transforms into a runnable container image. The sample application should be simple,
but it should exercise the crucial steps of <literal>assemble</literal> and <literal>run</literal> scripts.</simpara>
</section>
<section xml:id="images-test-s2i-generating-scripts-and-tools_create-images">
<title>Generating scripts and tools</title>
<simpara>The S2I tooling comes with powerful generation tools to speed up the process of
creating a new S2I image. The <literal>s2i create</literal> command produces all the necessary S2I
scripts and testing tools along with the <literal>Makefile</literal>:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ s2i create &lt;image_name&gt; &lt;destination_directory&gt;</programlisting>
<simpara>The generated <literal>test/run</literal> script must be adjusted to be
useful, but it provides a good starting point to begin developing.</simpara>
<note>
<simpara>The <literal>test/run</literal> script produced by the <literal>s2i create</literal> command requires that the sample application sources are inside the <literal>test/test-app</literal> directory.</simpara>
</note>
</section>
<section xml:id="images-test-s21-testing-locally_create-images">
<title>Testing locally</title>
<simpara>The easiest way to run the S2I image tests locally is to use the generated
<literal>Makefile</literal>.</simpara>
<simpara>If you did not use the <literal>s2i create</literal> command, you can copy the
following <literal>Makefile</literal> template and replace the <literal>IMAGE_NAME</literal> parameter with
your image name.</simpara>
<formalpara>
<title>Sample <literal>Makefile</literal></title>
<para>
<screen>IMAGE_NAME = openshift/ruby-20-centos7
CONTAINER_ENGINE := $(shell command -v podman 2&gt; /dev/null | echo docker)

build:
	${CONTAINER_ENGINE} build -t $(IMAGE_NAME) .

.PHONY: test
test:
	${CONTAINER_ENGINE} build -t $(IMAGE_NAME)-candidate .
	IMAGE_NAME=$(IMAGE_NAME)-candidate test/run</screen>
</para>
</formalpara>
</section>
<section xml:id="images-test-s21-basic-testing-workflow_create-images">
<title>Basic testing workflow</title>
<simpara>The <literal>test</literal> script assumes you have already built the image you want to
test. If required, first build the S2I image. Run one of the following commands:</simpara>
<itemizedlist>
<listitem>
<simpara>If you use Podman, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ podman build -t &lt;builder_image_name&gt;</programlisting>
</listitem>
<listitem>
<simpara>If you use Docker, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ docker build -t &lt;builder_image_name&gt;</programlisting>
</listitem>
</itemizedlist>
<simpara>The following steps describe the default workflow to test S2I image builders:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Verify the <literal>usage</literal> script is working:</simpara>
<itemizedlist>
<listitem>
<simpara>If you use Podman, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ podman run &lt;builder_image_name&gt; .</programlisting>
</listitem>
<listitem>
<simpara>If you use Docker, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ docker run &lt;builder_image_name&gt; .</programlisting>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Build the image:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ s2i build file:///path-to-sample-app _&lt;BUILDER_IMAGE_NAME&gt;_ _&lt;OUTPUT_APPLICATION_IMAGE_NAME&gt;_</programlisting>
</listitem>
<listitem>
<simpara>Optional: if you support <literal>save-artifacts</literal>, run step 2 once again to
verify that saving and restoring artifacts works properly.</simpara>
</listitem>
<listitem>
<simpara>Run the container:</simpara>
<itemizedlist>
<listitem>
<simpara>If you use Podman, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ podman run &lt;output_application_image_name&gt;</programlisting>
</listitem>
<listitem>
<simpara>If you use Docker, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ docker run &lt;output_application_image_name&gt;</programlisting>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Verify the container is running and the application is responding.</simpara>
</listitem>
</orderedlist>
<simpara>Running these steps is generally enough to tell if the builder image is
working as expected.</simpara>
</section>
<section xml:id="images-test-s21-using-openshift-for-building-the-image_create-images">
<title>Using OpenShift Container Platform for building the image</title>
<simpara>Once you have a <literal>Dockerfile</literal> and the other artifacts that make up your new
S2I builder image, you can put them in a git repository and use OpenShift Container Platform
to build and push the image. Define a Docker build that points
to your repository.</simpara>
<simpara>If your OpenShift Container Platform instance is hosted on a public IP address, the build can
be triggered each time you push into your S2I builder image GitHub repository.</simpara>
<simpara>You can also use the <literal>ImageChangeTrigger</literal> to trigger a rebuild of your applications that are
based on the S2I builder image you updated.</simpara>
</section>
</section>
</chapter>
<chapter xml:id="_managing-images">
<title>Managing images</title>
<section xml:id="managing-images-overview">
<title>Managing images overview</title>
<simpara role="_abstract">Image streams in OpenShift Container Platform provide a layer of abstraction over container images, enabling automation for your CI/CD pipelines. You can configure builds and deployments to watch image streams and automatically trigger new builds or deployments when images are updated.</simpara>
<simpara>The main advantage of using image streams is the automation they enable for your continuous integration and continuous delivery (CI/CD) pipelines. For example:</simpara>
<itemizedlist>
<listitem>
<simpara>Image streams allow OpenShift Container Platform resources like Builds and Deployments to "watch" them.</simpara>
</listitem>
<listitem>
<simpara>When a new image is added to the stream, or when an existing tag is modified to point to a new image, the watching resources receive notifications.</simpara>
</listitem>
<listitem>
<simpara>When notifications are received, the watching resources can automatically react by performing a new build or a new deployment.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="tagging-images">
<title>Tagging images</title>
<simpara role="_abstract">Image tags identify specific versions of container images in image streams. You can use image tags to organize images and control which versions your builds and deployments use.</simpara>
<section xml:id="images-tag_tagging-images">
<title>Understanding image tags in image streams</title>
<simpara role="_abstract">Image tags in OpenShift Container Platform help you organize, identify, and reference specific versions of container images in image streams. Tags are human-readable labels that act as pointers to particular image layers and digests.</simpara>
<simpara>Tags function as mutable pointers within an image stream. When a new image is imported or tagged into the stream, the tag is updated to point to the new image&#8217;s immutable SHA digest. A single image digest can have multiple tags simultaneously assigned to it. For example, the <literal>:v3.11.59-2</literal> and <literal>:latest</literal> tags are assigned to the same image digest.</simpara>
<simpara>Tags offer two main benefits:</simpara>
<itemizedlist>
<listitem>
<simpara>Tags serve as the primary mechanism for builds and deployments to request a specific version of an image from an image stream.</simpara>
</listitem>
<listitem>
<simpara>Tags help maintain clarity and allow for easy promotion of images between environments. For example, you can promote an image from the <literal>:test</literal> tag to the <literal>:prod</literal> tag.</simpara>
</listitem>
</itemizedlist>
<simpara>While image tags are primarily used for referencing images in configurations, OpenShift Container Platform provides the <literal>oc tag</literal> command for managing tags directly within image streams. This command is similar to the <literal>podman tag</literal> or <literal>docker tag</literal> commands, but it operates on image streams instead of directly on local images. It is used to create a new tag pointer or update an existing tag pointer within an image stream to point to a new image.</simpara>
<simpara>Image tags are appended to the image name or image stream name by using a colon (<literal>:</literal>) as a separator.</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="3">
<colspec colname="col_1" colwidth="25*"/>
<colspec colname="col_2" colwidth="25*"/>
<colspec colname="col_3" colwidth="50*"/>
<thead>
<row>
<entry align="left" valign="top">Context</entry>
<entry align="left" valign="top">Syntax Format</entry>
<entry align="left" valign="top">Example</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">External Registry</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>&lt;registry_path&gt;:&lt;tag&gt;</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>registry.access.redhat.com/openshift3/jenkins-2-rhel7:v3.11.59-2</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Local Image Stream</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>&lt;image_stream_name&gt;:&lt;tag&gt;</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>jenkins:latest</literal></simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
</section>
<section xml:id="images-tagging-conventions_tagging-images">
<title>Image tag conventions</title>
<simpara role="_abstract">Image tag naming conventions in OpenShift Container Platform provide guidelines for creating tags that enable effective image pruning and maintain manageable image streams. Use consistent naming patterns to avoid tags that point to single revisions and never update.</simpara>
<simpara>Tags that are too specific effectively pin the tag to a single image revision that is never updated. For example, if you create a tag named <literal>v2.0.1-may-2019</literal>, the tag points to just one revision of an image and is never updated. If you use default image pruning options, such an image is never removed.</simpara>
<simpara>To ensure proper garbage collection, use broader, more generic tags that are designed to be updated when a new image revision is built. The following table provides some recommended tagging conventions using the format <literal>&lt;image_name&gt;:&lt;image_tag&gt;</literal>.</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Image tag naming conventions</title>
<?dbhtml table-width="50%"?>
<?dbfo table-width="50%"?>
<?dblatex table-width="50%"?>
<tgroup cols="2">
<colspec colname="col_1" colwidth="106.25*"/>
<colspec colname="col_2" colwidth="106.25*"/>
<thead>
<row>
<entry align="left" valign="top">Description</entry>
<entry align="left" valign="top">Example</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Major/Minor Version</emphasis> (Ideal for mutable pointers)</simpara></entry>
<entry align="left" valign="top"><simpara><literal>myimage:v2.0</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Full Revision</emphasis> (Often used for tracking, but requires manual pruning)</simpara></entry>
<entry align="left" valign="top"><simpara><literal>myimage:v2.0.1</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Architecture</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>myimage:v2.0-x86_64</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Base image</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>myimage:v1.2-centos7</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Latest</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>myimage:latest</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Latest stable</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara><literal>myimage:stable</literal></simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<note>
<simpara>If your team requires the use of unique, date-specific, or highly revisioned tags like <literal>v2.0.1-may-2019</literal>, you must periodically inspect old and unsupported images and <literal>istags</literal> and remove them. Otherwise, you can experience increasing resource usage caused by retaining old images.</simpara>
</note>
</section>
<section xml:id="images-add-tags-to-imagestreams_tagging-images">
<title>Adding tags to image streams</title>
<simpara role="_abstract">To organize images and create aliases for specific versions or automatically track changes to source tags in OpenShift Container Platform, you can add tags to image streams with the <literal>oc tag</literal> command.</simpara>
<simpara>There are two types of tags available in OpenShift Container Platform:</simpara>
<itemizedlist>
<listitem>
<simpara>Permanent tags: A permanent tag points to a specific image in time. If the permanent tag is in use and the source changes, the tag does not change for the destination.</simpara>
</listitem>
<listitem>
<simpara>Tracking tags: A tracking tag means that the destination tag&#8217;s metadata is updated during the import of the source tag.</simpara>
</listitem>
</itemizedlist>
<simpara>The default behavior creates a permanent tag that is pinned to an image ID.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Optional: Add a tag to an image stream by entering the following command. The default behavior creates a permanent tag that is pinned to an image ID:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;source_reference&gt; &lt;destination_image_stream&gt;:&lt;destination_tag&gt;</programlisting>
<simpara>For example, to configure the <literal>ruby</literal> image stream <literal>static-2.0</literal> tag to always refer to the specific image that the <literal>ruby:2.0</literal> tag points to now, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag ruby:2.0 ruby:static-2.0</programlisting>
<simpara>This creates a new image stream tag named <literal>static-2.0</literal> in the <literal>ruby</literal> image stream. The new tag directly references the image ID that the <literal>ruby:2.0</literal> image stream tag pointed to at the time <literal>oc tag</literal> was run, and the image it points to never changes.</simpara>
</listitem>
<listitem>
<simpara>Optional: Use the <literal>--alias=true</literal> flag to create a tracking tag. This ensures the destination tag automatically updates (tracks) when the source tag changes to point to a new image. For example, to ensure that the <literal>ruby:latest</literal> tag always reflects whatever image is currently tagged as <literal>ruby:2.0</literal>, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag --alias=true ruby:2.0 ruby:latest</programlisting>
<note>
<simpara>A Tracking Tag created with <literal>--alias=true</literal> automatically updates its image ID whenever the source tag changes. Use the <literal>latest</literal> or <literal>stable</literal> tracking tags for creating common, long-lived aliases. This tracking behavior only works correctly within a single image stream. Trying to create a cross-image stream alias produces an error.</simpara>
</note>
</listitem>
<listitem>
<simpara>Optional: Use the <literal>--scheduled=true</literal> flag to have the destination tag be refreshed, or re-imported, periodically. The period is configured globally at the system level. For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;source_reference&gt; &lt;destination_image_stream&gt;:&lt;destination_tag&gt; --scheduled=true</programlisting>
</listitem>
<listitem>
<simpara>Optional: Use the <literal>--reference</literal> flag to create an image stream tag that is not imported. The tag permanently points to the source location, regardless of changes to the source image. For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;source_reference&gt; &lt;destination_image_stream&gt;:&lt;destination_tag&gt; --reference</programlisting>
</listitem>
<listitem>
<simpara>Optional. Use the <literal>--insecure</literal> flag if the source registry is not secured with a valid HTTPS certificate. This flag tells the image stream to skip certificate verification during the import progress. For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;source_reference&gt; &lt;destination_image_stream&gt;:&lt;destination_tag&gt; --insecure</programlisting>
</listitem>
<listitem>
<simpara>Optional: Use the <literal>--reference-policy=local</literal> flag to instruct OpenShift Container Platform to always fetch the tagged image from the integrated registry. The registry uses the pull-through feature to serve the image to the client. By default, the image blobs are mirrored locally by the registry. As a result, they can be pulled more quickly the next time they are needed. The  <literal>--reference-policy=local</literal> flag also allows for pulling from insecure registries without a need to supply the <literal>--insecure</literal> flag to the container runtime provided that the image stream has an insecure annotation or the tag has an insecure import policy. For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;source_reference&gt; &lt;destination_image_stream&gt;:&lt;destination_tag&gt; --reference-policy=local</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-remove-tag-imagestream_tagging-images">
<title>Removing tags from image streams</title>
<simpara role="_abstract">To keep your image streams clean and maintain organized image references in OpenShift Container Platform, you can remove unused or outdated image stream tags. Remove tags by using the <literal>oc delete istag</literal> or <literal>oc tag -d</literal> commands.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Remove a tag from an image stream by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc delete istag/&lt;name&gt;:&lt;tag&gt;</programlisting>
<simpara>For example, to remove the <literal>ruby:latest</literal> tag from the <literal>ruby</literal> image stream, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc delete istag/ruby:latest</programlisting>
</listitem>
<listitem>
<simpara>Alternatively, you can remove a tag using the <literal>oc tag -d</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag -d &lt;name&gt;:&lt;tag&gt;</programlisting>
<simpara>For example, to remove the <literal>ruby:latest</literal> tag from the <literal>ruby</literal> image stream, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag -d ruby:latest</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-referencing-images-imagestreams_tagging-images">
<title>Using image stream reference syntax</title>
<simpara role="_abstract">To ensure that your builds and deployments use the intended image version in OpenShift Container Platform, you must use the correct reference syntax format.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>To reference an image by a mutable tag (<literal>ImageStreamTag</literal>) from an image stream within your cluster, use the <literal>&lt;image_stream_name&gt;:&lt;tag&gt;</literal> format in your build or deployment. For example:</simpara>
<programlisting language="yaml" linenumbering="unnumbered"># ...
spec:
  containers:
    - name: my-app
      image: &lt;image_stream_name&gt;:&lt;tag&gt;</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>spec.containers.image</literal></term>
<listitem>
<simpara>Specifies the image to use from the image stream. For example, <literal>ruby:2.0</literal>.</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
<listitem>
<simpara>To reference a specific, immutable image ID, or digest, within an image stream, use the <literal>&lt;image_stream_name&gt;@&lt;image_id&gt;</literal> format in your build or deployment. For example:</simpara>
<programlisting language="yaml" linenumbering="unnumbered"># ...
spec:
  containers:
    - name: my-app
      image: &lt;image_stream_name&gt;@&lt;image_id&gt;</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>spec.containers.image</literal></term>
<listitem>
<simpara>Specifies the image to use from the image stream. For example, <literal>ruby@sha256:3a335d7d8a452970c5b4054ad7118ff134b3a6b50a2bb6d0c07c746e8986b28e</literal>.</simpara>
</listitem>
</varlistentry>
</variablelist>
<note>
<simpara>Using the image ID with the <literal>@id</literal> syntax ensures your configuration always uses the exact same image, even if the tag is later updated to point to a different image.</simpara>
</note>
</listitem>
<listitem>
<simpara>To reference an image from an external registry by using the DockerImage format, use the standard Docker pull specification: <literal>&lt;registry&gt;/&lt;namespace&gt;/&lt;image_name&gt;:&lt;tag&gt;</literal>. For example:</simpara>
<programlisting language="yaml" linenumbering="unnumbered"># ...
spec:
  source:
    type: Dockerfile
  strategy:
    type: Docker
    dockerStrategy:
      from:
        kind: DockerImage
        name: &lt;registry&gt;/&lt;namespace&gt;/&lt;image_name&gt;:&lt;tag&gt;</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>spec.strategy.dockerStrategy.from.name</literal></term>
<listitem>
<simpara>Specifies the image to use from the external registry. For example, <literal>registry.redhat.io/rhel7:latest</literal>.</simpara>
</listitem>
</varlistentry>
</variablelist>
<note>
<simpara>When no tag is specified in a <literal>DockerImage</literal> reference, the <literal>latest</literal> tag is assumed.</simpara>
</note>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-reference-types_tagging-images">
<title>Understanding image stream reference types</title>
<simpara role="_abstract">By using image streams in OpenShift Container Platform, you can reference container images by using different reference types. These reference types define which specific image version your builds and deployments use.</simpara>
<simpara><literal>ImageStreamImage</literal> objects are automatically created in OpenShift Container Platform when you import or tag an image into the image stream. You never have to explicitly define an <literal>ImageStreamImage</literal> object in any image stream definition that you use to create image streams.</simpara>
<note>
<simpara>Example image stream definitions often contain definitions of <literal>ImageStreamTag</literal> and references to <literal>DockerImage</literal>, but never contain definitions of <literal>ImageStreamImage</literal>.</simpara>
</note>
<table frame="all" rowsep="1" colsep="1">
<title>Imagestream reference types</title>
<tgroup cols="3">
<colspec colname="col_1" colwidth="25*"/>
<colspec colname="col_2" colwidth="50*"/>
<colspec colname="col_3" colwidth="25*"/>
<thead>
<row>
<entry align="left" valign="top">Reference Type</entry>
<entry align="left" valign="top">Description</entry>
<entry align="left" valign="top">Syntax Examples</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>ImageStreamTag</literal></simpara></entry>
<entry align="left" valign="top"><simpara>References or retrieves an image for a given image stream and human-readable tag.</simpara></entry>
<entry align="left" valign="top"><simpara><literal>image_stream_name:tag</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>ImageStreamImage</literal></simpara></entry>
<entry align="left" valign="top"><simpara>References or retrieves an image for a given image stream and immutable SHA ID (digest).</simpara></entry>
<entry align="left" valign="top"><simpara><literal>image_stream_name@id</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>DockerImage</literal></simpara></entry>
<entry align="left" valign="top"><simpara>References or retrieves an image from an external registry. Uses the standard <literal>docker pull</literal> specification.</simpara></entry>
<entry align="left" valign="top"><simpara><literal>openshift/ruby-20-centos7:2.0</literal>, <literal>registry.redhat.io/rhel7:latest</literal>, <literal>centos/ruby-22-centos7@sha256:3a335d7d&#8230;&#8203;</literal></simpara></entry>
</row>
</tbody>
</tgroup>
</table>
</section>
</section>
<section xml:id="image-pull-policy">
<title>Image pull policy</title>
<simpara role="_abstract">To manage image updates and optimize pod startup performance in OpenShift Container Platform, you can configure the <literal>imagePullPolicy</literal> parameter in your container specifications. This setting controls when container images are pulled from registries.</simpara>
<section xml:id="images-image-pull-policy-overview_image-pull-policy">
<title>About the imagePullPolicy parameter</title>
<simpara role="_abstract">To control when OpenShift Container Platform pulls container images from registries or uses locally cached copies when starting containers, you can configure the <literal>imagePullPolicy</literal> parameter. This policy helps you manage image updates and optimize pod startup performance.</simpara>
<simpara>The following table lists the possible values for the <literal>imagePullPolicy</literal> parameter:</simpara>
<table frame="all" rowsep="1" colsep="1">
<title><literal>imagePullPolicy</literal> values</title>
<?dbhtml table-width="50%"?>
<?dbfo table-width="50%"?>
<?dblatex table-width="50%"?>
<tgroup cols="2">
<colspec colname="col_1" colwidth="106.25*"/>
<colspec colname="col_2" colwidth="106.25*"/>
<thead>
<row>
<entry align="left" valign="top">Value</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>Always</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Always pull the image.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>IfNotPresent</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Only pull the image if it does not already exist on the node.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>Never</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Never pull the image.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<simpara>The following example sets the <literal>imagePullPolicy</literal> parameter to <literal>IfNotPresent</literal> for the image tagged <literal>v1.2.3</literal>:</simpara>
<formalpara>
<title>Example <literal>imagePullPolicy</literal> configuration</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: apps/v1
kind: Deployment
# ...
spec:
  # ...
  template:
    spec:
      containers:
      - name: my-app-container
        image: registry.example.com/myapp:v1.2.3
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080</programlisting>
</para>
</formalpara>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>spec.template.spec.containers.image</literal></term>
<listitem>
<simpara>Specifies is the image to use. In this example, the image tag is explicitly set to <literal>v1.2.3</literal>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>spec.template.spec.containers.imagePullPolicy</literal></term>
<listitem>
<simpara>Specifies the policy to use. In this example, the policy is set to <literal>IfNotPresent</literal> because the image tag is not <literal>latest</literal>.</simpara>
</listitem>
</varlistentry>
</variablelist>
<section xml:id="images-image-pull-policy-default-behavior_image-pull-policy">
<title>Omitting the imagePullPolicy parameter</title>
<simpara role="_abstract">When you omit the <literal>imagePullPolicy</literal> parameter, OpenShift Container Platform automatically determines the policy based on the image tag. This default behavior ensures that the <literal>latest</literal> tag always pulls the newest image, while specific version tags use locally cached images when available to improve efficiency.</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="3">
<colspec colname="col_1" colwidth="33.3333*"/>
<colspec colname="col_2" colwidth="33.3333*"/>
<colspec colname="col_3" colwidth="33.3334*"/>
<thead>
<row>
<entry align="left" valign="top">Image tag</entry>
<entry align="left" valign="top"><literal>imagePullPolicy</literal> setting</entry>
<entry align="left" valign="top">Behavior</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>latest</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>Always</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Always pulls the image. This policy helps ensure that the container always uses the latest version of the image.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Any other tag (for example, <literal>v1.2.3</literal>, <literal>stable</literal>, <literal>production</literal>)</simpara></entry>
<entry align="left" valign="top"><simpara><literal>IfNotPresent</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Pull only if necessary. This policy uses the locally cached version of the image if it exists on the node, avoiding unnecessary pulls from the registry.</simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
</section>
</section>
</section>
<section xml:id="using-image-pull-secrets">
<title>Using image pull secrets</title>
<simpara role="_abstract">To authenticate with container registries and pull images across OpenShift Container Platform projects or from secured registries, you can configure and use image pull secrets.</simpara>
<simpara>You first obtain the registry authentication credentials, which are typically found in the <literal>~/.docker/config.json</literal> file for Docker or the <literal>~/.config/containers/auth.json</literal> file for Podman, created by the <link xlink:href="https://console.redhat.com/openshift/install/pull-secret">pull secret from Red Hat OpenShift Cluster Manager</link> process. This content is then used to create or update the global <literal>pullSecret</literal> object within your cluster, allowing access to images from <link xlink:href="https://quay.io/">quay.io</link> and <link xlink:href="https://registry.redhat.io">registry.redhat.io</link>.</simpara>
<note>
<simpara>If you are using the OpenShift image registry and are pulling from image streams located in the same project, then your pod service account should already have the correct permissions. No additional action should be required.</simpara>
</note>
<section xml:id="images-allow-pods-to-reference-images-across-projects_using-image-pull-secrets">
<title>Allowing pods to reference images across projects</title>
<simpara role="_abstract">To allow pods in one OpenShift Container Platform project to reference images from another project, you can bind a service account to the <literal>system:image-puller</literal> role in the target project. Use the <literal>oc policy add-role-to-user</literal> or <literal>oc policy add-role-to-group</literal> command to grant cross-project image access.</simpara>
<note>
<simpara>When you create a pod service account or a namespace, wait until the service account is provisioned with a Docker pull secret. If you create a pod before its service account is fully provisioned, the pod fails to access the OpenShift image registry.</simpara>
</note>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Allow pods in <literal>project-a</literal> to reference images in <literal>project-b</literal> by entering the following command. In this example, the service account <literal>default</literal> in <literal>project-a</literal> is bound to the <literal>system:image-puller</literal> role in <literal>project-b</literal>:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc policy add-role-to-user \
    system:image-puller system:serviceaccount:project-a:default \
    --namespace=project-b</programlisting>
</listitem>
<listitem>
<simpara>Optional: Allow access for any service account in <literal>project-a</literal> by using the <literal>add-role-to-group</literal> flag. For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc policy add-role-to-group \
    system:image-puller system:serviceaccounts:project-a \
    --namespace=project-b</programlisting>
</listitem>
</orderedlist>
</section>
<section xml:id="images-allow-pods-to-reference-images-from-secure-registries_using-image-pull-secrets">
<title>Allowing pods to reference images from other secured registries</title>
<simpara role="_abstract">Pull secrets enable pods in OpenShift Container Platform to authenticate with secured registries and pull container images. Docker and Podman store authentication credentials in configuration files that you can use to create pull secrets for your service accounts.</simpara>
<simpara>The following files store your authentication information if you have previously logged in to a secured or insecure registry:</simpara>
<itemizedlist>
<listitem>
<simpara><emphasis role="strong">Docker</emphasis>: By default, Docker uses <literal>$HOME/.docker/config.json</literal>.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Podman</emphasis>: By default, Podman uses <literal>$HOME/.config/containers/auth.json</literal>.</simpara>
</listitem>
</itemizedlist>
<note>
<simpara>Both Docker and Podman credential files and the associated pull secret can contain multiple references to the same registry if they have unique paths, for example, <literal>quay.io</literal> and <literal>quay.io/&lt;example_repository&gt;</literal>. However, neither Docker nor Podman support multiple entries for the exact same registry path.</simpara>
</note>
<formalpara>
<title>Example <literal>config.json</literal> file</title>
<para>
<programlisting language="json" linenumbering="unnumbered">{
   "auths":{
      "cloud.openshift.com":{
         "auth":"b3Blb=",
         "email":"you@example.com"
      },
      "quay.io":{
         "auth":"b3Blb=",
         "email":"you@example.com"
      },
      "quay.io/repository-main":{
         "auth":"b3Blb=",
         "email":"you@example.com"
      }
   }
}</programlisting>
</para>
</formalpara>
<formalpara>
<title>Example pull secret</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
data:
  .dockerconfigjson: ewogICAiYXV0aHMiOnsKICAgICAgIm0iOnsKICAgICAgIsKICAgICAgICAgImF1dGgiOiJiM0JsYj0iLAogICAgICAgICAiZW1haWwiOiJ5b3VAZXhhbXBsZS5jb20iCiAgICAgIH0KICAgfQp9Cg==
kind: Secret
metadata:
  creationTimestamp: "2021-09-09T19:10:11Z"
  name: pull-secret
  namespace: default
  resourceVersion: "37676"
  uid: e2851531-01bc-48ba-878c-de96cfe31020
type: Opaque</programlisting>
</para>
</formalpara>
<section xml:id="creating-pull-secret_using-image-pull-secrets">
<title>Creating a pull secret</title>
<simpara role="_abstract">To authenticate with container registries in OpenShift Container Platform, you can create pull secrets from existing Docker or Podman authentication files. You can also create secrets by providing registry credentials directly by using the <literal>oc create secret docker-registry</literal> command.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create a secret from an existing authentication file:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>For Docker clients using <literal>.docker/config.json</literal>, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create secret generic &lt;pull_secret_name&gt; \
    --from-file=.dockerconfigjson=&lt;path/to/.docker/config.json&gt; \
    --type=kubernetes.io/dockerconfigjson</programlisting>
</listitem>
<listitem>
<simpara>For Podman clients using <literal>.config/containers/auth.json</literal>, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create secret generic &lt;pull_secret_name&gt; \
     --from-file=&lt;path/to/.config/containers/auth.json&gt; \
     --type=kubernetes.io/podmanconfigjson</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Optional: If you do not already have a Docker credentials file for the secured registry, you can create a secret by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create secret docker-registry &lt;pull_secret_name&gt; \
    --docker-server=&lt;registry_server&gt; \
    --docker-username=&lt;user_name&gt; \
    --docker-password=&lt;password&gt; \
    --docker-email=&lt;email&gt;</programlisting>
</listitem>
</orderedlist>
</section>
<section xml:id="using-pull-secret_using-image-pull-secrets">
<title>Using a pull secret in a workload</title>
<simpara role="_abstract">To allow workloads to pull images from private registries in OpenShift Container Platform, you can link the pull secret to a service account by entering the <literal>oc secrets link</literal> command or by defining it directly in your workload configuration YAML file.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Link the pull secret to a service account by entering the following command. Note that the name of the service account should match the name of the service account that pod uses. The default service account is <literal>default</literal>.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc secrets link default &lt;pull_secret_name&gt; --for=pull</programlisting>
</listitem>
<listitem>
<simpara>Verify the change by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get serviceaccount default -o yaml</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
imagePullSecrets:
- name: default-dockercfg-123456
- name: &lt;pull_secret_name&gt;
kind: ServiceAccount
metadata:
  annotations:
    openshift.io/internal-registry-pull-secret-ref: &lt;internal_registry_pull_secret&gt;
  creationTimestamp: "2025-03-03T20:07:52Z"
  name: default
  namespace: default
  resourceVersion: "13914"
  uid: 9f62dd88-110d-4879-9e27-1ffe269poe3
secrets:
- name: &lt;pull_secret_name&gt;</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Optional: Instead of linking the secret to a service account, you can alternatively reference it directly in your pod or workload definition. This is useful for GitOps workflows such as ArgoCD. For example:</simpara>
<formalpara>
<title>Example pod specification</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: Pod
metadata:
  name: &lt;secure_pod_name&gt;
spec:
  containers:
  - name: &lt;container_name&gt;
    image: quay.io/my-private-image
  imagePullSecrets:
  - name: &lt;pull_secret_name&gt;</programlisting>
</para>
</formalpara>
<formalpara>
<title>Example ArgoCD workflow</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: &lt;example_workflow&gt;
spec:
  entrypoint: &lt;main_task&gt;
  imagePullSecrets:
  - name: &lt;pull_secret_name&gt;</programlisting>
</para>
</formalpara>
</listitem>
</orderedlist>
</section>
<section xml:id="images-pulling-from-private-registries_using-image-pull-secrets">
<title>Pulling from private registries with delegated authentication</title>
<simpara role="_abstract">To pull images from private registries that delegate authentication to a separate service in OpenShift Container Platform, you can create pull secrets for both the authentication server and the registry endpoint. Use the <literal>oc create secret docker-registry</literal> command to create separate secrets for each service.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create a secret for the delegated authentication server by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create secret docker-registry \
    --docker-server=sso.redhat.com \
    --docker-username=developer@example.com \
    --docker-password=******** \
    --docker-email=unused \
    redhat-connect-sso</programlisting>
</listitem>
<listitem>
<simpara>Create a secret for the private registry by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create secret docker-registry \
    --docker-server=privateregistry.example.com \
    --docker-username=developer@example.com \
    --docker-password=******** \
    --docker-email=unused \
    private-registry</programlisting>
</listitem>
</orderedlist>
</section>
</section>
</section>
</chapter>
<chapter xml:id="managing-image-streams">
<title>Managing image streams</title>
<simpara role="_abstract">To create and update container images and track version changes in OpenShift Container Platform, you can use image streams and tags. Add, update, remove, and import image stream tags to manage your container images.</simpara>
<section xml:id="images-imagestream-use_image-streams-managing">
<title>Using image streams</title>
<simpara role="_abstract">Image streams provide an abstraction for referencing container images from within OpenShift Container Platform. You can use image streams to manage image versions and automate builds and deployments in your cluster.</simpara>
<simpara>Image streams do not contain actual image data, but present a single virtual view of related images, similar to an image repository.</simpara>
<simpara>You can configure builds and deployments to watch an image stream for notifications when new images are added and react by performing a build or deployment, respectively.</simpara>
<simpara>For example, if a deployment is using a certain image and a new version of that image is created, a deployment could be automatically performed to pick up the new version of the image.</simpara>
<simpara>However, if the image stream tag used by the deployment or build is not updated, then even if the container image in the container image registry is updated, the build or deployment continues using the previous, presumably known good
image.</simpara>
<simpara>The source images can be stored in any of the following:</simpara>
<itemizedlist>
<listitem>
<simpara>OpenShift Container Platform&#8217;s integrated registry.</simpara>
</listitem>
<listitem>
<simpara>An external registry, for example registry.redhat.io or quay.io.</simpara>
</listitem>
<listitem>
<simpara>Other image streams in the OpenShift Container Platform cluster.</simpara>
</listitem>
</itemizedlist>
<simpara>When you define an object that references an image stream tag, such as a build or deployment configuration, you point to an image stream tag and not the repository. When you build or deploy your application, OpenShift Container Platform queries the repository using the image stream tag to locate the associated ID of the image and uses that exact image.</simpara>
<simpara>The image stream metadata is stored in the etcd instance along with other cluster information.</simpara>
<simpara>Using image streams has several significant benefits:</simpara>
<itemizedlist>
<listitem>
<simpara>You can tag, rollback a tag, and quickly deal with images, without having to re-push using the command line.</simpara>
</listitem>
<listitem>
<simpara>You can trigger builds and deployments when a new image is pushed to the registry. Also, OpenShift Container Platform has generic triggers for other resources, such as Kubernetes objects.</simpara>
</listitem>
<listitem>
<simpara>You can mark a tag for periodic re-import. If the source image has changed, that change is picked up and reflected in the image stream, which triggers the build or deployment flow, depending upon the build or deployment configuration.</simpara>
</listitem>
<listitem>
<simpara>You can share images using fine-grained access control and quickly distribute images across your teams.</simpara>
</listitem>
<listitem>
<simpara>If the source image changes, the image stream tag still points to a known-good version of the image, ensuring that your application does not break unexpectedly.</simpara>
</listitem>
<listitem>
<simpara>You can configure security around who can view and use the images through permissions on the image stream objects.</simpara>
</listitem>
<listitem>
<simpara>Users that lack permission to read or list images on the cluster level can still retrieve the images tagged in a project using image streams.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-configure_image-streams-managing">
<title>Configuring image streams</title>
<simpara role="_abstract">To customize image retrieval and security policies for your applications, configure image streams within OpenShift Container Platform. This process lets you define image pull specifications, manage tags, and control access permissions necessary for reliable application deployment.</simpara>
<simpara>An <literal>ImageStream</literal> object file contains the following elements.</simpara>
<formalpara xml:id="image-stream-object-definition_image-streams-managing">
<title>Imagestream object definition</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  annotations:
    openshift.io/generated-by: OpenShiftNewApp
  labels:
    app: ruby-sample-build
    template: application-template-stibuild
  name: origin-ruby-sample
  namespace: test
spec: {}
status:
  dockerImageRepository: 172.30.56.218:5000/test/origin-ruby-sample
  tags:
  - items:
    - created: 2017-09-02T10:15:09Z
      dockerImageReference: 172.30.56.218:5000/test/origin-ruby-sample@sha256:47463d94eb5c049b2d23b03a9530bf944f8f967a0fe79147dd6b9135bf7dd13d
      generation: 2
      image: sha256:909de62d1f609a717ec433cc25ca5cf00941545c83a01fb31527771e1fab3fc5
    - created: 2017-09-01T13:40:11Z
      dockerImageReference: 172.30.56.218:5000/test/origin-ruby-sample@sha256:909de62d1f609a717ec433cc25ca5cf00941545c83a01fb31527771e1fab3fc5
      generation: 1
      image: sha256:47463d94eb5c049b2d23b03a9530bf944f8f967a0fe79147dd6b9135bf7dd13d
    tag: latest</programlisting>
</para>
</formalpara>
<simpara>where</simpara>
<variablelist>
<varlistentry>
<term><literal>name</literal></term>
<listitem>
<simpara>Specifies the name of the image stream</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>ruby-sample</literal></term>
<listitem>
<simpara>Specifies the Docker repository path where new images can be pushed to add or update them in this image stream.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>dockerImageReference</literal></term>
<listitem>
<simpara>Specifies the SHA identifier that this image stream tag currently references. Resources that reference this image stream tag use this identifier</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>image</literal></term>
<listitem>
<simpara>Specifies the SHA identifier that this image stream tag previously referenced. You can use it to rollback to an older image.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>tag</literal></term>
<listitem>
<simpara>Specifies the image stream tag name.</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="images-using-imagestream-images_image-streams-managing">
<title>Image stream images</title>
<simpara role="_abstract">To precisely identify and manage the actual image content associated with a specific tag, reference and use image stream images in OpenShift Container Platform. This ensures your application deployments reliably target immutable image definitions.</simpara>
<simpara>An image stream image points from within an image stream to a particular image ID.</simpara>
<simpara>Image stream images allow you to retrieve metadata about an image from a particular image stream where it is tagged.</simpara>
<simpara>Image stream image objects are automatically created in OpenShift Container Platform whenever you import or tag an image into the image stream. You should never have to explicitly define an image stream image object in any image stream definition that you use to create image streams.</simpara>
<simpara>The image stream image consists of the image stream name and image ID from the repository, delimited by an <literal>@</literal> sign:</simpara>
<screen>&lt;image-stream-name&gt;@&lt;image-id&gt;</screen>
<simpara>To refer to the image in the <literal>ImageStream</literal> object example, the image stream image looks like:</simpara>
<screen>origin-ruby-sample@sha256:47463d94eb5c049b2d23b03a9530bf944f8f967a0fe79147dd6b9135bf7dd13d</screen>
</section>
<section xml:id="images-using-imagestream-tags_image-streams-managing">
<title>Image stream tags</title>
<simpara role="_abstract">To maintain human-readable references to immutable images, utilize image stream tags within OpenShift Container Platform. These tags are essential because they enable your builds and deployments to accurately target specific, stable image content.</simpara>
<simpara>An image stream tag is a named pointer to an image in an image stream. It is abbreviated as <literal>istag</literal>. An image stream tag is used to reference or retrieve an image for a given image stream and tag.</simpara>
<simpara>Image stream tags can reference any local or externally managed image. It contains a history of images represented as a stack of all images the tag ever pointed to. Whenever a new or existing image is tagged under a particular image stream tag, it is placed at the first position in the history stack. The image previously occupying the top position is available at the second position. This allows for easy rollbacks to make tags point to historical images again.</simpara>
<simpara>The following image stream tag is from an <literal>ImageStream</literal> object:</simpara>
<formalpara>
<title>Image stream tag with two images in its history</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: my-image-stream
# ...
  tags:
  - items:
    - created: 2017-09-02T10:15:09Z
      dockerImageReference: 172.30.56.218:5000/test/origin-ruby-sample@sha256:47463d94eb5c049b2d23b03a9530bf944f8f967a0fe79147dd6b9135bf7dd13d
      generation: 2
      image: sha256:909de62d1f609a717ec433cc25ca5cf00941545c83a01fb31527771e1fab3fc5
    - created: 2017-09-01T13:40:11Z
      dockerImageReference: 172.30.56.218:5000/test/origin-ruby-sample@sha256:909de62d1f609a717ec433cc25ca5cf00941545c83a01fb31527771e1fab3fc5
      generation: 1
      image: sha256:47463d94eb5c049b2d23b03a9530bf944f8f967a0fe79147dd6b9135bf7dd13d
    tag: latest
# ...</programlisting>
</para>
</formalpara>
<simpara>Image stream tags can be permanent tags or tracking tags.</simpara>
<itemizedlist>
<listitem>
<simpara>Permanent tags are version-specific tags that point to a particular version of an image, such as Python 3.5.</simpara>
</listitem>
<listitem>
<simpara>Tracking tags are reference tags that follow another image stream tag and can be updated to change which image they follow, like a symlink. These new levels are not guaranteed to be backwards-compatible.</simpara>
<simpara>For example, the <literal>latest</literal> image stream tags that ship with OpenShift Container Platform are tracking tags. This means consumers of the <literal>latest</literal> image stream tag are updated to the newest level of the framework provided by the image when a new level becomes available. A <literal>latest</literal> image stream tag to <literal>v3.10</literal> can be changed to <literal>v3.11</literal> at any time. It is important to be aware that these <literal>latest</literal> image stream tags behave differently than the Docker <literal>latest</literal> tag. The <literal>latest</literal> image stream tag, in this case, does not point to the latest image in the Docker repository. It points to another image stream tag, which might not be the latest version of an image. For example, if the <literal>latest</literal> image stream tag points to <literal>v3.10</literal> of an image, when the <literal>3.11</literal> version is released, the <literal>latest</literal> tag is not automatically updated to <literal>v3.11</literal>, and remains at <literal>v3.10</literal> until it is manually updated to point to a <literal>v3.11</literal> image stream tag.</simpara>
<note>
<simpara>Tracking tags are limited to a single image stream and cannot reference other
image streams.</simpara>
</note>
</listitem>
</itemizedlist>
<simpara>You can create your own image stream tags for your own needs.</simpara>
<simpara>The image stream tag is composed of the name of the image stream and a tag,
separated by a colon:</simpara>
<screen>&lt;imagestream name&gt;:&lt;tag&gt;</screen>
<simpara>For example, to refer to the
<literal>sha256:47463d94eb5c049b2d23b03a9530bf944f8f967a0fe79147dd6b9135bf7dd13d</literal> image
in the <literal>ImageStream</literal> object example earlier, the image stream tag
would be:</simpara>
<screen>origin-ruby-sample:latest</screen>
</section>
<section xml:id="images-using-imagestream-change-triggers_image-streams-managing">
<title>Image stream change triggers</title>
<simpara role="_abstract">To automate your application lifecycle and ensure they use the latest code, configure image stream triggers in OpenShift Container Platform. Image stream triggers allow your builds and deployments to be automatically invoked when a new version of an upstream image is available.</simpara>
<simpara>For example, builds and deployments can be automatically started when an image
stream tag is modified. This is achieved by monitoring that particular image
stream tag and notifying the build or deployment when a change is detected.</simpara>
</section>
<section xml:id="working-with-image-streams_image-streams-managing">
<title>Working with image streams</title>
<simpara role="_abstract">To organize and manage container images in OpenShift Container Platform, you can use image streams and image stream tags. By using image streams, you can track image versions and simplify deployments.</simpara>
<important>
<simpara>Do not run workloads in or share access to default projects. Default projects are reserved for running core cluster components.</simpara>
<simpara>The following default projects are considered highly privileged: <literal>default</literal>, <literal>kube-public</literal>, <literal>kube-system</literal>, <literal>openshift</literal>, <literal>openshift-infra</literal>, <literal>openshift-node</literal>, and other system-created projects that have the <literal>openshift.io/run-level</literal> label set to <literal>0</literal> or <literal>1</literal>. Functionality that relies on admission plugins, such as pod security admission, security context constraints, cluster resource quotas, and image reference resolution, does not work in highly privileged projects.</simpara>
</important>
<section xml:id="images-getting-info-about-imagestreams_image-streams-managing">
<title>Getting information about image streams</title>
<simpara role="_abstract">To efficiently manage and monitor your image streams in OpenShift Container Platform, retrieve information about their versions. You can get general information about the image stream and detailed information about all the tags it is pointing to, ensuring your deployed applications rely on the correct image versions.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>To get general information about the image stream and detailed information about all the tags it is pointing to, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe is/&lt;image-name&gt;</programlisting>
<simpara>For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe is/python</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Name:			python
Namespace:		default
Created:		About a minute ago
Labels:			&lt;none&gt;
Annotations:		openshift.io/image.dockerRepositoryCheck=2017-10-02T17:05:11Z
Docker Pull Spec:	docker-registry.default.svc:5000/default/python
Image Lookup:		local=false
Unique Images:		1
Tags:			1

3.5
  tagged from centos/python-35-centos7

  * centos/python-35-centos7@sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25
      About a minute ago</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>To get all of the information available about a particular image stream tag, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe istag/&lt;image-stream&gt;:&lt;tag-name&gt;</programlisting>
<simpara>For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe istag/python:latest</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Image Name:	sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25
Docker Image:	centos/python-35-centos7@sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25
Name:		sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25
Created:	2 minutes ago
Image Size:	251.2 MB (first layer 2.898 MB, last binary layer 72.26 MB)
Image Created:	2 weeks ago
Author:		&lt;none&gt;
Arch:		amd64
Entrypoint:	container-entrypoint
Command:	/bin/sh -c $STI_SCRIPTS_PATH/usage
Working Dir:	/opt/app-root/src
User:		1001
Exposes Ports:	8080/tcp
Docker Labels:	build-date=20170801</programlisting>
</para>
</formalpara>
<note>
<simpara>More information is output than shown.</simpara>
</note>
</listitem>
<listitem>
<simpara>Enter the following command to discover which architecture or operating system that an image stream tag supports:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get istag &lt;image-stream-tag&gt; -ojsonpath="{range .image.dockerImageManifests[*]}{.os}/{.architecture}{'\n'}{end}"</programlisting>
<simpara>For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get istag busybox:latest -ojsonpath="{range .image.dockerImageManifests[*]}{.os}/{.architecture}{'\n'}{end}"</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">linux/amd64
linux/arm
linux/arm64
linux/386
linux/mips64le
linux/ppc64le
linux/riscv64
linux/s390x</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-adding-tags_image-streams-managing">
<title>Adding tags to an image stream</title>
<simpara role="_abstract">To accurately manage and track specific versions of your container images, add tags to your image streams within OpenShift Container Platform, This ensures reliable referencing and deployment throughout your environment.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Add a tag that points to one of the existing tags by using the `oc tag`command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;image-name:tag1&gt; &lt;image-name:tag2&gt;</programlisting>
<simpara>For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag python:3.5 python:latest</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Tag python:latest set to python@sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25.</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Confirm the image stream has two tags, one, <literal>3.5</literal>, pointing at the external container image and another tag, <literal>latest</literal>, pointing to the same image because it was created based on the first tag.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe is/python</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Name:			python
Namespace:		default
Created:		5 minutes ago
Labels:			&lt;none&gt;
Annotations:		openshift.io/image.dockerRepositoryCheck=2017-10-02T17:05:11Z
Docker Pull Spec:	docker-registry.default.svc:5000/default/python
Image Lookup:		local=false
Unique Images:		1
Tags:			2

latest
  tagged from python@sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25

  * centos/python-35-centos7@sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25
      About a minute ago

3.5
  tagged from centos/python-35-centos7

  * centos/python-35-centos7@sha256:49c18358df82f4577386404991c51a9559f243e0b1bdc366df25
      5 minutes ago</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-external-image-tags_image-streams-managing">
<title>Adding tags for an external image</title>
<simpara role="_abstract">To enable OpenShift Container Platform resources to track and consume container images sourced from external registries, add tags to the corresponding image streams. This action integrates external image content securely into your cluster&#8217;s local image management system.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Add tags pointing to internal or external images, by using the <literal>oc tag</literal> command for all tag-related operations:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;repository/image&gt; &lt;image-name:tag&gt;</programlisting>
<simpara>For example, this command maps the <literal>docker.io/python:3.6.0</literal> image to the <literal>3.6</literal> tag in the <literal>python</literal> image stream.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag docker.io/python:3.6.0 python:3.6</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Tag python:3.6 set to docker.io/python:3.6.0.</programlisting>
</para>
</formalpara>
<simpara>If the external image is secured, you must create a secret with credentials for accessing that registry.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-update-tag_image-streams-managing">
<title>Updating image stream tags</title>
<simpara role="_abstract">To maintain flexibility and consistency in deployment definitions, update an image stream tag to reflect a different tag in OpenShift Container Platform. Specifically, you can update a tag to reflect another tag in an image stream, which is essential for managing image versions effectively.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Update a tag:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;image-name:tag&gt; &lt;image-name:latest&gt;</programlisting>
<simpara>For example, the following updates the <literal>latest</literal> tag to reflect the <literal>3.6</literal> tag in an image stream:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag python:3.6 python:latest</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Tag python:latest set to python@sha256:438208801c4806548460b27bd1fbcb7bb188273d13871ab43f.</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-remove-tag_image-streams-managing">
<title>Removing image stream tags</title>
<simpara role="_abstract">To maintain control over your image history and simplify management within OpenShift Container Platform, you can remove old tags from an image stream. This action helps ensure that your resources track only the current and necessary image references.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Remove old tags from an image stream:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag -d &lt;image-name:tag&gt;</programlisting>
<simpara>For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag -d python:3.6</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Deleted tag default/python:3.6</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Additional resources</title>
<listitem>
<simpara><link linkend="images-samples-operator-deprecated-image-stream_configuring-samples-operator">Removing deprecated image stream tags from the Cluster Samples Operator</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-import_image-streams-managing">
<title>Configuring periodic importing of image stream tags</title>
<simpara role="_abstract">To maintain up-to-date image definitions from an external container image registry, configure periodic importing of image stream tags. This process allows you to quickly re-import images for critical security updates by using the <literal>--scheduled</literal> flag.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Schedule importing images:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;repository/image&gt; &lt;image-name:tag&gt; --scheduled</programlisting>
<simpara>For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag docker.io/python:3.6.0 python:3.6 --scheduled</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Tag python:3.6 set to import docker.io/python:3.6.0 periodically.</programlisting>
</para>
</formalpara>
<simpara>This command causes OpenShift Container Platform to periodically update this particular image stream tag. This period is a cluster-wide setting set to 15 minutes by default.</simpara>
</listitem>
<listitem>
<simpara>Remove the periodic check, re-run above command but omit the <literal>--scheduled</literal> flag. This will reset its behavior to default.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc tag &lt;repositiory/image&gt; &lt;image-name:tag&gt;</programlisting>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="images-imagestream-import-images-image-streams_image-streams-managing">
<title>Importing and working with images and image streams</title>
<simpara role="_abstract">To bring container images into your OpenShift Container Platform cluster and manage their references, you can import images from external registries and organize them by using image streams. By using this process, you can maintain a centralized registry of container images for your applications.</simpara>
<simpara>The following sections describe how to import, and work with, image streams.</simpara>
<section xml:id="images-imagestream-import-images-private-registry_image-streams-managing">
<title>Importing images and image streams from private registries</title>
<simpara role="_abstract">To securely manage content from external sources, configure your image streams to import tag and image metadata from private registries requiring authentication. This procedure is essential if you change the registry that the Cluster Samples Operator uses for pulling content to something other than the default <link xlink:href="https://registry.redhat.io">registry.redhat.io</link>.</simpara>
<note>
<simpara>When importing from insecure or secure registries, the registry URL defined in the secret must include the <literal>:80</literal> port suffix or the secret is not used when attempting to import from the registry.</simpara>
</note>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>You must create a <literal>secret</literal> object that is used to store your credentials by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create secret generic &lt;secret_name&gt; --from-file=.dockerconfigjson=&lt;file_absolute_path&gt; --type=kubernetes.io/dockerconfigjson</programlisting>
</listitem>
<listitem>
<simpara>After the secret is configured, create the new image stream or enter the <literal>oc import-image</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;imagestreamtag&gt; --from=&lt;image&gt; --confirm</programlisting>
<simpara>During the import process, OpenShift Container Platform picks up the secrets and provides them to the remote party.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="images-imagestream-import-import-mode_image-streams-managing">
<title>Working with manifest lists</title>
<simpara role="_abstract">To precisely manage multi-architecture or variant images contained within a manifest list, use the <literal>--import-mode</literal> flag with <literal>oc import-image</literal> or <literal>oc tag</literal> CLI commands. This functionality allows you to import a single sub-manifest, or all manifests, of a manifest list, providing fine-grained control over your image stream content.</simpara>
<simpara>In some cases, users might want to use sub-manifests directly. When <literal>oc adm prune images</literal> is run, or the <literal>CronJob</literal> pruner runs, they cannot detect when a sub-manifest list is used. As a result, an administrator using <literal>oc adm prune images</literal>, or the <literal>CronJob</literal> pruner, might delete entire manifest lists, including sub-manifests.</simpara>
<simpara>To avoid this limitation, you can use the manifest list by tag or by digest instead.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Create an image stream that includes multi-architecture images, and sets the import mode to <literal>PreserveOriginal</literal>, by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;multiarch-image-stream-tag&gt;  --from=&lt;registry&gt;/&lt;project_name&gt;/&lt;image-name&gt; \
--import-mode='PreserveOriginal' --reference-policy=local --confirm</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">---
Arch:           &lt;none&gt;
Manifests:      linux/amd64     sha256:6e325b86566fafd3c4683a05a219c30c421fbccbf8d87ab9d20d4ec1131c3451
                linux/arm64     sha256:d8fad562ffa75b96212c4a6dc81faf327d67714ed85475bf642729703a2b5bf6
                linux/ppc64le   sha256:7b7e25338e40d8bdeb1b28e37fef5e64f0afd412530b257f5b02b30851f416e1
---</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Alternatively, enter the following command to import an image with the <literal>Legacy</literal> import mode, which discards manifest lists and imports a single sub-manifest:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;multiarch-image-stream-tag&gt;  --from=&lt;registry&gt;/&lt;project_name&gt;/&lt;image-name&gt; \
--import-mode='Legacy' --confirm</programlisting>
<note>
<simpara>The <literal>--import-mode=</literal> default value is <literal>Legacy</literal>. Excluding this value, or failing to specify either <literal>Legacy</literal> or <literal>PreserveOriginal</literal>, imports a single sub-manifest. An invalid import mode returns the following error: <literal>error: valid ImportMode values are Legacy or PreserveOriginal</literal>.</simpara>
</note>
</listitem>
</itemizedlist>
<section xml:id="images-imagestream-periodic-import-list_image-streams-managing">
<title>Configuring periodic importing of manifest lists</title>
<simpara role="_abstract">To maintain up-to-date image references for complex, multi-architecture images, configure periodic importing of manifest lists. To periodically re-import a manifest list, you can use the <literal>--scheduled</literal> flag, ensuring your image stream tracks the latest versions from external registries.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Set the image stream to periodically update the manifest list by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;multiarch-image-stream-tag&gt;  --from=&lt;registry&gt;/&lt;project_name&gt;/&lt;image-name&gt; \
--import-mode='PreserveOriginal' --scheduled=true</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-imagestream-ssl-import-list_image-streams-managing">
<title>Configuring SSL/TLS when importing manifest lists</title>
<simpara role="_abstract">To control connection security and access policies for manifest lists sourced from external repositories, configure SSL/TLS settings during image importing. To configure SSL/TLS when importing a manifest list, you can use the <literal>--insecure</literal> flag to bypass standard certificate validation requirements if necessary.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Set <literal>--insecure=true</literal> so that importing a manifest list skips SSL/TLS verification. For example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;multiarch-image-stream-tag&gt; --from=&lt;registry&gt;/&lt;project_name&gt;/&lt;image-name&gt; \
--import-mode='PreserveOriginal' --insecure=true</programlisting>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="images-imagestream-specify-architecture_image-streams-managing">
<title>Specifying architecture for --import-mode</title>
<simpara role="_abstract">To control the architecture of your imported images and ensure proper deployment, use the <literal>--import-mode=</literal> flag. You can swap your imported image stream between multi-architecture and single architecture by excluding or including the <literal>--import-mode=</literal> flag as needed.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Run the following command to update your image stream from multi-architecture to single architecture by excluding the <literal>--import-mode=</literal> flag:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;multiarch-image-stream-tag&gt; --from=&lt;registry&gt;/&lt;project_name&gt;/&lt;image-name&gt;</programlisting>
</listitem>
<listitem>
<simpara>Run the following command to update your image stream from single-architecture to multi-architecture:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc import-image &lt;multiarch_image_stream_tag&gt;  --from=&lt;registry&gt;/&lt;project_name&gt;/&lt;image_name&gt; \
--import-mode='PreserveOriginal'</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="importmode-configuration-fields_image-streams-managing">
<title>Configuration fields for --import-mode</title>
<simpara role="_abstract">To implement multi-architecture image management using the <literal>--import-mode</literal> flag, reference the necessary configuration fields. These fields define precise parameters for selecting and importing specific manifests into your OpenShift Container Platform cluster.</simpara>
<simpara>The following table describes the options available for the <literal>--import-mode=</literal> flag:</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="2">
<colspec colname="col_1" colwidth="27.2727*"/>
<colspec colname="col_2" colwidth="72.7273*"/>
<thead>
<row>
<entry align="left" valign="top">Parameter</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">Legacy</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara>The default option for <literal>--import-mode</literal>. When specified, the manifest list is discarded, and a single sub-manifest is imported. The platform is chosen in the following order of priority:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Tag annotations</simpara>
</listitem>
<listitem>
<simpara>Control plane architecture</simpara>
</listitem>
<listitem>
<simpara>Linux/AMD64</simpara>
</listitem>
<listitem>
<simpara>The first manifest in the list</simpara>
</listitem>
</orderedlist></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><emphasis role="strong">PreserveOriginal</emphasis></simpara></entry>
<entry align="left" valign="top"><simpara>When specified, the original manifest is preserved. For manifest lists, the manifest list and all of its sub-manifests are imported.</simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
</section>
</section>
</chapter>
<chapter xml:id="using-imagestreams-with-kube-resources">
<title>Using image streams with Kubernetes resources</title>
<simpara role="_abstract">To use image streams with both OpenShift Container Platform native resources and standard Kubernetes resources, reference them in your resource definitions. Image streams work with resources such as <literal>Build</literal>, <literal>DeploymentConfigs</literal>, <literal>Job</literal>, <literal>ReplicationController</literal>, <literal>ReplicaSet</literal>, and <literal>Deployment</literal> resources.</simpara>
<section xml:id="images-managing-images-enabling-imagestreams-kube_using-imagestreams-with-kube-resources">
<title>Enabling image streams with Kubernetes resources</title>
<simpara role="_abstract">When using Kubernetes resources, you must reference image streams located within the same project by specifying a single segment value, such as <literal>ruby:2.5</literal>, which identifies the image stream name and its tag. This ensures the resource correctly targets the local image stream within its scope.</simpara>
<important>
<simpara>Do not run workloads in or share access to default projects. Default projects are reserved for running core cluster components.</simpara>
<simpara>The following default projects are considered highly privileged: <literal>default</literal>, <literal>kube-public</literal>, <literal>kube-system</literal>, <literal>openshift</literal>, <literal>openshift-infra</literal>, <literal>openshift-node</literal>, and other system-created projects that have the <literal>openshift.io/run-level</literal> label set to <literal>0</literal> or <literal>1</literal>. Functionality that relies on admission plugins, such as pod security admission, security context constraints, cluster resource quotas, and image reference resolution, does not work in highly privileged projects.</simpara>
</important>
<simpara>There are two ways to enable image streams with Kubernetes resources:</simpara>
<itemizedlist>
<listitem>
<simpara>Enabling image stream resolution on a specific resource. This allows only this resource to use the image stream name in the image field.</simpara>
</listitem>
<listitem>
<simpara>Enabling image stream resolution on an image stream. This allows all resources pointing to this image stream to use it in the image field.</simpara>
</listitem>
</itemizedlist>
<simpara>You can use <literal>oc set image-lookup</literal> to enable image stream resolution on a specific resource or image stream resolution on an image stream.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>To allow all resources to reference the image stream named <literal>mysql</literal>, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc set image-lookup mysql</programlisting>
<simpara>This sets the <literal>Imagestream.spec.lookupPolicy.local</literal> field to true.</simpara>
<formalpara>
<title>Imagestream with image lookup enabled</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  annotations:
    openshift.io/display-name: mysql
  name: mysql
  namespace: myproject
spec:
  lookupPolicy:
    local: true</programlisting>
</para>
</formalpara>
<simpara>When enabled, the behavior is enabled for all tags within the image stream.</simpara>
</listitem>
<listitem>
<simpara>Then you can query the image streams and see if the option is set:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc set image-lookup imagestream --list</programlisting>
</listitem>
<listitem>
<simpara>Optional: You can enable image lookup on a specific resource.</simpara>
<simpara>To allow the Kubernetes deployment named <literal>mysql</literal> to use image streams, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc set image-lookup deploy/mysql</programlisting>
<simpara>This sets the <literal>alpha.image.policy.openshift.io/resolve-names</literal> annotation
on the deployment.</simpara>
<formalpara>
<title>Deployment with image lookup enabled</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: myproject
spec:
  replicas: 1
  template:
    metadata:
      annotations:
        alpha.image.policy.openshift.io/resolve-names: '*'
    spec:
      containers:
      - image: mysql:latest
        imagePullPolicy: Always
        name: mysql</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Optional: To disable image lookup, pass <literal>--enabled=false</literal>:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc set image-lookup deploy/mysql --enabled=false</programlisting>
</listitem>
</orderedlist>
</section>
</chapter>
<chapter xml:id="triggering-updates-on-imagestream-changes">
<title>Triggering updates on image stream changes</title>
<simpara role="_abstract">When image stream tags update in OpenShift Container Platform, the platform automatically rolls out new images to deployments and builds that reference those tags. You configure this automatic triggering behavior differently depending on the type of resource that uses the image stream.</simpara>
<section xml:id="openshift-resources">
<title>OpenShift Container Platform resources</title>
<simpara>OpenShift Container Platform deployment configurations and build configurations can be automatically triggered by changes to image stream tags. The triggered action can be run using the new value of the image referenced by the updated image stream tag.</simpara>
</section>
<section xml:id="images-triggering-updates-imagestream-changes-kubernetes-about_triggering-updates-on-imagestream-changes">
<title>Triggering Kubernetes resources</title>
<simpara role="_abstract">To enable Kubernetes resources, such as <literal>Deployments</literal> and <literal>StatefulSets</literal>, to seamlessly consume new image versions, configure image stream change triggers in OpenShift Container Platform. This ensures your application deployments are automatically updated when the associated image stream detects a change.</simpara>
<simpara>Kubernetes resources do not have fields for triggering, unlike deployment and build configurations, which include as part of their API definition a set of fields for controlling triggers. Instead, you can use annotations in OpenShift Container Platform to request triggering.</simpara>
<simpara>The annotation is defined as follows:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: Pod
metadata:
  annotations:
    image.openshift.io/triggers:
      [
       {
         "from": {
           "kind": "ImageStreamTag",
           "name": "example:latest",
           "namespace": "myapp"
         },
         "fieldPath": "spec.template.spec.containers[?(@.name==\"web\")].image",
         "paused": false
       },
      # ...
      ]
# ...</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>kind</literal></term>
<listitem>
<simpara>Specifies the resource to trigger from, and must have the value <literal>ImageStreamTag</literal>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>name</literal></term>
<listitem>
<simpara>Specifies the name of an image stream tag.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>namespace</literal></term>
<listitem>
<simpara>Specifies the namespace of the object. This field is optional.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>fieldPath</literal></term>
<listitem>
<simpara>Specifies the JSON path to change. This field is limited and accepts only a JSON path expression that precisely matches a container by ID or index. For pods, the JSON path is <literal>spec.containers[?(@.name='web')].image</literal>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>paused</literal></term>
<listitem>
<simpara>Specifies whether or not the trigger is paused. This field is optional, and defaults to the value <literal>false</literal>. Set the value to <literal>true</literal> to temporarily disable this trigger.</simpara>
</listitem>
</varlistentry>
</variablelist>
<simpara>When one of the core Kubernetes resources contains both a pod template and this annotation, OpenShift Container Platform attempts to update the object by using the image currently associated with the image stream tag that is referenced by trigger. The update is performed against the <literal>fieldPath</literal> specified.</simpara>
<simpara>Examples of core Kubernetes resources that can contain both a pod template and annotation include:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>CronJobs</literal></simpara>
</listitem>
<listitem>
<simpara><literal>Deployments</literal></simpara>
</listitem>
<listitem>
<simpara><literal>StatefulSets</literal></simpara>
</listitem>
<listitem>
<simpara><literal>DaemonSets</literal></simpara>
</listitem>
<listitem>
<simpara><literal>Jobs</literal></simpara>
</listitem>
<listitem>
<simpara><literal>ReplicationControllers</literal></simpara>
</listitem>
<listitem>
<simpara><literal>Pods</literal></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-triggering-updates-imagestream-changes-kubernetes-cli_triggering-updates-on-imagestream-changes">
<title>Setting the image trigger on Kubernetes resources</title>
<simpara role="_abstract">To enable automatic updates for your deployed applications managed by Kubernetes, use the command-line interface (CLI) to set an image stream change trigger on Kubernetes resources. This ensures that resources, like <literal>Deployments</literal> and <literal>StatefulSets</literal>, are automatically invoked when a new version of an upstream image is available.</simpara>
<simpara>When adding an image trigger to deployments, you can use the <literal>oc set triggers</literal> command. For example, the sample command in this procedure adds an image change trigger to the deployment named <literal>example</literal> so that when the <literal>example:latest</literal> image stream tag is updated, the <literal>web</literal> container inside the deployment updates with the new image value. This command sets the correct <literal>image.openshift.io/triggers</literal> annotation on the deployment resource.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Trigger Kubernetes resources by entering the <literal>oc set triggers</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc set triggers deploy/example --from-image=example:latest -c web</programlisting>
<formalpara>
<title>Example deployment with trigger annotation</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    image.openshift.io/triggers: '[{"from":{"kind":"ImageStreamTag","name":"example:latest"},"fieldPath":"spec.template.spec.containers[?(@.name==\"container\")].image"}]'
# ...</programlisting>
</para>
</formalpara>
<simpara>Unless the deployment is paused, this pod template update automatically causes a deployment to occur with the new image value.</simpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="image-configuration-classic">
<title>Image configuration resources</title>
<simpara role="_abstract">You can configure an image registry to store and serve container images.</simpara>
<section xml:id="images-configuration-parameters_image-configuration">
<title>Image controller configuration parameters</title>
<simpara role="_abstract">You can configure certain parameters that handle images cluster-wide in the <literal>spec</literal> of the <literal>image.config.openshift.io/cluster</literal> resource.</simpara>
<note>
<simpara>The following non-configurable parameters are not listed in the table:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>DisableScheduledImport</literal></simpara>
</listitem>
<listitem>
<simpara><literal>MaxImagesBulkImportedPerRepository</literal></simpara>
</listitem>
<listitem>
<simpara><literal>MaxScheduledImportsPerMinute</literal></simpara>
</listitem>
<listitem>
<simpara><literal>ScheduledImageImportMinimumIntervalSeconds</literal></simpara>
</listitem>
<listitem>
<simpara><literal>InternalRegistryHostname</literal></simpara>
</listitem>
</itemizedlist>
</note>
<table frame="all" rowsep="1" colsep="1">
<title>Image controller configuration parameters</title>
<tgroup cols="2">
<colspec colname="col_1" colwidth="27.2727*"/>
<colspec colname="col_2" colwidth="72.7273*"/>
<thead>
<row>
<entry align="left" valign="top">Field name</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>kind.Image</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Holds cluster-wide information about how to handle images. The canonical, and only valid name for this CR is <literal>cluster</literal>.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>allowedRegistriesForImport</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Limits the container image registries from which normal users can import images. Set this list to the registries that you trust to contain valid images, and that you want applications to be able to import from. Users with permission to create images or <literal>ImageStreamMappings</literal> from the API are not affected by this policy. Typically only cluster administrators have the appropriate permissions.</simpara>
<simpara>Every element of this list contains a location of the registry specified by the registry domain name.</simpara>
<simpara><literal>domainName</literal>: Specifies a domain name for the registry. If the registry uses a non-standard <literal>80</literal> or <literal>443</literal> port, the port should be included in the domain name as well.</simpara>
<simpara><literal>insecure</literal>: Insecure indicates whether the registry is secure or insecure. By default, if not otherwise specified, the registry is assumed to be secure.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>additionalTrustedCA</literal></simpara></entry>
<entry align="left" valign="top"><simpara>A reference to a config map containing additional CAs that should be trusted during <literal>image stream import</literal>, <literal>pod image pull</literal>, <literal>openshift-image-registry pullthrough</literal>, and builds.</simpara>
<simpara>The namespace for this config map is <literal>openshift-config</literal>. The format of the config map is to use the registry hostname as the key, and the PEM-encoded certificate as the value, for each additional registry CA to trust.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>externalRegistryHostnames</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Provides the hostnames for the default external image registry. The external hostname should be set only when the image registry is exposed externally. The first value is used in <literal>publicDockerImageRepository</literal> field in image streams. The value must be in <literal>hostname[:port]</literal> format.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>registrySources</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Contains configuration that determines how the container runtime should treat individual registries when accessing images for builds and pods. For example, whether or not to allow insecure access. It does not contain configuration for the internal cluster registry.</simpara>
<simpara><literal>insecureRegistries</literal>: Registries that do not have a valid TLS certificate or only support HTTP connections. To specify all subdomains, add the asterisk (<literal>*</literal>) wildcard character as a prefix to the domain name. For example, <literal>*.example.com</literal>. You can specify an individual repository within a registry. For example: <literal>reg1.io/myrepo/myapp:latest</literal>.</simpara>
<simpara><literal>blockedRegistries</literal>: Registries for which image pull and push actions are denied. To specify all subdomains, add the asterisk (<literal>*</literal>) wildcard character as a prefix to the domain name. For example, <literal>*.example.com</literal>. You can specify an individual repository within a registry. For example: <literal>reg1.io/myrepo/myapp:latest</literal>. All other registries are allowed.</simpara>
<simpara><literal>allowedRegistries</literal>: Registries for which image pull and push actions are allowed. To specify all subdomains, add the asterisk (<literal>*</literal>) wildcard character as a prefix to the domain name. For example, <literal>*.example.com</literal>. You can specify an individual repository within a registry. For example: <literal>reg1.io/myrepo/myapp:latest</literal>. All other registries are blocked.</simpara>
<simpara><literal>containerRuntimeSearchRegistries</literal>: Registries for which image pull and push actions are allowed using image short names. All other registries are blocked.</simpara>
<simpara>You can set either <literal>blockedRegistries</literal> or <literal>allowedRegistries</literal>, but not both.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<warning>
<simpara>When you define the <literal>allowedRegistries</literal> parameter, all registries, including <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the default OpenShift image registry, are blocked unless explicitly listed. You must add all of the registries that your payload images require to the <literal>allowedRegistries</literal> list. For example, list <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the <literal>internalRegistryHostname</literal> registries. For disconnected clusters, you must also add your mirror registries. Otherwise, you risk pod failure.</simpara>
</warning>
<simpara>The <literal>status</literal> field of the <literal>image.config.openshift.io/cluster</literal> resource holds observed values from the cluster.</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Image controller status field parameters</title>
<tgroup cols="2">
<colspec colname="col_1" colwidth="27.2727*"/>
<colspec colname="col_2" colwidth="72.7273*"/>
<thead>
<row>
<entry align="left" valign="top">Parameter</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>internalRegistryHostname</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Set by the Image Registry Operator, which controls the <literal>internalRegistryHostname</literal>. It sets the hostname for the default OpenShift image registry. The value must be in <literal>hostname[:port]</literal> format. For backward compatibility, you can still use the <literal>OPENSHIFT_DEFAULT_REGISTRY</literal> environment variable, but this setting overrides the environment variable.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>externalRegistryHostnames</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Set by the Image Registry Operator, provides the external hostnames for the image registry when it is exposed externally. The first value is used in <literal>publicDockerImageRepository</literal> field in image streams. The values must be in <literal>hostname[:port]</literal> format.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
</section>
<section xml:id="images-configuration-mco-and-registry-changes_image-configuration">
<title>Machine Config Operator behavior and registry changes</title>
<simpara role="_abstract">The Machine Config Operator (MCO) watches the <literal>image.config.openshift.io/cluster</literal> custom resource (CR) for any changes to registries and takes specific steps when the registry changes.</simpara>
<simpara>When changes to the registry are applied to the <literal>image.config.openshift.io/cluster</literal> CR, the MCO performs the following sequential actions:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Cordons the node; certain parameters result in drained nodes, and others do not</simpara>
</listitem>
<listitem>
<simpara>Applies changes by restarting CRI-O</simpara>
</listitem>
<listitem>
<simpara>Uncordons the node</simpara>
<note>
<simpara>The MCO does not restart nodes when it detects changes. During this period, you might experience service unavailability.</simpara>
</note>
</listitem>
</orderedlist>
<section xml:id="images-configuration-mco-and-blocking-registry-sources_image-configuration">
<title>When allowing and blocking registry sources</title>
<simpara>The MCO watches the <literal>image.config.openshift.io/cluster</literal> resource for any changes to the registries. When the MCO detects a change, it triggers a rollout on nodes in machine config pool (MCP). The allowed registries list is used to update the image signature policy in the <literal>/etc/containers/policy.json</literal> file on each node. Changes to the <literal>/etc/containers/policy.json</literal> file do not require the node to drain.</simpara>
</section>
<section xml:id="images-configuration-mco-and-shortnames_image-configuration">
<title>When using the containerRuntimeSearchRegistries parameter</title>
<simpara>After the nodes return to the <literal>Ready</literal> state, if the <literal>containerRuntimeSearchRegistries</literal> parameter is added, the MCO creates a file in the <literal>/etc/containers/registries.conf.d</literal> directory on each node with the listed registries. The file overrides the default list of unqualified search registries in the <literal>/etc/containers/registries.conf</literal> file. There is no way to fall back to the default list of unqualified search registries.</simpara>
<important>
<simpara>The <literal>containerRuntimeSearchRegistries</literal> parameter works only with the Podman and CRI-O container engines. The registries in the list can be used only in pod specs, not in builds and image streams.</simpara>
</important>
</section>
</section>
<section xml:id="images-configuration-file_image-configuration">
<title>Configuring image registry settings</title>
<simpara role="_abstract">You can configure image registry settings by editing the <literal>image.config.openshift.io/cluster</literal> custom resource (CR).</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Edit the <literal>image.config.openshift.io/cluster</literal> CR by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit image.config.openshift.io/cluster</programlisting>
<simpara>The following is an example <literal>image.config.openshift.io/cluster</literal> CR:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: config.openshift.io/v1
kind: Image
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  creationTimestamp: "2019-05-17T13:44:26Z"
  generation: 1
  name: cluster
  resourceVersion: "8302"
  selfLink: /apis/config.openshift.io/v1/images/cluster
  uid: e34555da-78a9-11e9-b92b-06d6c7da38dc
spec:
  allowedRegistriesForImport:
    - domainName: quay.io
      insecure: false
  additionalTrustedCA:
    name: myconfigmap
  registrySources:
    allowedRegistries:
    - example.com
    - quay.io
    - registry.redhat.io
    - image-registry.openshift-image-registry.svc:5000
    - reg1.io/myrepo/myapp:latest
    insecureRegistries:
    - insecure.com
status:
  internalRegistryHostname: image-registry.openshift-image-registry.svc:5000</programlisting>
<note>
<simpara>When you use the <literal>allowedRegistries</literal>, <literal>blockedRegistries</literal>, or <literal>insecureRegistries</literal> parameter, you can specify an individual repository within a registry. For example: <literal>reg1.io/myrepo/myapp:latest</literal>.</simpara>
<simpara>Avoid insecure external registries to reduce possible security risks.</simpara>
</note>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Verification</title>
<listitem>
<simpara>To verify your changes, list your nodes by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get nodes</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                                         STATUS                     ROLES                  AGE   VERSION
ip-10-0-137-182.us-east-2.compute.internal   Ready,SchedulingDisabled   worker                 65m   v1.34.2
ip-10-0-139-120.us-east-2.compute.internal   Ready,SchedulingDisabled   control-plane          74m   v1.34.2
ip-10-0-176-102.us-east-2.compute.internal   Ready                      control-plane          75m   v1.34.2
ip-10-0-188-96.us-east-2.compute.internal    Ready                      worker                 65m   v1.34.2
ip-10-0-200-59.us-east-2.compute.internal    Ready                      worker                 63m   v1.34.2
ip-10-0-223-123.us-east-2.compute.internal   Ready                      control-plane          73m   v1.34.2</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
<section xml:id="images-configuration-allowed_image-configuration">
<title>Adding specific registries to an allowlist</title>
<simpara role="_abstract">You can add an allowlist of registries, or an individual repository, within a registry for image pull and push actions by editing the <literal>image.config.openshift.io/cluster</literal> custom resource (CR).</simpara>
<simpara>OpenShift Container Platform applies the changes to this CR to all nodes in the cluster.</simpara>
<simpara>When pulling or pushing images, the container runtime searches the registries listed under the <literal>registrySources</literal> parameter in the <literal>image.config.openshift.io/cluster</literal> CR. If you created a list of registries under the <literal>allowedRegistries</literal> parameter, the container runtime searches only those registries. Registries not in your allowlist are blocked.</simpara>
<warning>
<simpara>When you define the <literal>allowedRegistries</literal> parameter, all registries, including <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the default OpenShift image registry, are blocked unless explicitly listed. You must add all of the registries that your payload images require to the <literal>allowedRegistries</literal> list. For example, list <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the <literal>internalRegistryHostname</literal> registries. For disconnected clusters, you must also add your mirror registries. Otherwise, you risk pod failure.</simpara>
</warning>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Edit the <literal>image.config.openshift.io/cluster</literal> custom resource by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit image.config.openshift.io/cluster</programlisting>
<simpara>The following is an example <literal>image.config.openshift.io/cluster</literal> CR with an allowed list:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: config.openshift.io/v1
kind: Image
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  creationTimestamp: "2019-05-17T13:44:26Z"
  generation: 1
  name: cluster
  resourceVersion: "8302"
  selfLink: /apis/config.openshift.io/v1/images/cluster
  uid: e34555da-78a9-11e9-b92b-06d6c7da38dc
spec:
  registrySources:
    allowedRegistries:
    - example.com
    - quay.io
    - registry.redhat.io
    - reg1.io/myrepo/myapp:latest
    - image-registry.openshift-image-registry.svc:5000
status:
  internalRegistryHostname: image-registry.openshift-image-registry.svc:5000</programlisting>
<note>
<simpara>If your cluster uses the <literal>registrySources.insecureRegistries</literal> parameter, ensure that any insecure registries are included in the allowed list.</simpara>
<simpara>For example:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">spec:
  registrySources:
    insecureRegistries:
    - insecure.com
    allowedRegistries:
    - example.com
    - quay.io
    - registry.redhat.io
    - insecure.com
    - image-registry.openshift-image-registry.svc:5000</programlisting>
</note>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-configuration-blocked_image-configuration">
<title>Blocking specific registries</title>
<simpara role="_abstract">You can block any registry, or an individual repository, within a registry by editing the <literal>image.config.openshift.io/cluster</literal> custom resource (CR).</simpara>
<simpara>OpenShift Container Platform applies the changes to this CR to all nodes in the cluster.</simpara>
<simpara>When pulling or pushing images, the container runtime searches the registries listed under the <literal>registrySources</literal> parameter in the <literal>image.config.openshift.io/cluster</literal> CR. If you created a list of registries under the <literal>blockedRegistries</literal> parameter, the container runtime does not search those registries. All other registries are allowed.</simpara>
<warning>
<simpara>To prevent pod failure, do not add the <literal>registry.redhat.io</literal> and <literal>quay.io</literal> registries to the <literal>blockedRegistries</literal> list. Payload images within your environment require access to these registries.</simpara>
</warning>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Edit the <literal>image.config.openshift.io/cluster</literal> custom resource by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit image.config.openshift.io/cluster</programlisting>
<simpara>The following is an example <literal>image.config.openshift.io/cluster</literal> CR with a blocked list:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: config.openshift.io/v1
kind: Image
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  creationTimestamp: "2019-05-17T13:44:26Z"
  generation: 1
  name: cluster
  resourceVersion: "8302"
  selfLink: /apis/config.openshift.io/v1/images/cluster
  uid: e34555da-78a9-11e9-b92b-06d6c7da38dc
spec:
  registrySources:
    blockedRegistries:
    - untrusted.com
    - reg1.io/myrepo/myapp:latest
status:
  internalRegistryHostname: image-registry.openshift-image-registry.svc:5000</programlisting>
<simpara>You cannot set both the <literal>blockedRegistries</literal> and <literal>allowedRegistries</literal> parameters. You must select one or the other.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-configuration-insecure_image-configuration">
<title>Allowing insecure registries</title>
<simpara role="_abstract">You can add insecure registries, or an individual repository, within a registry by editing the <literal>image.config.openshift.io/cluster</literal> custom resource (CR).</simpara>
<simpara>OpenShift Container Platform applies the changes to this CR to all nodes in the cluster. Registries that do not use valid SSL certificates or do not require HTTPS connections are considered insecure.</simpara>
<important>
<simpara>Avoid insecure external registries to reduce possible security risks.</simpara>
</important>
<warning>
<simpara>When you define the <literal>allowedRegistries</literal> parameter, all registries, including <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the default OpenShift image registry, are blocked unless explicitly listed. You must add all of the registries that your payload images require to the <literal>allowedRegistries</literal> list. For example, list <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the <literal>internalRegistryHostname</literal> registries. For disconnected clusters, you must also add your mirror registries. Otherwise, you risk pod failure.</simpara>
</warning>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Edit the <literal>image.config.openshift.io/cluster</literal> custom resource (CR) by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit image.config.openshift.io/cluster</programlisting>
<simpara>The following is an example <literal>image.config.openshift.io/cluster</literal> CR with an insecure registries list:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: config.openshift.io/v1
kind: Image
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  creationTimestamp: "2019-05-17T13:44:26Z"
  generation: 1
  name: cluster
  resourceVersion: "8302"
  selfLink: /apis/config.openshift.io/v1/images/cluster
  uid: e34555da-78a9-11e9-b92b-06d6c7da38dc
spec:
  registrySources:
    insecureRegistries:
    - insecure.com
    - reg4.io/myrepo/myapp:latest
    allowedRegistries:
    - example.com
    - quay.io
    - registry.redhat.io
    - insecure.com
    - reg4.io/myrepo/myapp:latest
    - image-registry.openshift-image-registry.svc:5000
status:
  internalRegistryHostname: image-registry.openshift-image-registry.svc:5000</programlisting>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="images-configuration-shortname-con_image-configuration">
<title>About adding registries that allow image short names</title>
<simpara role="_abstract">With an image short name, you can search for images without including the fully qualified domain name in the pull <literal>spec</literal> parameter.</simpara>
<simpara>For example, you could use <literal>rhel7/etcd</literal> instead of <literal>registry.access.redhat.com/rhe7/etcd</literal>. You can add registries to search for an image short name by editing the <literal>image.config.openshift.io/cluster</literal> custom resource (CR).</simpara>
<simpara>You might use short names in situations where using the full path is not practical. For example, if your cluster references multiple internal registries whose DNS changes often, you would need to update the fully qualified domain names in your pull specs with each change. In this case, using an image short name might be beneficial.</simpara>
<simpara>When pulling or pushing images, the container runtime searches the registries listed under the <literal>registrySources</literal> parameter in the <literal>image.config.openshift.io/cluster</literal> CR. If you created a list of registries under the <literal>containerRuntimeSearchRegistries</literal> parameter, when pulling an image with a short name, the container runtime searches those registries.</simpara>
<section xml:id="images-configuration-shortname-when-not-to-use_image-configuration">
<title>When not to use image short names</title>
<simpara role="_abstract">To avoid deployment failures and security risks when using public registries in OpenShift Container Platform, use fully-qualified image names instead of short names. Short names work with Red&#160;Hat internal or private registries, but public registries that require authentication might not deploy images with short names.</simpara>
<simpara>You cannot list multiple public registries under the <literal>containerRuntimeSearchRegistries</literal> parameter if each public registry requires different credentials and a cluster does not list the public registry in the global pull secret.</simpara>
<simpara>For a public registry that requires authentication, you can use an image short name only if the registry has its credentials stored in the global pull secret.</simpara>
<warning>
<simpara>If you list public registries under the <literal>containerRuntimeSearchRegistries</literal> parameter (including the <literal>registry.redhat.io</literal>, <literal>docker.io</literal>, and <literal>quay.io</literal> registries), you expose your credentials to all the registries on the list, and you risk network and registry attacks. Because you can only have one pull secret for pulling images, as defined by the global pull secret, that secret is used to authenticate against every registry in that list. Therefore, if you include public registries in the list, you introduce a security risk.</simpara>
</warning>
</section>
<section xml:id="images-configuration-shortname_image-configuration">
<title>Adding registries that allow image short names</title>
<simpara role="_abstract">You can add registries to search for an image short name by editing the <literal>image.config.openshift.io/cluster</literal> custom resource (CR). OpenShift Container Platform applies the changes to this CR to all nodes in the cluster.</simpara>
<warning>
<simpara>When you define the <literal>allowedRegistries</literal> parameter, all registries, including <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the default OpenShift image registry, are blocked unless explicitly listed. You must add all of the registries that your payload images require to the <literal>allowedRegistries</literal> list. For example, list <literal>registry.redhat.io</literal>, <literal>quay.io</literal>, and the <literal>internalRegistryHostname</literal> registries. For disconnected clusters, you must also add your mirror registries. Otherwise, you risk pod failure.</simpara>
</warning>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Edit the <literal>image.config.openshift.io/cluster</literal> custom resource:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit image.config.openshift.io/cluster</programlisting>
<simpara>The following is an example <literal>image.config.openshift.io/cluster</literal> CR:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: config.openshift.io/v1
kind: Image
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  creationTimestamp: "2019-05-17T13:44:26Z"
  generation: 1
  name: cluster
  resourceVersion: "8302"
  selfLink: /apis/config.openshift.io/v1/images/cluster
  uid: e34555da-78a9-11e9-b92b-06d6c7da38dc
spec:
  allowedRegistriesForImport:
    - domainName: quay.io
      insecure: false
  additionalTrustedCA:
    name: myconfigmap
  registrySources:
    containerRuntimeSearchRegistries:
    - reg1.io
    - reg2.io
    - reg3.io
    allowedRegistries:
    - example.com
    - quay.io
    - registry.redhat.io
    - reg1.io
    - reg2.io
    - reg3.io
    - image-registry.openshift-image-registry.svc:5000
...
status:
  internalRegistryHostname: image-registry.openshift-image-registry.svc:5000</programlisting>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-configuration-cas_image-configuration">
<title>Configuring additional trust stores for image registry access</title>
<simpara role="_abstract">You can add references to a config map that has additional certificate authorities (CAs) to be trusted during image registry access to the <literal>image.config.openshift.io/cluster</literal> custom resource (CR).</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>The certificate authorities (CAs) must be PEM-encoded.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create a config map in the <literal>openshift-config</literal> namespace, then and use the config map name in the <literal>AdditionalTrustedCA</literal> parameter of the <literal>image.config.openshift.io</literal> CR. This adds CAs that should be trusted when the cluster contacts external image registries.</simpara>
<formalpara>
<title>Image registry CA config map example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: ConfigMap
metadata:
  name: my-registry-ca
data:
  registry.example.com: |
    -----BEGIN CERTIFICATE-----
    ...
    -----END CERTIFICATE-----
  registry-with-port.example.com..5000: |
    -----BEGIN CERTIFICATE-----
    ...
    -----END CERTIFICATE-----</programlisting>
</para>
</formalpara>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>data:registry.example.com:</literal></term>
<listitem>
<simpara>An example hostname of a registry for which this CA is to be trusted.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>data:registry-with-port.example.com..5000:</literal></term>
<listitem>
<simpara>An example hostname of a registry with the port for which this CA is to be trusted. If the registry has a port, such as <literal>registry-with-port.example.com:5000</literal>, <literal>:</literal> must be replaced with <literal>..</literal>.</simpara>
<simpara>The PEM certificate content is the value for each additional registry CA to trust.</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
<listitem>
<simpara>Optional. Configure an additional CA by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create configmap registry-config --from-file=&lt;external_registry_address&gt;=ca.crt -n openshift-config</programlisting>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit image.config.openshift.io cluster</programlisting>
<programlisting language="yaml" linenumbering="unnumbered">spec:
  additionalTrustedCA:
    name: registry-config</programlisting>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="images-configuration-registry-mirror_image-configuration">
<title>Understanding image registry repository mirroring</title>
<simpara role="_abstract">By setting up container registry repository mirroring, you can perform the following tasks:</simpara>
<itemizedlist>
<listitem>
<simpara>Configure your OpenShift Container Platform cluster to redirect requests to pull images from a repository on a source image registry and have it resolved by a repository on a mirrored image registry.</simpara>
</listitem>
<listitem>
<simpara>Identify multiple mirrored repositories for each target repository, to make sure that if one mirror is down, another can be used.</simpara>
</listitem>
</itemizedlist>
<simpara>Repository mirroring in OpenShift Container Platform includes the following attributes:</simpara>
<itemizedlist>
<listitem>
<simpara>Image pulls are resilient to registry downtimes.</simpara>
</listitem>
<listitem>
<simpara>Clusters in disconnected environments can pull images from critical locations, such as <literal>quay.io</literal>, and have registries behind a company firewall provide the requested images.</simpara>
</listitem>
<listitem>
<simpara>A particular order of registries is tried when an image pull request is made, with the permanent registry typically being the last one tried.</simpara>
</listitem>
<listitem>
<simpara>The mirror information you enter is added to the <literal>/etc/containers/registries.conf</literal> file on every node in the OpenShift Container Platform cluster.</simpara>
</listitem>
<listitem>
<simpara>When a node makes a request for an image from the source repository, it tries each mirrored repository in turn until it finds the requested content. If all mirrors fail, the cluster tries the source repository. If successful, the image is pulled to the node.</simpara>
</listitem>
</itemizedlist>
<simpara>You can set up repository mirroring in the following ways:</simpara>
<itemizedlist>
<listitem>
<simpara>At OpenShift Container Platform installation:</simpara>
<simpara>By pulling container images needed by OpenShift Container Platform and then bringing those images behind your company&#8217;s firewall, you can install OpenShift Container Platform into a data center that is in a disconnected environment.</simpara>
</listitem>
<listitem>
<simpara>After OpenShift Container Platform installation:</simpara>
<simpara>If you did not configure mirroring during OpenShift Container Platform installation, you can do so postinstallation by using any of the following custom resource (CR) objects:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>ImageDigestMirrorSet</literal> (IDMS). This object allows you to pull images from a mirrored registry by using digest specifications. The IDMS CR enables you to set a fall back policy that allows or stops continued attempts to pull from the source registry if the image pull fails.</simpara>
</listitem>
<listitem>
<simpara><literal>ImageTagMirrorSet</literal> (ITMS). This object allows you to pull images from a mirrored registry by using image tags. The ITMS CR enables you to set a fall back policy that allows or stops continued attempts to pull from the source registry if the image pull fails.</simpara>
</listitem>
<listitem>
<simpara><literal>ImageContentSourcePolicy</literal> (ICSP). This object allows you to pull images from a mirrored registry by using digest specifications. The ICSP CR always falls back to the source registry if the mirrors do not work.</simpara>
<important>
<simpara>Using an <literal>ImageContentSourcePolicy</literal> (ICSP) object to configure repository mirroring is a deprecated feature. Deprecated functionality is still included in OpenShift Container Platform and continues to be supported. It will be removed in a future release and is not recommended for new deployments.</simpara>
<simpara>If you have existing YAML files that you used to create <literal>ImageContentSourcePolicy</literal> objects, you can use the <literal>oc adm migrate icsp</literal> command to convert those files to a <literal>ImageDigestMirrorSet</literal> YAML files. For more information, see "Converting ImageContentSourcePolicy (ICSP) files for image registry repository mirroring".</simpara>
</important>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
<simpara>Each of these custom resource objects identify the following information:</simpara>
<itemizedlist>
<listitem>
<simpara>The source of the container image repository you want to mirror.</simpara>
</listitem>
<listitem>
<simpara>A separate entry for each mirror repository you want to offer the content</simpara>
</listitem>
</itemizedlist>
<simpara>Note the following actions and how they affect node drain behavior:</simpara>
<itemizedlist>
<listitem>
<simpara>If you create an IDMS or ICSP CR object, the MCO does not drain or reboot the node.</simpara>
</listitem>
<listitem>
<simpara>If you create an ITMS CR object, the MCO drains and reboots the node.</simpara>
</listitem>
<listitem>
<simpara>If you delete an ITMS, IDMS, or ICSP CR object, the MCO drains and reboots the node.</simpara>
</listitem>
<listitem>
<simpara>If you modify an ITMS, IDMS, or ICSP CR object, the MCO drains and reboots the node.</simpara>
<important>
<itemizedlist>
<listitem>
<simpara>When the MCO detects any of the following changes, it applies the update without draining or rebooting the node:</simpara>
<itemizedlist>
<listitem>
<simpara>Changes to the SSH key in the <literal>spec.config.passwd.users.sshAuthorizedKeys</literal> parameter of a machine config.</simpara>
</listitem>
<listitem>
<simpara>Changes to the global pull secret or pull secret in the <literal>openshift-config</literal> namespace.</simpara>
</listitem>
<listitem>
<simpara>Automatic rotation of the <literal>/etc/kubernetes/kubelet-ca.crt</literal> certificate authority (CA) by the Kubernetes API Server Operator.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>When the MCO detects changes to the <literal>/etc/containers/registries.conf</literal> file, such as editing an <literal>ImageDigestMirrorSet</literal>, <literal>ImageTagMirrorSet</literal>, or <literal>ImageContentSourcePolicy</literal> object, it drains the corresponding nodes, applies the changes, and uncordons the nodes. The node drain does not happen for the following changes:</simpara>
<itemizedlist>
<listitem>
<simpara>The addition of a registry with the <literal>pull-from-mirror = "digest-only"</literal> parameter set for each mirror.</simpara>
</listitem>
<listitem>
<simpara>The addition of a mirror with the <literal>pull-from-mirror = "digest-only"</literal> parameter set in a registry.</simpara>
</listitem>
<listitem>
<simpara>The addition of items to the <literal>unqualified-search-registries</literal> list.</simpara>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
</important>
</listitem>
</itemizedlist>
<simpara>For new clusters, you can use IDMS, ITMS, and ICSP CRs objects as needed. However, using IDMS and ITMS is recommended.</simpara>
<simpara>If you upgraded a cluster, any existing ICSP objects remain stable, and both IDMS and ICSP objects are supported. Workloads that use ICSP objects continue to function as expected. However, if you want to take advantage of the fallback policies introduced in the IDMS CRs, you can migrate current workloads to IDMS objects by using the <literal>oc adm migrate icsp</literal> command as shown in the <emphasis role="strong">Converting ImageContentSourcePolicy (ICSP) files for image registry repository mirroring</emphasis> section that follows. Migrating to IDMS objects does not require a cluster reboot.</simpara>
<note>
<simpara>If your cluster uses an <literal>ImageDigestMirrorSet</literal>, <literal>ImageTagMirrorSet</literal>, or <literal>ImageContentSourcePolicy</literal> object to configure repository mirroring, you can use only global pull secrets for mirrored registries. You cannot add a pull secret to a project.</simpara>
</note>
<section xml:id="images-configuration-registry-mirror-configuring_image-configuration">
<title>Configuring image registry repository mirroring</title>
<simpara role="_abstract">You can create postinstallation mirror configuration custom resources (CR) to redirect image pull requests from a source image registry to a mirrored image registry.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the <literal>dedicated-admin</literal> role.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Configure mirrored repositories, by either:</simpara>
<itemizedlist>
<listitem>
<simpara>Setting up a mirrored repository with Red&#160;Hat Quay. You can copy images from one repository to another and also automatically sync those repositories repeatedly over time by using Red&#160;Hat Quay.</simpara>
<itemizedlist>
<listitem>
<simpara><link xlink:href="https://access.redhat.com/documentation/en-us/red_hat_quay/3/html/manage_red_hat_quay/repo-mirroring-in-red-hat-quay">Red&#160;Hat Quay Repository Mirroring</link></simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Using a tool such as <literal>skopeo</literal> to copy images manually from the source repository to the mirrored repository.</simpara>
<simpara>For example, after installing the skopeo RPM package on a {op-system-base-full system}, use the <literal>skopeo</literal> command as shown in the following example:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ skopeo copy --all \
docker://registry.access.redhat.com/ubi9/ubi-minimal:latest@sha256:5cf... \
docker://example.io/example/ubi-minimal</programlisting>
<simpara>In this example, you have a container image registry named <literal>example.io</literal> and image repository named <literal>example</literal>. You want to copy the <literal>ubi9/ubi-minimal</literal> image from <literal>registry.access.redhat.com</literal> to <literal>example.io</literal>. After you create the mirrored registry, you can configure your OpenShift Container Platform cluster to redirect requests made to the source repository to the mirrored repository.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Create a postinstallation mirror configuration custom resource (CR), by using one of the following examples:</simpara>
<itemizedlist>
<listitem>
<simpara>Create an <literal>ImageDigestMirrorSet</literal> or <literal>ImageTagMirrorSet</literal> CR, as needed, replacing the source and mirrors with your own registry and repository pairs and images:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: config.openshift.io/v1
kind: ImageDigestMirrorSet
metadata:
  name: ubi9repo
spec:
  imageDigestMirrors:
  - mirrors:
    - example.io/example/ubi-minimal
    - example.com/example2/ubi-minimal
    source: registry.access.redhat.com/ubi9/ubi-minimal
    mirrorSourcePolicy: AllowContactingSource
  - mirrors:
    - mirror.example.com/redhat
    source: registry.example.com/redhat
    mirrorSourcePolicy: AllowContactingSource
  - mirrors:
    - mirror.example.com
    source: registry.example.com
    mirrorSourcePolicy: AllowContactingSource
  - mirrors:
    - mirror.example.net/image
    source: registry.example.com/example/myimage
    mirrorSourcePolicy: AllowContactingSource
  - mirrors:
    - mirror.example.net
    source: registry.example.com/example
    mirrorSourcePolicy: AllowContactingSource
  - mirrors:
    - mirror.example.net/registry-example-com
    source: registry.example.com
    mirrorSourcePolicy: AllowContactingSource</programlisting>
</listitem>
<listitem>
<simpara>Create an <literal>ImageContentSourcePolicy</literal> custom resource, replacing the source and mirrors with your own registry and repository pairs and images:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: operator.openshift.io/v1alpha1
kind: ImageContentSourcePolicy
metadata:
  name: mirror-ocp
spec:
  repositoryDigestMirrors:
  - mirrors:
    - mirror.registry.com:443/ocp/release
    source: quay.io/openshift-release-dev/ocp-release
  - mirrors:
    - mirror.registry.com:443/ocp/release
    source: quay.io/openshift-release-dev/ocp-v4.0-art-dev</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>- mirror.registry.com:443/ocp/release</literal></term>
<listitem>
<simpara>Specifies the name of the mirror image registry and repository.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>source: quay.io/openshift-release-dev/ocp-release</literal></term>
<listitem>
<simpara>Specifies the online registry and repository containing the content that is mirrored.</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Create the new object by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create -f registryrepomirror.yaml</programlisting>
<simpara>After the object is created, the Machine Config Operator (MCO) drains the nodes for <literal>ImageTagMirrorSet</literal> objects only. The MCO does not drain the nodes for <literal>ImageDigestMirrorSet</literal> and <literal>ImageContentSourcePolicy</literal> objects.</simpara>
</listitem>
<listitem>
<simpara>To check that the mirrored configuration settings are applied, do the following on one of the nodes.</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>List your nodes:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get node</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                           STATUS                     ROLES    AGE  VERSION
ip-10-0-137-44.ec2.internal    Ready                      worker   7m   v1.34.2
ip-10-0-138-148.ec2.internal   Ready                      master   11m  v1.34.2
ip-10-0-139-122.ec2.internal   Ready                      master   11m  v1.34.2
ip-10-0-147-35.ec2.internal    Ready                      worker   7m   v1.34.2
ip-10-0-153-12.ec2.internal    Ready                      worker   7m   v1.34.2
ip-10-0-154-10.ec2.internal    Ready                      master   11m  v1.34.2</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Start the debugging process to access the node:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc debug node/ip-10-0-147-35.ec2.internal</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Starting pod/ip-10-0-147-35ec2internal-debug ...
To use host binaries, run `chroot /host`</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Change your root directory to <literal>/host</literal>:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">sh-4.2# chroot /host</programlisting>
</listitem>
<listitem>
<simpara>Check the <literal>/etc/containers/registries.conf</literal> file to make sure the changes were made:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">sh-4.2# cat /etc/containers/registries.conf</programlisting>
<simpara>The following output represents a <literal>registries.conf</literal> file where postinstallation mirror configuration CRs are applied.</simpara>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">unqualified-search-registries = ["registry.access.redhat.com", "docker.io"]
short-name-mode = ""

[[registry]]
  prefix = ""
  location = "registry.access.redhat.com/ubi9/ubi-minimal"

  [[registry.mirror]]
    location = "example.io/example/ubi-minimal"
    pull-from-mirror = "digest-only"

  [[registry.mirror]]
    location = "example.com/example/ubi-minimal"
    pull-from-mirror = "digest-only"

[[registry]]
  prefix = ""
  location = "registry.example.com"

  [[registry.mirror]]
    location = "mirror.example.net/registry-example-com"
    pull-from-mirror = "digest-only"

[[registry]]
  prefix = ""
  location = "registry.example.com/example"

  [[registry.mirror]]
    location = "mirror.example.net"
    pull-from-mirror = "digest-only"

[[registry]]
  prefix = ""
  location = "registry.example.com/example/myimage"

  [[registry.mirror]]
    location = "mirror.example.net/image"
    pull-from-mirror = "digest-only"

[[registry]]
  prefix = ""
  location = "registry.example.com"

  [[registry.mirror]]
    location = "mirror.example.com"
    pull-from-mirror = "digest-only"

[[registry]]
  prefix = ""
  location = "registry.example.com/redhat"

  [[registry.mirror]]
    location = "mirror.example.com/redhat"
    pull-from-mirror = "digest-only"
[[registry]]
  prefix = ""
  location = "registry.access.redhat.com/ubi9/ubi-minimal"
  blocked = true

  [[registry.mirror]]
    location = "example.io/example/ubi-minimal-tag"
    pull-from-mirror = "tag-only"</programlisting>
</para>
</formalpara>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>[[registry]].location = "registry.access.redhat.com/ubi9/ubi-minimal"</literal></term>
<listitem>
<simpara>The repository listed in a pull spec.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>[[registry.mirror]].location = "example.io/example/ubi-minimal"</literal></term>
<listitem>
<simpara>Indicates the mirror for that repository.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>[[registry.mirror]].pull-from-mirror = "digest-only"</literal></term>
<listitem>
<simpara>Means that the image pull from the mirror is a digest reference image.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>[[registry]].blocked = true</literal></term>
<listitem>
<simpara>Indicates that the <literal>NeverContactSource</literal> parameter is set for this repository.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>[[registry.mirror]].pull-from-mirror = "tag-only"</literal></term>
<listitem>
<simpara>Indicates that the image pull from the mirror is a tag reference image.</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
<listitem>
<simpara>Pull an image to the node from the source and check if it is resolved by the mirror.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">sh-4.2# podman pull --log-level=debug registry.access.redhat.com/ubi9/ubi-minimal@sha256:5cf...</programlisting>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<formalpara>
<title>Troubleshooting</title>
<para>If the repository mirroring procedure does not work as described, use the following information about how repository mirroring works to help troubleshoot the problem:</para>
</formalpara>
<itemizedlist>
<listitem>
<simpara>The first working mirror is used to supply the pulled image.</simpara>
</listitem>
<listitem>
<simpara>The main registry is only used if no other mirror works.</simpara>
</listitem>
<listitem>
<simpara>From the system context, the <literal>Insecure</literal> flags are used as fallback.</simpara>
</listitem>
<listitem>
<simpara>The format of the <literal>/etc/containers/registries.conf</literal> file has changed recently. It is now version 2 and in TOML format.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="images-configuration-registry-mirror-config-params_image-configuration">
<title>Image registry repository mirroring configuration parameters</title>
<simpara role="_abstract">You can use the following table for information about parameters when configuring your image repository for mirroring.</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="2">
<colspec colname="col_1" colwidth="33.3333*"/>
<colspec colname="col_2" colwidth="66.6667*"/>
<thead>
<row>
<entry align="left" valign="top"><emphasis role="strong">Parameter</emphasis></entry>
<entry align="left" valign="top"><emphasis role="strong">Values and Information</emphasis></entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>apiVersion:</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Required. The value must be <literal>config.openshift.io/v1</literal> API.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>kind:</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The kind of object according to the pull type. The <literal>ImageDigestMirrorSet</literal> type pulls a digest reference image The <literal>ImageTagMirrorSet</literal> type pulls a tag reference image.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>spec: imageDigestMirrors:</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The type of image pull method. Use <literal>imageDigestMirrors`for an `ImageDigestMirrorSet</literal> CR. Use <literal>imageTagMirrors</literal> for an <literal>ImageTagMirrorSet</literal> CR.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>- mirrors: - example.io/example/ubi-minimal</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The name of the mirrored image registry and repository.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>- mirrors: -example.com/example2/ubi-minimal</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The value of this parameter is the name of a secondary mirror repository for each target repository. If one mirror is down the target repository can use the secondary mirror.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>source: registry.access.redhat.com/ubi9/ubi-minimal</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The registry and repository source. The source is the repository that is listed in an image pull specification.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>mirrorSourcePolicy: AllowContactingSource</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Optional parameter that indicates the fallback policy if the image pull fails. The <literal>AllowContactingSource</literal> value allows continued attempts to pull the image from the source repository. Default value. <literal>NeverContactSource</literal> prevents continued attempts to pull the image from the source repository.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>source: registry.example.com/redhat</literal>: An optional parameter that indicates a namespace inside a registry. Setting a namespace inside a registry allows use of any image in that namespace. If you use a registry domain as a source, the object applies to all of the repositories from the registry.</simpara></entry>
<entry align="left" valign="top"><simpara><literal>source: registry.example.com</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Optional parameter that indicates a registry. Allows us of any image in that registry. If you specify a registry name, the object applies to all repositories from a source registry to a mirror registry.</simpara></entry>
<entry align="left" valign="top"><simpara><literal>source: registry.example.com/example/myimage</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Pulls the image <literal>registry.example.com/example/myimage@sha256:&#8230;&#8203;</literal> from the mirror <literal>mirror.example.net/image@sha256:..</literal>.</simpara></entry>
<entry align="left" valign="top"><simpara><literal>source: registry.example.com/example</literal></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Pulls the image <literal>registry.example.com/example/image@sha256:&#8230;&#8203;</literal> in the source registry namespace from the mirror <literal>mirror.example.net/image@sha256:&#8230;&#8203;</literal>.</simpara></entry>
<entry align="left" valign="top"><simpara><literal>source: registry.example.com</literal></simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
</section>
<section xml:id="images-configuration-registry-mirror-convert_image-configuration">
<title>Converting ImageContentSourcePolicy (ICSP) files for image registry repository mirroring</title>
<simpara role="_abstract">Using an <literal>ImageContentSourcePolicy</literal> (ICSP) object to configure repository mirroring is a deprecated feature.</simpara>
<simpara>This functionality is still included in OpenShift Container Platform and continues to be supported; however, it will be removed in a future release of this product and is not recommended for new deployments.</simpara>
<simpara>ICSP objects are being replaced by <literal>ImageDigestMirrorSet</literal> and <literal>ImageTagMirrorSet</literal> objects to configure repository mirroring. If you have existing YAML files that you used to create <literal>ImageContentSourcePolicy</literal> objects, you can use the <literal>oc adm migrate icsp</literal> command to convert those files to an <literal>ImageDigestMirrorSet</literal> YAML file. The command updates the API to the current version, changes the <literal>kind</literal> value to <literal>ImageDigestMirrorSet</literal>, and changes <literal>spec.repositoryDigestMirrors</literal> to <literal>spec.imageDigestMirrors</literal>. The rest of the file is not changed.</simpara>
<simpara>Because the migration does not change the <literal>registries.conf</literal> file, the cluster does not need to reboot.</simpara>
<simpara>For more information about <literal>ImageDigestMirrorSet</literal> or <literal>ImageTagMirrorSet</literal> objects, see "Configuring image registry repository mirroring" in the previous section.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the <literal>dedicated-admin</literal> role.</simpara>
</listitem>
<listitem>
<simpara>Ensure that you have <literal>ImageContentSourcePolicy</literal> objects on your cluster.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Use the following command to convert one or more <literal>ImageContentSourcePolicy</literal> YAML files to an <literal>ImageDigestMirrorSet</literal> YAML file:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc adm migrate icsp &lt;file_name&gt;.yaml &lt;file_name&gt;.yaml &lt;file_name&gt;.yaml --dest-dir &lt;path_to_the_directory&gt;</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>&lt;file_name&gt;</literal></term>
<listitem>
<simpara>Specifies the name of the source <literal>ImageContentSourcePolicy</literal> YAML. You can list multiple file names.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>--dest-dir</literal></term>
<listitem>
<simpara>Optional: Specifies a directory for the output <literal>ImageDigestMirrorSet</literal> YAML. If unset, the file is written to the current directory.</simpara>
<simpara>For example, the following command converts the <literal>icsp.yaml</literal> and <literal>icsp-2.yaml</literal> file and saves the new YAML files to the <literal>idms-files</literal> directory.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc adm migrate icsp icsp.yaml icsp-2.yaml --dest-dir idms-files</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">wrote ImageDigestMirrorSet to idms-files/imagedigestmirrorset_ubi8repo.5911620242173376087.yaml
wrote ImageDigestMirrorSet to idms-files/imagedigestmirrorset_ubi9repo.6456931852378115011.yaml</programlisting>
</para>
</formalpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
<listitem>
<simpara>Create the CR object by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc create -f &lt;path_to_the_directory&gt;/&lt;file-name&gt;.yaml</programlisting>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>&lt;path_to_the_directory&gt;</literal></term>
<listitem>
<simpara>Specifies the path to the directory, if you used the <literal>--dest-dir</literal> flag.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>&lt;file_name&gt;</literal></term>
<listitem>
<simpara>Specifies the name of the <literal>ImageDigestMirrorSet</literal> YAML.</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
<listitem>
<simpara>Remove the ICSP objects after the IDMS objects are rolled out.</simpara>
</listitem>
</orderedlist>
</section>
</section>
</chapter>
<chapter xml:id="_using-images">
<title>Using images</title>
<section xml:id="using-images-overview">
<title>Using images overview</title>
<simpara role="_abstract">To build and deploy containerized applications in OpenShift Container Platform, you can use Source-to-Image (S2I), database, and other container images. These images provide the base components you need to run applications on your cluster.</simpara>
<simpara>Red&#160;Hat official container images are provided in the Red&#160;Hat Registry at <link xlink:href="https://registry.redhat.io">registry.redhat.io</link>. OpenShift Container Platform&#8217;s supported S2I, database, and Jenkins images are provided in the <literal>openshift4</literal> repository in the Red&#160;Hat Quay Registry. For example, <literal>quay.io/openshift-release-dev/ocp-v4.0-&lt;address&gt;</literal> is the name of the OpenShift Application Platform image.</simpara>
<simpara>The xPaaS middleware images are provided in their respective product repositories on the Red Hat Registry but suffixed with a <literal>-openshift</literal>. For example, <literal>registry.redhat.io/jboss-eap-6/eap64-openshift</literal> is the name of the JBoss EAP image.</simpara>
<simpara>All Red&#160;Hat supported images covered in this section are described in the <link xlink:href="https://catalog.redhat.com/software/containers/explore">Container images section of the Red Hat Ecosystem Catalog</link>. For every version of each image, you can find details on its contents and usage. Browse or search for the image that interests you.</simpara>
<important>
<simpara>The newer versions of container images are not compatible with earlier versions of OpenShift Container Platform. Verify and use the correct version of container images, based on your version of OpenShift Container Platform.</simpara>
</important>
</section>
<section xml:id="using-s21-images">
<title>Source-to-image</title>
<simpara role="_abstract">To create containerized applications in OpenShift Container Platform without manually configuring runtime environments, you can use Source-to-Image (S2I) images. S2I images are runtime base images for languages like Node.js, Python, and Java that you can insert your code into.</simpara>
<simpara>You can use the <link xlink:href="https://access.redhat.com/documentation/en-us/red_hat_software_collections/3/html-single/using_red_hat_software_collections_container_images/index">Red&#160;Hat Software Collections</link> images as a foundation for applications that rely on specific runtime environments such as Node.js, Perl, or Python.</simpara>
<simpara>You can use the <link xlink:href="https://docs.redhat.com/en/documentation/red_hat_build_of_openjdk/11/html/using_source-to-image_for_openshift_with_red_hat_build_of_openjdk_11/openjdk-overview-s2i-openshift">Introduction to source-to-image for OpenShift</link> documentation as a reference for runtime environments that use Java.</simpara>
<simpara>S2I images are also available though the <link linkend="configuring-samples-operator">Cluster Samples Operator</link>.</simpara>
<section xml:id="accessing-s2i-builder-images-in-developer-console_using-s21-images">
<title>Accessing S2I builder images in the OpenShift Container Platform Developer Console</title>
<simpara role="_abstract">You can access S2I builder images through the Developer Console in the web console.
You need these images to build containerized applications from your source code.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Log in to the OpenShift Container Platform web console using your login credentials. The default view for the OpenShift Container Platform web console is the <emphasis role="strong">Administrator</emphasis> perspective.</simpara>
</listitem>
<listitem>
<simpara>Use the perspective switcher to switch to the <emphasis role="strong">Developer</emphasis> perspective.</simpara>
</listitem>
<listitem>
<simpara>In the <emphasis role="strong">+Add</emphasis> view, use the <emphasis role="strong">Project</emphasis> drop-down list to select an existing project or create a new project.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">All services</emphasis> in the <emphasis role="strong">Developer Catalog</emphasis> tile.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Builder Images</emphasis> under <emphasis role="strong">Type</emphasis> to see the available S2I images.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="images-s2i-build-process-overview_using-s21-images">
<title>Source-to-image build process overview</title>
<simpara role="_abstract">Source-to-image (S2I) is a build process in OpenShift Container Platform that injects your source code into a container image. S2I automates the creation of ready-to-run container images from your application source code without manual configuration.</simpara>
<simpara>S2I performs the following steps:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Runs the <literal>FROM &lt;builder image&gt;</literal> command</simpara>
</listitem>
<listitem>
<simpara>Copies the source code to a defined location in the builder image</simpara>
</listitem>
<listitem>
<simpara>Runs the assemble script in the builder image</simpara>
</listitem>
<listitem>
<simpara>Sets the run script in the builder image as the default command</simpara>
</listitem>
</orderedlist>
<simpara>Buildah then creates the container image.</simpara>
</section>
<section xml:id="additional-resources_using-s21-images" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara><link linkend="configuring-samples-operator">Configuring the Cluster Samples Operator</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="images-create-s2i_create-images">Creating images from source code with source-to-image</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="images-test-s2i_create-images">About testing source-to-image images</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="images-create-s2i_create-images">Creating images from source code with source-to-image</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="customizing-s2i-images">
<title>Customizing source-to-image images</title>
<simpara role="_abstract">To modify the default assemble and run script behavior in OpenShift Container Platform, you can customize source-to-image (S2I) builder images.
You can adapt S2I builders to meet your specific application requirements when the default scripts are not suitable.</simpara>
<section xml:id="images-using-customizing-s2i-images-scripts-embedded_customizing-s2i-images">
<title>Invoking scripts embedded in an image</title>
<simpara role="_abstract">To extend builder image behavior while preserving supported script logic and upgrade compatibility in OpenShift Container Platform, you can start embedded S2I image scripts by creating wrapper scripts.
These wrapper scripts run custom logic and then call the default scripts from the image.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Inspect the value of the <literal>io.openshift.s2i.scripts-url</literal> label to determine the location of the scripts inside of the builder image:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ podman inspect --format='{{ index .Config.Labels "io.openshift.s2i.scripts-url" }}' wildfly/wildfly-centos7</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">image:///usr/libexec/s2i</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Create a script that includes an invocation of one of the standard scripts wrapped in other commands:</simpara>
<formalpara>
<title><literal>.s2i/bin/assemble</literal> script</title>
<para>
<programlisting language="bash" linenumbering="unnumbered">#!/bin/bash
echo "Before assembling"

/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc</programlisting>
</para>
</formalpara>
<simpara>This example shows a custom assemble script that prints the message, runs the standard assemble script from the image, and prints another message depending on the exit code of the assemble script.</simpara>
<important>
<simpara>When wrapping the run script, you must use <literal>exec</literal> for invoking it to ensure signals are handled properly. The use of <literal>exec</literal> also precludes the ability to run additional commands after invoking the default image run script.</simpara>
</important>
<formalpara>
<title><literal>.s2i/bin/run</literal> script</title>
<para>
<programlisting language="bash" linenumbering="unnumbered">#!/bin/bash
echo "Before running application"
exec /usr/libexec/s2i/run</programlisting>
</para>
</formalpara>
</listitem>
</orderedlist>
</section>
</section>
</chapter>
</book>
