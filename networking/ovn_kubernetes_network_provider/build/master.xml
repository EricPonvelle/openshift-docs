<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE book [
<!ENTITY % sgml.features "IGNORE">
<!ENTITY % xml.features "INCLUDE">
<!ENTITY % DOCBOOK_ENTS PUBLIC "-//OASIS//ENTITIES DocBook Character Entities V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/dbcentx.mod">
%DOCBOOK_ENTS;
]>
<?asciidoc-toc?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0">
<info>
<title>OVN-Kubernetes network plugin</title>
<date>2026-02-25</date>
<title>OVN-Kubernetes network plugin</title>
<productname>OpenShift Container Platform</productname>
<productnumber>4.16</productnumber>
<subtitle>Enter a short description here.</subtitle>
<abstract>
    <para>A short overview and summary of the book's subject and purpose, traditionally no more than one paragraph long.</para>
</abstract>
<authorgroup>
    <orgname>Red Hat OpenShift Documentation Team</orgname>
</authorgroup>
<xi:include href="Common_Content/Legal_Notice.xml" xmlns:xi="http://www.w3.org/2001/XInclude" />
</info>
<chapter xml:id="about-ovn-kubernetes">
<title>About the OVN-Kubernetes network plugin</title>
<simpara>The OpenShift Container Platform cluster uses a virtualized network for pod and service networks.</simpara>
<simpara>Part of Red Hat OpenShift Networking, the OVN-Kubernetes network plugin is the default network provider for OpenShift Container Platform. OVN-Kubernetes is based on Open Virtual Network (OVN) and provides an overlay-based networking implementation. A cluster that uses the OVN-Kubernetes plugin also runs Open vSwitch (OVS) on each node. OVN configures OVS on each node to implement the declared network configuration.</simpara>
<note>
<simpara>OVN-Kubernetes is the default networking solution for OpenShift Container Platform and single-node OpenShift deployments.</simpara>
</note>
<simpara>OVN-Kubernetes, which arose from the OVS project, uses many of the same constructs, such as open flow rules, to decide how packets travel through the network. For more information, see the <link xlink:href="https://www.ovn.org/en/">Open Virtual Network website</link>.</simpara>
<simpara>OVN-Kubernetes is a series of daemons for OVS that transform virtual network configurations into <literal>OpenFlow</literal> rules. <literal>OpenFlow</literal> is a protocol for communicating with network switches and routers, providing a means for remotely controlling the flow of network traffic on a network device. This means that network administrators can configure, manage, and watch the flow of network traffic.</simpara>
<simpara>OVN-Kubernetes provides more of the advanced functionality not available with <literal>OpenFlow</literal>. OVN supports distributed virtual routing, distributed logical switches, access control, Dynamic Host Configuration Protocol (DHCP), and DNS. OVN implements distributed virtual routing within logic flows that equate to open flows. For example, if you have a pod that sends out a DHCP request to the DHCP server on the network, a logic flow rule in the request helps the OVN-Kubernetes handle the packet. This means that the server can respond with gateway, DNS server, IP address, and other information.</simpara>
<simpara>OVN-Kubernetes runs a daemon on each node. There are daemon sets for the databases and for the OVN controller that run on every node. The OVN controller programs the Open vSwitch daemon on the nodes to support the following network provider features:</simpara>
<itemizedlist>
<listitem>
<simpara>Egress IPs</simpara>
</listitem>
<listitem>
<simpara>Firewalls</simpara>
</listitem>
<listitem>
<simpara>Hardware offloading</simpara>
</listitem>
<listitem>
<simpara>Hybrid networking</simpara>
</listitem>
<listitem>
<simpara>Internet Protocol Security (IPsec) encryption</simpara>
</listitem>
<listitem>
<simpara>IPv6</simpara>
</listitem>
<listitem>
<simpara>Multicast.</simpara>
</listitem>
<listitem>
<simpara>Network policy and network policy logs</simpara>
</listitem>
<listitem>
<simpara>Routers</simpara>
</listitem>
</itemizedlist>
<section xml:id="nw-ovn-kubernetes-purpose_about-ovn-kubernetes">
<title>OVN-Kubernetes purpose</title>
<simpara>The OVN-Kubernetes network plugin is an open-source, fully-featured Kubernetes CNI plugin that uses Open Virtual Network (OVN) to manage network traffic flows. OVN is a community developed, vendor-agnostic network virtualization solution. The OVN-Kubernetes network plugin uses the following technologies:</simpara>
<itemizedlist>
<listitem>
<simpara>OVN to manage network traffic flows.</simpara>
</listitem>
<listitem>
<simpara>Kubernetes network policy support and logs, including ingress and egress rules.</simpara>
</listitem>
<listitem>
<simpara>The Generic Network Virtualization Encapsulation (Geneve) protocol, rather than Virtual Extensible LAN (VXLAN), to create an overlay network between nodes.</simpara>
</listitem>
</itemizedlist>
<simpara>The OVN-Kubernetes network plugin supports the following capabilities:</simpara>
<itemizedlist>
<listitem>
<simpara>Hybrid clusters that can run both Linux and Microsoft Windows workloads. This environment is known as <emphasis>hybrid networking</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Offloading of network data processing from the host central processing unit (CPU) to compatible network cards and data processing units (DPUs). This is known as <emphasis>hardware offloading</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>IPv4-primary dual-stack networking on bare-metal, VMware vSphere, IBM Power&#174;, IBM Z&#174;, and Red&#160;Hat OpenStack Platform (RHOSP) platforms.</simpara>
</listitem>
<listitem>
<simpara>IPv6 single-stack networking on RHOSP and bare metal platforms.</simpara>
</listitem>
<listitem>
<simpara>IPv6-primary dual-stack networking for a cluster running on a bare-metal, a VMware vSphere, or an RHOSP platform.</simpara>
</listitem>
<listitem>
<simpara>Egress firewall devices and egress IP addresses.</simpara>
</listitem>
<listitem>
<simpara>Egress router devices that operate in redirect mode.</simpara>
</listitem>
<listitem>
<simpara>IPsec encryption of intracluster communications.</simpara>
</listitem>
</itemizedlist>
<simpara>Red&#160;Hat does not support the following postinstallation configurations that use the OVN-Kubernetes network plugin:</simpara>
<itemizedlist>
<listitem>
<simpara>Configuring the primary network interface, including using the NMState Operator to configure bonding for the interface.</simpara>
</listitem>
<listitem>
<simpara>Configuring a sub-interface or additional network interface on a network device that uses the Open vSwitch (OVS) or an OVN-Kubernetes <literal>br-ex</literal> bridge network.</simpara>
</listitem>
<listitem>
<simpara>Creating additional virtual local area networks (VLANs) on the primary network interface.</simpara>
</listitem>
<listitem>
<simpara>Using the primary network interface, such as <literal>eth0</literal> or <literal>bond0</literal>, that you created for a node during cluster installation to create additional secondary networks.</simpara>
</listitem>
</itemizedlist>
<simpara>Red&#160;Hat does support the following postinstallation configurations that use the OVN-Kubernetes network plugin:</simpara>
<itemizedlist>
<listitem>
<simpara>Creating additional VLANs from the base physical interface, such as <literal>eth0.100</literal>, where you configured the primary network interface as a VLAN for a node during cluster installation. This works because the Open vSwitch (OVS) bridge attaches to the initial VLAN sub-interface, such as <literal>eth0.100</literal>, leaving the base physical interface available for new configurations.</simpara>
</listitem>
<listitem>
<simpara>Creating an additional OVN secondary network with a <literal>localnet</literal> topology network requires that you define the secondary network in a <literal>NodeNetworkConfigurationPolicy</literal> (NNCP) object. After you create the network, pods or virtual machines (VMs) can then attach to the network. These secondary networks give a dedicated connection to the physical network, which might or might not use VLAN tagging. You cannot access these networks from the host network of a node where the host does not have the required setup, such as the required network settings.</simpara>
</listitem>
<listitem>
<simpara>Defining additional VLANs in the <literal>vlanID</literal> parameter of the <literal>NetworkAttachmentDefinition</literal> (NAD) custom resource (CR). By mapping the <literal>br-ex</literal> bridge to a physical interface, the interface acts as a trunk port that can carry multiple VLANs. OVN-Kubernetes can then manage tagging and untagging of network packets, also known as <emphasis>Ethernet frames</emphasis>. To ensure strong connectivity, configure the physical switch ports as trunks.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="nw-ovn-kubernetes-limitations_about-ovn-kubernetes">
<title>OVN-Kubernetes IPv6 and dual-stack limitations</title>
<simpara>The OVN-Kubernetes network plugin has the following limitations:</simpara>
<itemizedlist>
<listitem>
<simpara>For clusters configured for dual-stack networking, both IPv4 and IPv6 traffic must use the same network interface as the default gateway.</simpara>
<simpara>If this requirement is not met, pods on the host in the <literal>ovnkube-node</literal> daemon set enter the <literal>CrashLoopBackOff</literal> state.</simpara>
<simpara>If you display a pod with a command such as <literal>oc get pod -n openshift-ovn-kubernetes -l app=ovnkube-node -o yaml</literal>, the <literal>status</literal> field has more than one message about the default gateway, as shown in the following output:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">I1006 16:09:50.985852   60651 helper_linux.go:73] Found default gateway interface br-ex 192.168.127.1
I1006 16:09:50.985923   60651 helper_linux.go:73] Found default gateway interface ens4 fe80::5054:ff:febe:bcd4
F1006 16:09:50.985939   60651 ovnkube.go:130] multiple gateway interfaces detected: br-ex ens4</programlisting>
<simpara>The only resolution is to reconfigure the host networking so that both IP families use the same network interface for the default gateway.</simpara>
</listitem>
<listitem>
<simpara>For clusters configured for dual-stack networking, both the IPv4 and IPv6 routing tables must contain the default gateway.</simpara>
<simpara>If this requirement is not met, pods on the host in the <literal>ovnkube-node</literal> daemon set enter the <literal>CrashLoopBackOff</literal> state.</simpara>
<simpara>If you display a pod with a command such as <literal>oc get pod -n openshift-ovn-kubernetes -l app=ovnkube-node -o yaml</literal>, the <literal>status</literal> field has more than one message about the default gateway, as shown in the following output:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">I0512 19:07:17.589083  108432 helper_linux.go:74] Found default gateway interface br-ex 192.168.123.1
F0512 19:07:17.589141  108432 ovnkube.go:133] failed to get default gateway interface</programlisting>
<simpara>The only resolution is to reconfigure the host networking so that both IP families contain the default gateway.</simpara>
</listitem>
<listitem>
<simpara>If you set the <literal>ipv6.disable</literal> parameter to <literal>1</literal> in the <literal>kernelArgument</literal> section of the <literal>MachineConfig</literal> custom resource (CR) for your cluster, OVN-Kubernetes pods enter a <literal>CrashLoopBackOff</literal> state. Additionally, updating your cluster to a later version of OpenShift Container Platform fails because the Network Operator remains on a <literal>Degraded</literal> state. Red&#160;Hat does not support disabling IPv6 addresses for your cluster so do not set the <literal>ipv6.disable</literal> parameter to <literal>1</literal>.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="nw-ovn-kubernetes-session-affinity_about-ovn-kubernetes">
<title>Session affinity</title>
<simpara>Session affinity is a feature that applies to Kubernetes <literal>Service</literal> objects. You can use <emphasis>session affinity</emphasis> if you want to ensure that each time you connect to a &lt;service_VIP&gt;:&lt;Port&gt;, the traffic is always load balanced to the same back end. For more information, including how to set session affinity based on a client&#8217;s IP address, see <link xlink:href="https://kubernetes.io/docs/reference/networking/virtual-ips/#session-affinity">Session affinity</link>.</simpara>
<section xml:id="nw-ovn-kubernetes-session-affinity-stickyness-timeout_about-ovn-kubernetes">
<title>Stickiness timeout for session affinity</title>
<simpara>The OVN-Kubernetes network plugin for OpenShift Container Platform calculates the stickiness timeout for a session from a client based on the last packet. For example, if you run a <literal>curl</literal> command 10 times, the sticky session timer starts from the tenth packet not the first. As a result, if the client is continuously contacting the service, then the session never times out. The timeout starts when the service has not received a packet for the amount of time set by the <link xlink:href="https://kubernetes.io/docs/reference/networking/virtual-ips/#session-stickiness-timeout"><literal>timeoutSeconds</literal></link> parameter.</simpara>
</section>
</section>
</chapter>
<chapter xml:id="migrate-from-openshift-sdn-osd">
<title>Migrating from OpenShift SDN network plugin to OVN-Kubernetes network plugin</title>
<simpara>As an OpenShift Container Platform cluster administrator, you can initiate the migration from the OpenShift SDN network plugin to the OVN-Kubernetes network plugin and verify the migration status using the OCM CLI.</simpara>
<simpara>Some considerations before starting migration initiation are:</simpara>
<itemizedlist>
<listitem>
<simpara>The cluster version must be 4.16.43 and above.</simpara>
</listitem>
<listitem>
<simpara>The migration process cannot be interrupted.</simpara>
</listitem>
<listitem>
<simpara>Migrating back to the SDN network plugin is not possible.</simpara>
</listitem>
<listitem>
<simpara>Cluster nodes will be rebooted during migration.</simpara>
</listitem>
<listitem>
<simpara>There will be no impact to workloads that are resilient to node disruptions.</simpara>
</listitem>
<listitem>
<simpara>Migration time can vary between several minutes and hours, depending on the cluster size and workload configurations.</simpara>
</listitem>
</itemizedlist>
<section xml:id="migrate-sdn-ovn-ocm-cli_migrate-from-openshift-sdn-osd">
<title>Initiating migration using the OpenShift Cluster Manager API command-line interface (ocm) CLI</title>
<warning>
<simpara>You can only initiate migration on clusters that are version 4.16.43 and above.</simpara>
</warning>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You installed the <link xlink:href="https://console.redhat.com/openshift/downloads">OpenShift Cluster Manager API command-line interface (<literal>ocm</literal>)</link>.</simpara>
</listitem>
</itemizedlist>
<important>
<simpara>OpenShift Cluster Manager API command-line interface (<literal>ocm</literal>) is a Developer Preview feature only.
For more information about the support scope of Red Hat Developer Preview features, see <link xlink:href="https://access.redhat.com/support/offerings/devpreview/">Developer Preview Support Scope</link>.</simpara>
</important>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create a JSON file with the following content:</simpara>
<programlisting language="json" linenumbering="unnumbered">{
  "type": "sdnToOvn"
}</programlisting>
<itemizedlist>
<listitem>
<simpara>Optional: Within the JSON file, you can configure internal subnets using any or all of the options <literal>join</literal>, <literal>masquerade</literal>, and <literal>transit</literal>, along with a single CIDR per option, as shown in the following example:</simpara>
<programlisting language="json" linenumbering="unnumbered">{
  "type": "sdnToOvn",
  "sdn_to_ovn": {
    "transit_ipv4": "192.168.255.0/24",
    "join_ipv4": "192.168.255.0/24",
    "masquerade_ipv4": "192.168.255.0/24"
  }
}</programlisting>
<note>
<simpara>OVN-Kubernetes reserves the following IP address ranges:</simpara>
<simpara><literal>100.64.0.0/16</literal>. This IP address range is used for the <literal>internalJoinSubnet</literal> parameter of OVN-Kubernetes by default.</simpara>
<simpara><literal>100.88.0.0/16</literal>. This IP address range is used for the <literal>internalTransSwitchSubnet</literal> parameter of OVN-Kubernetes by default.</simpara>
<simpara>If these IP addresses have been used by OpenShift SDN or any external networks that might communicate with this cluster, you must patch them to use a different IP address range before initiating the limited live migration. For more information, see <emphasis>Patching OVN-Kubernetes address ranges</emphasis> in the <emphasis>Additional resources</emphasis> section.</simpara>
</note>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>To initiate the migration, run the following post request in a terminal window:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ ocm post /api/clusters_mgmt/v1/clusters/{cluster_id}/migrations <co xml:id="CO1-1"/>
  --body=myjsonfile.json <co xml:id="CO1-2"/></programlisting>
<calloutlist>
<callout arearefs="CO1-1">
<para>Replace <literal>{cluster_id}</literal> with the ID of the cluster you want to migrate to the OVN-Kubernetes network plugin.</para>
</callout>
<callout arearefs="CO1-2">
<para>Replace <literal>myjsonfile.json</literal> with the name of the JSON file you created in the previous step.</para>
</callout>
</calloutlist>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="json" linenumbering="unnumbered">{
  "kind": "ClusterMigration",
  "href": "/api/clusters_mgmt/v1/clusters/2gnts65ra30sclb114p8qdc26g5c8o3e/migrations/2gois8j244rs0qrfu9ti2o790jssgh9i",
  "id": "7sois8j244rs0qrhu9ti2o790jssgh9i",
  "cluster_id": "2gnts65ra30sclb114p8qdc26g5c8o3e",
  "type": "sdnToOvn",
  "state": {
    "value": "scheduled",
    "description": ""
  },
  "sdn_to_ovn": {
    "transit_ipv4": "100.65.0.0/16",
    "join_ipv4": "100.66.0.0/16"
  },
  "creation_timestamp": "2025-02-05T14:56:34.878467542Z",
  "updated_timestamp": "2025-02-05T14:56:34.878467542Z"
}</programlisting>
</para>
</formalpara>
</listitem>
</orderedlist>
<itemizedlist>
<title>Verification</title>
<listitem>
<simpara>To check the status of the migration, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ ocm get cluster &lt;cluster_id&gt;/migrations <co xml:id="CO2-1"/></programlisting>
<calloutlist>
<callout arearefs="CO2-1">
<para>Replace <literal>&lt;cluster_id&gt;</literal> with the ID of the cluster that the migration was applied to.</para>
</callout>
</calloutlist>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://docs.openshift.com/container-platform/4.16/networking/ovn_kubernetes_network_provider/migrate-from-openshift-sdn.html#patching-ovnk-address-ranges_migrate-from-openshift-sdn">Patching OVN-Kubernetes address ranges</link></simpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="configuring-a-cluster-wide-proxy">
<title>Configuring a cluster-wide proxy</title>
<simpara>If you are using an existing Virtual Private Cloud (VPC), you can configure a cluster-wide proxy during
an OpenShift Container Platform
cluster installation or after the cluster is installed. When you enable a proxy, the core cluster components are denied direct access to the internet, but the proxy does not affect user workloads.</simpara>
<note>
<simpara>Only cluster system egress traffic is proxied, including calls to the cloud provider API.</simpara>
</note>
<simpara>You can enable a proxy only for OpenShift Container Platform clusters that use the Customer Cloud Subscription (CCS) model.</simpara>
<simpara>If you use a cluster-wide proxy, you are responsible for maintaining the availability of the proxy to the cluster. If the proxy becomes unavailable, then it might impact the health and supportability of the cluster.</simpara>
<section xml:id="cluster-wide-proxy-prereqs_configuring-a-cluster-wide-proxy">
<title>Prerequisites for configuring a cluster-wide proxy</title>
<simpara>To configure a cluster-wide proxy, you must meet the following requirements. These requirements are valid when you configure a proxy during installation or postinstallation.</simpara>
<section xml:id="cluster-wide-proxy-general-prereqs_configuring-a-cluster-wide-proxy">
<title>General requirements</title>
<itemizedlist>
<listitem>
<simpara>You are the cluster owner.</simpara>
</listitem>
<listitem>
<simpara>Your account has sufficient privileges.</simpara>
</listitem>
<listitem>
<simpara>You have an existing Virtual Private Cloud (VPC) for your cluster.</simpara>
</listitem>
<listitem>
<simpara>You are using the Customer Cloud Subscription (CCS) model for your cluster.</simpara>
</listitem>
<listitem>
<simpara>The proxy can access the VPC for the cluster and the private subnets of the VPC. The proxy must also be accessible from the VPC for the cluster and from the private subnets of the VPC.</simpara>
</listitem>
<listitem>
<simpara>You have added the following endpoints to your VPC endpoint:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>ec2.&lt;aws_region&gt;.amazonaws.com</literal></simpara>
</listitem>
<listitem>
<simpara><literal>elasticloadbalancing.&lt;aws_region&gt;.amazonaws.com</literal></simpara>
</listitem>
<listitem>
<simpara><literal>s3.&lt;aws_region&gt;.amazonaws.com</literal></simpara>
<simpara>These endpoints are required to complete requests from the nodes to the AWS EC2 API. Because the proxy works at the container level and not at the node level, you must route these requests to the AWS EC2 API through the AWS private network. Adding the public IP address of the EC2 API to your allowlist in your proxy server is not enough.</simpara>
<important>
<simpara>When using a cluster-wide proxy, you must configure the <literal>s3.&lt;aws_region&gt;.amazonaws.com</literal> endpoint as type <literal>Gateway</literal>.</simpara>
</important>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
</section>
<section xml:id="cluster-wide-proxy-network-prereqs_configuring-a-cluster-wide-proxy">
<title>Network requirements</title>
<simpara>If your proxy re-encrypts egress traffic, you must create exclusions to several domain and port combinations required by OpenShift.</simpara>
<simpara>Your proxy must exclude re-encrypting the following OpenShift URLs:</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>URLs to exclude from egress traffic re-encryption</title>
<tgroup cols="3">
<colspec colname="col_1" colwidth="46.1538*"/>
<colspec colname="col_2" colwidth="7.6923*"/>
<colspec colname="col_3" colwidth="46.1539*"/>
<thead>
<row>
<entry align="left" valign="top">Address</entry>
<entry align="left" valign="top">Protocol/Port</entry>
<entry align="left" valign="top">Function</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>observatorium-mst.api.openshift.com</literal></simpara></entry>
<entry align="left" valign="top"><simpara>https/443</simpara></entry>
<entry align="left" valign="top"><simpara>Required. Used for Managed OpenShift-specific telemetry.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>sso.redhat.com</literal></simpara></entry>
<entry align="left" valign="top"><simpara>https/443</simpara></entry>
<entry align="left" valign="top"><simpara>The <link xlink:href="https://console.redhat.com/openshift">https://console.redhat.com/openshift</link> site uses authentication from <literal>sso.redhat.com</literal> to download the cluster pull secret and use Red Hat SaaS solutions to facilitate monitoring of your subscriptions, cluster inventory, and chargeback reporting.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara>For the installation prerequisites for OpenShift Container Platform clusters that use the Customer Cloud Subscription (CCS) model, see <link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/4.16/html-single/planning_your_environment/#aws-ccs">Customer Cloud Subscriptions on AWS</link> or <link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/4.16/html-single/planning_your_environment/#gcp-ccs">Customer Cloud Subscriptions on Google Cloud</link>.</simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="configuring-a-proxy-trust-bundle-responsibilities_configuring-a-cluster-wide-proxy">
<title>Responsibilities for additional trust bundles</title>
<simpara>If you supply an additional trust bundle, you are responsible for the following requirements:</simpara>
<itemizedlist>
<listitem>
<simpara>Ensuring that the contents of the additional trust bundle are valid</simpara>
</listitem>
<listitem>
<simpara>Ensuring that the certificates, including intermediary certificates, contained in the additional trust bundle have not expired</simpara>
</listitem>
<listitem>
<simpara>Tracking the expiry and performing any necessary renewals for certificates contained in the additional trust bundle</simpara>
</listitem>
<listitem>
<simpara>Updating the cluster configuration with the updated additional trust bundle</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="configuring-a-proxy-during-installation_configuring-a-cluster-wide-proxy">
<title>Configuring a proxy during installation</title>
<simpara>You can configure an HTTP or HTTPS proxy when you install an OpenShift Container Platform with Customer Cloud Subscription (CCS) cluster into an existing Virtual Private Cloud (VPC). You can configure the proxy during installation by using Red Hat OpenShift Cluster Manager.</simpara>
<section xml:id="configuring-a-proxy-during-installation-ocm_configuring-a-cluster-wide-proxy">
<title>Configuring a proxy during installation using OpenShift Cluster Manager</title>
<simpara>If you are installing
an OpenShift Container Platform
cluster into an existing Virtual Private Cloud (VPC), you can use Red Hat OpenShift Cluster Manager to enable a cluster-wide HTTP or HTTPS proxy during installation.
You can enable a proxy only for clusters that use the Customer Cloud Subscription (CCS) model.</simpara>
<simpara>Prior to the installation, you must verify that the proxy is accessible from the VPC that the cluster is being installed into. The proxy must also be accessible from the private subnets of the VPC.</simpara>
<simpara>For detailed steps to configure a cluster-wide proxy during installation by using OpenShift Cluster Manager, see <emphasis>Creating a cluster on AWS</emphasis> or <emphasis>Creating a cluster on Google Cloud</emphasis>.</simpara>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/4.16/html-single/openshift_dedicated_clusters_on_aws/#osd-create-aws-cluster-ccs_osd-creating-a-cluster-on-aws">Creating a cluster on AWS</link></simpara>
</listitem>
<listitem>
<simpara><link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/4.16/html-single/openshift_dedicated_clusters_on_google_cloud/#osd-creating-a-cluster-on-gcp-with-workload-identity-federation">Creating a cluster on Google Cloud with Workload Identity Federation authentication</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="configuring-a-proxy-after-installation_configuring-a-cluster-wide-proxy">
<title>Configuring a proxy after installation</title>
<simpara>You can configure an HTTP or HTTPS proxy after you install an OpenShift Container Platform with Customer Cloud Subscription (CCS) cluster into an existing Virtual Private Cloud (VPC). You can configure the proxy after installation by using Red Hat OpenShift Cluster Manager.</simpara>
</section>
<section xml:id="configuring-a-proxy-after-installation-ocm_configuring-a-cluster-wide-proxy">
<title>Configuring a proxy after installation using OpenShift Cluster Manager</title>
<simpara>You can use Red Hat OpenShift Cluster Manager to add a cluster-wide proxy configuration to an existing OpenShift Container Platform cluster in a Virtual Private Cloud (VPC).
You can enable a proxy only for clusters that use the Customer Cloud Subscription (CCS) model.</simpara>
<simpara>You can also use OpenShift Cluster Manager to update an existing cluster-wide proxy configuration. For example, you might need to update the network address for the proxy or replace the additional trust bundle if any of the certificate authorities for the proxy expire.</simpara>
<important>
<simpara>The cluster applies the proxy configuration to the control plane and compute nodes. While applying the configuration, each cluster node is temporarily placed in an unschedulable state and drained of its workloads. Each node is restarted as part of the process.</simpara>
</important>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You have an OpenShift Container Platform cluster that uses the Customer Cloud Subscription (CCS) model.</simpara>
</listitem>
<listitem>
<simpara>Your cluster is deployed in a VPC.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Navigate to <link xlink:href="https://console.redhat.com/openshift">OpenShift Cluster Manager</link> and select your cluster.</simpara>
</listitem>
<listitem>
<simpara>Under the <emphasis role="strong">Virtual Private Cloud (VPC)</emphasis> section on the <emphasis role="strong">Networking</emphasis> page, click <emphasis role="strong">Edit cluster-wide proxy</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Edit cluster-wide proxy</emphasis> page, provide your proxy configuration details:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Enter a value in at least one of the following fields:</simpara>
<itemizedlist>
<listitem>
<simpara>Specify a valid <emphasis role="strong">HTTP proxy URL</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Specify a valid <emphasis role="strong">HTTPS proxy URL</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>In the <emphasis role="strong">Additional trust bundle</emphasis> field, provide a PEM encoded X.509 certificate bundle.</simpara>
<simpara>If you are replacing an existing trust bundle file, select <emphasis role="strong">Replace file</emphasis> to view the field. The bundle is added to the trusted certificate store for the cluster nodes. An additional trust bundle file is required if you use a TLS-inspecting proxy unless the identity certificate for the proxy is signed by an authority from the Red Hat Enterprise Linux CoreOS (RHCOS) trust bundle. This requirement applies regardless of whether the proxy is transparent or requires explicit configuration using the <literal>http-proxy</literal> and <literal>https-proxy</literal> arguments.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Confirm</emphasis>.</simpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<itemizedlist>
<title>Verification</title>
<listitem>
<simpara>Under the <emphasis role="strong">Virtual Private Cloud (VPC)</emphasis> section on the <emphasis role="strong">Networking</emphasis> page, verify that the proxy configuration for your cluster is as expected.</simpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="nw-ovn-kubernetes-enabling-multicast">
<title>Enabling multicast for a project</title>
<section xml:id="nw-about-multicast_ovn-kubernetes-enabling-multicast">
<title>About multicast</title>
<simpara role="_abstract">With IP multicast, data is broadcast to many IP addresses simultaneously.</simpara>
<important>
<itemizedlist>
<listitem>
<simpara>At this time, multicast is best used for low-bandwidth coordination or service discovery and not a high-bandwidth solution.</simpara>
</listitem>
<listitem>
<simpara>By default, network policies affect all connections in a namespace. However, multicast is unaffected by network policies. If multicast is enabled in the same namespace as your network policies, it is always allowed, even if there is a <literal>deny-all</literal> network policy.</simpara>
</listitem>
<listitem>
<simpara>Cluster administrators must consider the implications of the exemption of multicast from network policies before enabling it.</simpara>
</listitem>
</itemizedlist>
</important>
<simpara>Multicast traffic between OpenShift Container Platform pods is disabled by default. If you are using the OVN-Kubernetes network plugin, you can enable multicast on a per-project basis.</simpara>
</section>
<section xml:id="nw-enabling-multicast_ovn-kubernetes-enabling-multicast">
<title>Enabling multicast between pods</title>
<simpara role="_abstract">You can enable multicast between pods for your project.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Install the OpenShift CLI (<literal>oc</literal>).</simpara>
</listitem>
<listitem>
<simpara>You must log in to the cluster with a user that has the <literal>cluster-admin</literal>
or the <literal>dedicated-admin</literal>
role.</simpara>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Run the following command to enable multicast for a project. Replace <literal>&lt;namespace&gt;</literal> with the namespace for the project you want to enable multicast for.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc annotate namespace &lt;namespace&gt; \
    k8s.ovn.org/multicast-enabled=true</programlisting>
<tip>
<simpara>You can alternatively apply the following YAML to add the annotation:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: Namespace
metadata:
  name: &lt;namespace&gt;
  annotations:
    k8s.ovn.org/multicast-enabled: "true"</programlisting>
</tip>
</listitem>
</itemizedlist>
<formalpara>
<title>Verification</title>
<para>To verify that multicast is enabled for a project, complete the following procedure:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Change your current project to the project that you enabled multicast for. Replace <literal>&lt;project&gt;</literal> with the project name.</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc project &lt;project&gt;</programlisting>
</listitem>
<listitem>
<simpara>Create a pod to act as a multicast receiver:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ cat &lt;&lt;EOF| oc create -f -
apiVersion: v1
kind: Pod
metadata:
  name: mlistener
  labels:
    app: multicast-verify
spec:
  containers:
    - name: mlistener
      image: registry.access.redhat.com/ubi9
      command: ["/bin/sh", "-c"]
      args:
        ["dnf -y install socat hostname &amp;&amp; sleep inf"]
      ports:
        - containerPort: 30102
          name: mlistener
          protocol: UDP
EOF</programlisting>
</listitem>
<listitem>
<simpara>Create a pod to act as a multicast sender:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ cat &lt;&lt;EOF| oc create -f -
apiVersion: v1
kind: Pod
metadata:
  name: msender
  labels:
    app: multicast-verify
spec:
  containers:
    - name: msender
      image: registry.access.redhat.com/ubi9
      command: ["/bin/sh", "-c"]
      args:
        ["dnf -y install socat &amp;&amp; sleep inf"]
EOF</programlisting>
</listitem>
<listitem>
<simpara>In a new terminal window or tab, start the multicast listener.</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Get the IP address for the Pod:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ POD_IP=$(oc get pods mlistener -o jsonpath='{.status.podIP}')</programlisting>
</listitem>
<listitem>
<simpara>Start the multicast listener by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc exec mlistener -i -t -- \
    socat UDP4-RECVFROM:30102,ip-add-membership=224.1.0.1:$POD_IP,fork EXEC:hostname</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Start the multicast transmitter.</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Get the pod network IP address range:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ CIDR=$(oc get Network.config.openshift.io cluster \
    -o jsonpath='{.status.clusterNetwork[0].cidr}')</programlisting>
</listitem>
<listitem>
<simpara>To send a multicast message, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc exec msender -i -t -- \
    /bin/bash -c "echo | socat STDIO UDP4-DATAGRAM:224.1.0.1:30102,range=$CIDR,ip-multicast-ttl=64"</programlisting>
<simpara>If multicast is working, the previous command returns the following output:</simpara>
<programlisting language="text" linenumbering="unnumbered">mlistener</programlisting>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
</section>
</chapter>
</book>
