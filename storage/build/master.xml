<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE book [
<!ENTITY % sgml.features "IGNORE">
<!ENTITY % xml.features "INCLUDE">
<!ENTITY % DOCBOOK_ENTS PUBLIC "-//OASIS//ENTITIES DocBook Character Entities V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/dbcentx.mod">
%DOCBOOK_ENTS;
]>
<?asciidoc-toc?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0">
<info>
<title>Storage</title>
<date>2026-02-25</date>
<title>Storage</title>
<productname>OpenShift Container Platform</productname>
<productnumber>4.16</productnumber>
<subtitle>Enter a short description here.</subtitle>
<abstract>
    <para>A short overview and summary of the book's subject and purpose, traditionally no more than one paragraph long.</para>
</abstract>
<authorgroup>
    <orgname>Red Hat OpenShift Documentation Team</orgname>
</authorgroup>
<xi:include href="Common_Content/Legal_Notice.xml" xmlns:xi="http://www.w3.org/2001/XInclude" />
</info>
<chapter xml:id="storage-overview">
<title>OpenShift Container Platform storage overview</title>
<simpara>OpenShift Container Platform supports multiple types of storage, both for on-premise and cloud providers. You can manage container storage for persistent and non-persistent data in an OpenShift Container Platform cluster.</simpara>
<section xml:id="openshift-storage-common-terms_storage-overview">
<title>Glossary of common terms for OpenShift Container Platform storage</title>
<simpara>This glossary defines common terms that are used in the storage content.</simpara>
<variablelist>
<varlistentry>
<term>Access modes</term>
<listitem>
<simpara>Volume access modes describe volume capabilities. You can use access modes to match persistent volume claim (PVC) and persistent volume (PV). The following are the examples of access modes:</simpara>
<itemizedlist>
<listitem>
<simpara>ReadWriteOnce (RWO)</simpara>
</listitem>
<listitem>
<simpara>ReadOnlyMany (ROX)</simpara>
</listitem>
<listitem>
<simpara>ReadWriteMany (RWX)</simpara>
</listitem>
<listitem>
<simpara>ReadWriteOncePod (RWOP)</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Config map</term>
<listitem>
<simpara>A config map provides a way to inject configuration data into pods. You can reference the data stored in a config map in a volume of type <literal>ConfigMap</literal>. Applications running in a pod can use this data.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Container Storage Interface (CSI)</term>
<listitem>
<simpara>An API specification for the management of container storage across different container orchestration (CO) systems.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Dynamic Provisioning</term>
<listitem>
<simpara>The framework allows you to create storage volumes on-demand, eliminating the need for cluster administrators to pre-provision persistent storage.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Ephemeral storage</term>
<listitem>
<simpara>Pods and containers can require temporary or transient local storage for their operation. The lifetime of this ephemeral storage does not extend beyond the life of the individual pod, and this ephemeral storage cannot be shared across pods.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>fsGroup</term>
<listitem>
<simpara>The fsGroup defines a file system group ID of a pod.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>hostPath</term>
<listitem>
<simpara>A hostPath volume in an OpenShift Container Platform cluster mounts a file or directory from the host node’s filesystem into your pod.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>KMS key</term>
<listitem>
<simpara>The Key Management Service (KMS) helps you achieve the required level of encryption of your data across different services. you can use the KMS key to encrypt, decrypt, and re-encrypt data.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Local volumes</term>
<listitem>
<simpara>A local volume represents a mounted local storage device such as a disk, partition or directory.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Nested mount points</term>
<listitem>
<simpara>A nested mount point is a mount point that attempts to use a mount point created by a previous volume.</simpara>
<formalpara>
<title>Example pod definition with nested mount points</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">kind: Pod
apiVersion: v1
metadata:
  name: webapp
  labels:
    name: webapp
spec:
  containers:
    - name: webapp
      image: nginx
      ports:
        - containerPort: 80
          name: "http-server"
      volumeMounts:
      - mountPath: /mnt/web
        name: web
      - mountPath: /mnt/web/redis <co xml:id="CO1-1"/>
        name: redis
  volumes:
    - name: redis
      persistentVolumeClaim:
       claimName: "redis"
    - name: web
      persistentVolumeClaim:
        claimName: "web"</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO1-1">
<para>Nested mount point</para>
</callout>
</calloutlist>
<simpara>Do <emphasis>not</emphasis> use nested mount points because OpenShift Container Platform does not guarantee the order in which mount points are created. Such usage is prone to race conditions and undefined behavior.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>OpenShift Data Foundation</term>
<listitem>
<simpara>A provider of agnostic persistent storage for OpenShift Container Platform supporting file, block, and object storage, either in-house or in hybrid clouds</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Persistent storage</term>
<listitem>
<simpara>Pods and containers can require permanent storage for their operation. OpenShift Container Platform uses the Kubernetes persistent volume (PV) framework to allow cluster administrators to provision persistent storage for a cluster. Developers can use PVC to request PV resources without having specific knowledge of the underlying storage infrastructure.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Persistent volumes (PV)</term>
<listitem>
<simpara>OpenShift Container Platform uses the Kubernetes persistent volume (PV) framework to allow cluster administrators to provision persistent storage for a cluster. Developers can use PVC to request PV resources without having specific knowledge of the underlying storage infrastructure.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Persistent volume claims (PVCs)</term>
<listitem>
<simpara>You can use a PVC to mount a PersistentVolume into a Pod. You can access the storage without knowing the details of the cloud environment.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Pod</term>
<listitem>
<simpara>One or more containers with shared resources, such as volume and IP addresses, running in your OpenShift Container Platform cluster.
A pod is the smallest compute unit defined, deployed, and managed.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Reclaim policy</term>
<listitem>
<simpara>A policy that tells the cluster what to do with the volume after it is released. A volume’s reclaim policy can be <literal>Retain</literal>, <literal>Recycle</literal>, or <literal>Delete</literal>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Role-based access control (RBAC)</term>
<listitem>
<simpara>Role-based access control (RBAC) is a method of regulating access to computer or network resources based on the roles of individual users within your organization.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Stateless applications</term>
<listitem>
<simpara>A stateless application is an application program that does not save client data generated in one session for use in the next session with that client.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Stateful applications</term>
<listitem>
<simpara>A stateful application is an application program that saves data to persistent disk storage. A server, client, and applications can use a persistent disk storage. You can use the <literal>Statefulset</literal> object in OpenShift Container Platform to manage the deployment and scaling of a set of Pods, and provides guarantee about the ordering and uniqueness of these Pods.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Static provisioning</term>
<listitem>
<simpara>A cluster administrator creates a number of PVs. PVs contain the details of storage. PVs exist in the Kubernetes API and are available for consumption.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Storage</term>
<listitem>
<simpara>OpenShift Container Platform supports many types of storage, both for on-premise and cloud providers. You can manage container storage for persistent and non-persistent data in
an OpenShift Container Platform cluster.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Storage class</term>
<listitem>
<simpara>A storage class provides a way for administrators to describe the classes of storage they offer. Different classes might map to quality of service levels, backup policies, arbitrary policies determined by the cluster administrators.</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="storage-types">
<title>Storage types</title>
<simpara>OpenShift Container Platform storage is broadly classified into two categories, namely ephemeral storage and persistent storage.</simpara>
<section xml:id="ephemeral-storage">
<title>Ephemeral storage</title>
<simpara>Pods and containers are ephemeral or transient in nature and designed for stateless applications. Ephemeral storage allows administrators and developers to better manage the local storage for some of their operations. For more information about ephemeral storage overview, types, and management, see <link linkend="understanding-ephemeral-storage">Understanding ephemeral storage</link>.</simpara>
</section>
<section xml:id="persistent-storage">
<title>Persistent storage</title>
<simpara>Stateful applications deployed in containers require persistent storage. OpenShift Container Platform uses a pre-provisioned storage framework called persistent volumes (PV) to allow cluster administrators to provision persistent storage. The data inside these volumes can exist beyond the lifecycle of an individual pod. Developers can use persistent volume claims (PVCs) to request storage requirements. For more information about persistent storage overview, configuration, and lifecycle, see <link linkend="understanding-persistent-storage">Understanding persistent storage</link>.</simpara>
</section>
</section>
<section xml:id="container-storage-interface">
<title>Container Storage Interface (CSI)</title>
<simpara>CSI is an API specification for the management of container storage across different container orchestration (CO) systems. You can manage the storage volumes within the container native environments, without having specific knowledge of the underlying storage infrastructure. With the CSI, storage works uniformly across different container orchestration systems, regardless of the storage vendors you are using. For more information about CSI, see <link linkend="persistent-storage-csi">Using Container Storage Interface (CSI)</link>.</simpara>
</section>
<section xml:id="dynamic-provisioning-overview">
<title>Dynamic Provisioning</title>
<simpara>Dynamic Provisioning allows you to create storage volumes on-demand, eliminating the need for cluster administrators to pre-provision storage. For more information about dynamic provisioning, see <link linkend="dynamic-provisioning">Dynamic provisioning</link>.</simpara>
</section>
</chapter>
<chapter xml:id="understanding-ephemeral-storage">
<title>Understanding ephemeral storage</title>
<simpara role="_abstract">To manage temporary data that exists only for the duration of a workload, review the properties of ephemeral storage.</simpara>
<section xml:id="storage-ephemeral-storage-overview_understanding-ephemeral-storage">
<title>Overview of ephemeral storage</title>
<simpara role="_abstract">To manage local storage for stateless applications, use ephemeral storage. Ephemeral storage is designed for the transient nature of pods and containers. You can use this feature to handle operations that require storage only for the duration of the lifecycle for the workload. Both developers and administrators can use the feature.</simpara>
<simpara>In addition to persistent storage, pods and containers can require ephemeral or transient local storage for their operation. The lifetime of this ephemeral storage does not extend beyond the life of the individual pod, and this ephemeral storage cannot be shared across pods.</simpara>
<simpara>Pods use ephemeral local storage for scratch space, caching, and logs. Issues related to the lack of local storage accounting and isolation include the following:</simpara>
<itemizedlist>
<listitem>
<simpara>Pods cannot detect how much local storage is available to them.</simpara>
</listitem>
<listitem>
<simpara>Pods cannot request guaranteed local storage.</simpara>
</listitem>
<listitem>
<simpara>Local storage is a best-effort resource.</simpara>
</listitem>
<listitem>
<simpara>Pods can be evicted due to other pods filling the local storage, after which new pods are not admitted until sufficient storage is reclaimed.</simpara>
</listitem>
</itemizedlist>
<simpara>Unlike persistent volumes, ephemeral storage is unstructured and the space is shared between all pods running on a node, in addition to other uses by the system, the container runtime, and OpenShift Container Platform. The ephemeral storage framework allows pods to specify their transient local storage needs. It also allows OpenShift Container Platform to schedule pods where appropriate, and to protect the node against excessive use of local storage.</simpara>
<simpara>While the ephemeral storage framework allows administrators and developers to better manage local storage, I/O throughput and latency are not directly affected.</simpara>
</section>
<section xml:id="storage-ephemeral-storage-types_understanding-ephemeral-storage">
<title>Types of ephemeral storage</title>
<simpara role="_abstract">To provision ephemeral local storage, you can create the primary partition by using either root or runtime methods. This storage is always made available in the primary partition, providing temporary space for your workloads.</simpara>
<section xml:id="_root">
<title>Root</title>
<simpara>This partition holds the kubelet root directory, <literal>/var/lib/kubelet/</literal> by default, and <literal>/var/log/</literal> directory. This partition can be shared between user pods, the OS, and Kubernetes system daemons. This partition can be consumed by pods through <literal>EmptyDir</literal> volumes, container logs, image layers, and container-writable layers. Kubelet manages shared access and isolation of this partition. This partition is ephemeral, and applications cannot expect any performance SLAs, such as disk IOPS, from this partition.</simpara>
</section>
<section xml:id="_runtime">
<title>Runtime</title>
<simpara>This is an optional partition that runtimes can use for overlay file systems. OpenShift Container Platform attempts to identify and provide shared access along with isolation to this partition. Container image layers and writable layers are stored here. If the runtime partition exists, the <literal>root</literal> partition does not hold any image layer or other writable storage.</simpara>
</section>
</section>
<section xml:id="storage-ephemeral-storage-manage_understanding-ephemeral-storage">
<title>Ephemeral storage management</title>
<simpara role="_abstract">Cluster administrators can manage ephemeral storage within a project by setting quotas that define the limit ranges and number of requests for ephemeral storage across all pods in a non-terminal state. Developers can also set requests and limits on this compute resource at the pod and container level.</simpara>
<simpara>You can manage local ephemeral storage by specifying requests and limits. Each container in a pod can specify the following:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>spec.containers[].resources.limits.ephemeral-storage</literal></simpara>
</listitem>
<listitem>
<simpara><literal>spec.containers[].resources.requests.ephemeral-storage</literal></simpara>
</listitem>
</itemizedlist>
<section xml:id="storage-ephemeral-storage-limits-requests-units_understanding-ephemeral-storage">
<title>Ephemeral storage limits and requests units</title>
<simpara>Limits and requests for ephemeral storage are measured in byte quantities. You can express storage as a plain integer or as a fixed-point number using one of these suffixes: E, P, T, G, M, k. You can also use the power-of-two equivalents: Ei, Pi, Ti, Gi, Mi, Ki.</simpara>
<simpara>For example, the following quantities all represent approximately the same value: 128974848, 129e6, 129M, and 123Mi.</simpara>
<important>
<simpara>The suffixes for each byte quantity are case-sensitive. Be sure to use the correct case. Use the case-sensitive "M", such as used in "400M", to set the request at 400 megabytes. Use the case-sensitive "400Mi" to request 400 mebibytes. If you specify "400m" of ephemeral storage, the storage request is only 0.4 bytes.</simpara>
</important>
</section>
<section xml:id="storage-ephemeral-storage-requests-limits_understanding-ephemeral-storage">
<title>Ephemeral storage requests and limits example</title>
<simpara>The following example configuration file shows a pod with two containers:</simpara>
<itemizedlist>
<listitem>
<simpara>Each container requests 2GiB of local ephemeral storage.</simpara>
</listitem>
<listitem>
<simpara>Each container has a limit of 4GiB of local ephemeral storage.</simpara>
</listitem>
<listitem>
<simpara>At the pod level, kubelet works out an overall pod storage limit by adding up the limits of all the containers in that pod.</simpara>
<itemizedlist>
<listitem>
<simpara>In this case, the total storage usage at the pod level is the sum of the disk usage from all containers plus the pod&#8217;s <literal>emptyDir</literal> volumes.</simpara>
</listitem>
<listitem>
<simpara>Therefore, the pod has a request of 4GiB of local ephemeral storage, and a limit of 8GiB of local ephemeral storage.</simpara>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
<formalpara>
<title>Example ephemeral storage configuration with quotas and limits</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: Pod
metadata:
  name: frontend
spec:
  containers:
  - name: app
    image: images.my-company.example/app:v4
    resources:
      requests:
        ephemeral-storage: "2Gi" <co xml:id="CO2-1"/>
      limits:
        ephemeral-storage: "4Gi" <co xml:id="CO2-2"/>
    volumeMounts:
    - name: ephemeral
      mountPath: "/tmp"
  - name: log-aggregator
    image: images.my-company.example/log-aggregator:v6
    resources:
      requests:
        ephemeral-storage: "2Gi"
      limits:
        ephemeral-storage: "4Gi"
    volumeMounts:
    - name: ephemeral
      mountPath: "/tmp"
  volumes:
    - name: ephemeral
      emptyDir: {}</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO2-1">
<para>Container request for local ephemeral storage.</para>
</callout>
<callout arearefs="CO2-2">
<para>Container limit for local ephemeral storage.</para>
</callout>
</calloutlist>
</section>
<section xml:id="storage-ephemeral-storage-scheduling-eviction_understanding-ephemeral-storage">
<title>Ephemeral storage configuration effects pod scheduling and eviction</title>
<simpara>The settings in the pod spec affect both how the scheduler makes a decision about scheduling pods and when kubelet evicts pods.</simpara>
<itemizedlist>
<listitem>
<simpara>First, the scheduler ensures that the sum of the resource requests of the scheduled containers is less than the capacity of the node. In this case, the pod can be assigned to a node only if the node&#8217;s available ephemeral storage (allocatable resource) is more than 4GiB.</simpara>
</listitem>
<listitem>
<simpara>Second, at the container level, because the first container sets a resource limit, kubelet eviction manager measures the disk usage of this container and evicts the pod if the storage usage of the container exceeds its limit (4GiB). The kubelet eviction manager also marks the pod for eviction if the total usage exceeds the overall pod storage limit (8GiB).</simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="storage-ephemeral-storage-monitoring_understanding-ephemeral-storage">
<title>Monitoring ephemeral storage</title>
<simpara role="_abstract">To monitor ephemeral storage usage, use the <literal>/bin/df</literal> utility. By using this tool, you can track disk space consumption on the volumes where ephemeral container data resides, specifically <literal>/var/lib/kubelet</literal> and <literal>/var/lib/containers</literal>.</simpara>
<simpara>When you use the <literal>df</literal> command, the available space for only <literal>/var/lib/kubelet</literal> is shown if <literal>/var/lib/containers</literal> is placed on a separate disk by the cluster administrator.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>To show the human-readable values of used and available space in <literal>/var/lib</literal>, enter the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ df -h /var/lib</programlisting>
<simpara>The output shows the ephemeral storage usage in <literal>/var/lib</literal>:</simpara>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Filesystem  Size  Used Avail Use% Mounted on
/dev/disk/by-partuuid/4cd1448a-01    69G   32G   34G  49% /</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="understanding-persistent-storage">
<title>Understanding persistent storage</title>
<section xml:id="persistent-storage-overview_understanding-persistent-storage">
<title>Persistent storage overview</title>
<simpara>Stateful applications deployed in containers require persistent storage. {microshift-short} uses a pre-provisioned storage framework called persistent volumes (PV) to allow node administrators to provision persistent storage. The data inside these volumes can exist beyond the lifecycle of an individual pod. Developers can use persistent volume claims (PVCs) to request storage requirements.</simpara>
<simpara>Managing storage is a distinct problem from managing compute resources. OpenShift Container Platform uses the Kubernetes persistent volume (PV) framework to allow cluster administrators to provision persistent storage for a cluster. Developers can use persistent volume claims (PVCs) to request PV resources without having specific knowledge of the underlying storage infrastructure.</simpara>
<simpara>PVCs are specific to a project, and are created and used by developers as a means to use a PV. PV resources on their own are not scoped to any single project; they can be shared across the entire OpenShift Container Platform node and claimed from any project. After a PV is bound to a PVC, that PV can not then be bound to additional PVCs. This has the effect of scoping a bound PV to a single namespace, that of the binding project.</simpara>
<simpara>PVs are defined by a <literal>PersistentVolume</literal> API object, which represents a piece of existing storage in the cluster that was either statically provisioned by the cluster administrator or dynamically provisioned using a <literal>StorageClass</literal> object. It is a resource in the cluster just like a node is a cluster resource.</simpara>
<simpara>PVs are volume plugins like <literal>Volumes</literal> but have a lifecycle that is independent of any individual pod that uses the PV. PV objects capture the details of the implementation of the storage, be that NFS, iSCSI, or a cloud-provider-specific storage system.</simpara>
<important>
<simpara>High availability of storage in the infrastructure is left to the underlying storage provider.</simpara>
</important>
<simpara>PVCs are defined by a <literal>PersistentVolumeClaim</literal> API object, which represents a request for storage by a developer. It is similar to a pod in that pods consume node resources and PVCs consume PV resources. For example, pods can request specific levels of resources, such as CPU and memory, while PVCs can request specific storage capacity and access modes. For example, they can be mounted once read-write or many times read-only.</simpara>
</section>
<section xml:id="lifecycle-volume-claim_understanding-persistent-storage">
<title>Lifecycle of a volume and claim</title>
<simpara>PVs are resources in the cluster. PVCs are requests for those resources
and also act as claim checks to the resource. The interaction between PVs
and PVCs have the following lifecycle.</simpara>
<section xml:id="provisioning_understanding-persistent-storage">
<title>Provision storage</title>
<simpara>In response to requests from a developer defined in a PVC, a cluster
administrator configures one or more dynamic provisioners that provision
storage and a matching PV.</simpara>
</section>
<section xml:id="binding_understanding-persistent-storage">
<title>Bind claims</title>
<simpara>When you create a PVC, you request a specific amount of storage, specify the
required access mode, and create a storage class to describe and classify
the storage. The control loop in the master watches for new PVCs and binds
the new PVC to an appropriate PV. If an appropriate PV does not exist, a
provisioner for the storage class creates one.</simpara>
<simpara>The size of all PVs might exceed your PVC size. This is especially true
with manually provisioned PVs. To minimize the excess, OpenShift Container Platform
binds to the smallest PV that matches all other criteria.</simpara>
<simpara>Claims remain unbound indefinitely if a matching volume does not exist or
can not be created with any available provisioner servicing a storage
class. Claims are bound as matching volumes become available. For example,
a cluster with many manually provisioned 50Gi volumes would not match a
PVC requesting 100Gi. The PVC can be bound when a 100Gi PV is added to the
cluster.</simpara>
</section>
<section xml:id="using-pods_understanding-persistent-storage">
<title>Use pods and claimed PVs</title>
<simpara>Pods use claims as volumes. The cluster inspects the claim to find the bound
volume and mounts that volume for a pod. For those volumes that support
multiple access modes, you must specify which mode applies when you use
the claim as a volume in a pod.</simpara>
<simpara>Once you have a claim and that claim is bound, the bound PV belongs to you
for as long as you need it. You can schedule pods and access claimed
PVs by including <literal>persistentVolumeClaim</literal> in the pod&#8217;s volumes block.</simpara>
<note>
<simpara>If you attach persistent volumes that have high file counts to pods, those pods can fail or can take a long time to start. For
more information, see <link xlink:href="https://access.redhat.com/solutions/6221251">When using Persistent Volumes with high file counts in OpenShift, why do pods fail to start or take an excessive amount of time to achieve "Ready" state?</link>.</simpara>
</note>
</section>
<section xml:id="releasing_understanding-persistent-storage">
<title>Release a persistent volume</title>
<simpara>When you are finished with a volume, you can delete the PVC object from
the API, which allows reclamation of the resource. The volume is
considered released when the claim is deleted, but it is not yet available
for another claim. The previous claimant&#8217;s data remains on the volume and
must be handled according to policy.</simpara>
</section>
<section xml:id="reclaiming_understanding-persistent-storage">
<title>Reclaim policy for persistent volumes</title>
<simpara>The reclaim policy of a persistent volume tells the cluster what to do with the volume after it is released. A volume&#8217;s reclaim policy can be
<literal>Retain</literal>, <literal>Recycle</literal>, or <literal>Delete</literal>.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>Retain</literal> reclaim policy allows manual reclamation of the resource for
those volume plugins that support it.</simpara>
</listitem>
<listitem>
<simpara><literal>Recycle</literal> reclaim policy recycles the volume back into the pool of
unbound persistent volumes once it is released from its claim.</simpara>
</listitem>
</itemizedlist>
<important>
<simpara>The <literal>Recycle</literal> reclaim policy is deprecated in OpenShift Container Platform 4. Dynamic provisioning is recommended for equivalent and better
functionality.</simpara>
</important>
<itemizedlist>
<listitem>
<simpara><literal>Delete</literal> reclaim policy deletes  both the <literal>PersistentVolume</literal> object
from OpenShift Container Platform and the associated storage asset in external
infrastructure, such as Amazon Elastic Block Store (Amazon EBS) or VMware vSphere.</simpara>
</listitem>
</itemizedlist>
<note>
<simpara>Dynamically provisioned volumes are always deleted.</simpara>
</note>
</section>
<section xml:id="reclaim-manual_understanding-persistent-storage">
<title>Reclaiming a persistent volume manually</title>
<simpara>When a persistent volume claim (PVC) is deleted, the persistent volume (PV) still exists and is considered "released". However, the PV is not yet available for another claim because the data of the previous claimant remains on the volume.</simpara>
<formalpara>
<title>Procedure</title>
<para>To manually reclaim the PV as a cluster administrator:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Delete the PV by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc delete pv &lt;pv_name&gt;</programlisting>
<simpara>The associated storage asset in the external infrastructure, such as an AWS EBS or GCE PD volume, still exists after the PV is deleted.</simpara>
</listitem>
<listitem>
<simpara>Clean up the data on the associated storage asset.</simpara>
</listitem>
<listitem>
<simpara>Delete the associated storage asset. Alternately, to reuse the same storage asset, create a new PV with the storage asset definition.</simpara>
</listitem>
</orderedlist>
<simpara>The reclaimed PV is now available for use by another PVC.</simpara>
</section>
<section xml:id="reclaim-policy_understanding-persistent-storage">
<title>Changing the reclaim policy of a persistent volume</title>
<simpara>You can change the reclaim policy of a persistent volume.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>List the persistent volumes in your cluster:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get pv</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                                       CAPACITY   ACCESSMODES   RECLAIMPOLICY   STATUS    CLAIM             STORAGECLASS     REASON    AGE
 pvc-b6efd8da-b7b5-11e6-9d58-0ed433a7dd94   4Gi        RWO           Delete          Bound     default/claim1    manual                     10s
 pvc-b95650f8-b7b5-11e6-9d58-0ed433a7dd94   4Gi        RWO           Delete          Bound     default/claim2    manual                     6s
 pvc-bb3ca71d-b7b5-11e6-9d58-0ed433a7dd94   4Gi        RWO           Delete          Bound     default/claim3    manual                     3s</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Choose one of your persistent volumes and change its reclaim policy:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch pv &lt;your-pv-name&gt; -p '{"spec":{"persistentVolumeReclaimPolicy":"Retain"}}'</programlisting>
</listitem>
<listitem>
<simpara>Verify that your chosen persistent volume has the right policy:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get pv</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                                       CAPACITY   ACCESSMODES   RECLAIMPOLICY   STATUS    CLAIM             STORAGECLASS     REASON    AGE
 pvc-b6efd8da-b7b5-11e6-9d58-0ed433a7dd94   4Gi        RWO           Delete          Bound     default/claim1    manual                     10s
 pvc-b95650f8-b7b5-11e6-9d58-0ed433a7dd94   4Gi        RWO           Delete          Bound     default/claim2    manual                     6s
 pvc-bb3ca71d-b7b5-11e6-9d58-0ed433a7dd94   4Gi        RWO           Retain          Bound     default/claim3    manual                     3s</programlisting>
</para>
</formalpara>
<simpara>In the preceding output, the volume bound to claim <literal>default/claim3</literal> now has a <literal>Retain</literal> reclaim policy. The volume will not be automatically deleted when a user deletes claim <literal>default/claim3</literal>.</simpara>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="persistent-volumes_understanding-persistent-storage">
<title>Persistent volumes</title>
<simpara>Each PV contains a <literal>spec</literal> and <literal>status</literal>, which is the specification and status of the volume, for example:</simpara>
<formalpara>
<title><literal>PersistentVolume</literal> object definition example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv0001 <co xml:id="CO3-1"/>
spec:
  capacity:
    storage: 5Gi <co xml:id="CO3-2"/>
  accessModes:
    - ReadWriteOnce <co xml:id="CO3-3"/>
  persistentVolumeReclaimPolicy: Retain <co xml:id="CO3-4"/>
  ...
status:
  ...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO3-1">
<para>Name of the persistent volume.</para>
</callout>
<callout arearefs="CO3-2">
<para>The amount of storage available to the volume.</para>
</callout>
<callout arearefs="CO3-3">
<para>The access mode, defining the read-write and mount permissions.</para>
</callout>
<callout arearefs="CO3-4">
<para>The reclaim policy, indicating how the resource should be handled once it is released.</para>
</callout>
</calloutlist>
<simpara>You can view the name of a PVC that is bound to a PV by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get pv &lt;pv_name&gt; -o jsonpath='{.spec.claimRef.name}'</programlisting>
<section xml:id="types-of-persistent-volumes_understanding-persistent-storage">
<title>Types of PVs</title>
<simpara>OpenShift Container Platform supports the following persistent volume plugins:</simpara>
<itemizedlist>
<listitem>
<simpara>AWS Elastic Block Store (EBS), which is installed by default.</simpara>
</listitem>
<listitem>
<simpara>GCP Persistent Disk</simpara>
</listitem>
<listitem>
<simpara>GCP Filestore</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="pv-capacity_understanding-persistent-storage">
<title>Capacity</title>
<simpara>Generally, a persistent volume (PV) has a specific storage capacity. This is set by using the <literal>capacity</literal> attribute of the PV.</simpara>
<simpara>Currently, storage capacity is the only resource that can be set or requested. Future attributes may include IOPS, throughput, and so on.</simpara>
</section>
<section xml:id="pv-access-modes_understanding-persistent-storage">
<title>Access modes</title>
<simpara>A persistent volume can be mounted on a host in any way supported by the resource provider. Providers have different capabilities and each PV&#8217;s access modes are set to the specific modes supported by that particular volume. For example, NFS can support multiple read-write clients, but a specific NFS PV might be exported on the server as read-only. Each PV gets its own set of access modes describing that specific PV&#8217;s capabilities.</simpara>
<simpara>Claims are matched to volumes with similar access modes. The only two matching criteria are access modes and size. A claim&#8217;s access modes represent a request. Therefore, you might be granted more, but never less. For example, if a claim requests RWO, but the only volume available is an NFS PV (RWO+ROX+RWX), the claim would then match NFS because it supports RWO.</simpara>
<simpara>Direct matches are always attempted first. The volume&#8217;s modes must match or contain more modes than you requested. The size must be greater than or equal to what is expected. If two types of volumes, such as NFS and iSCSI, have the same set of access modes, either of them can match a claim with those modes. There is no ordering between types of volumes and no way to choose one type over another.</simpara>
<simpara>All volumes with the same modes are grouped, and then sorted by size, smallest to largest. The binder gets the group with matching modes and iterates over each, in size order, until one size matches.</simpara>
<important>
<simpara>Volume access modes describe volume capabilities. They are not enforced constraints. The storage provider is responsible for runtime errors resulting from invalid use of the resource. Errors in the provider show up at runtime as mount errors.</simpara>
</important>
<simpara>The following table lists the access modes:</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Access modes</title>
<tgroup cols="3">
<colspec colname="col_1" colwidth="20*"/>
<colspec colname="col_2" colwidth="20*"/>
<colspec colname="col_3" colwidth="60*"/>
<thead>
<row>
<entry align="left" valign="top">Access Mode</entry>
<entry align="left" valign="top">CLI abbreviation</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>ReadWriteOnce</simpara></entry>
<entry align="left" valign="top"><simpara><literal>RWO</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The volume can be mounted as read-write by a single node.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>ReadWriteOncePod</simpara></entry>
<entry align="left" valign="top"><simpara><literal>RWOP</literal></simpara></entry>
<entry align="left" valign="top"><simpara>The volume can be mounted as read-write by a single pod on a single node.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<table frame="all" rowsep="1" colsep="1">
<title>Supported access modes for persistent volumes</title>
<?dbhtml table-width="100%"?>
<?dbfo table-width="100%"?>
<?dblatex table-width="100%"?>
<tgroup cols="5">
<colspec colname="col_1" colwidth="85*"/>
<colspec colname="col_2" colwidth="85*"/>
<colspec colname="col_3" colwidth="85*"/>
<colspec colname="col_4" colwidth="85*"/>
<colspec colname="col_5" colwidth="85*"/>
<thead>
<row>
<entry align="left" valign="top">Volume plugin</entry>
<entry align="center" valign="top">ReadWriteOnce <superscript>[1]</superscript></entry>
<entry align="center" valign="top">ReadWriteOncePod</entry>
<entry align="center" valign="top">ReadOnlyMany</entry>
<entry align="center" valign="top">ReadWriteMany</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>AWS EBS <superscript>[2]</superscript></simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>AWS EFS</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>GCP Persistent Disk</simpara></entry>
<entry align="center" valign="top"><simpara>✅ <superscript>[4]</superscript></simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅ <superscript>[4]</superscript></simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>GCP Filestore</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>LVM Storage</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
</row>
</tbody>
</tgroup>
</table>
<para role="small">
<orderedlist numeration="arabic">
<listitem>
<simpara>ReadWriteOnce (RWO) volumes cannot be mounted on multiple nodes. If a node fails, the system does not allow the attached RWO volume to be mounted on a new node because it is already assigned to the failed node. If you encounter a multi-attach error message as a result, force delete the pod on a shutdown or crashed node to avoid data loss in critical workloads, such as when dynamic persistent volumes are attached.</simpara>
</listitem>
<listitem>
<simpara>Use a recreate deployment strategy for pods that rely on AWS EBS.</simpara>
</listitem>
<listitem>
<simpara>Only raw block volumes support the <literal>ReadWriteMany</literal> (RWX) access mode for Fibre Channel and iSCSI. For more information, see "Block volume support".</simpara>
</listitem>
<listitem>
<simpara>For GCP hyperdisk-balanced disks:</simpara>
<itemizedlist>
<listitem>
<simpara>The supported access modes are:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>ReadWriteOnce</literal></simpara>
</listitem>
<listitem>
<simpara><literal>ReadWriteMany</literal></simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Cloning and snapshotting is disabled for disks with <literal>ReadWriteMany</literal> access mode enabled.</simpara>
</listitem>
<listitem>
<simpara>You can attach a single hyperdisk-balanced disk volume in <literal>ReadWriteMany</literal> to a maximum of 8 instances.</simpara>
</listitem>
<listitem>
<simpara>You can only resize a disk in <literal>ReadWriteMany</literal> if you detach the disk from all instances.</simpara>
</listitem>
<listitem>
<simpara><link xlink:href="https://cloud.google.com/compute/docs/disks/attach-disks">Additional limitations</link>.</simpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
</para>
</section>
<section xml:id="pv-phase_understanding-persistent-storage">
<title>Phase</title>
<simpara>Volumes can be found in one of the following phases:</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Volume phases</title>
<tgroup cols="2">
<colspec colname="col_1" colwidth="33.3333*"/>
<colspec colname="col_2" colwidth="66.6667*"/>
<thead>
<row>
<entry align="left" valign="top">Phase</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>Available</simpara></entry>
<entry align="left" valign="top"><simpara>A free resource not yet bound to a claim.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Bound</simpara></entry>
<entry align="left" valign="top"><simpara>The volume is bound to a claim.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Released</simpara></entry>
<entry align="left" valign="top"><simpara>The claim was deleted, but the resource is not yet reclaimed by the
cluster.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Failed</simpara></entry>
<entry align="left" valign="top"><simpara>The volume has failed its automatic reclamation.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<section xml:id="_last-phase-transition-time">
<title>Last phase transition time</title>
<simpara>The <literal>LastPhaseTransitionTime</literal> field has a timestamp that updates every time a persistent volume (PV) transitions to a different phase (<literal>pv.Status.Phase</literal>). To find the time of the last phase transition for a PV, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get pv &lt;pv_name&gt; -o json | jq '.status.lastPhaseTransitionTime' <co xml:id="CO4-1"/></programlisting>
<calloutlist>
<callout arearefs="CO4-1">
<para>Specify the name of the PV that you want to see the last phase transition.</para>
</callout>
</calloutlist>
</section>
<section xml:id="pv-mount-options_understanding-persistent-storage">
<title>Mount options</title>
<simpara>You can specify mount options while mounting a PV by using the attribute <literal>mountOptions</literal>.</simpara>
<simpara>For example:</simpara>
<formalpara>
<title>Mount options example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv0001
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  mountOptions: <co xml:id="CO5-1"/>
    - nfsvers=4.1
  nfs:
    path: /tmp
    server: 172.17.0.2
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    name: claim1
    namespace: default</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO5-1">
<para>Specified mount options are used while mounting the PV to the disk.</para>
</callout>
</calloutlist>
<simpara>The following PV types support mount options:</simpara>
<itemizedlist>
<listitem>
<simpara>AWS Elastic Block Store (EBS)</simpara>
</listitem>
<listitem>
<simpara>AWS Elastic File Storage (EFS)</simpara>
</listitem>
<listitem>
<simpara>GCE Persistent Disk</simpara>
</listitem>
</itemizedlist>
</section>
</section>
</section>
<section xml:id="storage-persistent-storage-pvc_understanding-persistent-storage">
<title>Persistent volume claims</title>
<simpara role="_abstract">To define storage requirements for your workloads, review the structure of a <literal>PersistentVolumeClaim</literal> (PVC). This object includes a <literal>spec</literal> field to configure the request and a <literal>status</literal> field to monitor the current state of the claim.</simpara>
<formalpara>
<title><literal>PersistentVolumeClaim</literal> object definition example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: myclaim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: gold
status:
# ...</programlisting>
</para>
</formalpara>
<simpara>where:</simpara>
<variablelist>
<varlistentry>
<term><literal>apiVersion</literal></term>
<listitem>
<simpara>Specifies the name of the PVC.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>spec.accessModes</literal></term>
<listitem>
<simpara>Specifies the access mode, defining the read/write and mount permissions.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>requests.storage</literal></term>
<listitem>
<simpara>Specifies the amount of storage available to the PVC.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term><literal>storageClassName</literal></term>
<listitem>
<simpara>Specifies the name of the <literal>StorageClass</literal> required by the claim.</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="block-volume-support_understanding-persistent-storage">
<title>Block volume support</title>
<simpara>OpenShift Container Platform can statically provision raw block volumes. These volumes do not have a file system, and can provide performance benefits for applications that either write to the disk directly or implement their own storage service.</simpara>
<simpara>Raw block volumes are provisioned by specifying <literal>volumeMode: Block</literal> in the PV and PVC specification.</simpara>
<important>
<simpara>Pods using raw block volumes must be configured to allow privileged containers.</simpara>
</important>
<simpara>The following table displays which volume plugins support block volumes.</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Block volume support</title>
<?dbhtml table-width="100%"?>
<?dbfo table-width="100%"?>
<?dblatex table-width="100%"?>
<tgroup cols="4">
<colspec colname="col_1" colwidth="106.25*"/>
<colspec colname="col_2" colwidth="106.25*"/>
<colspec colname="col_3" colwidth="106.25*"/>
<colspec colname="col_4" colwidth="106.25*"/>
<thead>
<row>
<entry align="left" valign="top">Volume Plugin</entry>
<entry align="left" valign="top">Manually provisioned</entry>
<entry align="left" valign="top">Dynamically provisioned</entry>
<entry align="left" valign="top">Fully supported</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>Amazon Elastic Block Store (Amazon EBS)</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Amazon Elastic File Storage (Amazon EFS)</simpara></entry>
<entry align="left" valign="top"></entry>
<entry align="left" valign="top"></entry>
<entry align="left" valign="top"></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>GCP</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>LVM Storage</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
<entry align="left" valign="top"><simpara>✅</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<section xml:id="block-volume-examples_understanding-persistent-storage">
<title>Block volume examples</title>
<formalpara>
<title>PV example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolume
metadata:
  name: block-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  volumeMode: Block <co xml:id="CO6-1"/>
  persistentVolumeReclaimPolicy: Retain
  fc:
    targetWWNs: ["50060e801049cfd1"]
    lun: 0
    readOnly: false</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO6-1">
<para><literal>volumeMode</literal> must be set to <literal>Block</literal> to indicate that this PV is a raw
block volume.</para>
</callout>
</calloutlist>
<formalpara>
<title>PVC example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: block-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block <co xml:id="CO7-1"/>
  resources:
    requests:
      storage: 10Gi</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO7-1">
<para><literal>volumeMode</literal> must be set to <literal>Block</literal> to indicate that a raw block PVC
is requested.</para>
</callout>
</calloutlist>
<formalpara>
<title><literal>Pod</literal> specification example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: Pod
metadata:
  name: pod-with-block-volume
spec:
  containers:
    - name: fc-container
      image: fedora:26
      command: ["/bin/sh", "-c"]
      args: [ "tail -f /dev/null" ]
      volumeDevices:  <co xml:id="CO8-1"/>
        - name: data
          devicePath: /dev/xvda <co xml:id="CO8-2"/>
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: block-pvc <co xml:id="CO8-3"/></programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO8-1">
<para><literal>volumeDevices</literal>, instead of <literal>volumeMounts</literal>, is used for block
devices. Only <literal>PersistentVolumeClaim</literal> sources can be used with
raw block volumes.</para>
</callout>
<callout arearefs="CO8-2">
<para><literal>devicePath</literal>, instead of <literal>mountPath</literal>, represents the path to the
physical device where the raw block is mapped to the system.</para>
</callout>
<callout arearefs="CO8-3">
<para>The volume source must be of type <literal>persistentVolumeClaim</literal> and must
match the name of the PVC as expected.</para>
</callout>
</calloutlist>
<table frame="all" rowsep="1" colsep="1">
<title>Accepted values for <literal>volumeMode</literal></title>
<tgroup cols="2">
<colspec colname="col_1" colwidth="33.3333*"/>
<colspec colname="col_2" colwidth="66.6667*"/>
<thead>
<row>
<entry align="left" valign="top">Value</entry>
<entry align="left" valign="top">Default</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>Yes</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>No</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<table frame="all" rowsep="1" colsep="1">
<title>Binding scenarios for block volumes</title>
<tgroup cols="3">
<colspec colname="col_1" colwidth="16.6666*"/>
<colspec colname="col_2" colwidth="33.3333*"/>
<colspec colname="col_3" colwidth="50.0001*"/>
<thead>
<row>
<entry align="left" valign="top">PV <literal>volumeMode</literal></entry>
<entry align="left" valign="top">PVC <literal>volumeMode</literal></entry>
<entry align="left" valign="top">Binding result</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Unspecified</simpara></entry>
<entry align="left" valign="top"><simpara>Unspecified</simpara></entry>
<entry align="left" valign="top"><simpara>Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>Unspecified</simpara></entry>
<entry align="left" valign="top"><simpara>Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Unspecified</simpara></entry>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Unspecified</simpara></entry>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>No Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>Unspecified</simpara></entry>
<entry align="left" valign="top"><simpara>No Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>No Bind</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Block</simpara></entry>
<entry align="left" valign="top"><simpara>Filesystem</simpara></entry>
<entry align="left" valign="top"><simpara>No Bind</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
<important>
<simpara>Unspecified values result in the default value of <literal>Filesystem</literal>.</simpara>
</important>
</section>
</section>
<section xml:id="storage-persistent-storage-fsGroup_understanding-persistent-storage">
<title>Reduce pod timeouts by using fsGroup</title>
<simpara role="_abstract">To reduce pod timeouts when using a storage volume with many files, configure the <literal>fsGroup</literal> field. By specifying this field, you can manage how file ownership and permissions are applied, preventing delays caused by the default recursive permission changes on large volumes.</simpara>
<simpara>This can occur because, by default, OpenShift Container Platform recursively changes ownership and permissions for the contents of each volume to match the <literal>fsGroup</literal> specified in the <literal>securityContext</literal> of the pod when that volume is mounted. For volumes with many files, checking and changing ownership and permissions can be time consuming, slowing pod startup. You can use the <literal>fsGroupChangePolicy</literal> field inside a <literal>securityContext</literal> to control the way that OpenShift Container Platform checks and manages ownership and permissions for a volume.</simpara>
<simpara><literal>fsGroupChangePolicy</literal> defines behavior for changing ownership and permission of the volume before being exposed inside a pod. This field only applies to volume types that support <literal>fsGroup</literal>-controlled ownership and permissions. This field has two possible values:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>OnRootMismatch</literal>: Only change permissions and ownership if permission and ownership of root directory does not match with expected permissions of the volume. This can help shorten the time it takes to change ownership and permission of a volume to reduce pod timeouts.</simpara>
</listitem>
<listitem>
<simpara><literal>Always</literal>: (Default) Always change permission and ownership of the volume when a volume is mounted.</simpara>
</listitem>
</itemizedlist>
<note>
<simpara>The <literal>fsGroupChangePolicy</literal> field has no effect on ephemeral volume types, such as secret, configMap, and emptydir.</simpara>
</note>
<simpara>You can set <literal>fsGroupChangePolicy</literal> at either the namespace or pod level.</simpara>
<section xml:id="using_fsGroup_namespace_understanding-persistent-storage">
<title>Changing fsGroup at the namespace level</title>
<simpara>After applying the desired setting for <literal>fsGroupChangePolicy</literal> at the namespace level, all subsequently created pods in that namespace inherit the setting. However, if desired, you can override the inherited <literal>fsGroupChangePolicy</literal> setting for individual pods. Setting <literal>fsGroupChangePolicy</literal> at the pod level overrides inheritance from the namespace level setting for that pod.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Logged in to a running OpenShift Container Platform cluster with administrator privileges.</simpara>
</listitem>
<listitem>
<simpara>Access to the OpenShift Container Platform console.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To set <literal>fsGroupChangePolicy</literal> per namespace:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Select the desired namespace:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Administration</emphasis> &gt; <emphasis role="strong">Namespaces</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Namespaces</emphasis> page, click the desired namespace. The <emphasis role="strong">Namespace details</emphasis> page appears.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Add the <literal>fsGroupChangePolicy</literal> label to the namespace:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>On the <emphasis role="strong">Namespace details</emphasis> page, next to <emphasis role="strong">Labels</emphasis>, click <emphasis role="strong">Edit</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>In the <emphasis role="strong">Edit labels</emphasis> dialog, add the label <literal>storage.openshift.io/fsgroup-change-policy</literal> and set it equal to either:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>OnRootMismatch</literal>: Specifies only changing permissions and ownership if the permission and the ownership of root directory does not match with expected permissions of the volume, thus helping to avoid pod timeout problems.</simpara>
</listitem>
<listitem>
<simpara><literal>Always</literal>: (Default) Specifies always changing permission and ownership of the volume when a volume is mounted.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Save</emphasis>.</simpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<formalpara>
<title>Verification</title>
<para>Start up a pod in the previously edited namespace and observe that the parameter <literal>spec.securityContext.fsGroupChangePolicy</literal> contains the value that you set for the namespace.</para>
</formalpara>
<formalpara>
<title>Example pod YAML file showing <literal>fsGroupChangePolicy</literal> setting</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">securityContext:
  seLinuxOptions:
    level: 's0:c27,c24'
  runAsNonRoot: true
  fsGroup: 1000750000
  fsGroupChangePolicy: OnRootMismatch <co xml:id="CO9-1"/>
  ...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO9-1">
<para>This value is inherited from the namespace.</para>
</callout>
</calloutlist>
</section>
<section xml:id="using_fsGroup_pod_understanding-persistent-storage">
<title>Changing fsGroup at the pod level</title>
<simpara>You can set the set the <literal>fsGroupChangePolicy</literal> parameter in a new or existing deployment, and then the pods that it manages will have this parameter value. You can similarly do this for a statefulset. You cannot edit an existing pod to set <literal>fsGroupChangePolicy</literal>; however, you can set this parameter when creating a new pod.</simpara>
<simpara>This procedure describes how to set the <literal>fsGroupChangePolicy</literal> parameter in an existing deployment.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform console.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To set the <literal>fsGroupChangePolicy</literal> parameter in an existing deployment:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Click <emphasis role="strong">Workloads</emphasis> &gt; <emphasis role="strong">Deployments</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Deployment</emphasis> page, click the desired deployment.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Deployment details</emphasis> page, click the <emphasis role="strong">YAML</emphasis> tab.</simpara>
</listitem>
<listitem>
<simpara>Edit the deployment&#8217;s YAML file under <literal>spec.template.spec.securityContext</literal> using the following example file:</simpara>
<formalpara>
<title>Example deployment YAML file setting <literal>fsGroupChangePolicy</literal></title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">...
spec:
replicas: 3
selector:
matchLabels:
app: my-app
template:
metadata:
creationTimestamp: null
labels:
app: my-app
spec:
containers:
- name: container
image: 'image-registry.openshift-image-registry.svc:5000/openshift/httpd:latest'
ports:
- containerPort: 8080
protocol: TCP
resources: {}
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
imagePullPolicy: Always
restartPolicy: Always
terminationGracePeriodSeconds: 30
dnsPolicy: ClusterFirst
securityContext:
  fsGroupChangePolicy: OnRootMismatch <co xml:id="CO10-1"/>
...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO10-1">
<para><literal>OnRootMismatch</literal> specifies skipping recursive permission change, thus helping to avoid pod timeout problems. The default value is <literal>Always</literal>, which always changes permission and ownership of the volume when a volume is mounted.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Save</emphasis>.</simpara>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="using_selinuxChangePolicy_overview_understanding-persistent-storage">
<title>Reducing pod timeouts using seLinuxChangePolicy</title>
<simpara>SELinux (Security-Enhanced Linux) is a security mechanism that assigns security labels (contexts) to all objects (files, processes, network ports, etc.) on a system. These labels determine what a process can access. In OpenShift Container Platform, SELinux helps prevent containers from escaping and accessing the host system or other containers.</simpara>
<simpara>When a pod starts, the container runtime recursively relabels all files on a volume to match the pod&#8217;s SELinux context. For volumes with many files, this can significantly increase pod startup times.</simpara>
<simpara>Mount option specifies avoiding recursive relabeling of all files by attempting to mount the volume with the correct SELinux label directly using the -o context mount option, thus helping to avoid pod timeout problems.</simpara>
<formalpara>
<title>RWOP and SELinux mount option</title>
<para>ReadWriteOncePod (RWOP) persistent volumes use the SELinux mount feature by default.</para>
</formalpara>
<simpara>The mount option feature is driver dependent, and enabled by default in AWS EBS
and Red Hat OpenShift Data Foundation. For third-party drivers, contact your storage vendor.</simpara>
<formalpara>
<title>RWO and RWX and SELinux mount option</title>
<para>ReadWriteOnce (RWO) and ReadWriteMany (RWX) volumes use recursive relabeling by default.</para>
</formalpara>
<important>
<simpara>In a future OpenShift Container Platform version, RWO and RWX volumes will use <emphasis role="strong">mount option by default</emphasis>.</simpara>
</important>
<simpara>To assist you with the upcoming move to the mount option default, OpenShift Container Platform 4.20 reports SELinux-related conflicts when creating pods, and on running pods, to make you aware of potential conflicts, and to help you resolve them. For more information about this reporting, see this <link xlink:href="https://access.redhat.com/solutions/7131398">Kowledge Base article</link>.</simpara>
<simpara>If you are unable to resolve the SELinux-related conflicts, you can proactively opt-out of the future move to mount option as default for selected pods or namespaces. To opt out, see <emphasis>Opting out of the SELinux mount option default</emphasis>.</simpara>
<section xml:id="using_selinuxChangePolicy_testing-mountoption-rwo-rwx_understanding-persistent-storage">
<title>Testing the RWO and RWX and SELinux mount option feature</title>
<simpara>In OpenShift Container Platform 4.21, you can evaluate the mount option feature for RWO and RWX volumes as a Technology Preview feature.</simpara>
<important>
<simpara>RWO/RWX SELinux mount is a Technology Preview feature only. Technology Preview features are not supported with Red&#160;Hat production service level agreements (SLAs) and might not be functionally complete. Red Hat does not recommend using them in production. These features provide early access to upcoming product features, enabling customers to test functionality and provide feedback during the development process.</simpara>
<simpara>For more information about the support scope of Red Hat Technology Preview features, see <link xlink:href="https://access.redhat.com/support/offerings/techpreview/">Technology Preview Features Support Scope</link>.</simpara>
</important>
<simpara>To evaluate the mount option feature:</simpara>
<itemizedlist>
<listitem>
<simpara>Enable Feature Gates. For information about enabling Feature Gates, see <emphasis>Section Enabling features using feature gates</emphasis>.</simpara>
<simpara>RWO and RWX volumes now have mount option as the default behavior.</simpara>
</listitem>
</itemizedlist>
<simpara>Carefully test your applications and observe how they are using storage. Consult this <link xlink:href="https://access.redhat.com/solutions/7131398">Knowledge Base article</link> and consider opting out from using mount option if you are experiencing issues. See Section <emphasis>Opting out of the SELinux mount option default</emphasis>.</simpara>
</section>
<section xml:id="using_selinuxChangePolicy_pod-opt-out_understanding-persistent-storage">
<title>Opting out of the SELinux mount option default</title>
<simpara>If you want to opt out of the future move to mount option as default, you can affirmatively set the  <literal>seLinuxChangePolicy</literal> parameter to <literal>Recursive</literal> at either the individual pod or namespace level.</simpara>
<section xml:id="using_selinuxChangePolicy_namespace_understanding-persistent-storage">
<title>Changing seLinuxChangePolicy at the namespace level</title>
<simpara>After applying the desired setting for <literal>seLinuxChangePolicy</literal> at the namespace level, all subsequently created pods in that namespace inherit the setting. However, if desired, you can override the inherited <literal>seLinuxChangePolicy</literal> setting for individual pods. Setting <literal>seLinuxChangePolicy</literal> at the pod level overrides inheritance from the namespace level setting for that pod.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Logged in to a running OpenShift Container Platform cluster with administrator privileges.</simpara>
</listitem>
<listitem>
<simpara>Access to the OpenShift Container Platform console.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To set <literal>SELinuxChangePolicy</literal> per namespace:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Select the desired namespace:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Administration</emphasis> &gt; <emphasis role="strong">Namespaces</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Namespaces</emphasis> page, click the desired namespace. The <emphasis role="strong">Namespace details</emphasis> page appears.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Add the <literal>seLinuxChangePolicy</literal> label to the namespace:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>On the <emphasis role="strong">Namespace details</emphasis> page, next to <emphasis role="strong">Labels</emphasis>, click <emphasis role="strong">Edit</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>In the <emphasis role="strong">Edit labels</emphasis> dialog, add the label <literal>storage.openshift.io/selinux-change-policy=Recursive</literal>.</simpara>
<simpara>This specifies recursively relabeling all files on pod volumes to the appropriate SELinux context.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Save</emphasis>.</simpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<formalpara>
<title>Verification</title>
<para>Start up a pod in the previously edited namespace and observe that the parameter <literal>spec.securityContext.seLinuxChangePolicy</literal> is set to <literal>Recursive</literal>.</para>
</formalpara>
<formalpara>
<title>Example pod YAML file showing <literal>seLinuxChangePolicy</literal> setting</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">securityContext:
    seLinuxOptions:
      level: 's0:c27,c19'
    runAsNonRoot: true
    fsGroup: 1000740000
    seccompProfile:
      type: RuntimeDefault
    seLinuxChangePolicy: Recursive <co xml:id="CO11-1"/>
  ...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO11-1">
<para>This value is inherited from the namespace.</para>
</callout>
</calloutlist>
</section>
<section xml:id="using_selinuxChangePolicy_pod_understanding-persistent-storage">
<title>Changing seLinuxChangePolicy at the pod level</title>
<simpara>You can set the set the <literal>seLinuxChangePolicy</literal> parameter in a new or existing Deployment, and then the pods that it manages will have this parameter value. You can similarly do this for a StatefulSet. You cannot edit an existing pod to set <literal>seLinuxChangePolicy</literal>; however, you can set this parameter when creating a new pod.</simpara>
<simpara>This procedure describes how to set the <literal>seLinuxChangePolicy</literal> parameter in an existing deployment.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform console.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To set the <literal>seLinuxChangePolicy</literal> parameter in an existing deployment:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Click <emphasis role="strong">Workloads</emphasis> &gt; <emphasis role="strong">Deployments</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Deployment</emphasis> page, click the desired deployment.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Deployment details</emphasis> page, click the <emphasis role="strong">YAML</emphasis> tab.</simpara>
</listitem>
<listitem>
<simpara>Edit the deployment&#8217;s YAML file under <literal>spec.template.spec.securityContext</literal> as per the following example file:</simpara>
<formalpara>
<title>Example deployment YAML file setting <literal>seLinuxChangePolicy</literal></title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">  ...
securityContext:
  seLinuxChangePolicy: Recursive <co xml:id="CO12-1"/>
  ...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO12-1">
<para>Specifies recursively relabeling all files on all pod volumes to the appropriate SELinux context.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Save</emphasis>.</simpara>
</listitem>
</orderedlist>
</section>
</section>
</section>
</chapter>
<chapter xml:id="_configuring-persistent-storage">
<title>Configuring persistent storage</title>
<section xml:id="persistent-storage-aws">
<title>Persistent storage using AWS Elastic Block Store</title>
<simpara>OpenShift Container Platform clusters are prebuilt with two storage classes that use Amazon Elastic Block Store (Amazon EBS) volumes. These storage classes are ready to use and some familiarity with Kubernetes and AWS is assumed.</simpara>
<simpara>The following are the two prebuilt storage classes:</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="2">
<colspec colname="col_1" colwidth="50*"/>
<colspec colname="col_2" colwidth="50*"/>
<thead>
<row>
<entry align="left" valign="top">Name</entry>
<entry align="left" valign="top">Provisioner</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>gp2-csi</simpara></entry>
<entry align="left" valign="top"><simpara>ebs.csi.aws.com</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>gp3-csi (default)</simpara></entry>
<entry align="left" valign="top"><simpara>ebs.csi.aws.com</simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
<simpara>The gp3-csi storage class is set as default; however, you can select any of the storage classes as the default storage class.</simpara>
<simpara>The Kubernetes persistent volume framework allows administrators to provision a cluster with persistent storage and gives users a way to request those resources without having any knowledge of the underlying infrastructure.
You can dynamically provision Amazon EBS volumes.
Persistent volumes are not bound to a single project or namespace; they can be shared across the OpenShift Container Platform cluster.
Persistent volume claims are specific to a project or namespace and can be requested by users.
You can define a KMS key to encrypt container-persistent volumes on AWS.
By default, newly created clusters using OpenShift Container Platform version 4.10 and later use gp3 storage and the <link xlink:href="https://github.com/openshift/aws-ebs-csi-driver">AWS EBS CSI driver</link>.</simpara>
<important>
<simpara>High-availability of storage in the infrastructure is left to the underlying
storage provider.</simpara>
</important>
<section xml:id="storage-create-storage-class_persistent-storage-aws">
<title>Creating the EBS storage class</title>
<simpara>Storage classes are used to differentiate and delineate storage levels and
usages. By defining a storage class, users can obtain dynamically provisioned
persistent volumes.</simpara>
</section>
<section xml:id="creating-volume-claim_persistent-storage-aws">
<title>Creating the persistent volume claim</title>
<formalpara>
<title>Prerequisites</title>
<para>Storage must exist in the underlying infrastructure before it can be mounted as
a volume in OpenShift Container Platform.</para>
</formalpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>In the OpenShift Container Platform web console, click <emphasis role="strong">Storage</emphasis> &#8594; <emphasis role="strong">Persistent Volume Claims</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>In the persistent volume claims overview, click <emphasis role="strong">Create Persistent Volume Claim</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Define the desired options on the page that appears.</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Select the previously-created storage class from the drop-down menu.</simpara>
</listitem>
<listitem>
<simpara>Enter a unique name for the storage claim.</simpara>
</listitem>
<listitem>
<simpara>Select the access mode. This selection determines the read and write access for the storage claim.</simpara>
</listitem>
<listitem>
<simpara>Define the size of the storage claim.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Create</emphasis> to create the persistent volume claim and generate a persistent
volume.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="volume-format-AWS_persistent-storage-aws">
<title>Volume format</title>
<simpara>Before OpenShift Container Platform mounts the volume and passes it to a container, it checks that the volume contains a file system as specified by the <literal>fsType</literal> arameter in the persistent volume definition. If the device is not formatted with the file system, all data from the device is erased and the device is automatically formatted with the given file system.</simpara>
<simpara>This verification enables you to use unformatted AWS volumes as persistent volumes, because OpenShift Container Platform formats them before the first use.</simpara>
</section>
<section xml:id="maximum-number-of-ebs-volumes-on-a-node_persistent-storage-aws">
<title>Maximum number of EBS volumes on a node</title>
<simpara>By default, OpenShift Container Platform supports a maximum of 39 EBS volumes attached to one
node. This limit is consistent with the
<link xlink:href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/volume_limits.html#linux-specific-volume-limits">AWS volume limits</link>. The volume limit depends on the instance type.</simpara>
<important>
<simpara>As a cluster administrator, you must use either in-tree or Container Storage Interface (CSI) volumes and their respective storage classes, but never both volume types at the same time. The maximum attached EBS volume number is counted separately for in-tree and CSI volumes, which means you could have up to 39 EBS volumes of each type.</simpara>
</important>
<simpara>For information about accessing additional storage options, such as volume snapshots, that are not possible with in-tree volume plug-ins, see <link linkend="persistent-storage-csi-ebs">AWS Elastic Block Store CSI Driver Operator</link>.</simpara>
</section>
<section xml:id="aws-container-persistent-volumes-encrypt_persistent-storage-aws">
<title>Encrypting container persistent volumes on AWS with a KMS key</title>
<simpara>Defining a KMS key to encrypt container-persistent volumes on AWS is useful when you have explicit compliance and security guidelines when deploying to AWS.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Underlying infrastructure must contain storage.</simpara>
</listitem>
<listitem>
<simpara>You must create a customer KMS key on AWS.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create a storage class:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">$ cat &lt;&lt; EOF | oc create -f -
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: &lt;storage-class-name&gt; <co xml:id="CO13-1"/>
parameters:
  fsType: ext4 <co xml:id="CO13-2"/>
  encrypted: "true"
  kmsKeyId: keyvalue <co xml:id="CO13-3"/>
provisioner: ebs.csi.aws.com
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
EOF</programlisting>
<calloutlist>
<callout arearefs="CO13-1">
<para>Specifies the name of the storage class.</para>
</callout>
<callout arearefs="CO13-2">
<para>File system that is created on provisioned volumes.</para>
</callout>
<callout arearefs="CO13-3">
<para>Specifies the full Amazon Resource Name (ARN) of the key to use when encrypting the container-persistent volume. If you do not provide any key, but the <literal>encrypted</literal> field is set to <literal>true</literal>, then the default KMS key is used. See <link xlink:href="https://docs.aws.amazon.com/kms/latest/developerguide/find-cmk-id-arn.html">Finding the key ID and key ARN on AWS</link> in the AWS documentation.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Create a persistent volume claim (PVC) with the storage class specifying the KMS key:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">$ cat &lt;&lt; EOF | oc create -f -
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: &lt;storage-class-name&gt;
  resources:
    requests:
      storage: 1Gi
EOF</programlisting>
</listitem>
<listitem>
<simpara>Create workload containers to consume the PVC:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">$ cat &lt;&lt; EOF | oc create -f -
kind: Pod
metadata:
  name: mypod
spec:
  containers:
    - name: httpd
      image: quay.io/centos7/httpd-24-centos7
      ports:
        - containerPort: 80
      volumeMounts:
        - mountPath: /mnt/storage
          name: data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: mypvc
EOF</programlisting>
</listitem>
</orderedlist>
</section>
<section xml:id="additional-resources_persistent-storage-aws" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara>See <link linkend="persistent-storage-csi-ebs">AWS Elastic Block Store CSI Driver Operator</link> for information about accessing additional storage options, such as volume snapshots, that are not possible with in-tree volume plugins.</simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-using-gce">
<title>Persistent storage using GCE Persistent Disk</title>
<simpara>OpenShift Container Platform supports GCE Persistent Disk volumes (gcePD).
You can provision your OpenShift Container Platform cluster with persistent storage
using GCE.
Some familiarity with Kubernetes and GCE is assumed.</simpara>
<simpara>The Kubernetes persistent volume framework allows administrators to
provision a cluster with persistent storage and gives users a way to
request those resources without having any knowledge of the underlying
infrastructure.</simpara>
<simpara>GCE Persistent Disk volumes can be provisioned dynamically.</simpara>
<simpara>Persistent volumes are not bound to a single project or namespace;
they can be shared across the OpenShift Container Platform cluster.
Persistent volume claims are specific to a project or namespace and can be
requested by users.</simpara>
<important>
<simpara>High availability of storage in the infrastructure is left to the underlying
storage provider.</simpara>
</important>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://cloud.google.com/compute/docs/disks/">GCE Persistent Disk</link></simpara>
</listitem>
</itemizedlist>
<section xml:id="storage-create-storage-class_persistent-storage-gce">
<title>Creating the GCE storage class</title>
<simpara>Storage classes are used to differentiate and delineate storage levels and
usages. By defining a storage class, users can obtain dynamically provisioned
persistent volumes.</simpara>
</section>
<section xml:id="creating-volume-claim_persistent-storage-gce">
<title>Creating the persistent volume claim</title>
<formalpara>
<title>Prerequisites</title>
<para>Storage must exist in the underlying infrastructure before it can be mounted as
a volume in OpenShift Container Platform.</para>
</formalpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>In the OpenShift Container Platform web console, click <emphasis role="strong">Storage</emphasis> &#8594; <emphasis role="strong">Persistent Volume Claims</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>In the persistent volume claims overview, click <emphasis role="strong">Create Persistent Volume Claim</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Define the desired options on the page that appears.</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Select the previously-created storage class from the drop-down menu.</simpara>
</listitem>
<listitem>
<simpara>Enter a unique name for the storage claim.</simpara>
</listitem>
<listitem>
<simpara>Select the access mode. This selection determines the read and write access for the storage claim.</simpara>
</listitem>
<listitem>
<simpara>Define the size of the storage claim.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Create</emphasis> to create the persistent volume claim and generate a persistent
volume.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="volume-format-GCE_persistent-storage-gce">
<title>Volume format</title>
<simpara>Before OpenShift Container Platform mounts the volume and passes it to a container, it checks that the volume contains a file system as specified by the <literal>fsType</literal> arameter in the persistent volume definition. If the device is not formatted with the file system, all data from the device is erased and the device is automatically formatted with the given file system.</simpara>
<simpara>This verification enables you to use unformatted GCE volumes as persistent volumes, because OpenShift Container Platform formats them before the first use.</simpara>
</section>
</section>
</chapter>
<chapter xml:id="_using-container-storage-interface-csi">
<title>Using Container Storage Interface (CSI)</title>
<section xml:id="persistent-storage-csi">
<title>Configuring CSI volumes</title>
<simpara>The Container Storage Interface (CSI) allows OpenShift Container Platform to consume
storage from storage back ends that implement the
<link xlink:href="https://github.com/container-storage-interface/spec">CSI interface</link>
as persistent storage.</simpara>
<note>
<simpara>OpenShift Container Platform 4.16 supports version 1.6.0 of the <link xlink:href="https://github.com/container-storage-interface/spec">CSI specification</link>.</simpara>
</note>
<section xml:id="persistent-storage-csi-architecture_persistent-storage-csi">
<title>CSI architecture</title>
<simpara>CSI drivers are typically shipped as container images. These containers
are not aware of OpenShift Container Platform where they run. To use CSI-compatible
storage back end in OpenShift Container Platform, the cluster administrator must deploy
several components that serve as a bridge between OpenShift Container Platform and the
storage driver.</simpara>
<simpara>The following diagram provides a high-level overview about the components
running in pods in the OpenShift Container Platform cluster.</simpara>
<informalfigure>
<mediaobject>
<imageobject>
<imagedata fileref="images/csi-arch-rev1.png"/>
</imageobject>
<textobject><phrase>Architecture of CSI components</phrase></textobject>
</mediaobject>
</informalfigure>
<simpara>It is possible to run multiple CSI drivers for different storage back ends.
Each driver needs its own external controllers deployment and daemon set
with the driver and CSI registrar.</simpara>
<section xml:id="external-csi-contollers_persistent-storage-csi">
<title>External CSI controllers</title>
<simpara>External CSI controllers is a deployment that deploys one or more pods
with five containers:</simpara>
<itemizedlist>
<listitem>
<simpara>The snapshotter container watches <literal>VolumeSnapshot</literal> and <literal>VolumeSnapshotContent</literal> objects and is responsible for the creation and deletion of <literal>VolumeSnapshotContent</literal> object.</simpara>
</listitem>
<listitem>
<simpara>The resizer container is a sidecar container that watches for <literal>PersistentVolumeClaim</literal> updates and triggers <literal>ControllerExpandVolume</literal> operations against a CSI endpoint if you request more storage on <literal>PersistentVolumeClaim</literal> object.</simpara>
</listitem>
<listitem>
<simpara>An external CSI attacher container translates <literal>attach</literal> and <literal>detach</literal>
calls from OpenShift Container Platform to respective <literal>ControllerPublish</literal> and
<literal>ControllerUnpublish</literal> calls to the CSI driver.</simpara>
</listitem>
<listitem>
<simpara>An external CSI provisioner container that translates <literal>provision</literal> and
<literal>delete</literal> calls from OpenShift Container Platform to respective <literal>CreateVolume</literal> and
<literal>DeleteVolume</literal> calls to the CSI driver.</simpara>
</listitem>
<listitem>
<simpara>A CSI driver container.</simpara>
</listitem>
</itemizedlist>
<simpara>The CSI attacher and CSI provisioner containers communicate with the CSI
driver container using UNIX Domain Sockets, ensuring that no CSI
communication leaves the pod. The CSI driver is not accessible from
outside of the pod.</simpara>
<note>
<simpara>The <literal>attach</literal>, <literal>detach</literal>, <literal>provision</literal>, and <literal>delete</literal> operations typically require
the CSI driver to use credentials to the storage backend. Run the CSI
controller pods on infrastructure nodes so the credentials are never leaked
to user processes, even in the event of a catastrophic security breach
on a compute node.</simpara>
</note>
<note>
<simpara>The external attacher must also run for CSI drivers that do not support
third-party <literal>attach</literal> or <literal>detach</literal> operations. The external attacher will
not issue any <literal>ControllerPublish</literal> or <literal>ControllerUnpublish</literal> operations to
the CSI driver. However, it still must run to implement the necessary
OpenShift Container Platform attachment API.</simpara>
</note>
</section>
<section xml:id="csi-driver-daemonset_persistent-storage-csi">
<title>CSI driver daemon set</title>
<simpara>The CSI driver daemon set runs a pod on every node that allows
OpenShift Container Platform to mount storage provided by the CSI driver to the node
and use it in user workloads (pods) as persistent volumes (PVs). The pod
with the CSI driver installed contains the following containers:</simpara>
<itemizedlist>
<listitem>
<simpara>A CSI driver registrar, which registers the CSI driver into the
<literal>openshift-node</literal> service running on the node. The <literal>openshift-node</literal> process
running on the node then directly connects with the CSI driver using the
UNIX Domain Socket available on the node.</simpara>
</listitem>
<listitem>
<simpara>A CSI driver.</simpara>
</listitem>
</itemizedlist>
<simpara>The CSI driver deployed on the node should have as few credentials to the
storage back end as possible. OpenShift Container Platform will only use the node plugin
set of CSI calls such as <literal>NodePublish</literal>/<literal>NodeUnpublish</literal> and
<literal>NodeStage</literal>/<literal>NodeUnstage</literal>, if these calls are implemented.</simpara>
</section>
</section>
<section xml:id="persistent-storage-csi-drivers-supported_persistent-storage-csi">
<title>CSI drivers supported by OpenShift Container Platform</title>
<simpara role="_abstract">OpenShift Container Platform installs certain CSI drivers by default, giving users storage options that are not possible with in-tree volume plugins.</simpara>
<simpara>To create CSI-provisioned persistent volumes that mount to these supported storage assets, OpenShift Container Platform installs the necessary CSI driver Operator, the CSI driver, and the required storage class by default. For more details about the default namespace of the Operator and driver, see the documentation for the specific CSI Driver Operator.</simpara>
<important>
<simpara>The AWS EFS and GCP Filestore CSI drivers are not installed by default, and must be installed manually. For instructions on installing the AWS EFS CSI driver, see <link xlink:href="https://docs.redhat.com/documentation/openshift_dedicated/4/html/storage/using-container-storage-interface-csi#persistent-storage-efs-csi-driver-operator-setup_persistent-storage-csi-aws-efs">Setting up AWS Elastic File Service CSI Driver Operator</link>. For instructions on installing the GCP Filestore CSI driver, see <link xlink:href="https://docs.redhat.com/documentation/openshift_container_platform/4.16/html/storage/using-container-storage-interface-csi#persistent-storage-csi-google-cloud-file-overview">Google Cloud Filestore CSI Driver Operator</link>.</simpara>
</important>
<simpara>The following table describes the CSI drivers that are
supported by OpenShift Container Platform, and which CSI features they support, such as volume snapshots and resize.</simpara>
<important>
<simpara>If your CSI driver is not listed in the following table, you must follow the installation instructions provided by your CSI storage vendor to use their supported CSI features.</simpara>
</important>
<simpara>For a list of third-party-certified CSI drivers, see the <emphasis>Red Hat ecosystem portal</emphasis> under <emphasis>Additional resources</emphasis>.</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>Supported CSI drivers and features in OpenShift Container Platform</title>
<tgroup cols="6">
<colspec colname="col_1" colwidth="16.6666*"/>
<colspec colname="col_2" colwidth="16.6666*"/>
<colspec colname="col_3" colwidth="16.6666*"/>
<colspec colname="col_4" colwidth="16.6666*"/>
<colspec colname="col_5" colwidth="16.6666*"/>
<colspec colname="col_6" colwidth="16.667*"/>
<thead>
<row>
<entry align="left" valign="top">CSI driver</entry>
<entry align="center" valign="top">CSI volume snapshots</entry>
<entry align="center" valign="top">CSI volume group snapshots <superscript>[1]</superscript></entry>
<entry align="center" valign="top">CSI cloning</entry>
<entry align="center" valign="top">CSI resize</entry>
<entry align="center" valign="top">Inline ephemeral volumes</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>AWS EBS</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>AWS EFS</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Google Compute Platform (GCP) persistent disk (PD)</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"><simpara>✅<superscript>[2]</superscript></simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>GCP Filestore</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>LVM Storage</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"><simpara>✅</simpara></entry>
<entry align="center" valign="top"></entry>
</row>
</tbody>
</tgroup>
</table>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://catalog.redhat.com/">Red Hat ecosystem portal</link></simpara>
</listitem>
<listitem>
<simpara><link xlink:href="https://access.redhat.com/articles/third-party-software-support">Third-party support policy</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="csi-dynamic-provisioning_persistent-storage-csi">
<title>Dynamic provisioning</title>
<simpara>Dynamic provisioning of persistent storage depends on the capabilities of
the CSI driver and underlying storage back end. The provider of the CSI
driver should document how to create a storage class in OpenShift Container Platform and
the parameters available for configuration.</simpara>
<simpara>The created storage class can be configured to enable dynamic provisioning.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Create a default storage class that ensures all PVCs that do not require
any special storage class are provisioned by the installed CSI driver.</simpara>
<programlisting language="shell" linenumbering="unnumbered"># oc create -f - &lt;&lt; EOF
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: &lt;storage-class&gt; <co xml:id="CO14-1"/>
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: &lt;provisioner-name&gt; <co xml:id="CO14-2"/>
parameters:
  csi.storage.k8s.io/fstype: xfs  <co xml:id="CO14-3"/>
EOF</programlisting>
<calloutlist>
<callout arearefs="CO14-1">
<para>The name of the storage class that will be created.</para>
</callout>
<callout arearefs="CO14-2">
<para>The name of the CSI driver that has been installed.</para>
</callout>
<callout arearefs="CO14-3">
<para>The vSphere CSI driver supports all of the file systems supported by the underlying Red Hat Core operating system release, including XFS and Ext4.</para>
</callout>
</calloutlist>
</listitem>
</itemizedlist>
</section>
<section xml:id="csi-example-usage_persistent-storage-csi">
<title>Example using the CSI driver</title>
<simpara>The following example installs a default MySQL template without any
changes to the template.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>The CSI driver has been deployed.</simpara>
</listitem>
<listitem>
<simpara>A storage class has been created for dynamic provisioning.</simpara>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Create the MySQL template:</simpara>
<programlisting language="terminal" linenumbering="unnumbered"># oc new-app mysql-persistent</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">--&gt; Deploying template "openshift/mysql-persistent" to project default
...</programlisting>
</para>
</formalpara>
<programlisting language="terminal" linenumbering="unnumbered"># oc get pvc</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME           STATUS         VOLUME                                   CAPACITY
mysql          Bound          kubernetes-dynamic-pv-3271ffcb4e1811e8   1Gi

ACCESS MODES   STORAGECLASS   AGE
RWO            gp3-csi        3s</programlisting>
</para>
</formalpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-sc-manage">
<title>Managing the default storage class</title>
<section xml:id="_overview">
<title>Overview</title>
<simpara>Managing the default storage class allows you to accomplish several different objectives:</simpara>
<itemizedlist>
<listitem>
<simpara>Enforcing static provisioning by disabling dynamic provisioning.</simpara>
</listitem>
<listitem>
<simpara>When you have other preferred storage classes, preventing the storage operator from re-creating the initial default storage class.</simpara>
</listitem>
<listitem>
<simpara>Renaming, or otherwise changing, the default storage class</simpara>
</listitem>
</itemizedlist>
<simpara>To accomplish these objectives, you change the setting for the <literal>spec.storageClassState</literal> field in the <literal>ClusterCSIDriver</literal> object. The possible settings for this field are:</simpara>
<itemizedlist>
<listitem>
<simpara><emphasis role="strong">Managed</emphasis>: (Default) The Container Storage Interface (CSI) operator is actively managing its default storage class, so that most manual changes made by a cluster administrator to the default storage class are removed, and the default storage class is continuously re-created if you attempt to manually delete it.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Unmanaged</emphasis>: You can modify the default storage class. The CSI operator is not actively managing storage classes, so that it is not reconciling the default storage class it creates automatically.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Removed</emphasis>: The CSI operators deletes the default storage class.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="persistent-storage-csi-sc-managing_persistent-storage-csi-sc-manage">
<title>Managing the default storage class using the web console</title>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform web console.</simpara>
</listitem>
<listitem>
<simpara>Access to the cluster with cluster-admin privileges.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To manage the default storage class using the web console:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Log in to the web console.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Administration</emphasis> &gt; <emphasis role="strong">CustomResourceDefinitions</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">CustomResourceDefinitions</emphasis> page, type <literal>clustercsidriver</literal> to find the <literal>ClusterCSIDriver</literal> object.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">ClusterCSIDriver</emphasis>, and then click the <emphasis role="strong">Instances</emphasis> tab.</simpara>
</listitem>
<listitem>
<simpara>Click the name of the desired instance, and then click the <emphasis role="strong">YAML</emphasis> tab.</simpara>
</listitem>
<listitem>
<simpara>Add the <literal>spec.storageClassState</literal> field with a value of <literal>Managed</literal>, <literal>Unmanaged</literal>, or <literal>Removed</literal>.</simpara>
<formalpara>
<title>Example</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">...
spec:
  driverConfig:
    driverType: ''
  logLevel: Normal
  managementState: Managed
  observedConfig: null
  operatorLogLevel: Normal
  storageClassState: Unmanaged <co xml:id="CO15-1"/>
...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO15-1">
<para><literal>spec.storageClassState</literal> field set to "Unmanaged"</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Save</emphasis>.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="persistent-storage-csi-sc-managing-cli_persistent-storage-csi-sc-manage">
<title>Managing the default storage class using the CLI</title>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster with cluster-admin privileges.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To manage the storage class using the CLI, run the following command:</para>
</formalpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch clustercsidriver $DRIVERNAME --type=merge -p "{\"spec\":{\"storageClassState\":\"${STATE}\"}}" <co xml:id="CO16-1"/></programlisting>
<calloutlist>
<callout arearefs="CO16-1">
<para>Where <literal>${STATE}</literal> is "Removed" or "Managed" or "Unmanaged".</para>
<simpara>Where <literal>$DRIVERNAME</literal> is the provisioner name. You can find the provisioner name by running the command <literal>oc get sc</literal>.</simpara>
</callout>
</calloutlist>
</section>
<section xml:id="persistent-storage-csi-sc-multiple-none_persistent-storage-csi-sc-manage">
<title>Absent or multiple default storage classes</title>
<section xml:id="_multiple-default-storage-classes">
<title>Multiple default storage classes</title>
<simpara>Multiple default storage classes can occur if you mark a non-default storage class as default and do not unset the existing default storage class, or you create a default storage class when a default storage class is already present. With multiple default storage classes present, any persistent volume claim (PVC) requesting the default storage class (<literal>pvc.spec.storageClassName</literal>=nil) gets the most recently created default storage class, regardless of the default status of that storage class, and the administrator receives an alert in the alerts dashboard that there are multiple default storage classes, <literal>MultipleDefaultStorageClasses</literal>.</simpara>
</section>
<section xml:id="_absent-default-storage-class">
<title>Absent default storage class</title>
<simpara>There are two possible scenarios where PVCs can attempt to use a non-existent default storage class:</simpara>
<itemizedlist>
<listitem>
<simpara>An administrator removes the default storage class or marks it as non-default, and then a user creates a PVC requesting the default storage class.</simpara>
</listitem>
<listitem>
<simpara>During installation, the installer creates a PVC requesting the default storage class, which has not yet been created.</simpara>
</listitem>
</itemizedlist>
<simpara>In the preceding scenarios, PVCs remain in the pending state indefinitely. To resolve this situation, create a default storage class or declare one of the existing storage classes as the default. As soon as the default storage class is created or declared, the PVCs get the new default storage class. If possible, the PVCs eventually bind to statically or dynamically provisioned PVs as usual, and move out of the pending state.</simpara>
</section>
</section>
<section xml:id="change-default-storage-class_persistent-storage-csi-sc-manage">
<title>Changing the default storage class</title>
<simpara>Use the following procedure to change the default storage class.</simpara>
<simpara>For example, if you have two defined storage classes, <literal>gp3</literal> and <literal>standard</literal>, and you want to change the default storage class from <literal>gp3</literal> to <literal>standard</literal>.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster with cluster-admin privileges.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To change the default storage class:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>List the storage classes:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get storageclass</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                 TYPE
gp3 (default)        ebs.csi.aws.com <co xml:id="CO17-1"/>
standard             ebs.csi.aws.com</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO17-1">
<para><literal>(default)</literal> indicates the default storage class.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Make the desired storage class the default.</simpara>
<simpara>For the desired storage class, set the <literal>storageclass.kubernetes.io/is-default-class</literal> annotation to <literal>true</literal> by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch storageclass standard -p '{"metadata": {"annotations": {"storageclass.kubernetes.io/is-default-class": "true"}}}'</programlisting>
<note>
<simpara>You can have multiple default storage classes for a short time. However, you should ensure that only one default storage class exists eventually.</simpara>
<simpara>With multiple default storage classes present, any persistent volume claim (PVC) requesting the default storage class (<literal>pvc.spec.storageClassName</literal>=nil) gets the most recently created default storage class, regardless of the default status of that storage class, and the administrator receives an alert in the alerts dashboard that there are multiple default storage classes, <literal>MultipleDefaultStorageClasses</literal>.</simpara>
</note>
</listitem>
<listitem>
<simpara>Remove the default storage class setting from the old default storage class.</simpara>
<simpara>For the old default storage class, change the value of the <literal>storageclass.kubernetes.io/is-default-class</literal> annotation to <literal>false</literal> by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch storageclass gp3 -p '{"metadata": {"annotations": {"storageclass.kubernetes.io/is-default-class": "false"}}}'</programlisting>
</listitem>
<listitem>
<simpara>Verify the changes:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get storageclass</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                 TYPE
gp3                  ebs.csi.aws.com
standard (default)   ebs.csi.aws.com</programlisting>
</para>
</formalpara>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-ebs">
<title>AWS Elastic Block Store CSI Driver Operator</title>
<section xml:id="_overview-2">
<title>Overview</title>
<simpara>OpenShift Container Platform is capable of provisioning persistent volumes (PVs) using the <link xlink:href="https://github.com/openshift/aws-ebs-csi-driver">AWS EBS CSI driver</link>.</simpara>
<simpara>Familiarity with <link linkend="understanding-persistent-storage">persistent storage</link> and <link linkend="persistent-storage-csi">configuring CSI volumes</link> is recommended when working with a Container Storage Interface (CSI) Operator and driver.</simpara>
<simpara>To create CSI-provisioned PVs that mount to AWS EBS storage assets, OpenShift Container Platform installs the <link xlink:href="https://github.com/openshift/aws-ebs-csi-driver-operator">AWS EBS CSI Driver Operator</link> (a Red Hat operator) and the AWS EBS CSI driver by default in the <literal>openshift-cluster-csi-drivers</literal> namespace.</simpara>
<itemizedlist>
<listitem>
<simpara>The <emphasis>AWS EBS CSI Driver Operator</emphasis> provides a StorageClass by default that you can use to create PVCs. You can disable this default storage class if desired (see <link linkend="persistent-storage-csi-sc-manage">Managing the default storage class</link>). You also have the option to create the AWS EBS StorageClass as described in <link linkend="persistent-storage-aws">Persistent storage using Amazon Elastic Block Store</link>.</simpara>
</listitem>
<listitem>
<simpara>The <emphasis>AWS EBS CSI driver</emphasis> enables you to create and mount AWS EBS PVs.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="csi-about_persistent-storage-csi-ebs">
<title>About CSI</title>
<simpara>Storage vendors have traditionally provided storage drivers as part of Kubernetes. With the implementation of the Container Storage Interface (CSI), third-party providers can instead deliver storage plugins using a standard interface without ever having to change the core Kubernetes code.</simpara>
<simpara>CSI Operators give OpenShift Container Platform users storage options, such as volume snapshots, that are not possible with in-tree volume plugins.</simpara>
<important>
<simpara>OpenShift Container Platform defaults to using the CSI plugin to provision Amazon Elastic Block Store (Amazon EBS) storage.</simpara>
</important>
<simpara>For information about dynamically provisioning AWS EBS persistent volumes in OpenShift Container Platform, see <link linkend="persistent-storage-aws">Persistent storage using Amazon Elastic Block Store</link>.</simpara>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link linkend="persistent-storage-aws">Persistent storage using Amazon Elastic Block Store</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="persistent-storage-csi">Configuring CSI volumes</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-aws-efs">
<title>AWS Elastic File Service CSI Driver Operator</title>
<important>
<simpara>This procedure is specific to the <link xlink:href="https://github.com/openshift/aws-efs-csi-driver-operator">AWS EFS CSI Driver Operator</link> (a Red Hat Operator), which is only applicable for OpenShift Container Platform 4.10 and later versions.</simpara>
</important>
<section xml:id="_overview-3">
<title>Overview</title>
<simpara>OpenShift Container Platform is capable of provisioning persistent volumes (PVs) using the Container Storage Interface (CSI) driver for AWS Elastic File Service (EFS).</simpara>
<simpara>Familiarity with <link linkend="understanding-persistent-storage">persistent storage</link> and <link linkend="persistent-storage-csi">configuring CSI volumes</link> is recommended when working with a CSI Operator and driver.</simpara>
<simpara>After installing the AWS EFS CSI Driver Operator, OpenShift Container Platform installs the AWS EFS CSI Operator and the AWS EFS CSI driver by default in the <literal>openshift-cluster-csi-drivers</literal> namespace. This allows the AWS EFS CSI Driver Operator to create CSI-provisioned PVs that mount to AWS EFS assets.</simpara>
<itemizedlist>
<listitem>
<simpara>The <emphasis>AWS EFS CSI Driver Operator</emphasis>, after being installed, does not create a storage class by default to use to create persistent volume claims (PVCs). However, you can manually create the AWS EFS <literal>StorageClass</literal>.
The AWS EFS CSI Driver Operator supports dynamic volume provisioning by allowing storage volumes to be created on-demand.
This eliminates the need for cluster administrators to pre-provision storage.</simpara>
</listitem>
<listitem>
<simpara>The <emphasis>AWS EFS CSI driver</emphasis> enables you to create and mount AWS EFS PVs.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="csi-about_persistent-storage-csi-aws-efs">
<title>About CSI</title>
<simpara>Storage vendors have traditionally provided storage drivers as part of Kubernetes. With the implementation of the Container Storage Interface (CSI), third-party providers can instead deliver storage plugins using a standard interface without ever having to change the core Kubernetes code.</simpara>
<simpara>CSI Operators give OpenShift Container Platform users storage options, such as volume snapshots, that are not possible with in-tree volume plugins.</simpara>
</section>
<section xml:id="persistent-storage-efs-csi-driver-operator-setup_persistent-storage-csi-aws-efs">
<title>Setting up the AWS EFS CSI Driver Operator</title>
<orderedlist numeration="arabic">
<listitem>
<simpara>If you are using AWS EFS with AWS Secure Token Service (STS), obtain a role Amazon Resource Name (ARN) for STS. This is required for installing the AWS EFS CSI Driver Operator.</simpara>
</listitem>
<listitem>
<simpara>Install the AWS EFS CSI Driver Operator.</simpara>
</listitem>
<listitem>
<simpara>Install the AWS EFS CSI Driver.</simpara>
</listitem>
</orderedlist>
<section xml:id="efs-sts_persistent-storage-csi-aws-efs">
<title>Obtaining a role Amazon Resource Name for Security Token Service</title>
<simpara>This procedure explains how to obtain a role Amazon Resource Name (ARN) to configure the AWS EFS CSI Driver Operator with OpenShift Container Platform on AWS Security Token Service (STS).</simpara>
<important>
<simpara>Perform this procedure before you install the AWS EFS CSI Driver Operator (see <emphasis>Installing the AWS EFS CSI Driver Operator</emphasis> procedure).</simpara>
</important>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the cluster-admin role.</simpara>
</listitem>
<listitem>
<simpara>AWS account credentials</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create an IAM policy JSON file with the following content:</simpara>
<programlisting language="json" linenumbering="unnumbered">{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "elasticfilesystem:DescribeAccessPoints",
        "elasticfilesystem:DescribeFileSystems",
        "elasticfilesystem:DescribeMountTargets",
        "ec2:DescribeAvailabilityZones",
        "elasticfilesystem:TagResource"
      ],
      "Resource": "*"
    },
    {
      "Effect": "Allow",
      "Action": [
        "elasticfilesystem:CreateAccessPoint"
      ],
      "Resource": "*",
      "Condition": {
        "StringLike": {
          "aws:RequestTag/efs.csi.aws.com/cluster": "true"
        }
      }
    },
    {
      "Effect": "Allow",
      "Action": "elasticfilesystem:DeleteAccessPoint",
      "Resource": "*",
      "Condition": {
        "StringEquals": {
          "aws:ResourceTag/efs.csi.aws.com/cluster": "true"
        }
      }
    }
  ]
}</programlisting>
</listitem>
<listitem>
<simpara>Create an IAM trust JSON file with the following content:</simpara>
<programlisting language="json" linenumbering="unnumbered">{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Federated": "arn:aws:iam::&lt;your_aws_account_ID&gt;:oidc-provider/&lt;openshift_oidc_provider&gt;"  <co xml:id="CO18-1"/>
      },
      "Action": "sts:AssumeRoleWithWebIdentity",
      "Condition": {
        "StringEquals": {
          "&lt;openshift_oidc_provider&gt;:sub": [  <co xml:id="CO18-2"/>
            "system:serviceaccount:openshift-cluster-csi-drivers:aws-efs-csi-driver-operator",
            "system:serviceaccount:openshift-cluster-csi-drivers:aws-efs-csi-driver-controller-sa"
          ]
        }
      }
    }
  ]
}</programlisting>
<calloutlist>
<callout arearefs="CO18-1">
<para>Specify your AWS account ID and the OpenShift OIDC provider endpoint.</para>
<simpara>Obtain your AWS account ID by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ aws sts get-caller-identity --query Account --output text</programlisting>
<simpara>Obtain the OpenShift OIDC endpoint by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ openshift_oidc_provider=`oc get authentication.config.openshift.io cluster \
  -o json | jq -r .spec.serviceAccountIssuer | sed -e "s/^https:\/\///"`; \
  echo $openshift_oidc_provider</programlisting>
</callout>
<callout arearefs="CO18-2">
<para>Specify the OpenShift OIDC endpoint again.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Create the IAM role:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">ROLE_ARN=$(aws iam create-role \
  --role-name "&lt;your_cluster_name&gt;-aws-efs-csi-operator" \
  --assume-role-policy-document file://&lt;your_trust_file_name&gt;.json \
  --query "Role.Arn" --output text); echo $ROLE_ARN</programlisting>
<simpara>Copy the role ARN. You will need it when you install the AWS EFS CSI Driver Operator.</simpara>
</listitem>
<listitem>
<simpara>Create the IAM policy:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">POLICY_ARN=$(aws iam create-policy \
  --policy-name "&lt;your_cluster_name&gt;-aws-efs-csi" \
  --policy-document file://&lt;your_policy_file_name&gt;.json \
  --query 'Policy.Arn' --output text); echo $POLICY_ARN</programlisting>
</listitem>
<listitem>
<simpara>Attach the IAM policy to the IAM role:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ aws iam attach-role-policy \
  --role-name "&lt;your_cluster_name&gt;-aws-efs-csi-operator" \
  --policy-arn $POLICY_ARN</programlisting>
</listitem>
</orderedlist>
<formalpara>
<title>Next steps</title>
<para><link linkend="persistent-storage-csi-olm-operator-install_persistent-storage-csi-aws-efs">Install the AWS EFS CSI Driver Operator</link>.</para>
</formalpara>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link linkend="persistent-storage-csi-olm-operator-install_persistent-storage-csi-aws-efs">Installing the AWS EFS CSI Driver Operator</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="persistent-storage-csi-efs-driver-install_persistent-storage-csi-aws-efs">Installing the AWS EFS CSI Driver</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="persistent-storage-csi-olm-operator-install_persistent-storage-csi-aws-efs">
<title>Installing the AWS EFS CSI Driver Operator</title>
<simpara>The AWS EFS CSI Driver Operator (a Red&#160;Hat Operator) is not installed in OpenShift Container Platform by default. Use the following procedure to install and configure the AWS EFS CSI Driver Operator in your cluster.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform web console.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To install the AWS EFS CSI Driver Operator from the web console:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Log in to the web console.</simpara>
</listitem>
<listitem>
<simpara>Install the AWS EFS CSI Operator:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Ecosystem</emphasis> &#8594; <emphasis role="strong">Software Catalog</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Locate the AWS EFS CSI Operator by typing <emphasis role="strong">AWS EFS CSI</emphasis> in the filter box.</simpara>
</listitem>
<listitem>
<simpara>Click the <emphasis role="strong">AWS EFS CSI Driver Operator</emphasis> button.</simpara>
</listitem>
</orderedlist>
<important>
<simpara>Be sure to select the <emphasis role="strong">AWS EFS CSI Driver Operator</emphasis> and not the <emphasis role="strong">AWS EFS Operator</emphasis>. The <emphasis role="strong">AWS EFS Operator</emphasis> is a community Operator and is not supported by Red Hat.</simpara>
</important>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>On the <emphasis role="strong">AWS EFS CSI Driver Operator</emphasis> page, click <emphasis role="strong">Install</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Install Operator</emphasis> page, ensure that:</simpara>
<itemizedlist>
<listitem>
<simpara>If you are using AWS EFS with AWS Secure Token Service (STS), in the <emphasis role="strong">role ARN</emphasis> field, enter the ARN role copied from the last step of the <emphasis>Obtaining a role Amazon Resource Name for Security Token Service</emphasis> procedure.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">All namespaces on the cluster (default)</emphasis> is selected.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Installed Namespace</emphasis> is set to <emphasis role="strong">openshift-cluster-csi-drivers</emphasis>.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Install</emphasis>.</simpara>
<simpara>After the installation finishes, the AWS EFS CSI Operator is listed in the <emphasis role="strong">Installed Operators</emphasis> section of the web console.</simpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<formalpara>
<title>Next steps</title>
<para><link linkend="persistent-storage-csi-efs-driver-install_persistent-storage-csi-aws-efs">Install the AWS EFS CSI Driver</link>.</para>
</formalpara>
</section>
<section xml:id="persistent-storage-csi-efs-driver-install_persistent-storage-csi-aws-efs">
<title>Installing the AWS EFS CSI Driver</title>
<simpara>After installing the <link xlink:href="https://github.com/openshift/aws-efs-csi-driver-operator">AWS EFS CSI Driver Operator</link> (a Red Hat operator), you install the <link xlink:href="https://github.com/openshift/aws-efs-csi-driver">AWS EFS CSI driver</link>.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform web console.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Click <emphasis role="strong">Administration</emphasis> &#8594; <emphasis role="strong">CustomResourceDefinitions</emphasis> &#8594; <emphasis role="strong">ClusterCSIDriver</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Instances</emphasis> tab, click <emphasis role="strong">Create ClusterCSIDriver</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Use the following YAML file:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: operator.openshift.io/v1
kind: ClusterCSIDriver
metadata:
    name: efs.csi.aws.com
spec:
  managementState: Managed</programlisting>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Create</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Wait for the following Conditions to change to a "True" status:</simpara>
<itemizedlist>
<listitem>
<simpara>AWSEFSDriverNodeServiceControllerAvailable</simpara>
</listitem>
<listitem>
<simpara>AWSEFSDriverControllerServiceControllerAvailable</simpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="storage-create-storage-class_persistent-storage-csi-aws-efs">
<title>Creating the AWS EFS storage class</title>
<simpara>Storage classes are used to differentiate and delineate storage levels and
usages. By defining a storage class, users can obtain dynamically provisioned
persistent volumes.</simpara>
<simpara>The <emphasis><link xlink:href="https://github.com/openshift/aws-efs-csi-driver-operator">AWS EFS CSI Driver Operator</link> (a Red Hat operator)</emphasis>, after being installed, does not create a storage class by default. However, you can manually create the AWS EFS storage class.</simpara>
<section xml:id="storage-create-storage-class-console_persistent-storage-csi-aws-efs">
<title>Creating the AWS EFS storage class using the console</title>
<orderedlist role="_abstract" numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>In the OpenShift Container Platform web console, click <emphasis role="strong">Storage</emphasis> &#8594; <emphasis role="strong">StorageClasses</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">StorageClasses</emphasis> page, click <emphasis role="strong">Create StorageClass</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">StorageClass</emphasis> page, perform the following steps:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Enter a name to reference the storage class.</simpara>
</listitem>
<listitem>
<simpara>Optional: Enter the description.</simpara>
</listitem>
<listitem>
<simpara>Select the reclaim policy.</simpara>
</listitem>
<listitem>
<simpara>Select <emphasis role="strong"><literal>efs.csi.aws.com</literal></emphasis> from the <emphasis role="strong">Provisioner</emphasis> drop-down list.</simpara>
</listitem>
<listitem>
<simpara>Optional: Set the configuration parameters for the selected provisioner.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Create</emphasis>.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="storage-create-storage-class-cli_persistent-storage-csi-aws-efs">
<title>Creating the AWS EFS storage class using the CLI</title>
<itemizedlist role="_abstract">
<title>Procedure</title>
<listitem>
<simpara>Create a <literal>StorageClass</literal> object:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: efs-sc
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap <co xml:id="CO19-1"/>
  fileSystemId: fs-a5324911 <co xml:id="CO19-2"/>
  directoryPerms: "700" <co xml:id="CO19-3"/>
  gidRangeStart: "1000" <co xml:id="CO19-4"/>
  gidRangeEnd: "2000" <co xml:id="CO19-5"/>
  basePath: "/dynamic_provisioning" <co xml:id="CO19-6"/></programlisting>
<calloutlist>
<callout arearefs="CO19-1">
<para><literal>provisioningMode</literal> must be <literal>efs-ap</literal> to enable dynamic provisioning.</para>
</callout>
<callout arearefs="CO19-2">
<para><literal>fileSystemId</literal> must be the ID of the EFS volume created manually.</para>
</callout>
<callout arearefs="CO19-3">
<para><literal>directoryPerms</literal> is the default permission of the root directory of the volume. In this example, the volume is accessible only by the owner.</para>
</callout>
<callout arearefs="CO19-4 CO19-5">
<para><literal>gidRangeStart</literal> and <literal>gidRangeEnd</literal> set the range of POSIX Group IDs (GIDs) that are used to set the GID of the AWS access point. If not specified, the default range is 50000-7000000. Each provisioned volume, and thus AWS access point, is assigned a unique GID from this range.</para>
</callout>
<callout arearefs="CO19-6">
<para><literal>basePath</literal> is the directory on the EFS volume that is used to create dynamically provisioned volumes. In this case, a PV is provisioned as “/dynamic_provisioning/&lt;random uuid&gt;” on the EFS volume. Only the subdirectory is mounted to pods that use the PV.</para>
</callout>
</calloutlist>
<note>
<simpara>A cluster admin can create several <literal>StorageClass</literal> objects, each using a different EFS volume.</simpara>
</note>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-efs-cross-account_persistent-storage-csi-aws-efs">
<title>AWS EFS CSI cross account support</title>
<simpara>Cross account support allows you to have
 an OpenShift Container Platform cluster
in one AWS account and mount your file system in another AWS account by using the AWS Elastic File System (EFS) Container Storage Interface (CSI) driver.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to
 an OpenShift Container Platform cluster
with administrator rights</simpara>
</listitem>
<listitem>
<simpara>Two valid AWS accounts</simpara>
</listitem>
<listitem>
<simpara>The EFS CSI Operator has been installed. For information about installing the EFS CSI Operator, see the <emphasis>Installing the AWS EFS CSI Driver Operator</emphasis> section.</simpara>
</listitem>
<listitem>
<simpara>Both the OpenShift Container Platform cluster and EFS file system must be located in the same AWS region.</simpara>
</listitem>
<listitem>
<simpara>Ensure that the two virtual private clouds (VPCs) used in the following procedure use different network Classless Inter-Domain Routing (CIDR) ranges.</simpara>
</listitem>
<listitem>
<simpara>Access to OpenShift Container Platform CLI (<literal>oc</literal>).</simpara>
</listitem>
<listitem>
<simpara>Access to AWS CLI.</simpara>
</listitem>
<listitem>
<simpara>Access to <literal>jq</literal> command-line JSON processor.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>The following procedure explains how to set up:</para>
</formalpara>
<itemizedlist>
<listitem>
<simpara>OpenShift Container Platform AWS Account A: Contains a Red Hat OpenShift Container Platform cluster v4.16, or later, deployed within a VPC</simpara>
</listitem>
<listitem>
<simpara>AWS Account B: Contains a VPC (including subnets, route tables, and network connectivity). The EFS filesystem will be created in this VPC.</simpara>
</listitem>
</itemizedlist>
<simpara>To use AWS EFS across accounts:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Set up the environment:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Configure environment variables by running the following commands:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export CLUSTER_NAME="&lt;CLUSTER_NAME&gt;" <co xml:id="CO20-1"/>
export AWS_REGION="&lt;AWS_REGION&gt;" <co xml:id="CO20-2"/>
export AWS_ACCOUNT_A_ID="&lt;ACCOUNT_A_ID&gt;" <co xml:id="CO20-3"/>
export AWS_ACCOUNT_B_ID="&lt;ACCOUNT_B_ID&gt;" <co xml:id="CO20-4"/>
export AWS_ACCOUNT_A_VPC_CIDR="&lt;VPC_A_CIDR&gt;" <co xml:id="CO20-5"/>
export AWS_ACCOUNT_B_VPC_CIDR="&lt;VPC_B_CIDR&gt;" <co xml:id="CO20-6"/>
export AWS_ACCOUNT_A_VPC_ID="&lt;VPC_A_ID&gt;" <co xml:id="CO20-7"/>
export AWS_ACCOUNT_B_VPC_ID="&lt;VPC_B_ID&gt;" <co xml:id="CO20-8"/>
export SCRATCH_DIR="&lt;WORKING_DIRECTORY&gt;" <co xml:id="CO20-9"/>
export CSI_DRIVER_NAMESPACE="openshift-cluster-csi-drivers" <co xml:id="CO20-10"/>
export AWS_PAGER="" <co xml:id="CO20-11"/></programlisting>
<calloutlist>
<callout arearefs="CO20-1">
<para>Cluster name of choice.</para>
</callout>
<callout arearefs="CO20-2">
<para>AWS region of choice.</para>
</callout>
<callout arearefs="CO20-3">
<para>AWS Account A ID.</para>
</callout>
<callout arearefs="CO20-4">
<para>AWS Account B ID.</para>
</callout>
<callout arearefs="CO20-5">
<para>CIDR range of VPC in Account A.</para>
</callout>
<callout arearefs="CO20-6">
<para>CIDR range of VPC in Account B.</para>
</callout>
<callout arearefs="CO20-7">
<para>VPC ID in Account A (cluster)</para>
</callout>
<callout arearefs="CO20-8">
<para>VPC ID in Account B (EFS cross account)</para>
</callout>
<callout arearefs="CO20-9">
<para>Any writeable directory of choice to use to store temporary files.</para>
</callout>
<callout arearefs="CO20-10">
<para>If your driver is installed in a non-default namespace, change this value.</para>
</callout>
<callout arearefs="CO20-11">
<para>Makes AWS CLI output everything directly to stdout.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Create the working directory by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">mkdir -p $SCRATCH_DIR</programlisting>
</listitem>
<listitem>
<simpara>Verify cluster connectivity by running the following command in the OpenShift Container Platform CLI:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc whoami</programlisting>
</listitem>
<listitem>
<simpara>Determine the OpenShift Container Platform cluster type and set node selector:</simpara>
<simpara>The EFS cross account feature requires assigning AWS IAM policies to nodes running EFS CSI controller pods. However, this is
not consistent for every OpenShift Container Platform type.</simpara>
<itemizedlist>
<listitem>
<simpara>If your cluster is deployed as a Hosted Control Plane (HyperShift), set the <literal>NODE_SELECTOR</literal> environment variable to hold the worker node label by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export NODE_SELECTOR=node-role.kubernetes.io/worker</programlisting>
</listitem>
<listitem>
<simpara>For all other OpenShift Container Platform types, set the <literal>NODE_SELECTOR</literal> environment variable to hold the master node label by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export NODE_SELECTOR=node-role.kubernetes.io/master</programlisting>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Configure AWS CLI profiles as environment variables for account switching by running the following commands:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_ACCOUNT_A="&lt;ACCOUNT_A_NAME&gt;"
export AWS_ACCOUNT_B="&lt;ACCOUNT_B_NAME&gt;"</programlisting>
</listitem>
<listitem>
<simpara>Ensure that your AWS CLI is configured with JSON output format as the default for both accounts by running the following commands:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_A}
aws configure get output
export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_B}
aws configure get output</programlisting>
<simpara>If the preceding commands return:</simpara>
<itemizedlist>
<listitem>
<simpara><emphasis role="strong">No value</emphasis>: The default output format is already set to JSON and no changes are required.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Any value</emphasis>: Reconfigure your AWS CLI to use JSON format. For information about changing output formats, see <emphasis>Setting the output format in the AWS CLI</emphasis> in the AWS documentation.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Unset <literal>AWS_PROFILE</literal> in your shell to prevent conflicts with <literal>AWS_DEFAULT_PROFILE</literal> by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">unset AWS_PROFILE</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Configure the AWS Account B IAM roles and policies:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Switch to your Account B profile by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_B}</programlisting>
</listitem>
<listitem>
<simpara>Define the IAM role name for the EFS CSI Driver Operator by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export ACCOUNT_B_ROLE_NAME=${CLUSTER_NAME}-cross-account-aws-efs-csi-operator</programlisting>
</listitem>
<listitem>
<simpara>Create the IAM trust policy file by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">cat &lt;&lt;EOF &gt; $SCRATCH_DIR/AssumeRolePolicyInAccountB.json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Principal": {
                "AWS": "arn:aws:iam::${AWS_ACCOUNT_A_ID}:root"
            },
            "Action": "sts:AssumeRole",
            "Condition": {}
        }
    ]
}
EOF</programlisting>
</listitem>
<listitem>
<simpara>Create the IAM role for the EFS CSI Driver Operator by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">ACCOUNT_B_ROLE_ARN=$(aws iam create-role \
  --role-name "${ACCOUNT_B_ROLE_NAME}" \
  --assume-role-policy-document file://$SCRATCH_DIR/AssumeRolePolicyInAccountB.json \
  --query "Role.Arn" --output text) \
&amp;&amp; echo $ACCOUNT_B_ROLE_ARN</programlisting>
</listitem>
<listitem>
<simpara>Create the IAM policy file by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">cat &lt;&lt; EOF &gt; $SCRATCH_DIR/EfsPolicyInAccountB.json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "VisualEditor0",
            "Effect": "Allow",
            "Action": [
                "ec2:DescribeNetworkInterfaces",
                "ec2:DescribeSubnets"
            ],
            "Resource": "*"
        },
        {
            "Sid": "VisualEditor1",
            "Effect": "Allow",
            "Action": [
                "elasticfilesystem:DescribeMountTargets",
                "elasticfilesystem:DeleteAccessPoint",
                "elasticfilesystem:ClientMount",
                "elasticfilesystem:DescribeAccessPoints",
                "elasticfilesystem:ClientWrite",
                "elasticfilesystem:ClientRootAccess",
                "elasticfilesystem:DescribeFileSystems",
                "elasticfilesystem:CreateAccessPoint",
                "elasticfilesystem:TagResource"
            ],
            "Resource": "*"
        }
    ]
}
EOF</programlisting>
</listitem>
<listitem>
<simpara>Create the IAM policy by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">ACCOUNT_B_POLICY_ARN=$(aws iam create-policy --policy-name "${CLUSTER_NAME}-efs-csi-policy" \
   --policy-document file://$SCRATCH_DIR/EfsPolicyInAccountB.json \
   --query 'Policy.Arn' --output text) \
&amp;&amp; echo ${ACCOUNT_B_POLICY_ARN}</programlisting>
</listitem>
<listitem>
<simpara>Attach the policy to the role by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">aws iam attach-role-policy \
   --role-name "${ACCOUNT_B_ROLE_NAME}" \
   --policy-arn "${ACCOUNT_B_POLICY_ARN}"</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Configure the AWS Account A IAM roles and policies:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Switch to your Account A profile by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_A}</programlisting>
</listitem>
<listitem>
<simpara>Create the IAM policy document by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">cat &lt;&lt; EOF &gt; $SCRATCH_DIR/AssumeRoleInlinePolicyPolicyInAccountA.json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "sts:AssumeRole",
      "Resource": "${ACCOUNT_B_ROLE_ARN}"
    }
  ]
}
EOF</programlisting>
</listitem>
<listitem>
<simpara>In AWS Account A, attach the AWS-managed policy "AmazonElasticFileSystemClientFullAccess" to the OpenShift Container Platform cluster master role by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">EFS_CLIENT_FULL_ACCESS_BUILTIN_POLICY_ARN=arn:aws:iam::aws:policy/AmazonElasticFileSystemClientFullAccess
declare -A ROLE_SEEN
for NODE in $(oc get nodes --selector="${NODE_SELECTOR}" -o jsonpath='{.items[*].metadata.name}'); do
    INSTANCE_PROFILE=$(aws ec2 describe-instances \
        --filters "Name=private-dns-name,Values=${NODE}" \
        --query 'Reservations[].Instances[].IamInstanceProfile.Arn' \
        --output text | awk -F'/' '{print $NF}' | xargs)
    MASTER_ROLE_ARN=$(aws iam get-instance-profile \
        --instance-profile-name "${INSTANCE_PROFILE}" \
        --query 'InstanceProfile.Roles[0].Arn' \
        --output text | xargs)
    MASTER_ROLE_NAME=$(echo "${MASTER_ROLE_ARN}" | awk -F'/' '{print $NF}' | xargs)
    echo "Checking role: '${MASTER_ROLE_NAME}'"
    if [[ -n "${ROLE_SEEN[$MASTER_ROLE_NAME]:-}" ]]; then
        echo "Already processed role: '${MASTER_ROLE_NAME}', skipping."
        continue
    fi
    ROLE_SEEN["$MASTER_ROLE_NAME"]=1
    echo "Assigning policy ${EFS_CLIENT_FULL_ACCESS_BUILTIN_POLICY_ARN} to role ${MASTER_ROLE_NAME}"
    aws iam attach-role-policy --role-name "${MASTER_ROLE_NAME}" --policy-arn "${EEFS_CLIENT_FULL_ACCESS_BUILTIN_POLICY_ARN}"
done</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Attach the policy to the IAM entity to allow role assumption:</simpara>
<simpara>This step depends on your cluster configuration. In both of the following scenarios, the EFS CSI Driver Operator uses an entity to authenticate to AWS, and this entity must be granted permission to assume roles in Account B.</simpara>
<simpara>If your cluster:</simpara>
<itemizedlist>
<listitem>
<simpara><emphasis role="strong">Does not have STS enabled</emphasis>: The EFS CSI Driver Operator uses an IAM User entity for AWS authentication. Continue with the step "Attach policy to IAM User to allow role assumption".</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Has STS enabled</emphasis>: The EFS CSI Driver Operator uses an IAM role entity for AWS authentication. Continue with the step "Attach policy to IAM Role to allow role assumption".</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Attach policy to IAM User to allow role assumption</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Identify the IAM User used by the EFS CSI Driver Operator by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">EFS_CSI_DRIVER_OPERATOR_USER=$(oc -n openshift-cloud-credential-operator get credentialsrequest/openshift-aws-efs-csi-driver -o json | jq -r '.status.providerStatus.user')</programlisting>
</listitem>
<listitem>
<simpara>Attach the policy to the IAM user by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">aws iam put-user-policy \
    --user-name "${EFS_CSI_DRIVER_OPERATOR_USER}"  \
    --policy-name efs-cross-account-inline-policy \
    --policy-document file://$SCRATCH_DIR/AssumeRoleInlinePolicyPolicyInAccountA.json</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Attach the policy to the IAM role to allow role assumption:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Identify the IAM role name currently used by the EFS CSI Driver Operator by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">EFS_CSI_DRIVER_OPERATOR_ROLE=$(oc -n ${CSI_DRIVER_NAMESPACE} get secret/aws-efs-cloud-credentials -o jsonpath='{.data.credentials}' | base64 -d | grep role_arn | cut -d'/' -f2) &amp;&amp; echo ${EFS_CSI_DRIVER_OPERATOR_ROLE}</programlisting>
</listitem>
<listitem>
<simpara>Attach the policy to the IAM role used by the EFS CSI Driver Operator by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered"> aws iam put-role-policy \
    --role-name "${EFS_CSI_DRIVER_OPERATOR_ROLE}"  \
    --policy-name efs-cross-account-inline-policy \
    --policy-document file://$SCRATCH_DIR/AssumeRoleInlinePolicyPolicyInAccountA.json</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Configure VPC peering:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Initiate a peering request from Account A to Account B by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_A}
PEER_REQUEST_ID=$(aws ec2 create-vpc-peering-connection --vpc-id "${AWS_ACCOUNT_A_VPC_ID}" --peer-vpc-id "${AWS_ACCOUNT_B_VPC_ID}" --peer-owner-id "${AWS_ACCOUNT_B_ID}" --query VpcPeeringConnection.VpcPeeringConnectionId --output text)</programlisting>
</listitem>
<listitem>
<simpara>Accept the peering request from Account B by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_B}
aws ec2 accept-vpc-peering-connection --vpc-peering-connection-id "${PEER_REQUEST_ID}"</programlisting>
</listitem>
<listitem>
<simpara>Retrieve the route table IDs for Account A and add routes to the Account B VPC by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_A}
for NODE in $(oc get nodes --selector=node-role.kubernetes.io/worker | tail -n +2 | awk '{print $1}')
do
    SUBNET=$(aws ec2 describe-instances --filters "Name=private-dns-name,Values=$NODE" --query 'Reservations[*].Instances[*].NetworkInterfaces[*].SubnetId' | jq -r '.[0][0][0]')
    echo SUBNET is ${SUBNET}
    ROUTE_TABLE_ID=$(aws ec2 describe-route-tables --filters "Name=association.subnet-id,Values=${SUBNET}" --query 'RouteTables[*].RouteTableId' | jq -r '.[0]')
    echo Route table ID is $ROUTE_TABLE_ID
    aws ec2 create-route --route-table-id ${ROUTE_TABLE_ID} --destination-cidr-block ${AWS_ACCOUNT_B_VPC_CIDR} --vpc-peering-connection-id ${PEER_REQUEST_ID}
done</programlisting>
</listitem>
<listitem>
<simpara>Retrieve the route table IDs for Account B and add routes to the Account A VPC by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_B}
for ROUTE_TABLE_ID in $(aws ec2 describe-route-tables   --filters "Name=vpc-id,Values=${AWS_ACCOUNT_B_VPC_ID}"   --query "RouteTables[].RouteTableId" | jq -r '.[]')
do
    echo Route table ID is $ROUTE_TABLE_ID
    aws ec2 create-route --route-table-id ${ROUTE_TABLE_ID} --destination-cidr-block ${AWS_ACCOUNT_A_VPC_CIDR} --vpc-peering-connection-id ${PEER_REQUEST_ID}
done</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Configure security groups in Account B to allow NFS traffic from Account A to EFS:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Switch to your Account B profile by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_B}</programlisting>
</listitem>
<listitem>
<simpara>Configure the VPC security groups for EFS access by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">SECURITY_GROUP_ID=$(aws ec2 describe-security-groups --filters Name=vpc-id,Values="${AWS_ACCOUNT_B_VPC_ID}" | jq -r '.SecurityGroups[].GroupId')
aws ec2 authorize-security-group-ingress \
 --group-id "${SECURITY_GROUP_ID}" \
 --protocol tcp \
 --port 2049 \
 --cidr "${AWS_ACCOUNT_A_VPC_CIDR}" | jq .</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Create a region-wide EFS filesystem in Account B:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Switch to your Account B profile by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export AWS_DEFAULT_PROFILE=${AWS_ACCOUNT_B}</programlisting>
</listitem>
<listitem>
<simpara>Create a region-wide EFS file system by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">CROSS_ACCOUNT_FS_ID=$(aws efs create-file-system --creation-token efs-token-1 \
--region ${AWS_REGION} \
--encrypted | jq -r '.FileSystemId') \
&amp;&amp; echo $CROSS_ACCOUNT_FS_ID</programlisting>
</listitem>
<listitem>
<simpara>Configure region-wide mount targets for EFS by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">for SUBNET in $(aws ec2 describe-subnets \
  --filters "Name=vpc-id,Values=${AWS_ACCOUNT_B_VPC_ID}" \
  --region ${AWS_REGION} \
  | jq -r '.Subnets.[].SubnetId'); do \
    MOUNT_TARGET=$(aws efs create-mount-target --file-system-id ${CROSS_ACCOUNT_FS_ID} \
    --subnet-id ${SUBNET} \
    --region ${AWS_REGION} \
    | jq -r '.MountTargetId'); \
    echo ${MOUNT_TARGET}; \
done</programlisting>
<simpara>This creates a mount point in each subnet of your VPC.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Configure the EFS Operator for cross-account access:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Define custom names for the secret and storage class that you will create in subsequent steps by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">export SECRET_NAME=my-efs-cross-account
export STORAGE_CLASS_NAME=efs-sc-cross</programlisting>
</listitem>
<listitem>
<simpara>Create a secret that references the role ARN in Account B by running the following command in the OpenShift Container Platform CLI:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">oc create secret generic ${SECRET_NAME} -n ${CSI_DRIVER_NAMESPACE} --from-literal=awsRoleArn="${ACCOUNT_B_ROLE_ARN}"</programlisting>
</listitem>
<listitem>
<simpara>Grant the CSI driver controller access to the newly created secret by running the following commands in the OpenShift Container Platform CLI:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">oc -n ${CSI_DRIVER_NAMESPACE} create role access-secrets --verb=get,list,watch --resource=secrets
oc -n ${CSI_DRIVER_NAMESPACE} create rolebinding --role=access-secrets default-to-secrets --serviceaccount=${CSI_DRIVER_NAMESPACE}:aws-efs-csi-driver-controller-sa</programlisting>
</listitem>
<listitem>
<simpara>Create a new storage class that references the EFS ID from Account B and the secret created previously by running the following command in the OpenShift Container Platform CLI:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">cat &lt;&lt; EOF | oc apply -f -
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: ${STORAGE_CLASS_NAME}
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: ${CROSS_ACCOUNT_FS_ID}
  directoryPerms: "700"
  gidRangeStart: "1000"
  gidRangeEnd: "2000"
  basePath: "/dynamic_provisioning"
  csi.storage.k8s.io/provisioner-secret-name: ${SECRET_NAME}
  csi.storage.k8s.io/provisioner-secret-namespace: ${CSI_DRIVER_NAMESPACE}
EOF</programlisting>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://docs.aws.amazon.com/cli/latest/userguide/cli-usage-output-format.html">Setting the output format in the AWS CLI</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="_one-zone-file-systems">
<title>One Zone file systems</title>
<section xml:id="efs-one-zone-overview_persistent-storage-csi-aws-efs">
<title>One Zone file systems overview</title>
<simpara>OpenShift Container Platform supports AWS Elastic File System (EFS) One Zone file system, which is an EFS storage option that stores data redundantly within a single Availability Zone (AZ). This contrasts with the default EFS storage option, which stores data redundantly across multiple AZs within a region.</simpara>
<simpara>Clusters upgraded from OpenShift Container Platform 4.19 are compatible with the regional EFS volumes.</simpara>
<note>
<simpara>Dynamic provisioning of One Zone volumes is supported only in single-zone clusters. All nodes in the cluster must be in the same AZ as the EFS volume that is used for the dynamic provisioning.</simpara>
<simpara>Manually provisioned One Zone volumes in regional clusters is supported, assuming that the persistent volumes (PVs) have correct <literal>spec.nodeAffinity</literal> that indicates the zone that the volume is in.</simpara>
</note>
<simpara>For Cloud Credential Operator (CCO) Mint mode or Passthrough, no extra configuration is required. However, for Security Token Service (STS), use the procedure in Section <emphasis>Setting up One Zone file systems with STS</emphasis>.</simpara>
</section>
<section xml:id="efs-one-zone-procedure_persistent-storage-csi-aws-efs">
<title>Setting up One Zone file systems with STS</title>
<simpara>The following procedure explains how to set up AWS One Zone file systems with Security Token Service (STS):</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the cluster-admin role.</simpara>
</listitem>
<listitem>
<simpara>AWS account credentials</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To configure One Zone file systems with STS:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Create <emphasis role="strong">two</emphasis> <literal>CredentialsRequests</literal> in the <literal>credrequests</literal> directory following the procedure under Section <emphasis>Obtaining a role Amazon Resource Name for Security Token Service</emphasis>.:</simpara>
<itemizedlist>
<listitem>
<simpara>For the <emphasis role="strong">controller</emphasis> <literal>CredentialsRequest</literal>, follow the procedure without any changes.</simpara>
</listitem>
<listitem>
<simpara>For the <emphasis role="strong">driver node</emphasis> <literal>CredentialsRequest</literal> use the following example file:</simpara>
<formalpara>
<title>Example CredentialsRequest YAML file for driver node</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: cloudcredential.openshift.io/v1
kind: CredentialsRequest
metadata:
  annotations:
    credentials.openshift.io/role-arns-vars: NODE_ROLEARN <co xml:id="CO21-1"/>
  name: openshift-aws-efs-csi-driver-node
  namespace: openshift-cloud-credential-operator
spec:
  providerSpec:
    apiVersion: cloudcredential.openshift.io/v1
    kind: AWSProviderSpec
    statementEntries:
    - action:
      - elasticfilesystem:DescribeMountTargets
      - ec2:DescribeAvailabilityZones
      effect: Allow
      resource: '*'
  secretRef:
    name: node-aws-efs-cloud-credentials
    namespace: openshift-cluster-csi-drivers
  serviceAccountNames:
  - aws-efs-csi-driver-node-sa</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO21-1">
<para>Set <literal>metadata.annotations.credentials.openshift.io/role-arns-vars</literal> to <literal>NODE_ROLEARN</literal>.</para>
</callout>
</calloutlist>
<formalpara>
<title>Example <literal>ccoctl</literal> output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">2025/08/26 14:05:24 Role arn:aws:iam::269733383066:role/my-arn-1-blll6-openshift-cluster-csi-drivers-aws-efs-cloud-cre created <co xml:id="CO22-1"/>
2025/08/26 14:05:24 Saved credentials configuration to: /home/my-arn/project/go/src/github.com/openshift/myinst/aws-sts-compact-1/manifests/openshift-cluster-csi-drivers-aws-efs-cloud-credentials-credentials.yaml
2025/08/26 14:05:24 Updated Role policy for Role my-arn-1-blll6-openshift-cluster-csi-drivers-aws-efs-cloud-cre
2025/08/26 14:05:24 Role arn:aws:iam::269733383066:role/my-arn-1-blll6-openshift-cluster-csi-drivers-node-aws-efs-clou created <co xml:id="CO22-2"/>
2025/08/26 14:05:24 Saved credentials configuration to: manifests/openshift-cluster-csi-drivers-node-aws-efs-cloud-credentials-credentials.yaml
2025/08/26 14:05:24 Updated Role policy for Role my-arn-1-blll6-openshift-cluster-csi-drivers-node-aws-efs-clou</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO22-1">
<para>Controller Amazon Resource Name (ARN)</para>
</callout>
<callout arearefs="CO22-2">
<para>Driver node ARN</para>
</callout>
</calloutlist>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Install the AWS EFS CSI driver using the controller ARN created earlier in this procedure.</simpara>
</listitem>
<listitem>
<simpara>Edit the operator&#8217;s subscription and add <literal>NODE_ROLEARN</literal> with the driver node&#8217;s ARN by running a command similar to the following:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc -n openshift-cluster-csi-drivers edit subscription aws-efs-csi-driver-operator
...
  config:
    env:
    - name: ROLEARN
      value: arn:aws:iam::269733383066:role/my-arn-1-blll6-openshift-cluster-csi-drivers-aws-efs-cloud-cre <co xml:id="CO23-1"/>
    - name: NODE_ROLEARN
      value: arn:aws:iam::269733383066:role/my-arn-1-blll6-openshift-cluster-csi-drivers-node-aws-efs-clou <co xml:id="CO23-2"/>
...</programlisting>
<calloutlist>
<callout arearefs="CO23-1">
<para>Controller ARN. Already exists.</para>
</callout>
<callout arearefs="CO23-2">
<para>Driver node ARN</para>
</callout>
</calloutlist>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="csi-dynamic-provisioning-aws-efs_persistent-storage-csi-aws-efs">
<title>Dynamic provisioning for Amazon Elastic File Storage</title>
<simpara role="_abstract">The <link xlink:href="https://github.com/openshift/aws-efs-csi-driver">AWS EFS CSI driver</link> supports a different form of dynamic provisioning than other CSI drivers. It provisions new PVs as subdirectories of a pre-existing EFS volume. The PVs are independent of each other. However, they all share the same EFS volume. When the volume is deleted, all PVs provisioned out of it are deleted too.
The EFS CSI driver creates an AWS Access Point for each such subdirectory. Due to AWS AccessPoint limits, you can only dynamically provision 1000 PVs from a single <literal>StorageClass</literal>/EFS volume.</simpara>
<important>
<simpara>Note that <literal>PVC.spec.resources</literal> is not enforced by EFS.</simpara>
<simpara>In the example below, you request 5 GiB of space. However, the created PV is limitless and can store any amount of data (like petabytes). A broken application, or even a rogue application, can cause significant expenses when it stores too much data on the volume.</simpara>
<simpara>Using monitoring of EFS volume sizes in AWS is strongly recommended.</simpara>
</important>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You have created Amazon Elastic File Storage (Amazon EFS) volumes.</simpara>
</listitem>
<listitem>
<simpara>You have created the AWS EFS storage class.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To enable dynamic provisioning:</para>
</formalpara>
<itemizedlist>
<listitem>
<simpara>Create a PVC (or StatefulSet or Template) as usual, referring to the <literal>StorageClass</literal> created previously.</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test
spec:
  storageClassName: efs-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi</programlisting>
</listitem>
</itemizedlist>
<simpara>If you have problems setting up dynamic provisioning, see <link linkend="efs-troubleshooting_persistent-storage-csi-aws-efs">AWS EFS troubleshooting</link>.</simpara>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link linkend="storage-create-storage-class_persistent-storage-csi-aws-efs">Creating the AWS EFS storage class</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="efs-create-static-pv_persistent-storage-csi-aws-efs">
<title>Creating static PVs with Amazon Elastic File Storage</title>
<simpara>It is possible to use an Amazon Elastic File Storage (Amazon EFS) volume as a single PV without any dynamic provisioning. The whole volume is mounted to pods.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You have created Amazon EFS volumes.</simpara>
</listitem>
</itemizedlist>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Create the PV using the following YAML file:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolume
metadata:
  name: efs-pv
spec:
  capacity: <co xml:id="CO24-1"/>
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: efs.csi.aws.com
    volumeHandle: fs-ae66151a <co xml:id="CO24-2"/>
    volumeAttributes:
      encryptInTransit: "false" <co xml:id="CO24-3"/></programlisting>
<calloutlist>
<callout arearefs="CO24-1">
<para><literal>spec.capacity</literal> does not have any meaning and is ignored by the CSI driver. It is used only when binding to a PVC. Applications can store any amount of data to the volume.</para>
</callout>
<callout arearefs="CO24-2">
<para><literal>volumeHandle</literal> must be the same ID as the EFS volume you created in AWS. If you are providing your own access point, <literal>volumeHandle</literal> should be <literal>&lt;EFS volume ID&gt;::&lt;access point ID&gt;</literal>. For example: <literal>fs-6e633ada::fsap-081a1d293f0004630</literal>.</para>
</callout>
<callout arearefs="CO24-3">
<para>If desired, you can disable encryption in transit. Encryption is enabled by default.</para>
</callout>
</calloutlist>
</listitem>
</itemizedlist>
<simpara>If you have problems setting up static PVs, see <link linkend="efs-troubleshooting_persistent-storage-csi-aws-efs">AWS EFS troubleshooting</link>.</simpara>
</section>
<section xml:id="efs-security_persistent-storage-csi-aws-efs">
<title>Amazon Elastic File Storage security</title>
<simpara>The following information is important for Amazon Elastic File Storage (Amazon EFS) security.</simpara>
<simpara>When using access points, for example, by using dynamic provisioning as described earlier, Amazon automatically replaces GIDs on files with the GID of the access point. In addition, EFS considers the user ID, group ID, and secondary group IDs of the access point when evaluating file system permissions. EFS ignores the NFS client&#8217;s IDs. For more information about access points, see <link xlink:href="https://docs.aws.amazon.com/efs/latest/ug/efs-access-points.html">https://docs.aws.amazon.com/efs/latest/ug/efs-access-points.html</link>.</simpara>
<simpara>As a consequence, EFS volumes silently ignore FSGroup; OpenShift Container Platform is not able to replace the GIDs of files on the volume with FSGroup. Any pod that can access a mounted EFS access point can access any file on it.</simpara>
<simpara>Unrelated to this, encryption in transit is enabled by default. For more information, see <link xlink:href="https://docs.aws.amazon.com/efs/latest/ug/encryption-in-transit.html">https://docs.aws.amazon.com/efs/latest/ug/encryption-in-transit.html</link>.</simpara>
</section>
<section xml:id="_aws-efs-storage-csi-usage-metrics">
<title>AWS EFS storage CSI usage metrics</title>
<section xml:id="efs-metrics-overview_persistent-storage-csi-aws-efs">
<title>Usage metrics overview</title>
<simpara>Amazon Web Services (AWS) Elastic File Service (EFS) storage Container Storage Interface (CSI) usage metrics allow you to monitor how much space is used by either dynamically or statically provisioned EFS volumes.</simpara>
<important>
<simpara>This features is disabled by default, because turning on metrics can lead to performance degradation.</simpara>
</important>
<simpara>The AWS EFS usage metrics feature collects volume metrics in the AWS EFS CSI Driver by recursively walking through the files in the volume. Because this effort can degrade performance, administrators must explicitly enable this feature.</simpara>
</section>
<section xml:id="efs-metrics-procedure-gui_persistent-storage-csi-aws-efs">
<title>Enabling usage metrics using the web console</title>
<simpara>To enable Amazon Web Services (AWS) Elastic File Service (EFS) Storage Container Storage Interface (CSI) usage metrics using the web console:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Click <emphasis role="strong">Administration</emphasis> &gt; <emphasis role="strong">CustomResourceDefinitions</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">CustomResourceDefinitions</emphasis> page next to the <emphasis role="strong">Name</emphasis> dropdown box, type <literal>clustercsidriver</literal>.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">CRD ClusterCSIDriver</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Click the <emphasis role="strong">YAML</emphasis> tab.</simpara>
</listitem>
<listitem>
<simpara>Under <literal>spec.aws.efsVolumeMetrics.state</literal>, set the value to <literal>RecursiveWalk</literal>.</simpara>
<simpara><literal>RecursiveWalk</literal> indicates that volume metrics collection in the AWS EFS CSI Driver is performed by recursively walking through the files in the volume.</simpara>
<formalpara>
<title>Example ClusterCSIDriver efs.csi.aws.com YAML file</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">spec:
    driverConfig:
        driverType: AWS
        aws:
            efsVolumeMetrics:
              state: RecursiveWalk
              recursiveWalk:
                refreshPeriodMinutes: 100
                fsRateLimit: 10</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Optional: To define how the recursive walk operates, you can also set the following fields:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>refreshPeriodMinutes</literal>: Specifies the refresh frequency for volume metrics in minutes. If this field is left blank, a reasonable default is chosen, which is subject to change over time. The current default is 240 minutes. The valid range is 1 to 43,200 minutes.</simpara>
</listitem>
<listitem>
<simpara><literal>fsRateLimit</literal>: Defines the rate limit for processing volume metrics in goroutines per file system. If this field is left blank, a reasonable default is chosen, which is subject to change over time. The current default is 5 goroutines. The valid range is 1 to 100 goroutines.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Save</emphasis>.</simpara>
</listitem>
</orderedlist>
<note>
<simpara>To <emphasis role="strong">disable</emphasis> AWS EFS CSI usage metrics, use the preceding procedure, but for <literal>spec.aws.efsVolumeMetrics.state</literal>, change the value from <literal>RecursiveWalk</literal> to <literal>Disabled</literal>.</simpara>
</note>
</section>
<section xml:id="efs-metrics-procedure-cli_persistent-storage-csi-aws-efs">
<title>Enabling usage metrics using the CLI</title>
<simpara>To enable Amazon Web Services (AWS) Elastic File Service (EFS) storage Container Storage Interface (CSI) usage metrics using the CLI:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Edit ClusterCSIDriver by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit clustercsidriver efs.csi.aws.com</programlisting>
</listitem>
<listitem>
<simpara>Under <literal>spec.aws.efsVolumeMetrics.state</literal>, set the value to <literal>RecursiveWalk</literal>.</simpara>
<simpara><literal>RecursiveWalk</literal> indicates that volume metrics collection in the AWS EFS CSI Driver is performed by recursively walking through the files in the volume.</simpara>
<formalpara>
<title>Example ClusterCSIDriver efs.csi.aws.com YAML file</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">spec:
    driverConfig:
        driverType: AWS
        aws:
            efsVolumeMetrics:
              state: RecursiveWalk
              recursiveWalk:
                refreshPeriodMinutes: 100
                fsRateLimit: 10</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Optional: To define how the recursive walk operates, you can also set the following fields:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>refreshPeriodMinutes</literal>: Specifies the refresh frequency for volume metrics in minutes. If this field is left blank, a reasonable default is chosen, which is subject to change over time. The current default is 240 minutes. The valid range is 1 to 43,200 minutes.</simpara>
</listitem>
<listitem>
<simpara><literal>fsRateLimit</literal>: Defines the rate limit for processing volume metrics in goroutines per file system. If this field is left blank, a reasonable default is chosen, which is subject to change over time. The current default is 5 goroutines. The valid range is 1 to 100 goroutines.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Save the changes to the <literal>efs.csi.aws.com</literal> object.</simpara>
</listitem>
</orderedlist>
<note>
<simpara>To <emphasis role="strong">disable</emphasis> AWS EFS CSI usage metrics, use the preceding procedure, but for <literal>spec.aws.efsVolumeMetrics.state</literal>, change the value from <literal>RecursiveWalk</literal> to <literal>Disabled</literal>.</simpara>
</note>
</section>
</section>
<section xml:id="efs-troubleshooting_persistent-storage-csi-aws-efs">
<title>Amazon Elastic File Storage troubleshooting</title>
<simpara>The following information provides guidance on how to troubleshoot issues with Amazon Elastic File Storage (Amazon EFS):</simpara>
<itemizedlist>
<listitem>
<simpara>The AWS EFS Operator and CSI driver run in namespace <literal>openshift-cluster-csi-drivers</literal>.</simpara>
</listitem>
<listitem>
<simpara>To initiate gathering of logs of the AWS EFS Operator and CSI driver, run the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc adm must-gather
[must-gather      ] OUT Using must-gather plugin-in image: quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:125f183d13601537ff15b3239df95d47f0a604da2847b561151fedd699f5e3a5
[must-gather      ] OUT namespace/openshift-must-gather-xm4wq created
[must-gather      ] OUT clusterrolebinding.rbac.authorization.k8s.io/must-gather-2bd8x created
[must-gather      ] OUT pod for plug-in image quay.io/openshift-release-dev/ocp-v4.0-art-dev@sha256:125f183d13601537ff15b3239df95d47f0a604da2847b561151fedd699f5e3a5 created</programlisting>
</listitem>
<listitem>
<simpara>To show AWS EFS Operator errors, view the <literal>ClusterCSIDriver</literal> status:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get clustercsidriver efs.csi.aws.com -o yaml</programlisting>
</listitem>
<listitem>
<simpara>If a volume cannot be mounted to a pod (as shown in the output of the following command):</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe pod
...
  Type     Reason       Age    From               Message
  ----     ------       ----   ----               -------
  Normal   Scheduled    2m13s  default-scheduler  Successfully assigned default/efs-app to ip-10-0-135-94.ec2.internal
  Warning  FailedMount  13s    kubelet            MountVolume.SetUp failed for volume "pvc-d7c097e6-67ec-4fae-b968-7e7056796449" : rpc error: code = DeadlineExceeded desc = context deadline exceeded <co xml:id="CO25-1"/>
  Warning  FailedMount  10s    kubelet            Unable to attach or mount volumes: unmounted volumes=[persistent-storage], unattached volumes=[persistent-storage kube-api-access-9j477]: timed out waiting for the condition</programlisting>
<calloutlist>
<callout arearefs="CO25-1">
<para>Warning message indicating volume not mounted.</para>
</callout>
</calloutlist>
<simpara>This error is frequently caused by AWS dropping packets between an OpenShift Container Platform node and Amazon EFS.</simpara>
<simpara>Check that the following are correct:</simpara>
<itemizedlist>
<listitem>
<simpara>AWS firewall and Security Groups</simpara>
</listitem>
<listitem>
<simpara>Networking: port number and IP addresses</simpara>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
</section>
<section xml:id="persistent-storage-csi-olm-operator-uninstall_persistent-storage-csi-aws-efs">
<title>Uninstalling the AWS EFS CSI Driver Operator</title>
<simpara>All EFS PVs are inaccessible after uninstalling the <link xlink:href="https://github.com/openshift/aws-efs-csi-driver-operator">AWS EFS CSI Driver Operator</link> (a Red Hat operator).</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform web console.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To uninstall the AWS EFS CSI Driver Operator from the web console:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Log in to the web console.</simpara>
</listitem>
<listitem>
<simpara>Stop all applications that use AWS EFS PVs.</simpara>
</listitem>
<listitem>
<simpara>Delete all AWS EFS PVs:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Storage</emphasis> &#8594; <emphasis role="strong">PersistentVolumeClaims</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Select each PVC that is in use by the AWS EFS CSI Driver Operator, click the drop-down menu on the far right of the PVC, and then click <emphasis role="strong">Delete PersistentVolumeClaims</emphasis>.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Uninstall the <link xlink:href="https://github.com/openshift/aws-efs-csi-driver">AWS EFS CSI driver</link>:</simpara>
<note>
<simpara>Before you can uninstall the Operator, you must remove the CSI driver first.</simpara>
</note>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Administration</emphasis> &#8594; <emphasis role="strong">CustomResourceDefinitions</emphasis> &#8594; <emphasis role="strong">ClusterCSIDriver</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Instances</emphasis> tab, for <emphasis role="strong">efs.csi.aws.com</emphasis>, on the far left side, click the drop-down menu, and then click <emphasis role="strong">Delete ClusterCSIDriver</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>When prompted, click <emphasis role="strong">Delete</emphasis>.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Uninstall the AWS EFS CSI Operator:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Ecosystem</emphasis> &#8594; <emphasis role="strong">Installed Operators</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Installed Operators</emphasis> page, scroll or type AWS EFS CSI into the <emphasis role="strong">Search by name</emphasis> box to find the Operator, and then click it.</simpara>
</listitem>
<listitem>
<simpara>On the upper, right of the <emphasis role="strong">Installed Operators &gt; Operator details</emphasis> page, click <emphasis role="strong">Actions</emphasis> &#8594; <emphasis role="strong">Uninstall Operator</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>When prompted on the <emphasis role="strong">Uninstall Operator</emphasis> window, click the <emphasis role="strong">Uninstall</emphasis> button to remove the Operator from the namespace. Any applications deployed by the Operator on the cluster need to be cleaned up manually.</simpara>
<simpara>After uninstalling, the AWS EFS CSI Driver Operator is no longer listed in the <emphasis role="strong">Installed Operators</emphasis> section of the web console.</simpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<note>
<simpara>Before you can destroy a cluster (<literal>openshift-install destroy cluster</literal>), you must delete the EFS volume in AWS.
An OpenShift Container Platform cluster cannot be destroyed when there is an EFS volume that uses the cluster&#8217;s VPC. Amazon does not allow deletion of such a VPC.</simpara>
</note>
</section>
<section xml:id="_additional-resources" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara><link linkend="persistent-storage-csi">Configuring CSI volumes</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-gcp-pd">
<title>GCP PD CSI Driver Operator</title>
<section xml:id="_overview-4">
<title>Overview</title>
<simpara>OpenShift Container Platform can provision persistent volumes (PVs) using the Container Storage Interface (CSI) driver for Google Cloud Platform (GCP) persistent disk (PD) storage.</simpara>
<simpara>Familiarity with <link linkend="understanding-persistent-storage">persistent storage</link> and <link linkend="persistent-storage-csi">configuring CSI volumes</link> is recommended when working with a Container Storage Interface (CSI) Operator and driver.</simpara>
<simpara>To create CSI-provisioned persistent volumes (PVs) that mount to GCP PD storage assets, OpenShift Container Platform installs the GCP PD CSI Driver Operator and the GCP PD CSI driver by default in the <literal>openshift-cluster-csi-drivers</literal> namespace.</simpara>
<itemizedlist>
<listitem>
<simpara><emphasis role="strong">GCP PD CSI Driver Operator</emphasis>: By default, the Operator provides a storage class that you can use to create PVCs. You can disable this default storage class if desired (see <link linkend="persistent-storage-csi-sc-manage">Managing the default storage class</link>). You also have the option to create the GCP PD storage class as described in <link linkend="persistent-storage-using-gce">Persistent storage using GCE Persistent Disk</link>.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">GCP PD driver</emphasis>: The driver enables you to create and mount GCP PD PVs.</simpara>
<simpara>GCP PD CSI driver supports the C3 instance type for bare metal and N4 machine series. The C3 instance type and N4 machine series support the hyperdisk-balanced disks. For more information, see Section <emphasis>C3 instance type for bare metal and N4 machine series</emphasis>.</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="csi-about_persistent-storage-csi-gcp-pd">
<title>About CSI</title>
<simpara>Storage vendors have traditionally provided storage drivers as part of Kubernetes. With the implementation of the Container Storage Interface (CSI), third-party providers can instead deliver storage plugins using a standard interface without ever having to change the core Kubernetes code.</simpara>
<simpara>CSI Operators give OpenShift Container Platform users storage options, such as volume snapshots, that are not possible with in-tree volume plugins.</simpara>
</section>
<section xml:id="persistent-storage-csi-gcp-pd-reduce-permissions_persistent-storage-csi-gcp-pd">
<title>Reducing permissions while using the GCP PD CSI Driver Operator</title>
<simpara role="_abstract">The default installation allows the Google Cloud Platform (GCP) persistent disk (PD) Container Storage Interface (CSI) Driver to impersonate any service account in the Google Cloud project. You can reduce the scope of permissions granted to the GCP PD CSI Driver service account in your Google Cloud project to only the required node service accounts.</simpara>
<simpara>To reduce permissions, grant the <literal>iam.serviceAccountUser</literal> role to the control plane and compute node service accounts, and then remove the <literal>iam.serviceAccountUser</literal> role from the project-wide service account, thus reducing the scope of the permission.</simpara>
<note>
<simpara>Reducing permissions only applies to GCP clusters using Workload Identity Federation (WIF).</simpara>
</note>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Grant scoped <literal>iam.serviceAccountUser</literal> role for node service accounts by running the following Bash commands:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">gcloud iam service-accounts add-iam-policy-binding "${MASTER_NODE_SA}" --project="${GOOGLE_PROJECT_ID}" --member="serviceAccount:${SERVICE_ACCOUNT_EMAIL}" --role="roles/iam.serviceAccountUser" --condition=None
gcloud iam service-accounts add-iam-policy-binding "${WORKER_NODE_SA}" --project="${GOOGLE_PROJECT_ID}" --member="serviceAccount:${SERVICE_ACCOUNT_EMAIL}" --role="roles/iam.serviceAccountUser" --condition=None</programlisting>
<itemizedlist>
<listitem>
<simpara><literal>GOOGLE_PROJECT_ID</literal>: The unique ID of your Google Cloud project.</simpara>
</listitem>
<listitem>
<simpara><literal>SERVICE_ACCOUNT_EMAIL</literal>: The email address of the "Member" (the person or service account) who is being granted the new permissions. To find the service account, on WIF clusters, there is a default service account on GCP for the CSI driver based on the cluster name, for example: <literal>${CLUSTER_NAME}-openshift-gcp-pd-csi-*</literal>.</simpara>
</listitem>
<listitem>
<simpara><literal>MASTER_NODE_SA</literal>: The email address of the service account used by your cluster&#8217;s master node.</simpara>
</listitem>
<listitem>
<simpara><literal>WORKER_NODE_SA</literal>: The email address of the service account used by your cluster&#8217;s worker nodes.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Remove project-level <literal>iam.serviceAccountUser</literal> role from the binding created by the installation program by running the following Bash commands:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">gcloud projects remove-iam-policy-binding "${GOOGLE_PROJECT_ID}" --member="serviceAccount:${SERVICE_ACCOUNT_EMAIL}" --role="roles/iam.serviceAccountUser" --condition=None</programlisting>
<itemizedlist>
<listitem>
<simpara><literal>SERVICE_ACCOUNT_EMAIL</literal>: The email address of the account losing the permission. For example, <literal>my-app-sa@my-project.iam.gserviceaccount.com</literal>. To find the service account, on WIF clusters, there is a default service account on GCP for the CSI driver based on the cluster name, for example: <literal>${CLUSTER_NAME}-openshift-gcp-pd-csi-*</literal>.</simpara>
</listitem>
<listitem>
<simpara><literal>GOOGLE_PROJECT_ID</literal>: The unique ID of the Google Cloud project where this is occurring. For example, <literal>prod-data-789</literal>.</simpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
</section>
<section xml:id="persistent-storage-csi-gcp-pd-storage-class-ref_persistent-storage-csi-gcp-pd">
<title>GCP PD CSI driver storage class parameters</title>
<simpara>The Google Cloud Platform (GCP) persistent disk (PD) Container Storage Interface (CSI) driver uses the CSI <literal>external-provisioner</literal> sidecar as a controller. This is a separate helper container that is deployed with the CSI driver. The sidecar manages persistent volumes (PVs) by triggering the <literal>CreateVolume</literal> operation.</simpara>
<simpara>The GCP PD CSI driver uses the <literal>csi.storage.k8s.io/fstype</literal> parameter key to support dynamic provisioning. The following table describes all the GCP PD CSI storage class parameters that are supported by OpenShift Container Platform.</simpara>
<table frame="all" rowsep="1" colsep="1">
<title>CreateVolume Parameters</title>
<tgroup cols="4">
<colspec colname="col_1" colwidth="18.1818*"/>
<colspec colname="col_2" colwidth="27.2727*"/>
<colspec colname="col_3" colwidth="18.1818*"/>
<colspec colname="col_4" colwidth="36.3637*"/>
<thead>
<row>
<entry align="left" valign="top">Parameter</entry>
<entry align="left" valign="top">Values</entry>
<entry align="left" valign="top">Default</entry>
<entry align="left" valign="top">Description</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara><literal>type</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>pd-ssd</literal>, <literal>pd-standard</literal>, or <literal>pd-balanced</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>pd-standard</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Allows you to choose between standard PVs or solid-state-drive PVs.</simpara><simpara>The driver does not validate the value, thus all the possible values are accepted.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>replication-type</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>none</literal> or <literal>regional-pd</literal></simpara></entry>
<entry align="left" valign="top"><simpara><literal>none</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Allows you to choose between zonal or regional PVs.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara><literal>disk-encryption-kms-key</literal></simpara></entry>
<entry align="left" valign="top"><simpara>Fully qualified resource identifier for the key to use to encrypt new disks.</simpara></entry>
<entry align="left" valign="top"><simpara>Empty string</simpara></entry>
<entry align="left" valign="top"><simpara>Uses customer-managed encryption keys (CMEK) to encrypt new disks.</simpara></entry>
</row>
</tbody>
</tgroup>
</table>
</section>
<section xml:id="c3-instance-type-for-bare-metal-and-n4-machine-series">
<title>C3 instance type for bare metal and N4 machine series</title>
<section xml:id="persistent-storage-csi-gcp-hyperdisk-limitations_persistent-storage-csi-gcp-pd">
<title>C3 and N4 instance type limitations</title>
<simpara>The GCP PD CSI driver support for the C3 instance type for bare metal and N4 machine series have the following limitations:</simpara>
<itemizedlist>
<listitem>
<simpara>You must set the volume size to at least 4Gi when you create hyperdisk-balanced disks. OpenShift Container Platform does not round up to the minimum size, so you must specify the correct size yourself.</simpara>
</listitem>
<listitem>
<simpara>Cloning volumes is not supported when using storage pools.</simpara>
</listitem>
<listitem>
<simpara>For cloning or resizing, hyperdisk-balanced disks original volume size must be 6Gi or greater.</simpara>
</listitem>
<listitem>
<simpara>The default storage class is standard-csi.</simpara>
<important>
<simpara>You need to manually create a storage class.</simpara>
<simpara>For information about creating the storage class, see Step 2 in Section <emphasis>Setting up hyperdisk-balanced disks</emphasis>.</simpara>
</important>
</listitem>
</itemizedlist>
</section>
<section xml:id="persistent-storage-csi-gcp-hyperdisk-storage-pools-overview_persistent-storage-csi-gcp-pd">
<title>Storage pools for hyperdisk-balanced disks overview</title>
<simpara>Hyperdisk storage pools can be used with Compute Engine for large-scale storage. A hyperdisk storage pool is a purchased collection of capacity, throughput, and IOPS, which you can then provision for your applications as needed. You can use hyperdisk storage pools to create and manage disks in pools and use the disks across multiple workloads. By managing disks in aggregate, you can save costs while achieving expected capacity and performance growth. By using only the storage that you need in hyperdisk storage pools, you reduce the complexity of forecasting capacity and reduce management by going from managing hundreds of disks to managing a single storage pool.</simpara>
<simpara>To set up storage pools, see <link linkend="persistent-storage-csi-gcp-hyperdisk-storage-pools-procedure_persistent-storage-csi-gcp-pd">Setting up hyperdisk-balanced disks</link>.</simpara>
</section>
<section xml:id="persistent-storage-csi-gcp-hyperdisk-storage-pools-procedure_persistent-storage-csi-gcp-pd">
<title>Setting up hyperdisk-balanced disks</title>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster with administrative privileges</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>Complete the following steps to set up hyperdisk-balanced disks:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Create an OpenShift Container Platform cluster on Google Cloud with attached disks provisioned with hyperdisk-balanced disks. This can be achieved by provisioning the cluster with compute node types that support hyperdisk-balanced disks, such as the C3 and N4 machine series from Google Cloud.</simpara>
</listitem>
<listitem>
<simpara>Once the OSD cluster is ready, navigate to the <emphasis role="strong">OpenShift console</emphasis> for Storage Class creation.
Within the console, navigate to the <emphasis role="strong">Storage</emphasis> section to create a Storage Class specifying the hyperdisk-balanced disk:</simpara>
<formalpara>
<title>Example StorageClass YAML file</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
 name: hyperdisk-sc <co xml:id="CO26-1"/>
 annotations:
   storageclass.kubernetes.io/is-default-class: 'true'
provisioner: pd.csi.storage.gke.io <co xml:id="CO26-2"/>
parameters:
 replication-type: none
 storage-pools: projects/myproject/zones/us-east1-c/storagePools/hyperdisk-storagepool <co xml:id="CO26-3"/>
 type: hyperdisk-balanced <co xml:id="CO26-4"/>
reclaimPolicy: Delete
allowVolumeExpansion: true
volumeBindingMode: Immediate</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO26-1">
<para>Specify the name for your storage class. In this example, the name is <literal>hyperdisk-sc</literal>.</para>
</callout>
<callout arearefs="CO26-2">
<para>Specify the GCP CSI provisioner as <literal>pd.csi.storage.gke.io</literal>.</para>
</callout>
<callout arearefs="CO26-3">
<para>If using storage pools, specify a list of specific storage pools that you want to use in the following format: <literal>projects/PROJECT_ID/zones/ZONE/storagePools/STORAGE_POOL_NAME</literal>.</para>
</callout>
<callout arearefs="CO26-4">
<para>Specify the disk type as <literal>hyperdisk-balanced</literal>.</para>
</callout>
</calloutlist>
<note>
<simpara>If you use storage pools, you must first create a Hyperdisk Storage Pool of the type "Hyperdisk Balanced" in the Google Cloud console prior to referencing it in the OpenShift Storage Class. The Hyperdisk Storage Pool must be created in the same zone as the compute node supporting Hyperdisk is installed in the cluster configuration. For more information about creating a Hyperdisk Storage Pool, see <link xlink:href="https://cloud.google.com/compute/docs/disks/create-storage-pools#create-pool">Create a Hyperdisk Storage Pool</link> in the Google Cloud documentation.</simpara>
</note>
</listitem>
<listitem>
<simpara>Create a persistent volume claim (PVC) that uses the hyperdisk-specific storage class using the following example YAML file:</simpara>
<formalpara>
<title>Example PVC YAML file</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
spec:
  storageClassName: hyperdisk-sc <co xml:id="CO27-1"/>
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2048Gi <co xml:id="CO27-2"/></programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO27-1">
<para>PVC references the storage pool-specific storage class. In this example, <literal>hyperdisk-sc</literal>.</para>
</callout>
<callout arearefs="CO27-2">
<para>Target storage capacity of the hyperdisk-balanced volume. In this example, <literal>2048Gi</literal>.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Create a deployment that uses the PVC that you just created. Using a deployment helps ensure that your application has access to the persistent storage even after the pod restarts and rescheduling:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Ensure a node pool with the specified machine series is up and running before creating the deployment. Otherwise, the pod fails to schedule.</simpara>
</listitem>
<listitem>
<simpara>Use the following example YAML file to create the deployment:</simpara>
<formalpara>
<title>Example deployment YAML file</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      nodeSelector:
        cloud.google.com/machine-family: n4 <co xml:id="CO28-1"/>
      containers:
      - name: postgres
        image: postgres:14-alpine
        args: [ "sleep", "3600" ]
        volumeMounts:
        - name: sdk-volume
          mountPath: /usr/share/data/
      volumes:
      - name: sdk-volume
        persistentVolumeClaim:
          claimName: my-pvc <co xml:id="CO28-2"/></programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO28-1">
<para>Specifies the machine family. In this example, it is <literal>n4</literal>.</para>
</callout>
<callout arearefs="CO28-2">
<para>Specifies the name of the PVC created in the preceding step. In this example, it is <literal>my-pfc</literal>.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Confirm that the deployment was successfully created by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get deployment</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME       READY   UP-TO-DATE   AVAILABLE   AGE
postgres   0/1     1            0           42s</programlisting>
</para>
</formalpara>
<simpara>It might take a few minutes for hyperdisk instances to complete provisioning and display a READY status.</simpara>
</listitem>
<listitem>
<simpara>Confirm that PVC <literal>my-pvc</literal> has been successfully bound to a persistent volume (PV) by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get pvc my-pvc</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME          STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS       VOLUMEATTRIBUTESCLASS  AGE
my-pvc        Bound    pvc-1ff52479-4c81-4481-aa1d-b21c8f8860c6   2Ti        RWO            hyperdisk-sc       &lt;unset&gt;                2m24s</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Confirm the expected configuration of your hyperdisk-balanced disk:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ gcloud compute disks list</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                                        LOCATION        LOCATION_SCOPE  SIZE_GB  TYPE                STATUS
instance-20240914-173145-boot               us-central1-a   zone            150      pd-standard         READY
instance-20240914-173145-data-workspace     us-central1-a   zone            100      pd-balanced         READY
c4a-rhel-vm                                 us-central1-a   zone            50       hyperdisk-balanced  READY <co xml:id="CO29-1"/></programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO29-1">
<para>Hyperdisk-balanced disk.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>If using storage pools, check that the volume is provisioned as specified in your storage class and PVC by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ gcloud compute storage-pools list-disks pool-us-east4-c --zone=us-east4-c</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                                      STATUS  PROVISIONED_IOPS  PROVISIONED_THROUGHPUT  SIZE_GB
pvc-1ff52479-4c81-4481-aa1d-b21c8f8860c6  READY   3000              140                     2048</programlisting>
</para>
</formalpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-gcp-pd-encrypted-pv_persistent-storage-csi-gcp-pd">
<title>Creating a custom-encrypted persistent volume</title>
<simpara>When you create a <literal>PersistentVolumeClaim</literal> object, OpenShift Container Platform provisions a new persistent volume (PV) and creates a <literal>PersistentVolume</literal> object. You can add a custom encryption key in Google Cloud Platform (GCP) to protect a PV in your cluster by encrypting the newly created PV.</simpara>
<simpara>For encryption, the newly attached PV that you create uses customer-managed encryption keys (CMEK) on a cluster by using a new or existing Google Cloud Key Management Service (KMS) key.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You are logged in to a running OpenShift Container Platform cluster.</simpara>
</listitem>
<listitem>
<simpara>You have created a Cloud KMS key ring and key version.</simpara>
</listitem>
</itemizedlist>
<simpara>For more information about CMEK and Cloud KMS resources, see <link xlink:href="https://cloud.google.com/kubernetes-engine/docs/how-to/using-cmek">Using customer-managed encryption keys (CMEK)</link>.</simpara>
<formalpara>
<title>Procedure</title>
<para>To create a custom-encrypted PV, complete the following steps:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Create a storage class with the Cloud KMS key. The following example enables dynamic provisioning of encrypted volumes:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: csi-gce-pd-cmek
provisioner: pd.csi.storage.gke.io
volumeBindingMode: "WaitForFirstConsumer"
allowVolumeExpansion: true
parameters:
  type: pd-standard
  disk-encryption-kms-key: projects/&lt;key-project-id&gt;/locations/&lt;location&gt;/keyRings/&lt;key-ring&gt;/cryptoKeys/&lt;key&gt; <co xml:id="CO30-1"/></programlisting>
<calloutlist>
<callout arearefs="CO30-1">
<para>This field must be the resource identifier for the key that will be used to encrypt new disks. Values are case-sensitive. For more information about providing key ID values, see <link xlink:href="https://cloud.google.com/kms/docs/resource-hierarchy#retrieve_resource_id">Retrieving a resource&#8217;s ID</link> and <link xlink:href="https://cloud.google.com/kms/docs/getting-resource-ids">Getting a Cloud KMS resource ID</link>.</para>
</callout>
</calloutlist>
<note>
<simpara>You cannot add the <literal>disk-encryption-kms-key</literal> parameter to an existing storage class. However, you can delete the storage class and recreate it with the same name and a different set of parameters. If you do this, the provisioner of the existing class must be <literal>pd.csi.storage.gke.io</literal>.</simpara>
</note>
</listitem>
<listitem>
<simpara>Deploy the storage class on your OpenShift Container Platform cluster using the <literal>oc</literal> command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe storageclass csi-gce-pd-cmek</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">Name:                  csi-gce-pd-cmek
IsDefaultClass:        No
Annotations:           None
Provisioner:           pd.csi.storage.gke.io
Parameters:            disk-encryption-kms-key=projects/key-project-id/locations/location/keyRings/ring-name/cryptoKeys/key-name,type=pd-standard
AllowVolumeExpansion:  true
MountOptions:          none
ReclaimPolicy:         Delete
VolumeBindingMode:     WaitForFirstConsumer
Events:                none</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Create a file named <literal>pvc.yaml</literal> that matches the name of your storage class object that you created in the previous step:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: podpvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-gce-pd-cmek
  resources:
    requests:
      storage: 6Gi</programlisting>
<note>
<simpara>If you marked the new storage class as default, you can omit the <literal>storageClassName</literal> field.</simpara>
</note>
</listitem>
<listitem>
<simpara>Apply the PVC on your cluster:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc apply -f pvc.yaml</programlisting>
</listitem>
<listitem>
<simpara>Get the status of your PVC and verify that it is created and bound to a newly provisioned PV:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get pvc</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME      STATUS    VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS     AGE
podpvc    Bound     pvc-e36abf50-84f3-11e8-8538-42010a800002   10Gi       RWO            csi-gce-pd-cmek  9s</programlisting>
</para>
</formalpara>
<note>
<simpara>If your storage class has the <literal>volumeBindingMode</literal> field set to <literal>WaitForFirstConsumer</literal>, you must create a pod to use the PVC before you can verify it.</simpara>
</note>
</listitem>
</orderedlist>
<simpara>Your CMEK-protected PV is now ready to use with your OpenShift Container Platform cluster.</simpara>
</section>
<section xml:id="resources-for-gcp" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara><link linkend="persistent-storage-using-gce">Persistent storage using GCE Persistent Disk</link></simpara>
</listitem>
<listitem>
<simpara><link linkend="persistent-storage-csi">Configuring CSI volumes</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-google-cloud-file">
<title>Google Cloud Filestore CSI Driver Operator</title>
<section xml:id="persistent-storage-csi-google-cloud-file-overview">
<title>Overview</title>
<simpara>OpenShift Container Platform is capable of provisioning persistent volumes (PVs) using the Container Storage Interface (CSI) driver for Google Compute Platform (GCP) Filestore Storage.</simpara>
<simpara>Familiarity with <link linkend="understanding-persistent-storage">persistent storage</link> and <link linkend="persistent-storage-csi">configuring CSI volumes</link> is recommended when working with a CSI Operator and driver.</simpara>
<simpara>To create CSI-provisioned PVs that mount to Google Cloud Filestore Storage assets, you install the Google Cloud Filestore CSI Driver Operator and the Google Cloud Filestore CSI driver in the <literal>openshift-cluster-csi-drivers</literal> namespace.</simpara>
<itemizedlist>
<listitem>
<simpara>The <emphasis>Google Cloud Filestore CSI Driver Operator</emphasis> does not provide a storage class by default, but <link linkend="persistent-storage-csi-google-cloud-file-create-sc_persistent-storage-csi-google-cloud-file">you can create one if needed</link>. The Google Cloud Filestore CSI Driver Operator supports dynamic volume provisioning by allowing storage volumes to be created on demand, eliminating the need for cluster administrators to pre-provision storage.</simpara>
</listitem>
<listitem>
<simpara>The <emphasis>Google Cloud Filestore CSI driver</emphasis> enables you to create and mount Google Cloud Filestore PVs.</simpara>
</listitem>
</itemizedlist>
<simpara>OpenShift Container Platform Google Cloud Filestore supports Workload Identity. This allows users to access Google Cloud resources using federated identities instead of a service account key. GCP Workload Identity must be enabled globally during installation, and then configured for the Google Cloud Filestore CSI Driver Operator.</simpara>
</section>
<section xml:id="csi-about_persistent-storage-csi-google-cloud-file">
<title>About CSI</title>
<simpara>Storage vendors have traditionally provided storage drivers as part of Kubernetes. With the implementation of the Container Storage Interface (CSI), third-party providers can instead deliver storage plugins using a standard interface without ever having to change the core Kubernetes code.</simpara>
<simpara>CSI Operators give OpenShift Container Platform users storage options, such as volume snapshots, that are not possible with in-tree volume plugins.</simpara>
</section>
<section xml:id="installing-the-gcp-filestore-csi-driver-operator">
<title>Installing the Google Cloud Filestore CSI Driver Operator</title>
<section xml:id="persistent-storage-csi-gcp-filestore-wif_persistent-storage-csi-google-cloud-file">
<title>Preparing to install the Google Cloud Filestore CSI Driver Operator with Workload Identity</title>
<simpara>If you are planning to use GCP Workload Identity with Google Compute Platform Filestore, you must obtain certain parameters that you will use during the installation of the Google Cloud Filestore Container Storage Interface (CSI) Driver Operator.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the cluster-admin role.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To prepare to install the Google Cloud Filestore CSI Driver Operator with Workload Identity:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Obtain the project number:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Obtain the project ID by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ export PROJECT_ID=$(oc get infrastructure/cluster -o jsonpath='{.status.platformStatus.gcp.projectID}')</programlisting>
</listitem>
<listitem>
<simpara>Obtain the project number, using the project ID, by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ gcloud projects describe $PROJECT_ID --format="value(projectNumber)"</programlisting>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Find the identity pool ID and the provider ID:</simpara>
<simpara>During cluster installation, the names of these resources are provided to the Cloud Credential Operator utility (<literal>ccoctl</literal>) with the <literal>--name parameter</literal>. See "Creating Google Cloud resources with the Cloud Credential Operator utility".</simpara>
</listitem>
<listitem>
<simpara>Create Workload Identity resources for the Google Cloud Filestore Operator:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Create a <literal>CredentialsRequest</literal> file using the following example file:</simpara>
<formalpara>
<title>Example Credentials Request YAML file</title>
<para>
<programlisting language="YAML" linenumbering="unnumbered">apiVersion: cloudcredential.openshift.io/v1
kind: CredentialsRequest
metadata:
  name: openshift-gcp-filestore-csi-driver-operator
  namespace: openshift-cloud-credential-operator
  annotations:
    include.release.openshift.io/self-managed-high-availability: "true"
    include.release.openshift.io/single-node-developer: "true"
spec:
  serviceAccountNames:
  - gcp-filestore-csi-driver-operator
  - gcp-filestore-csi-driver-controller-sa
  secretRef:
    name: gcp-filestore-cloud-credentials
    namespace: openshift-cluster-csi-drivers
  providerSpec:
    apiVersion: cloudcredential.openshift.io/v1
	kind: GCPProviderSpec
    predefinedRoles:
    - roles/file.editor
    - roles/resourcemanager.tagUser
    skipServiceCheck: true</programlisting>
</para>
</formalpara>
</listitem>
<listitem>
<simpara>Use the <literal>CredentialsRequest</literal> file to create a Google Cloud service account by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ ./ccoctl gcp create-service-accounts --name=&lt;filestore-service-account&gt; \<co xml:id="CO31-1"/>
  --workload-identity-pool=&lt;workload-identity-pool&gt; \<co xml:id="CO31-2"/>
  --workload-identity-provider=&lt;workload-identity-provider&gt; \<co xml:id="CO31-3"/>
  --project=&lt;project-id&gt; \<co xml:id="CO31-4"/>
  --credentials-requests-dir=/tmp/credreq <co xml:id="CO31-5"/></programlisting>
<calloutlist>
<callout arearefs="CO31-1">
<para>&lt;filestore-service-account&gt; is a user-chosen name.</para>
</callout>
<callout arearefs="CO31-2">
<para>&lt;workload-identity-pool&gt; comes from Step 2 above.</para>
</callout>
<callout arearefs="CO31-3">
<para>&lt;workload-identity-provider&gt; comes from Step 2 above.</para>
</callout>
<callout arearefs="CO31-4">
<para>&lt;project-id&gt; comes from Step 1.a above.</para>
</callout>
<callout arearefs="CO31-5">
<para>The name of directory where the <literal>CredentialsRequest</literal> file resides.</para>
</callout>
</calloutlist>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">2025/02/10 17:47:39 Credentials loaded from gcloud CLI defaults
2025/02/10 17:47:42 IAM service account filestore-service-account-openshift-gcp-filestore-csi-driver-operator created
2025/02/10 17:47:44 Unable to add predefined roles to IAM service account, retrying...
2025/02/10 17:47:59 Updated policy bindings for IAM service account filestore-service-account-openshift-gcp-filestore-csi-driver-operator
2025/02/10 17:47:59 Saved credentials configuration to: /tmp/install-dir/ <co xml:id="CO32-1"/>
openshift-cluster-csi-drivers-gcp-filestore-cloud-credentials-credentials.yaml</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO32-1">
<para>The current directory.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Find the service account email of the newly created service account by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ cat /tmp/install-dir/manifests/openshift-cluster-csi-drivers-gcp-filestore-cloud-credentials-credentials.yaml | yq '.data["service_account.json"]' | base64 -d | jq '.service_account_impersonation_url'</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">https://iamcredentials.googleapis.com/v1/projects/-/serviceAccounts/filestore-se-openshift-g-ch8cm@openshift-gce-devel.iam.gserviceaccount.com:generateAccessToken</programlisting>
</para>
</formalpara>
<simpara>In this example output, the service account email is <literal>filestore-se-openshift-g-ch8cm@openshift-gce-devel.iam.gserviceaccount.com</literal>.</simpara>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<formalpara>
<title>Results</title>
<para>You now have the following parameters that you need to install the Google Cloud Filestore CSI Driver Operator:</para>
</formalpara>
<itemizedlist>
<listitem>
<simpara>Project number - from Step 1.b</simpara>
</listitem>
<listitem>
<simpara>Pool ID - from Step 2</simpara>
</listitem>
<listitem>
<simpara>Provider ID - from Step 2</simpara>
</listitem>
<listitem>
<simpara>Service account email - from Step 3.c</simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="persistent-storage-csi-olm-operator-install_persistent-storage-csi-google-cloud-file">
<title>Installing the Google Cloud Filestore CSI Driver Operator</title>
<simpara>The Google Compute Platform (Google Cloud) Filestore Container Storage Interface (CSI) Driver Operator is not installed in OpenShift Container Platform by default.
Use the following procedure to install the Google Cloud Filestore CSI Driver Operator in your cluster.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the OpenShift Container Platform web console.</simpara>
</listitem>
<listitem>
<simpara>If using GCP Workload Identity, certain GCP Workload Identity parameters are needed. See the preceding Section <emphasis>Preparing to install the Google Cloud Filestore CSI Driver Operator with Workload Identity</emphasis>.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To install the Google Cloud Filestore CSI Driver Operator from the web console:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Log in to the <link xlink:href="https://console.redhat.com/openshift">OpenShift Cluster Manager</link>.</simpara>
</listitem>
<listitem>
<simpara>Select your cluster.</simpara>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Open console</emphasis> and log in with your credentials.</simpara>
</listitem>
<listitem>
<simpara>Enable the Filestore API in the GCE project by running the following command:</simpara>
<programlisting language="command" linenumbering="unnumbered">$ gcloud services enable file.googleapis.com  --project &lt;my_gce_project&gt; <co xml:id="CO33-1"/></programlisting>
<calloutlist>
<callout arearefs="CO33-1">
<para>Replace <literal>&lt;my_gce_project&gt;</literal> with your Google Cloud project.</para>
</callout>
</calloutlist>
<simpara>You can also do this using Google Cloud web console.</simpara>
</listitem>
<listitem>
<simpara>Install the Google Cloud Filestore CSI Operator:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">Ecosystem</emphasis> &#8594; <emphasis role="strong">Software Catalog</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Locate the Google Cloud Filestore CSI Operator by typing <emphasis role="strong">Google Cloud Filestore</emphasis> in the filter box.</simpara>
</listitem>
<listitem>
<simpara>Click the <emphasis role="strong">Google Cloud Filestore CSI Driver Operator</emphasis> button.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Google Cloud Filestore CSI Driver Operator</emphasis> page, click <emphasis role="strong">Install</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Install Operator</emphasis> page, ensure that:</simpara>
<itemizedlist>
<listitem>
<simpara><emphasis role="strong">All namespaces on the cluster (default)</emphasis> is selected.</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Installed Namespace</emphasis> is set to <emphasis role="strong">openshift-cluster-csi-drivers</emphasis>.</simpara>
<simpara>If using GCP Workload Identity, enter values for the following fields obtained from the procedure in Section <emphasis>Preparing to install the Google Cloud Filestore CSI Driver Operator with Workload Identity</emphasis>:</simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Google Cloud Project Number</emphasis></simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Google Cloud Pool ID</emphasis></simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Google Cloud Provider ID</emphasis></simpara>
</listitem>
<listitem>
<simpara><emphasis role="strong">Google Cloud Service Account Email</emphasis></simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Install</emphasis>.</simpara>
<simpara>After the installation finishes, the Google Cloud Filestore CSI Operator is listed in the <emphasis role="strong">Installed Operators</emphasis> section of the web console.</simpara>
</listitem>
</orderedlist>
</listitem>
<listitem>
<simpara>Install the Google Cloud Filestore CSI Driver:</simpara>
<orderedlist numeration="loweralpha">
<listitem>
<simpara>Click <emphasis role="strong">administration</emphasis> → <emphasis role="strong">CustomResourceDefinitions</emphasis> → <emphasis role="strong">ClusterCSIDriver</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>On the <emphasis role="strong">Instances</emphasis> tab, click <emphasis role="strong">Create ClusterCSIDriver</emphasis>.</simpara>
<simpara>Use the following YAML file:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: operator.openshift.io/v1
kind: ClusterCSIDriver
metadata:
    name: filestore.csi.storage.gke.io
spec:
  managementState: Managed</programlisting>
</listitem>
<listitem>
<simpara>Click <emphasis role="strong">Create</emphasis>.</simpara>
</listitem>
<listitem>
<simpara>Wait for the following Conditions to change to a "true" status:</simpara>
<itemizedlist>
<listitem>
<simpara>GCPFilestoreDriverCredentialsRequestControllerAvailable</simpara>
</listitem>
<listitem>
<simpara>GCPFilestoreDriverNodeServiceControllerAvailable</simpara>
</listitem>
<listitem>
<simpara>GCPFilestoreDriverControllerServiceControllerAvailable</simpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
</listitem>
</orderedlist>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link xlink:href="https://cloud.google.com/endpoints/docs/openapi/enable-api">Enabling an API in your Google Cloud</link>.</simpara>
</listitem>
<listitem>
<simpara><link xlink:href="https://support.google.com/googleapi/answer/6158841?hl=en">Enabling an API using the Google Cloud web console</link>.</simpara>
</listitem>
</itemizedlist>
</section>
</section>
<section xml:id="persistent-storage-csi-google-cloud-file-create-sc_persistent-storage-csi-google-cloud-file">
<title>Creating a storage class for GCP Filestore Storage</title>
<simpara>After installing the Operator, you should create a storage class for dynamic provisioning of Google Compute Platform (GCP) Filestore volumes.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>You are logged in to the running OpenShift Container Platform cluster.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To create a storage class:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Create a storage class using the following example YAML file:</simpara>
<formalpara>
<title>Example YAML file</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: filestore-csi
provisioner: filestore.csi.storage.gke.io
parameters:
  connect-mode: DIRECT_PEERING <co xml:id="CO34-1"/>
  network: network-name <co xml:id="CO34-2"/>
allowVolumeExpansion: true
volumeBindingMode: WaitForFirstConsumer</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO34-1">
<para>For a shared VPC, use the <literal>connect-mode</literal> parameter set to <literal>PRIVATE_SERVICE_ACCESS</literal>. For a non-shared VPC, the value is <literal>DIRECT_PEERING</literal>, which is the default setting.</para>
</callout>
<callout arearefs="CO34-2">
<para>Specify the name of the GCP virtual private cloud (VPC) network where Filestore instances should be created in.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Specify the name of the VPC network where Filestore instances should be created in.</simpara>
<simpara>It is recommended to specify the VPC network that the Filestore instances should be created in. If no VPC network is specified, the Container Storage Interface (CSI) driver tries to create the instances in the default VPC network of the project.</simpara>
<simpara>On IPI installations, the VPC network name is typically the cluster name with the suffix "-network". However, on UPI installations, the VPC network name can be any value chosen by the user.</simpara>
<simpara>For a shared VPC (<literal>connect-mode</literal> = <literal>PRIVATE_SERVICE_ACCESS</literal>), the network needs to be the full VPC name. For example: <literal>projects/shared-vpc-name/global/networks/gcp-filestore-network</literal>.</simpara>
<simpara>You can find out the VPC network name by inspecting the <literal>MachineSets</literal> objects with the following command:</simpara>
<programlisting language="command" linenumbering="unnumbered">$ oc -n openshift-machine-api get machinesets -o yaml | grep "network:"
            - network: gcp-filestore-network
(...)</programlisting>
<simpara>In this example, the VPC network name in this cluster is "gcp-filestore-network".</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="persistent-storage-csi-gcp-filestore-nfs-export-options_persistent-storage-csi-google-cloud-file">
<title>NFS export options</title>
<simpara>By default, a Filestore instance grants root level read/write access to all clients that share the same Google Cloud project and virtual private cloud (VPC) network. Network File System (NFS) export options can limit this access to certain IP ranges and specific user/group IDs for the Filestore instance. When creating a storage class, you can set these options using the <literal>nfs-export-options-on-create</literal> parameter.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster as a user with the cluster-admin role.</simpara>
</listitem>
<listitem>
<simpara>The Google Cloud Filestore CSI Driver Operator and Google Cloud Filestore CSI driver installed.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create a storage class using a file similar to the following sample YAML file:</simpara>
<note>
<simpara>For more information about creating a storage class, see Section <emphasis>Creating a storage class for GCP Filestore Operator</emphasis>.</simpara>
</note>
<formalpara>
<title>Example storage class YAML file with NFS export options</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
 name: SC-name
provisioner: filestore.csi.storage.gke.io
parameters:
 connect-mode: DIRECT_PEERING
 network: project-network
 nfs-export-options-on-create: '[ <co xml:id="CO35-1"/>
   {
     "accessMode": "READ_WRITE", <co xml:id="CO35-2"/>
     "squashMode": "NO_ROOT_SQUASH", <co xml:id="CO35-3"/>
     "anonUid": 65534 <co xml:id="CO35-4"/>
     "anonGid": 65534 <co xml:id="CO35-5"/>
     "ipRanges": [ <co xml:id="CO35-6"/>
       "10.0.0.0/16"
     ]
   }]'
allowVolumeExpansion: true</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO35-1">
<para><emphasis role="strong">NFS export options parameter</emphasis></para>
</callout>
<callout arearefs="CO35-2">
<para><emphasis role="strong">Access mode</emphasis>: Either <literal>READ_ONLY,</literal> which allows only read requests on the exported directory; or <literal>READ_WRITE</literal>, which allows both read and write requests. The default is <literal>READ_WRITE</literal>.</para>
</callout>
<callout arearefs="CO35-3">
<para><emphasis role="strong">Squash mode</emphasis>: Either <literal>NO_ROOT_SQUASH</literal>, which allows root access on the exported directory; or ROOT_SQUASH, which does not allow root access. The default is <literal>NO_ROOT_SQUASH</literal>.</para>
</callout>
<callout arearefs="CO35-4">
<para><emphasis role="strong">AnonUid</emphasis>: An integer representing the anonymous user ID with a default value of 65534. <literal>AnonUid</literal> can only be set with <literal>squashMode</literal> set to <literal>ROOT_SQUASH</literal>; Otherwise, an error occurs.</para>
</callout>
<callout arearefs="CO35-5">
<para><emphasis role="strong">AnonGid</emphasis>: An integer representing the anonymous group ID with a default value of 65534. <literal>AnonGid</literal> can only be set with <literal>squashMode</literal> set to <literal>ROOT_SQUASH</literal>. Otherwise, an error occurs.</para>
</callout>
<callout arearefs="CO35-6">
<para><emphasis role="strong">IP ranges</emphasis>: List of either an IPv4 addresses in the format {octet1}.{octet2}.{octet3}.{octet4}, or CIDR ranges in the format {octet1}.{octet2}.{octet3}.{octet4}/{mask size}, which can mount the file share. Overlapping IP ranges are not allowed, both within and across NfsExportOptions, otherwise, an error is returned. The limit is 64 IP ranges or addresses for each <literal>FileShareConfig</literal> among all NFS export options.</para>
</callout>
</calloutlist>
</listitem>
</orderedlist>
</section>
<section xml:id="persistent-storage-csi-google-cloud-file-delete-instances_persistent-storage-csi-google-cloud-file">
<title>Destroying clusters and GCP Filestore</title>
<simpara>Typically, if you destroy a cluster, the OpenShift Container Platform installer deletes all of the cloud resources that belong to that cluster. However, due to the special nature of the Google Compute Platform (GCP) Filestore resources, the automated cleanup process might not remove all of them in some rare cases.</simpara>
<simpara>Therefore, Red Hat recommends that you verify that all cluster-owned Filestore resources are deleted by the uninstall process.</simpara>
<formalpara>
<title>Procedure</title>
<para>To ensure that all GCP Filestore PVCs have been deleted:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Access your Google Cloud account using the GUI or CLI.</simpara>
</listitem>
<listitem>
<simpara>Search for any resources with the <literal>kubernetes-io-cluster-${CLUSTER_ID}=owned</literal> label.</simpara>
<simpara>Since the cluster ID is unique to the deleted cluster, there should not be any remaining resources with that cluster ID.</simpara>
</listitem>
<listitem>
<simpara>In the unlikely case there are some remaining resources, delete them.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="_additional-resources-2" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara><link linkend="persistent-storage-csi">Configuring CSI volumes</link></simpara>
</listitem>
</itemizedlist>
</section>
</section>
</chapter>
<chapter xml:id="generic-ephemeral-volumes">
<title>Generic ephemeral volumes</title>
<section xml:id="generic-ephemeral-vols-overview_generic-ephemeral-volumes">
<title>Overview of generic ephemeral volumes</title>
<simpara role="_abstract">To manage scratch data by using standard storage drivers, use generic ephemeral volumes. These volumes provide per-pod directories similar to <literal>emptyDir</literal> volumes but work with any driver that supports persistent volumes and dynamic provisioning, so that you can leverage existing storage infrastructure for temporary needs.</simpara>
<simpara>Generic ephemeral volumes are specified inline in the pod specification and follow the lifecycle of a pod. They are created and deleted along with the pod.</simpara>
<simpara>Generic ephemeral volumes have the following features:</simpara>
<itemizedlist>
<listitem>
<simpara>Storage can be local or network-attached.</simpara>
</listitem>
<listitem>
<simpara>Volumes can have a fixed size that pods cannot exceed.</simpara>
</listitem>
<listitem>
<simpara>Volumes might have some initial data, depending on the driver and parameters.</simpara>
</listitem>
<listitem>
<simpara>Typical operations on volumes are supported, assuming that the driver supports them, including snapshotting, cloning, resizing, and storage capacity tracking.</simpara>
</listitem>
</itemizedlist>
<note>
<simpara>Generic ephemeral volumes do not support offline snapshotting and resizing.</simpara>
</note>
</section>
<section xml:id="generic-ephemeral-vols-lifecycle_generic-ephemeral-volumes">
<title>Lifecycle and persistent volume claims</title>
<simpara role="_abstract">To bind the lifecycle of storage resources to a specific pod, configure persistent volume claim (PVC) parameters within the volume source of the pod. This setup ensures that the ephemeral volume controller creates the PVC in the same namespace upon pod creation and automatically deletes the PVC when the pod is removed.</simpara>
<simpara>Labels, annotations, and the whole set of fields for persistent volume claims (PVCs) are supported.</simpara>
<simpara>The ephemeral volume controller creates a PVC object from the template shown in the <emphasis>Creating generic ephemeral volumes</emphasis> procedure.</simpara>
<simpara>Volume binding and provisioning can be triggered in one of two ways:</simpara>
<itemizedlist>
<listitem>
<simpara>Either immediately, if the storage class uses immediate volume binding.</simpara>
<simpara>With immediate binding, the scheduler is forced to select a node that has access to the volume after it is available.</simpara>
</listitem>
<listitem>
<simpara>When the pod is tentatively scheduled onto a node (<literal>WaitForFirstConsumer</literal> volume binding mode).</simpara>
<simpara>This volume binding option is recommended for generic ephemeral volumes because then the scheduler can choose a suitable node for the pod.</simpara>
</listitem>
</itemizedlist>
<simpara>In terms of resource ownership, a pod that has generic ephemeral storage is the owner of the PVCs that provide that ephemeral storage. When the pod is deleted, the Kubernetes garbage collector deletes the PVC, which then usually triggers deletion of the volume because the default reclaim policy of storage classes is to delete volumes. You can create quasi-ephemeral local storage by using a storage class with a reclaim policy of retain. The storage outlives the pod, and in this case, you must ensure that volume clean-up happens separately. While these PVCs exist, they can be used like any other PVC. In particular, they can be referenced as data sources in volume cloning or snapshotting. The PVC object also holds the current status of the volume.</simpara>
<itemizedlist role="_additional-resources">
<title>Additional resources</title>
<listitem>
<simpara><link linkend="generic-ephemeral-vols-procedure_generic-ephemeral-volumes">Creating generic ephemeral volumes</link></simpara>
</listitem>
</itemizedlist>
</section>
<section xml:id="generic-ephemeral-security_generic-ephemeral-volumes">
<title>Security</title>
<simpara role="_abstract">You can enable the generic ephemeral volume feature so that if a user can create pods, they can also create persistent volume claims (PVCs) indirectly.</simpara>
<simpara>The generic ephemeral volume feature works even if these users do not have permission to create PVCs directly. Cluster administrators must be aware of this. If this does not fit your security model, use an admission webhook that rejects objects such as pods that have a generic ephemeral volume.</simpara>
<simpara>The normal namespace quota for PVCs still applies, so even if users are allowed to use this new mechanism, they cannot use it to circumvent other policies.</simpara>
</section>
<section xml:id="generic-ephemeral-vols-pvc-naming_generic-ephemeral-volumes">
<title>Persistent volume claim naming</title>
<simpara role="_abstract">To avoid resource conflicts, review the naming convention for automatically created persistent volume claims (PVCs). Because the system generates names by combining the pod name and volume name with a hyphen, you must ensure manually created resources do not inadvertently match this pattern.</simpara>
<simpara>For example, <literal>pod-a</literal> with volume <literal>scratch</literal> and <literal>pod</literal> with volume <literal>a-scratch</literal> both end up with the same PVC name, <literal>pod-a-scratch</literal>.</simpara>
<simpara>Such conflicts are detected, and a PVC is only used for an ephemeral volume if it was created for the pod. This check is based on the ownership relationship. An existing PVC is not overwritten or modified, but this does not resolve the conflict. Without the right PVC, a pod cannot start.</simpara>
<important>
<simpara>Be careful when naming pods and volumes inside the same namespace so that naming conflicts do not occur.</simpara>
</important>
</section>
<section xml:id="generic-ephemeral-vols-procedure_generic-ephemeral-volumes">
<title>Creating generic ephemeral volumes</title>
<simpara role="_abstract">Configure temporary storage by creating generic ephemeral volumes that use drivers that support dynamic provisioning.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Create the <literal>pod</literal> object definition and save it to a file.</simpara>
</listitem>
<listitem>
<simpara>Include the generic ephemeral volume information in the file.</simpara>
<formalpara>
<title>my-example-pod-with-generic-vols.yaml</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: Pod
apiVersion: v1
metadata:
  name: my-app
spec:
  containers:
    - name: my-frontend
      image: busybox:1.28
      volumeMounts:
      - mountPath: "/mnt/storage"
        name: data
      command: [ "sleep", "1000000" ]
  volumes:
    - name: data
      ephemeral:
        volumeClaimTemplate:
          metadata:
            labels:
              type: my-app-ephvol
          spec:
            accessModes: [ "ReadWriteOnce" ]
            storageClassName: "gp2-csi"
            resources:
              requests:
                storage: 1Gi</programlisting>
</para>
</formalpara>
<itemizedlist>
<listitem>
<simpara><literal>volumes.name</literal>:: Specifies the name for the generic ephemeral volume.</simpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
</section>
</chapter>
<chapter xml:id="expanding-persistent-volumes">
<title>Expanding persistent volumes</title>
<simpara role="_abstract">You can use the Container Storage Interface (CSI) to expand storage volumes after they have already been created.</simpara>
<section xml:id="add-volume-expansion_expanding-persistent-volumes">
<title>Enabling volume expansion support</title>
<simpara>Before you can expand persistent volumes, the <literal>StorageClass</literal> object must
have the <literal>allowVolumeExpansion</literal> field set to <literal>true</literal>.</simpara>
<itemizedlist>
<title>Procedure</title>
<listitem>
<simpara>Edit the <literal>StorageClass</literal> object and add the <literal>allowVolumeExpansion</literal> attribute by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc edit storageclass &lt;storage_class_name&gt; <co xml:id="CO36-1"/></programlisting>
<calloutlist>
<callout arearefs="CO36-1">
<para>Specifies the name of the storage class.</para>
</callout>
</calloutlist>
<simpara>The following example demonstrates adding this line at the bottom
of the storage class configuration.</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: storage.k8s.io/v1
kind: StorageClass
...
parameters:
  type: gp2
reclaimPolicy: Delete
allowVolumeExpansion: true <co xml:id="CO37-1"/></programlisting>
<calloutlist>
<callout arearefs="CO37-1">
<para>Setting this attribute to <literal>true</literal> allows PVCs to be
expanded after creation.</para>
</callout>
</calloutlist>
</listitem>
</itemizedlist>
</section>
<section xml:id="expanding-csi-volumes_expanding-persistent-volumes">
<title>Expanding CSI volumes</title>
<simpara role="_abstract">You can use the Container Storage Interface (CSI) to expand storage volumes after they have already been created.</simpara>
<important>
<simpara>Shrinking persistent volumes (PVs) is <emphasis>not</emphasis> supported.</simpara>
</important>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>The underlying CSI driver supports resize. See "CSI drivers supported by OpenShift Container Platform" in the "Additional resources" section.</simpara>
</listitem>
<listitem>
<simpara>Dynamic provisioning is used.</simpara>
</listitem>
<listitem>
<simpara>The controlling <literal>StorageClass</literal> object has <literal>allowVolumeExpansion</literal> set to <literal>true</literal>. For more information, see "Enabling volume expansion support."</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>For the persistent volume claim (PVC), set <literal>.spec.resources.requests.storage</literal> to the desired new size.</simpara>
</listitem>
<listitem>
<simpara>Watch the <literal>status.conditions</literal> field of the PVC to see if the resize has completed. OpenShift Container Platform adds the <literal>Resizing</literal> condition to the PVC during expansion, which is removed after expansion completes.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="expanding-local-volumes_expanding-persistent-volumes">
<title>Expanding local volumes</title>
<simpara role="_abstract">You can manually expand persistent volumes (PVs) and persistent volume claims (PVCs) created by using the local storage operator (LSO).</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Expand the underlying devices. Ensure that appropriate capacity is available on these devices.</simpara>
</listitem>
<listitem>
<simpara>Update the corresponding PV objects to match the new device sizes by editing the <literal>.spec.capacity</literal> field of the PV.</simpara>
</listitem>
<listitem>
<simpara>For the storage class that is used for binding the PVC to PV, set the <literal>allowVolumeExpansion</literal> field to <literal>true</literal>.</simpara>
</listitem>
<listitem>
<simpara>For the PVC, set the <literal>.spec.resources.requests.storage</literal> field to match the new size.</simpara>
<simpara>Kubelet automatically expands the underlying file system on the volume, if necessary, and updates the status field of the PVC to reflect the new size.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="expanding-pvc-filesystem_expanding-persistent-volumes">
<title>Expanding persistent volume claims (PVCs) with a file system</title>
<simpara role="_abstract">Expanding PVCs based on volume types that need file system resizing, such as GCE, EBS, and Cinder, is a two-step process. First, expand the volume objects in the cloud provider. Second, expand the file system on the node.</simpara>
<simpara>Expanding the file system on the node only happens when a new pod is started with the volume.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>The controlling <literal>StorageClass</literal> object must have <literal>allowVolumeExpansion</literal> set to <literal>true</literal>.</simpara>
</listitem>
</itemizedlist>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Edit the PVC and request a new size by editing <literal>spec.resources.requests</literal>. For example, the following expands the <literal>ebs</literal> PVC to 8 Gi:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ebs
spec:
  storageClass: "storageClassWithFlagSet"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi</programlisting>
<itemizedlist>
<listitem>
<simpara><literal>requests.storage</literal>: Specifies the size for the PVC.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara>After the cloud provider object has finished resizing, the PVC is set to <literal>FileSystemResizePending</literal>. Check the condition by entering the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc describe pvc &lt;pvc_name&gt;</programlisting>
</listitem>
<listitem>
<simpara>When the cloud provider object has finished resizing, the <literal>PersistentVolume</literal> object reflects the newly requested size in <literal>PersistentVolume.Spec.Capacity</literal>. At this point, you can create or re-create a pod from the PVC to finish the file system resizing. Once the pod is running, the newly requested size is available and the <literal>FileSystemResizePending</literal> condition is removed from the PVC.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="expanding-recovering-from-failure_expanding-persistent-volumes">
<title>Recovering from failure when expanding volumes</title>
<simpara role="_abstract">To retry a failed or pending resize request, update the <literal>spec.resources.requests.storage</literal> field in the persistent volume claim (PVC). You must specify a value larger than the original volume size to successfully retrigger the operation.</simpara>
<simpara>Entering a smaller resize value in the <literal>.spec.resources.requests.storage</literal> field for the existing PVC does not work.</simpara>
<orderedlist numeration="arabic">
<title>Procedure</title>
<listitem>
<simpara>Mark the persistent volume (PV) that is bound to the PVC with the <literal>Retain</literal> reclaim policy. Change the <literal>persistentVolumeReclaimPolicy</literal> field to <literal>Retain</literal>.</simpara>
</listitem>
<listitem>
<simpara>Delete the PVC.</simpara>
</listitem>
<listitem>
<simpara>Manually edit the PV and delete the <literal>claimRef</literal> entry from the PV specification to ensure that the newly created PVC can bind to the PV marked <literal>Retain</literal>. This marks the PV as <literal>Available</literal>.</simpara>
</listitem>
<listitem>
<simpara>Recreate the PVC in a smaller size, or a size that can be allocated by the underlying storage provider.</simpara>
</listitem>
<listitem>
<simpara>Set the <literal>volumeName</literal> field of the PVC to the name of the PV. This binds the PVC to the provisioned PV only.</simpara>
</listitem>
<listitem>
<simpara>Restore the reclaim policy on the PV.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="expanding-recovering-from-failure-view-status_expanding-persistent-volumes">
<title>Viewing the status of volume resize</title>
<simpara role="_abstract">You can view the status of volume resizing with the <literal>pvc.Status.AllocatedResourceStatus</literal> field. If a user changes the size of their PVCs, the <literal>pvc.Status.AllocatedResourceStatus</literal> field allows resource quota to be tracked accurately.</simpara>
<simpara>The possible values for <literal>pvc.Status.AllocatedResourceStatus</literal> are:</simpara>
<itemizedlist>
<listitem>
<simpara><literal>ControllerResizeInProgress</literal>: Controller resize attempt is in progress.</simpara>
</listitem>
<listitem>
<simpara><literal>ControllerResizeFailed</literal>: Controller resize attempt failed.</simpara>
</listitem>
<listitem>
<simpara><literal>NodeResizeInProgress</literal>: Node resize attempt is in progress.</simpara>
</listitem>
<listitem>
<simpara><literal>NodeResizeFailed</literal>: Node resize attempt failed.</simpara>
</listitem>
</itemizedlist>
<simpara>For a typical block volume, the field transitions between <literal>ControllerResizeInProgress</literal>, <literal>NodeResizePending</literal>, <literal>NodeResizeInProgress</literal>, and then nil/empty when the volume expansion finishes.</simpara>
</section>
<section xml:id="additional-resources_expanding-persistent-volumes" role="_additional-resources">
<title>Additional resources</title>
<itemizedlist>
<listitem>
<simpara><link xlink:href="https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html/storage/using-container-storage-interface-csi#csi-drivers-supported_persistent-storage-csi">CSI drivers supported by OpenShift Container Platform</link></simpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="dynamic-provisioning">
<title>Dynamic provisioning</title>
<section xml:id="about_dynamic-provisioning">
<title>About dynamic provisioning</title>
<simpara>The <literal>StorageClass</literal> resource object describes and classifies storage that can
be requested, as well as provides a means for passing parameters for
dynamically provisioned storage on demand. <literal>StorageClass</literal> objects can also
serve as a management mechanism for controlling different levels of
storage and access to the storage. Cluster Administrators (<literal>cluster-admin</literal>)
 or Storage Administrators (<literal>storage-admin</literal>) define and create the
<literal>StorageClass</literal> objects that users can request without needing any detailed
knowledge about the underlying storage volume sources.</simpara>
<simpara>The OpenShift Container Platform persistent volume framework enables this functionality
and allows administrators to provision a cluster with persistent storage.
The framework also gives users a way to request those resources without
having any knowledge of the underlying infrastructure.</simpara>
<simpara>Many storage types are available for use as persistent volumes in
OpenShift Container Platform. While all of them can be statically provisioned by an
administrator, some types of storage are created dynamically using the
built-in provider and plugin APIs.</simpara>
</section>
<section xml:id="available-plug-ins_dynamic-provisioning">
<title>Available dynamic provisioning plugins</title>
<simpara>OpenShift Container Platform provides the following provisioner plugins, which have
generic implementations for dynamic provisioning that use the cluster&#8217;s
configured provider&#8217;s API to create new storage resources:</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="3">
<colspec colname="col_1" colwidth="33.3333*"/>
<colspec colname="col_2" colwidth="33.3333*"/>
<colspec colname="col_3" colwidth="33.3334*"/>
<thead>
<row>
<entry align="left" valign="top">Storage type</entry>
<entry align="left" valign="top">Provisioner plugin name</entry>
<entry align="left" valign="top">Notes</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>Amazon Elastic Block Store (Amazon EBS)</simpara></entry>
<entry align="left" valign="top"><simpara><literal>ebs.csi.aws.com</literal></simpara></entry>
<entry align="left" valign="top"><simpara>For dynamic provisioning when using multiple clusters in different zones,
tag each node with <literal>Key=kubernetes.io/cluster/&lt;cluster_name&gt;,Value=&lt;cluster_id&gt;</literal>
where <literal>&lt;cluster_name&gt;</literal> and <literal>&lt;cluster_id&gt;</literal> are unique per cluster.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>GCE Persistent Disk (gcePD)</simpara></entry>
<entry align="left" valign="top"><simpara><literal>kubernetes.io/gce-pd</literal></simpara></entry>
<entry align="left" valign="top"><simpara>In multi-zone configurations, it is advisable to run one OpenShift Container Platform
cluster per GCE project to avoid PVs from being created in zones where
no node in the current cluster exists.</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>IBM Power&#174; Virtual Server Block</simpara></entry>
<entry align="left" valign="top"><simpara><literal>powervs.csi.ibm.com</literal></simpara></entry>
<entry align="left" valign="top"><simpara>After installation, the IBM Power&#174; Virtual Server Block CSI Driver Operator and IBM Power&#174; Virtual Server Block CSI Driver automatically create the required storage classes for dynamic provisioning.</simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
<important>
<simpara>Any chosen provisioner plugin also requires configuration for the relevant
cloud, host, or third-party provider as per the relevant documentation.</simpara>
</important>
</section>
<section xml:id="defining-storage-classes_dynamic-provisioning">
<title>Defining a storage class</title>
<simpara><literal>StorageClass</literal> objects are currently a globally scoped object and must be
created by <literal>cluster-admin</literal> or <literal>storage-admin</literal> users.</simpara>
<important>
<simpara>The Cluster Storage Operator might install a default storage class depending
on the platform in use. This storage class is owned and controlled by the
Operator. It cannot be deleted or modified beyond defining annotations
and labels. If different behavior is desired, you must define a custom
storage class.</simpara>
</important>
<simpara>The following sections describe the basic definition for a
<literal>StorageClass</literal> object and specific examples for each of the supported plugin types.</simpara>
<section xml:id="basic-storage-class-definition_dynamic-provisioning">
<title>Basic StorageClass object definition</title>
<simpara>The following resource shows the parameters and default values that you
use to configure a storage class. This example uses the AWS
ElasticBlockStore (EBS) object definition.</simpara>
<formalpara>
<title>Sample <literal>StorageClass</literal> definition</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: StorageClass <co xml:id="CO38-1"/>
apiVersion: storage.k8s.io/v1 <co xml:id="CO38-2"/>
metadata:
  name: &lt;storage-class-name&gt; <co xml:id="CO38-3"/>
  annotations: <co xml:id="CO38-4"/>
    storageclass.kubernetes.io/is-default-class: 'true'
    ...
provisioner: kubernetes.io/aws-ebs <co xml:id="CO38-5"/>
parameters: <co xml:id="CO38-6"/>
  type: gp3
...</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO38-1">
<para>(required) The API object type.</para>
</callout>
<callout arearefs="CO38-2">
<para>(required) The current apiVersion.</para>
</callout>
<callout arearefs="CO38-3">
<para>(required) The name of the storage class.</para>
</callout>
<callout arearefs="CO38-4">
<para>(optional) Annotations for the storage class.</para>
</callout>
<callout arearefs="CO38-5">
<para>(required) The type of provisioner associated with this storage class.</para>
</callout>
<callout arearefs="CO38-6">
<para>(optional) The parameters required for the specific provisioner, this will change from plug-in to plug-in.</para>
</callout>
</calloutlist>
</section>
<section xml:id="storage-class-annotations_dynamic-provisioning">
<title>Storage class annotations</title>
<simpara>To set a storage class as the cluster-wide default, add
the following annotation to your storage class metadata:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">storageclass.kubernetes.io/is-default-class: "true"</programlisting>
<simpara>For example:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
...</programlisting>
<simpara>This enables any persistent volume claim (PVC) that does not specify a
specific storage class to automatically be provisioned through the
default storage class. However, your cluster can have more than one storage class, but only one of them can be the default storage class.</simpara>
<note>
<simpara>The beta annotation <literal>storageclass.beta.kubernetes.io/is-default-class</literal> is
still working; however, it will be removed in a future release.</simpara>
</note>
<simpara>To set a storage class description, add the following annotation
to your storage class metadata:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">kubernetes.io/description: My Storage Class Description</programlisting>
<simpara>For example:</simpara>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  annotations:
    kubernetes.io/description: My Storage Class Description
...</programlisting>
</section>
<section xml:id="aws-definition_dynamic-provisioning">
<title>AWS Elastic Block Store (EBS) object definition</title>
<formalpara>
<title>aws-ebs-storageclass.yaml</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: &lt;storage-class-name&gt; <co xml:id="CO39-1"/>
provisioner: ebs.csi.aws.com
parameters:
  type: io1 <co xml:id="CO39-2"/>
  iopsPerGB: "10" <co xml:id="CO39-3"/>
  encrypted: "true" <co xml:id="CO39-4"/>
  kmsKeyId: keyvalue <co xml:id="CO39-5"/>
  fsType: ext4 <co xml:id="CO39-6"/></programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO39-1">
<para>(required) Name of the storage class. The persistent volume claim uses this storage class for provisioning the associated persistent volumes.</para>
</callout>
<callout arearefs="CO39-2">
<para>(required) Select from <literal>io1</literal>, <literal>gp3</literal>, <literal>sc1</literal>, <literal>st1</literal>. The default is <literal>gp3</literal>.
See the
<link xlink:href="http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html">AWS documentation</link>
for valid Amazon Resource Name (ARN) values.</para>
</callout>
<callout arearefs="CO39-3">
<para>Optional: Only for <emphasis role="strong">io1</emphasis> volumes. I/O operations per second per GiB.
The AWS volume plugin multiplies this with the size of the requested
volume to compute IOPS of the volume. The value cap is 20,000 IOPS, which
is the maximum supported by AWS. See the
<link xlink:href="http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html">AWS documentation</link>
for further details.</para>
</callout>
<callout arearefs="CO39-4">
<para>Optional: Denotes whether to encrypt the EBS volume. Valid values
are <literal>true</literal> or <literal>false</literal>.</para>
</callout>
<callout arearefs="CO39-5">
<para>Optional: The full ARN of the key to use when encrypting the volume.
If none is supplied, but <literal>encypted</literal> is set to <literal>true</literal>, then AWS generates a
key. See the
<link xlink:href="http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html">AWS documentation</link>
for a valid ARN value.</para>
</callout>
<callout arearefs="CO39-6">
<para>Optional: File system that is created on dynamically provisioned
volumes. This value is copied to the <literal>fsType</literal> field of dynamically
provisioned persistent volumes and the file system is created when the
volume is mounted for the first time. The default value is <literal>ext4</literal>.</para>
</callout>
</calloutlist>
</section>
<section xml:id="gce-persistentdisk-storage-class_dynamic-provisioning">
<title>GCE PersistentDisk (gcePD) object definition</title>
<formalpara>
<title>gce-pd-storageclass.yaml</title>
<para>
<programlisting language="yaml" linenumbering="unnumbered">apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: &lt;storage-class-name&gt; <co xml:id="CO40-1"/>
provisioner: kubernetes.io/gce-pd
parameters:
  type: pd-ssd <co xml:id="CO40-2"/>
  replication-type: none
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true
reclaimPolicy: Delete</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO40-1">
<para>Name of the storage class. The persistent volume claim uses this storage class for provisioning the associated persistent volumes.</para>
</callout>
<callout arearefs="CO40-2">
<para>Select <literal>pd-ssd</literal>, <literal>pd-standard</literal>, or <literal>hyperdisk-balanced</literal>. The default is <literal>pd-ssd</literal>.</para>
</callout>
</calloutlist>
</section>
</section>
<section xml:id="change-default-storage-class_dynamic-provisioning">
<title>Changing the default storage class</title>
<simpara>Use the following procedure to change the default storage class.</simpara>
<simpara>For example, if you have two defined storage classes, <literal>gp3</literal> and <literal>standard</literal>, and you want to change the default storage class from <literal>gp3</literal> to <literal>standard</literal>.</simpara>
<itemizedlist>
<title>Prerequisites</title>
<listitem>
<simpara>Access to the cluster with cluster-admin privileges.</simpara>
</listitem>
</itemizedlist>
<formalpara>
<title>Procedure</title>
<para>To change the default storage class:</para>
</formalpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>List the storage classes:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get storageclass</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                 TYPE
gp3 (default)        ebs.csi.aws.com <co xml:id="CO41-1"/>
standard             ebs.csi.aws.com</programlisting>
</para>
</formalpara>
<calloutlist>
<callout arearefs="CO41-1">
<para><literal>(default)</literal> indicates the default storage class.</para>
</callout>
</calloutlist>
</listitem>
<listitem>
<simpara>Make the desired storage class the default.</simpara>
<simpara>For the desired storage class, set the <literal>storageclass.kubernetes.io/is-default-class</literal> annotation to <literal>true</literal> by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch storageclass standard -p '{"metadata": {"annotations": {"storageclass.kubernetes.io/is-default-class": "true"}}}'</programlisting>
<note>
<simpara>You can have multiple default storage classes for a short time. However, you should ensure that only one default storage class exists eventually.</simpara>
<simpara>With multiple default storage classes present, any persistent volume claim (PVC) requesting the default storage class (<literal>pvc.spec.storageClassName</literal>=nil) gets the most recently created default storage class, regardless of the default status of that storage class, and the administrator receives an alert in the alerts dashboard that there are multiple default storage classes, <literal>MultipleDefaultStorageClasses</literal>.</simpara>
</note>
</listitem>
<listitem>
<simpara>Remove the default storage class setting from the old default storage class.</simpara>
<simpara>For the old default storage class, change the value of the <literal>storageclass.kubernetes.io/is-default-class</literal> annotation to <literal>false</literal> by running the following command:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc patch storageclass gp3 -p '{"metadata": {"annotations": {"storageclass.kubernetes.io/is-default-class": "false"}}}'</programlisting>
</listitem>
<listitem>
<simpara>Verify the changes:</simpara>
<programlisting language="terminal" linenumbering="unnumbered">$ oc get storageclass</programlisting>
<formalpara>
<title>Example output</title>
<para>
<programlisting language="terminal" linenumbering="unnumbered">NAME                 TYPE
gp3                  ebs.csi.aws.com
standard (default)   ebs.csi.aws.com</programlisting>
</para>
</formalpara>
</listitem>
</orderedlist>
</section>
</chapter>
</book>
